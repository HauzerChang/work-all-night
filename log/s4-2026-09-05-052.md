# S4 chunk 52(2026-09-05):修正 tag_pendant 決策檔框位置錯誤,複核 skirt

## 背景

chunk 51 留下候選項:對 `bodice`/`sleeve_right`/`tag_pendant`/`skirt` 這 4 個裁圖後有
跟鄰近部件重疊跡象的部件做進一步定位判定(前兩者是 chunk 48 已知的演算法歧異案例,後
兩者是 chunk 51 新觀察到、還沒深入定位的疑慮)。本次排程開始無活人裁決(自動排程觸發),
依既有慣例(chunk 26/49/50/51)執行零成本、不需授權的部分——聚焦 `tag_pendant`/`skirt`
兩項,`bodice`/`sleeve_right` 仍需使用者裁決處理方式(人工/完整版 SAM),不重複判斷。

## 執行

1. 用 bash+PIL 把 `tag_pendant`(`bbox_pct [57,44,69,59]`)、`skirt`
   (`bbox_pct [34,40,71,61]`)換算成像素裁圖檢視,並疊網格座標圖輔助定位。
2. `tag_pendant`:裁圖顯示框內是紅色胸衣衣料+金色刺繡紋樣,**完全沒有標籤(垂墜符紙木牌)
   的任何一部分**——跟 chunk 51 找到的 5 個部件是同一種「框完全沒對到內容」錯誤。用網格
   疊圖工具在 x:280-400, y:370-540 範圍定位,確認實際標籤位置在 x≈336-377, y≈413-530,
   改框 `bbox_pct` → `[73,46,82,59]`,confidence 從 `high` 下修為 `medium`。
3. `skirt`:裁圖顯示框內容確實是裙擺紅色衣料主體(含金色滾邊),左緣/下緣夾帶手/前臂/大腿
   是矩形框裁切非矩形重疊人形素材的正常 bleed,跟「框完全落錯」不是同一性質問題。**判定
   不需修正**,`notes` 記錄複核結論。

## 自我驗證

從修正後的 `suggestions.json` 重建 20 部件 `bbox_px` 決策檔
(`tools/mesh_gen/s4_data/chunk52/decision_reconstructed.json`),跑 `s4_decompose_cut.py
--contour rect --eval`:

- `AC1_parts_produced`:20/20 部件全部產出,`overall_pass: true`。
- 從實際 pipeline 輸出(`tools/mesh_gen/s4_data/chunk52/cut_rect/`)複核:`13_tag_pendant.png`
  乾淨完整的標籤(木牌+豎排文字+頂端綁繩結);`12_skirt.png` 裙擺紅色衣料主體清楚可見,
  確認 bleed 屬正常範圍。

## 誠實限制

- `bodice`/`sleeve_right` 本次仍未處理,維持 chunk 48 既有結論。
- 驗證用的 20 部件決策檔延續 chunk 49-51 做法,從 `suggestions.json` 重新換算,不是
  chunk 46-48 使用者手動調整過的那份真實決策檔,不能延續其統計數字。
- 沒有重跑 `--contour sam`(容器未持久化 MobileSAM 權重)。
- 至此 chunk 47-52 累計已對 `suggestions.json` 全部 20 個部件都做過至少一次裁圖複核
  (`bodice`/`sleeve_right` 除外,兩者是已知案例非未檢視),「框完全落錯」這類問題目前
  已無新的未檢視候選;剩餘懸而未決的是 `bodice`/`sleeve_right`(需使用者裁決)與
  `hair_front` 精確語意邊界(需使用者用 assist viewer 確認)。

## 檔案

- 修改 `assets/jiuwei_yanlian_char_crop.suggestions.json`:`tag_pendant` 的
  `bbox_pct`/`confidence`/`notes`;`skirt` 的 `notes`。
- 新增 `tools/mesh_gen/s4_data/chunk52/`(`decision_reconstructed.json`、
  `cut_rect/`[僅 `12_skirt.png`/`13_tag_pendant.png`/`manifest.json`]、定位過程疊圖、
  pipeline 輸出複核截圖)。
- 新增 `knowledge/s4-decompose-box-fix-tag-pendant.md`。
- `knowledge/README.md` 尾端 append。
- 更新 `STATE_S4.md`。
- 未修改任何 production 代碼。

## 下一步(留給使用者/後續排程裁決,均未執行)

1. `bodice`/`sleeve_right` 接受人工處理(chunk 48 選項 b)或測試完整版 SAM(chunk 48
   選項 c,成本較高,需使用者先確認)仍然有效——這是目前唯一剩下的「框有問題」未解案例。
2. `hair_front` 的精確語意邊界(跟 `head`/`fox_ears` 的重疊範圍)需要使用者用 assist
   viewer 手動確認,不建議用演算法猜(chunk 51 已提出,本次未變)。
3. 若要更全面盤點,`suggestions.json` 20 個部件的「框完全落錯」類型錯誤已檢視完畢
   (16/20 已修正或確認正確,`bodice`/`sleeve_right` 是已知案例待人工/SAM 決策);
   下一步可考慮轉向 chunk 46 提出、chunk 51 標記為「已裁決但未拆解成有界工作塊」的其他
   S4 候選(如候選 17 headless 生成式補圖,仍卡在 API key 授權)。
