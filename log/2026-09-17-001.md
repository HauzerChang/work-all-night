# 2026-09-17 run 001 — S1 candidate G-4''''':squash 擠壓段數隨檔位遞增(count-aware)

分支:本次由排程 harness 指派的工作分支 `claude/focused-dirac-cwzvds`(該分支 clone 時與 default
`claude/spine-main` 同一 commit `8da737a`;主排程固定分支策略見 `SCHEDULE.md`)。

## 這次做了什麼(一個有界工作塊)

從 STATE「下一步動作」建議挑 **(G-4''''') squash 接 tier 檔位差異化**。該候選有兩條路:幅度軸(需
**耦合 amplify**,較硬)或**段數軸**(nosc 已備參數,比照 G-4''' wobble count-aware)。選段數軸——它
**繞過**幅度耦合難題,且能誠實地把幅度耦合留為 boundary(不假裝已解)。依 RULES「每能力必配評估器」,
同塊交付生成接線 + 自我品質閘。

## 缺口與關鍵

- (G-4'''') 讓 `gen_squash` 產耦合 shear+非均勻 scale,但 squash **不接檔位差異化**(honest boundary:
  「count-aware nosc 未接」)。
- squash **幅度**軸被不變量擋住:簽章是體積守恆(scaleX·scaleY≡1),`_amp_scale` 只放大 identity 上方
  → 積≠1 破守恆;故 squash 未進 `MAIN_SHOW_CATS`。
- **段數**軸不受此限:段數是關鍵幀拓樸,以該檔位 nosc 用 `gen_squash(nosc=k)` **重生成**。**crux**:
  重生成的包絡對**任意** nosc 天然守恆(`scaleX·scaleY=(1+q_i)/(1+q_i)≡1` 代數恆等)→ 段數增多不破守恆;
  幅度軸(放大 Q)才會離開這條恆等。

## 做了什麼(全 additive)

- **`tools/analyzer/tier_variants.py`**:`COUNT_AWARE_CATS += {squash}`;新增 `COUNT_ONLY_CATS = {squash}`
  (只以段數差異化、**不**套幅度增益);新增 `TIER_SQUASH_CYCLES`(Super4→Legend7)+ `squash_cycles_for`。
- **`tools/analyzer/gen_animations.py`**:`build_animations(..., tier_squash_cycles=None)`,`_count_maps`
  加 squash 路由;tier-variant 閘擴為 `cat∈MAIN_SHOW_CATS or cat∈COUNT_ONLY_CATS`,**count-only(squash)
  只重生成段數、g 不作用**,且僅在有段數宣告時才產變體(無 tsc → squash 不產任何變體,向後相容)。
- **`tools/analyzer/build_spine.py`**:`--tier-variants` 一併帶 `squash_cycles_for(genre)`。
- **`tools/analyzer/validate_squash_count.py`**(新,5 AC,復用 squash_gen/shear_gen 判準)。
- **`tools/check_readiness.py`**:新增 cap `squash_count_generation` L2 → `spine-anim-forge`(仍 HOLD)。
- **`knowledge/s1-squash-count-generation.md`** + 索引;圖 `knowledge/figures/s1_squash_count.png`。

## 結果(`validate_squash_count.py` 5 AC 全 PASS,exit 0)

- Q1 present+backward-compat:每檔位 `squash__{tier}` finite/有 bone/雙通道;base 恆 4 段逐位元不變;
  **無 tsc 時 squash 不產任何變體**;不改動非-squash beat。
- Q2 **crux** count monotone:段數 [4,5,6,7]==宣告嚴格遞增,Super==base(4)。
- Q3 signature preserved:每檔位仍 shear 阻尼簽章(首尾 0+變號≥3+極值遞減)**且體積守恆耦合逐檔位保持**。
- Q4 **crux honesty** amplitude-flat:峰 |shearX| 16°、峰非均勻 0.298 **跨檔位恆定** → 純段數軸;正交守衛通過。
- Q5 negative controls:平段數→單調 FALSE;slot_reveal→None 不亂加;段數只作用 squash 不外洩;非守恆守衛→體積 FALSE。

端到端 `build_spine --animate --tier-variants --shear-pivot` 直出 `squash__{Super4,Mega5,Omg6,Legend7}`,
`validate_build` round-trip **overall_pass**。

## 回歸(17 閘全綠)

squash_gen / wobble_count / wobble_tier / tier_combo_count / tier_variants / shear_gen / shear_pivot /
scale_pivot / pivot_rotation / cascade / priors / priors_beats / priors_combo_charge / priors_cascade /
more_beats / beat_templates / deform_gen 全 PASS(`check_readiness` 整份跑完 exit 0,新 cap GREEN)。

## 關鍵發現

- **結構(段數)軸與幅度軸可分離**:幅度軸受不變量(體積守恆)阻擋時,段數軸仍能提供檔位差異化,
  且**天然保持不變量**。分水嶺是**重生成 vs 事後放大**(重生成從天然守恆的參數化家族取新成員)。
- **count-aware 概念第三次落地**(combo=scale 峰數、wobble=shear 振盪段數、squash=體積守恆擠壓段數)。

## honest boundary / 下一步

- 段數階梯 PROPOSAL(手感留使用者 A 類);**幅度耦合 amplify 仍為 boundary**(squash 振幅跨檔位恆定);
  shearY≡0;單一真值資產 → `spine-anim-forge` 仍 HOLD。
- 下一候選(見 STATE):(G-4'''''a) squash **幅度**軸(耦合 amplify,與本次段數軸正交可疊)、
  (G-4'''''') shearY / 三通道同時、(J-3) cascade count-aware。S5→L3 仍待多 rig 真值(使用者資源)。
