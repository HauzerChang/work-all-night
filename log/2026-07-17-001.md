# 2026-07-17 — session 001

## 這次做了什麼(S3×S4 端到端「件 → mesh」對真實生產 mesh 驗收)

推進 STATE 最高優先 bounded chunk:把切件餵進 S3 生成器,對 Award 生產 spine 的 3 個 **mesh 件**
(光暈 / 身體 / 左手)做**靜態幾何**對照(這是 S3 首次有「真實生產 mesh 真值」可比)。

1. **確認 uvs 對齊**:Award mesh `uvs` 已是 region-local 0..1(×crop size 即對齊切件);
   實測藝術家 mesh vs atlas crop alpha IoU 0.968~0.980(校正 handoff「需轉 region」的顧慮)。
2. **新閘 `tools/mesh_gen/validate_psd_to_mesh.py`**:兩來源(atlas 切件 / PSD 切件)× 3 件,
   量生成 IoU、藝術家 baseline IoU(同 frame)、格式/預算。
3. **初跑 5/6 過**:唯一 fail = 光暈 atlas(gen 0.929 < 藝術家 0.980)。掃參數定位病根:
   **blobby(Delaunay-v1)件覆蓋率 IoU 由 hull 密度(`epsilon_frac`)決定**,固定粗 0.008 對大件 hull 太疏。
4. **修:budget-targeted auto-epsilon**:`generate_mesh.generate(target_vertices=N)` 由粗到細取
   「頂點 ≤ N 的最細 hull」;thread 進 `generate_mesh_v2`(僅 Delaunay fallback 生效)。
   以生產級預算(藝術家 78~98)為目標 → **6 組全 PASS,IoU 0.992~0.995 ≥ 藝術家基準**,頂點 73~95 同級。
5. **回歸**:main_draw 4 mesh 走 strip 模式、不受 v1 改動影響,可命名的 3 件重驗仍 PASS
   (shadow2 的 fail 是 region 命名共用問題,clean tree 相同,非本次回歸)。
6. **視覺佐證**:`knowledge/figures/psd2mesh_robot_wireframe.png`(藝術家黃 vs 生成綠 線框疊圖)。

## 關鍵結論 / 教訓

- S3 blobby 件 IoU ← hull 密度(epsilon);與 v2 strip「IoU ← rows」對應。生產級件頂點預算應以藝術家/目標為準。
- **誠實邊界**:這 3 件無 deform timeline(骨骼 + weighted vertices 驅動)→ real-deform 閘不適用,
  只驗靜態;**S3 只產 unweighted → 缺 BBW 權重**,尚不能取代骨骼驅動的生產件。**下一步定為 S3 加權重**。

## 產出

- 新增 `tools/mesh_gen/validate_psd_to_mesh.py`、`knowledge/s3-psd-to-mesh-real.md`、
  `knowledge/figures/psd2mesh_robot_wireframe.png`
- 改 `tools/mesh_gen/generate_mesh.py`(auto-epsilon)、`generate_mesh_v2.py`(target_vertices thread)
- 更新 `knowledge/README.md`、`STATE.md`、本 log

## 下一步(見 STATE.md)

S3 加 BBW/骨骼權重(用 Award 的 weighted mesh 真值當對照),把 S3 從靜態拓樸推進到可對接骨骼驅動件。
