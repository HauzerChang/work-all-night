# 2026-07-17 — session 001

## 這次做了什麼(S3 端到端對真實生產 mesh 驗收 — STATE 最高優先候選 #1)

推進「PSD 件 → S3 mesh → 對照 Award 真實生產 mesh」的端到端整合 AC(有藝術家真值可比,純 CPU 自驅)。

1. **定位對象**:`Award.json` 掃描確認機器人 3 件是 mesh 且**皆 weighted、無 deform timeline**
   (光暈 78v/hull78、身體 98v、左手 80v);另發現 4 個非機器人角色 mesh(OMG/megawin×2/superwin)。
   atlas 確認 光暈/身體 為 `rotate:true`、左手 `rotate:false`。
2. **寫端到端閘** `tools/mesh_gen/validate_against_award.py`:`psd_slice` 切乾淨 alpha → `generate_mesh_v2(auto)`
   → 生成 mesh 與 Award 藝術家 mesh **各自對切件 alpha 算覆蓋 IoU**。判準:座標 sanity(藝術家 self-IoU≥0.80)
   + 生成 IoU ≥ 藝術家 self-IoU−0.02 + 格式/無退化/無孤兒。
3. **結果 3/3 PASS**:生成 IoU 0.933/0.966/0.964 vs 藝術家 self-IoU 0.949/0.948/0.977;頂點 35/60/59 vs 78/98/80。
4. **視覺自驗**:`knowledge/figures/award_robot_generated_vs_artist.png`(綠=生成、黃=藝術家)。

## 關鍵結論 / 決策

- **座標慣例對 rotate:true 區域成立**:Spine JSON mesh uvs 存於未旋轉 logical 區域;以 self-IoU 0.95 先驗
  慣例正確(避免重演 atlas_crop derotate 同類方向陷阱)。
- **auto 分流正確**:blob 件(光暈/身體/手)自動走 v1 Delaunay,不誤用 strip;印證「strip=窗簾、v1=blob」分工。
- **誠實邊界**:這 3 件無 deform timeline → 本閘只驗**靜態拓樸/覆蓋**,未驗 blob 變形穩健
  (S3 變形穩健由 main_draw 窗簾的 `validate_against_real` 另行驗過)。結論限定為「靜態達藝術家水準」。
- **thin-protrusion 覆蓋**是 v1 唯一明顯短板(光暈右上細突起,IoU 0.933 最低)→ 列為下一步改進候選。

## 產出

- `tools/mesh_gen/validate_against_award.py`
- `knowledge/s3-award-real-mesh-validation.md`(+索引)、`knowledge/figures/award_robot_generated_vs_artist.png`
- 更新 `STATE.md`、本 log

## 下一步(見 STATE.md)

(1) 切圖→Spine JSON 組裝 SkelToJson(把驗過的生成 mesh + 命名/padding/縮放慣例固化,端到端產可載入 JSON);
(2) v1 加高曲率邊界自適應加點,提升 blob thin-protrusion 覆蓋後對光暈重驗。
