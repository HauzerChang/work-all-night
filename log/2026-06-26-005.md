# 2026-06-26 — session 005

## 這次做了什麼(S4 真實驗收 + PSD→spine 對應 + 閘校正)

使用者提供 2 份真實生產 PSD(`Symbol_Ww` symbol、機器人拆件 big win 主角)+ 機器人對應的
真實 spine(`Award.json/atlas`)。

1. **探查真實 PSD**:Symbol_Ww 180×180/18 圖層;機器人 713×693/5 圖層;皆扁平(無群組)、中文圖層名、
   RGB、全可見;Symbol 有 opacity=153 的陰影層。
2. **抓修評估器 miscalibration(第三次)**:初測重組 MAE 30/99.99「失敗」→ 查出 `composite()` 透明區填白、
   重組填黑,在 alpha=0 的無意義 RGB 上比對造成**假性失敗**。改用 **premultiplied-alpha 比對** +
   reassemble **套圖層 opacity**。校正後三檔 premult_rgb 0.01~0.03、alpha 0 → **全 PASS**;
   負對照(漏件)premult_rgb 11~20 仍抓到 → 未過頭。
3. **PSD→spine ground truth 對應**:Award 有 slot `機器人拆件/{光暈,右手,頭,身體,左手}`,**一對一**對應
   PSD 5 圖層;size 全 +2px(atlas padding)。揭示真實慣例:**slot = `<PSD檔名>/<圖層名>`**;
   會 warp 的件(光暈/身體/左手)做 mesh、剛體(右手/頭)用 region+旋轉;5 件無 deform(靠骨骼)。
4. **收檔 + 校準契約**:Award.json/atlas + 2 PSD 收進 assets/;`s4-psd-contract.md` 以真實慣例校準
   (命名、扁平、opacity、mesh/region 分配、premult 驗收);新增 `s4-psd-to-spine-real.md`。

## 關鍵結論

- **S4 從合成驗證 → 真實驗收通過**(里程碑):切圖 pipeline 對真實生產 PSD 可用,且切件 size 與生產 spine 吻合。
- **真實命名慣例 = `<PSD檔名>/<圖層名>`** → 直接指導未來「件→Spine JSON」組裝。
- **評估器第三次 miscalibration**(前:stress_field、本次:composite 白底)→ 再次印證 RULES「閘要先校準+負對照」。

## 產出

- 改 `tools/mesh_gen/psd_slice.py`(premult 比對 + opacity)
- 新增 `knowledge/s4-psd-to-spine-real.md`;校準 `s4-psd-contract.md`;更新索引/STATE
- `assets/`:Award.json、Award.atlas、Symbol_Ww.psd、robot_parts.psd
- 本 log

## 下一步(見 STATE.md)

(1) 用機器人 3 個 mesh 件跑 S3 `generate_mesh_v2`,對照 Award 真實 mesh(光暈/身體/左手)做 IoU/deform
—— 端到端「PSD→件→mesh」對真實標的驗收(有真值、純 CPU 可自驅)。Award.png 若之後拿到可再做 texture 驗。
