# 2026-07-06 — session 002

## 這次做了什麼(S4 補圖能力 + 補圖閘 — 使用者聚焦)

使用者要求推進補圖(B),並定義工作流:**切圖定出區塊 → 被上層遮住的下層在被遮區補圖**
(例:「耳機右罩」切出後,其下方身體需補足)。提供新資產 `dj_cat_ai_final.psd`(zip)。

按 RULES「每能力必配評估器」,本有界塊 = **先立補圖閘 + CPU 補圖基線,並校準可信**。

### AC(動手前定義)
1. 補圖閘能對「好補圖 vs 負對照」機讀分辨(破洞 / 接縫)。
2. 評估器先校準(合成遮擋+真值)且負對照抓得到,才可信(RULES;此專案已多次 miscalibration)。
3. 在真實 dj_cat 上端到端跑通使用者工作流。

### 產出
- `tools/mesh_gen/inpaint.py` — CPU 補圖(telea/ns/extend)+ `occlusion_mask`(真洞偵測)。
- `tools/mesh_gen/inpaint_eval.py` — 補圖閘(hole_fill + seam_ratio;PSNR 僅校準)+ `--calibrate`。
- `knowledge/s4-inpaint-evaluator.md` + 索引;STATE;本 log。
- 視覺 demo 給使用者:dj_cat「頭 蓋 軀幹」2543px 洞 → 補後(hole_fill=1.0/seam=0.0 PASS)。

### 結果
- 補圖閘對 **flat(dj 軀幹)+ textured(robot 身體)兩畫風** 皆 `discriminates=True`:
  telea/ns/extend PASS(seam 0.64~0.76);noop/flat/noise FAIL(各在對的軸:破洞 / 接縫)。
- 門檻(校準得出):hole_fill≥0.999、seam_ratio≤1.5、PSNR≥11(校準用)。
- 端到端 dj_cat「頭 蓋 軀幹」2543px 破洞 telea 補通、閘 PASS。

## 關鍵發現 / 校準教訓
- **seam 分母不可用全域紋理**:洞在局部平坦區時,全域中位數被層內強邊界灌高、或平坦層中位數=0 → 除爆
  (實測 telea seam=2.6e7)。改用「洞周邊 3~15px 已知窄環局部紋理 + JND_FLOOR 40」→ 兩畫風乾淨分開。
- **`psd.composite()` 對 dj_cat 回傳全不透明**(content px 恰=772×427)→ psd_slice 的 AC3 孤兒率
  把整片空背景誤判為未覆蓋 → 假性 56% 失敗;**但個別切圖是對的**(AC2 premult 重組 0.50 通過)。第 5 坑。
- **使用者原例實測**:「耳機右罩→軀幹」僅 2px 洞(AI PSD 已把軀幹在耳機下畫全)→ 該對幾乎不需補;
  真正熱點在 頭→軀幹 / DJ台體→軀幹 / 鏡框→鏡片 等。

## 下一步(見 STATE.md 候選 2b)
- 硬化 psd_slice AC3(全不透明 composite 的孤兒定義)。
- grow 校準(補多遠才夠,對應動畫最大位移)。
- 高頻紋理大缺口:telea psnr~13,評估升 LaMa/GPU 的觸發點。
