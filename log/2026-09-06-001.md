# 2026-09-06 run 001 — S1 candidate (H):combo/charge 接進 genre 先驗庫

分支:`claude/spine-main`(依 `prompts/run.md` 步驟 0 釘固定分支;本 session 容器起於隨機
`claude/focused-dirac-sh3srw`,已 checkout `claude/spine-main`(起點 ab753ab=G-3)續累積,避免重做/增生分支)。

## 這次做了什麼(一個有界工作塊)

從 STATE「下一步動作」建議 **(H)**:如 candidate (E) 對 hit/reveal 所做,把 candidate 0g 的
**combo(連擊)/ charge(蓄力充能)** 主秀節拍**接進 genre 先驗庫**,讓 `build_spine --animate
--genre slot_bigwin` 直接輸出這兩個節拍。依 RULES「每能力必配評估器」,同塊交付**先驗庫接線 +
自我品質閘 + 內建負對照**,AC-first。

**診斷(缺口)**:接 (H) 前 `slot_bigwin` 先驗 beats 為 `In / burst / hit / Loop / Out`
(burst/hit 由 (E) 加入),**完全沒有 combo/charge** → 0g 的 combo/charge 模板對產線**從未被觸發**。
這是「評估器/模板就緒 ≠ 生成器接上」的又一次再現(0g 只在合成 fixture 驗模板本身)。

## 交付

- **`tools/analyzer/genre_priors.py`**(additive):`slot_bigwin` 新增 `combo`/`charge` beat +
  `_BIGWIN_ROLES` 對應 role 描述。beat key 即 `beat_category(name)` 的輸入
  (`combo`∈COMBO_KEYWORDS、`charge`∈CHARGE_KEYWORDS)→ 路由到 `gen_combo`/`gen_anticipate_hold`。
- **`tools/analyzer/validate_priors_combo_charge.py`**(新,5 AC):從**先驗庫**經
  `analyze_target.build_storyboard`(真實 robot 5 拆件 role)→ `build_animations`,證 combo/charge
  真的從先驗流到最終 animations。度量復用 `validate_more_beats`(判準一致)。
- **`tools/analyzer/validate_more_beats.py`**(強化):`has_charge_signature` 加 **squash-floor**
  (見下「關鍵發現」)。
- **`tools/analyzer/validate_priors_beats.py`**(E 閘連帶更新):負對照 P5(b) 用 `ALL_MAIN_SHOW_CATS`
  (combo/charge/cascade 依設計共享 anticipation+settle 簽章,不可列 offender)。
- `tools/check_readiness.py` 加 cap `combo_charge_priors_integration` L2;`skills/READINESS.md` 快照更新。
- `knowledge/s1-combo-charge-priors-integration.md` + 圖 `knowledge/figures/s1_combo_charge_priors.png`。

## 結果(`validate_priors_combo_charge.py` 5 AC 全 PASS,exit 0)

| AC | 內容 | 實測 |
|---|---|---|
| H1 | present+routing:clip 路由到 combo/charge 類別且真峰 ≥1.12 | combo 1.347 / charge 1.347 ✅ |
| H2 | 介面契約:combo/charge clip 首尾 setup identity(可插 Loop 間) | ✅ |
| H3 | 結構簽章:combo 遞增 impact 峰≥3、charge 峰前長蓄力≥0.35,**兩簽章互斥** | combo 峰 [1.207,1.28,1.347]、charge holdfrac 0.456 ✅ |
| H4 | coverage 保留:validate_priors 覆蓋率仍 1.0(combo/charge=prior_beats_unused) | 兩 genre 1.0 ✅ |
| H5 | 負對照:idle 產 0 combo/charge clip、非 combo/charge beat 無其簽章 | ✅ |

`build_spine --animate --genre slot_bigwin` → animations = In / burst / hit / **combo** / **charge** / Loop / Out。

## 關鍵發現:H5 逼出 charge vs reveal 的鑑別子(閘找出真實漏洞)

H5(b) 初跑 **FAIL**:`burst`(reveal 類別)被 `has_charge_signature` 誤判為 True。根因:
**charge 的 squash-hold 與 reveal 的 collapse-hold 峰前皆長時間低於 0.97** —— 原 charge 簽章只看
「峰前 <0.97 的時間佔比 ≥0.35」,分不出「壓縮蓄力」(squash ~0.85)與「塌陷藏起」(collapse ~0.02)。
修法=加 **squash-floor**:峰前**最低** scale 須 `>0.50`(charge >floor 通過、reveal <floor 排除)。
本質差異的量化:**windup 是壓縮,不是消失**。0g 閘回歸仍 6 AC 全 PASS(anticipate_hold squash 0.85 在 band)。

連帶:(E) 閘 P5(b) 假設「非 hit/reveal beat 皆無 hit 簽章」,但 combo/charge 依設計**共享**
anticipation+settle 的 `_hit_signature`(0g M5),故 (E) 閘負對照須把 combo/charge/cascade 也視為主秀類別
排除(新增 `ALL_MAIN_SHOW_CATS`)。**加 beat 到共享 genre → genre 級的閘須同步更新其「主秀類別」定義**。

## 回歸(全綠)

validate_priors(cov 1.0、unused=['burst','hit','combo','charge'])、validate_priors_combo_charge(5AC)、
validate_priors_beats(E,5AC)、validate_more_beats(0g,6AC,squash-floor 後)、validate_beat_templates(0f)、
validate_cascade(0h)、validate_anim(+selftest)、round-trip validate_build、`--pivot-rotate`/`--scale-pivot`
build + 0i/G-3 閘 全 PASS。

## 誠實界定 / honest boundary

- 主秀運動仍先驗手感(無唯一正解),閘驗**客觀結構簽章 + 介面契約 + 負對照**,非美感;連擊次數/蓄力時長/彈幅
  屬美術微調(A 類)留使用者。
- combo/charge 於 Award 無命名真值 → `prior_beats_unused`(PROPOSAL);單一真值資產。
- cap `combo_charge_priors_integration` L2 併入 `spine-anim-forge`,**區塊仍 HOLD**(運動基元先驗、單一真值資產,
  防固化;達 L3/打包屬 C 類使用者拍板)。

## 下一步(見 STATE「下一步動作」)

(H) 完成。續(擇一,皆自主):(I) cascade 也接進先驗庫(slot_bigwin 加 sweep/wave beat,驗 phase threading 端到端);
(J) tier 變體幅度差異化(Super→Legend 遞增);(G-1) rig×pivot per-bone 去重;(G-2) 主秀 beat limb 繞關節 AC;
(G-4) shear/非均勻 scale 仿射保形 AC。S5→L3 仍待 (D) 多 rig 真值(使用者資源)。
