# 2026-08-28 — session 002

## 這次做了什麼(使用者決策:S4 拆為獨立排程)

使用者要把 **S4(切圖+補圖)** 交給另一條排程單獨研究。完成交接文件 + 更新研究項目/排程規劃。

### 1. 交接文件(新增 3 檔)
- `handoff_S4.md` — S4 冷啟動交接:範圍界定(切圖✅ / 補圖⬜=主任務)、補圖難度降階分級
  (邊緣外擴→cv2.inpaint→LaMa→GPU/人工)、現成工具/知識/資產、**先做補圖閘再補圖**、第一個有界工作塊、
  升級停止政策、**檔案隔離契約**(S4 只寫 `STATE_S4.md`/`log/s4-*`/`knowledge/s4-*`/`tools/`)。
- `prompts/run_s4.md` — S4 排程每次執行指令(先切 `claude/spine-s4-inpainting` 分支、隔離契約、收尾 push 回該分支)。
- `STATE_S4.md` — S4 續跑狀態(狀態 `SETUP`;繼承切圖成果;下一步 = chunk 0 補圖閘 v1 + 邊緣外擴 baseline)。

### 2. 更新研究項目 / 排程規劃(主排程檔)
- `STATE.md`:標記「S4 已交接給獨立排程,主排程不再推進 S4」;候選 4 的「S2 補圖閘」隨 S4 移出(只留骨架閘)。
- `PLAN.md`:S4 節標「🔀 拆為獨立排程」+ 指向 3 個 S4 檔。
- `SCHEDULE.md`:新增「🔀 兩條並行 Routine」表(主研究 vibrant-franklin-bykdbi / S4 spine-s4-inpainting)、
  為何要分兩分支(避免 clone-from-default 互相覆蓋)、S4 Routine 建立步驟。

## 排程分工(交接後)
| Routine | 範圍 | 分支 | 指令 | 狀態 |
|---|---|---|---|---|
| 主研究 | S1/S2骨架閘/S3/S5 | `claude/vibrant-franklin-bykdbi` | `prompts/run.md` | `STATE.md` |
| S4 | 切圖+補圖 | `claude/spine-s4-inpainting` | `prompts/run_s4.md` | `STATE_S4.md` |

## 使用者待辦(需在 web UI 操作,session 內無法建 Routine)
- 在 claude.ai/code/routines 新建 S4 Routine,Prompt=「請依照 prompts/run_s4.md 的指示推進 S4…」。
- 確保 `claude/spine-s4-inpainting` 分支 + 這批檔進得了 S4 Routine 的 clone(即在 default branch,或首次執行自建分支)。

## 主排程下一步(不變)
S3 候選 2:自產 mesh 配 BBW/骨距權重 → 套 `weighted_deform.py` 變形閘,對照美術平滑度。
