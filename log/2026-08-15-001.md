# 2026-08-15 — session 001

## 這次做了什麼(S3+S4 端到端閉環,對真實生產標的驗收)

依 STATE.md 最高優先 bounded chunk:把 S4(PSD 切圖)與 S3(mesh 生成)串成端到端,
並用**真實生產 spine(Award)的機器人 mesh**當真值。

1. **新工具** `tools/mesh_gen/validate_psd_to_award.py`:
   `robot_parts.psd` 切件(psd_slice)→ `generate_mesh(_v2)` 生成 mesh →
   ① 生成 mesh 覆蓋率 vs ② Award 藝術家 mesh 覆蓋率(同一 N×N 正規化框比 IoU)。
2. **座標對齊自證**:Award mesh uvs 對件 alpha 做 8 朝向(4 旋轉×水平翻)搜尋最佳對齊。
3. 生成證據圖 `knowledge/figures/psd-to-award-mesh.png`(生成 mesh 線框疊切件 alpha)。

## AC 與結果(全 PASS)

AC:生成 mesh 覆蓋率 ≥ 藝術家 mesh 覆蓋率 − 0.03,且格式/無孤兒頂點。

| 件 | 生成 | gen_iou | 藝術家 | artist_iou | pass |
|---|---|---|---|---|---|
| 光暈 | 35v/49t | 0.9244 | 78v | 0.9393 | ✅ |
| 身體 | 60v/97t | 0.9599 | 98v | 0.9435 | ✅ 反超 |
| 左手 | 59v/97t | 0.9491 | 80v | 0.9768 | ✅ |

`validate_psd_to_award.py --gen v2` → exit 0(auto 三件皆退回 delaunay-v1,件為 blobby)。

## 關鍵結論

- **里程碑:S3+S4 對真實生產標的閉環成立** — 端到端由 PSD 產 mesh,覆蓋率達藝術家等級、頂點更少。
- **★ 新發現:Spine JSON mesh `uvs` 存「上正立作圖框」,非 atlas 旋轉框**(朝向自證 3 件全 rot0,
  含 atlas `rotate:true` 的光暈/身體)。runtime 靠 atlas `rotate` 旗標處理旋轉;JSON uvs 與打包旋轉/
  縮放無關 → 未來 SkelToJson 可直接 `u·W,v·H` 疊上正立切件。修正舊「uvs 為 atlas UV」的模糊描述。
- **變形穩健度(附加,非門檻)**:光暈/身體轉移真實窗簾位移場乾淨;左手 blobby Delaunay 極端拉伸
  自交(si=10/flip=3)。左手實無 deform timeline,不影響驗收;標記 blobby 大 deform 需補強。

## 產出

- 新增 `tools/mesh_gen/validate_psd_to_award.py`、`knowledge/s3s4-psd-to-award-endtoend.md`(+索引)、
  `knowledge/figures/psd-to-award-mesh.png`。
- 更新 `STATE.md`(下一步 + 進度摘要)、本 log。

## 下一步(見 STATE.md)

SkelToJson 組裝工具(由 PSD 直接產可載入 Spine JSON,達成 PLAN S3「寫入 Spine JSON」完成條件)。
