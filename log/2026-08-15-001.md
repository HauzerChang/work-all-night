# 2026-08-15 — session 001

## 這次做了什麼(S3×S4 端到端:PSD 件 → 生成 mesh → 對照 Award 真實生產 mesh)

推進 STATE 最高優先 bounded chunk:用 `robot_parts.psd` 的 3 個 mesh 件跑 S3,對照 Award 生產 spine 真值。

1. **切件**:`psd_slice` 切 robot_parts.psd → 光暈/身體/左手 PNG(件 alpha 當來源真值)。
2. **新工具 `validate_psd_to_award.py`**:件 → `generate_mesh_v2(auto)` → ①靜態 IoU vs 藝術家 mesh 基準
   ②`evaluate_mesh` 靜態 AC。含結構偵測(weighted? has_deform?)。
3. **AC1_iou 迭代(1 輪)**:初測 2/3 件 IoU 比藝術家低 ~1.3–1.6%(光暈/左手)。查因:v1 預設
   `epsilon_frac=0.008`(為合成小圖調)對真實件太粗,hull 僅 16–20 點。掃 epsilon → **0.002** 三件全過,
   頂點數(64–84)仍與藝術家(78–98)同量級。改 v1 預設 + v2 透傳 epsilon。
4. **回歸**:main_draw 4 mesh(strip 模式)`validate_against_real --gen v2` 全 `overall_pass` 不變
   (strip 不走 epsilon,不受影響)。

## 驗收結果(全 PASS)

| 件 | 生成 verts/IoU | 藝術家 verts/IoU | margin |
|---|---|---|---|
| 光暈 | 64 / 0.9796 | 78 / 0.9486 | +0.031 |
| 身體 | 77 / 0.9908 | 98 / 0.9477 | +0.043 |
| 左手 | 84 / 0.9901 | 80 / 0.9768 | +0.013 |

`validate_psd_to_award.py` overall_pass=true(exit 0)。

## 關鍵結論 / 教訓

- **結構事實(定調下一步)**:Award 這 3 件是 **weighted(1~2 骨/頂點)+ 無 deform timeline** —— 形變靠
  骨骼權重,不是 deform。S3 的 deform 閘對這類件不適用;要逼近它們 **S3 還缺 BBW 權重生成**(已列路線圖)。
  geometry-only ≠ 生產可用 rig。
- **UV 更正**:Award mesh uvs 是 region 局部 0..1(非整頁 atlas UV)→ 更正 session006 的假設。
- **調參尺度不變性**:epsilon_frac 是周長比例,0.002 對不同尺寸件皆收斂。

## 產出

- 新增 `tools/mesh_gen/validate_psd_to_award.py`
- 改 `tools/mesh_gen/generate_mesh.py`(epsilon 預設 0.008→0.002)、`generate_mesh_v2.py`(透傳 epsilon)
- 新增 `knowledge/s3-psd-to-award.md`;更新索引、STATE;本 log

## 下一步(見 STATE.md)

S3 加 **BBW 權重生成 + 權重品質閘(S2 樞紐)**:把已通過的 unweighted 幾何綁到骨,對照藝術家 weighted mesh
(權重和=1、影響骨數、平滑度)驗收。或先做「件→Spine JSON 組裝(SkelToJson)」固化命名慣例 + 2px padding。
