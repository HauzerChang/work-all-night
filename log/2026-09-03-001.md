# 2026-09-03 run 001 — S1 candidate 0g:主秀 beat 併入 genre 先驗庫

分支:本 session 由排程容器起於 `claude/focused-dirac-st5m9l`(harness「Git Development Branch
Requirements」指定的開發分支,且明令「未經允許不得 push 到其他分支」)。此分支與 `claude/spine-main`
同 commit 基底(含 0f),linear 累積。⚠️ **註**:`prompts/run.md`/`SCHEDULE.md` 的路線是釘 `claude/spine-main`;
本 session 因 harness 指定,成果落在 `claude/focused-dirac-st5m9l`。若要維持單一主線,使用者/下次 routine
可把本分支 fast-forward 併回 `claude/spine-main`(內容線性、無分叉)。

## 這次做了什麼(一個有界工作塊)

從 STATE「下一步動作」建議 **(E):主秀 beat 接進 genre 先驗庫**。candidate 0f 造了主秀模板
`gen_hit`/`gen_reveal`,但只用 `validate_beat_templates.py` 的**手搭 storyboard**(beat 名硬寫
`"hit"`/`"reveal"`)驗過。真正的 `build_spine --animate` 走 **genre 先驗庫的 beats**,而
**`slot_bigwin` 先驗只有 In/Loop/Out → 端到端沒有任何主秀 payoff**。本次把主秀節拍併進先驗庫:

- **顯式 `cat`(運動類別)欄位**:`genre_priors.PRIORS[*]["beats"][*]` 加可選 `"cat"`,直接宣告走哪個
  `gen_animations._DISPATCH` 類別,取代靠 beat 名關鍵字猜測(脆弱:`'burst'∈REVEAL_KEYWORDS`)。
  串接:`analyze_target.build_storyboard` 帶 cat(存在才帶,向後相容);
  `gen_animations.build_animations` 改 `cat = beat.get("cat") or beat_category(name)`(顯式優先)。
- **`slot_bigwin` 加 Burst(cat=hit)payoff**:接在 In 後、Loop 前(演出 In/Loop/Out → **In→Burst→Loop→Out**)。
  用 `gen_hit`(首尾 identity)→ 可無縫插在 In 與 Loop 之間。**不用 reveal**:reveal 起 collapsed 適合當入場、
  Burst 是中段 payoff 須起 identity → 正是需要顯式 cat 覆蓋 `beat_category("Burst")=='reveal'` 誤判之處。
- **`slot_reveal` open/hit 顯式標** reveal/hit(原靠關鍵字碰巧命中,現穩健)。
- **`tools/analyzer/validate_mainshow_wiring.py`**(新):走 build_spine --animate 相同路徑的 6 AC 端到端閘。
- `check_readiness.py` 的 `spine-anim-forge` 區塊新增 cap `mainshow_wiring`(L2)。

## 結果(`validate_mainshow_wiring.py` 6 AC 全 PASS,exit 0)

| AC | 判準 | 結果 |
|---|---|---|
| M1 bigwin_payoff | slot_bigwin 端到端產「宣告 cat==hit」的 beat(Burst)且具 hit 簽章(真峰≥1.12+反向預備+(scale−1)變號≥3) | ✅ |
| M2 reveal_mainshow | slot_reveal 產 open(reveal 簽章)+ hit(hit 簽章),皆對應 main_draw 真值 anim | ✅ |
| M3 cat_drives_dispatch | Burst cat=hit 起 identity;拔 cat 走關鍵字('burst'→reveal)起 collapsed;兩路徑相異 | ✅ |
| M4 chainable_interface | Burst 首尾 identity;open 首 collapsed 尾 identity | ✅ |
| M5 discriminative | In/Loop/Out 不具主秀 hit 簽章;stripped(拔 cat+Burst)→ 無任何 hit/reveal 類別 beat | ✅ |
| M6 regression | validate_priors(覆蓋 1.0)+ validate_beat_templates 仍 overall_pass | ✅ |

回歸(未破壞既有):`validate_anim --selftest`(對 `build_spine --animate` 產物,現含 Burst,In→Burst→Loop→Out)
4AC + AC5 負對照全 **PASS**;`validate_deform_gen` 7AC、`validate_build` round-trip、`validate_priors` 兩 genre
覆蓋率 1.0 皆 PASS。

## 關鍵發現

- **顯式 cat > beat 名關鍵字(解耦)**:M3 客觀證明——同一 beat 名 `"Burst"`,關鍵字路徑判 reveal(起 collapsed,
  破壞中段插入),顯式 cat 判 hit(起 identity,正確)。beat 名與運動類別本該解耦。
- **鑑別靠結構簽章、非單看 peak**:In/comeout 的特效件 intro scale 也可達 1.25(接近 hit 峰);`_hit_signature`
  (峰 + 反向預備 dip + (scale−1) 變號≥3)才穩定分開主秀 hit 與一般入場(一般入場 0.02→峰→1 只 1 次穿越 → FAIL)。

## 誠實限制

- **honest boundary**:`slot_bigwin` 真值 Award 只有 `{tier}_{In,Loop,Out}` 12 支——payoff 收在 In 一支內、
  無獨立 anim。故 Burst 是**可復用主秀模板提案**,非 Award 觀測節拍:`validate_priors` 列 `prior_beats_unused`
  (資訊、非 fail),覆蓋率仍 1.0。相對地 `slot_reveal` 的 open/hit 有 main_draw 真值 anim 支撐(已驗證主秀)。
- 緩動幅度/手感屬美術(A 類),閘只驗客觀結構簽章。
- `spine-anim-forge` 仍 **HOLD**(運動基元先驗、單一真值資產,防固化;達 L3 前不打包)。

## 產出檔案

- 新增:`tools/analyzer/validate_mainshow_wiring.py`、`knowledge/s1-mainshow-wiring.md`。
- 更新:`tools/analyzer/genre_priors.py`(beat 加 cat + slot_bigwin Burst beat/roles + slot_reveal 顯式 cat)、
  `tools/analyzer/analyze_target.py`(build_storyboard 帶 cat)、`tools/analyzer/gen_animations.py`(顯式 cat 優先分派)、
  `tools/analyzer/validate_analyzer_award.py`(④ 分鏡結構 exact-equal → subset:重現 Award 全節拍 + 透明列額外模板節拍;
  鑑別力保留,實測 missing-Out→FAIL)、`tools/check_readiness.py`(cap `mainshow_wiring`)、`STATE.md`、`knowledge/README.md`。

## 連帶調整(誠實記錄:動了一個真值閘)

slot_bigwin 加 Burst → 分析器對 Award 提的 beats={In,Burst,Loop,Out} 與 Award 觀測 {In,Loop,Out} 不再完全相等,
`validate_analyzer_award ④` 原 exact-equal FAIL。放寬為 **Award ⊆ 提案**(重現保真 + 透明 `extra_template_beats`),
缺任一真值節拍仍 FAIL(鑑別力保留)。額外 beats 皆來自 curated genre_priors,非分析器憑空造。放寬後該閘 5 項全 PASS。
check_readiness 全區塊跑完 exit 0,新 cap `mainshow_wiring` GREEN。

## 下一個 session 接手

從 STATE「下一步動作」:**(G) S1 (e) 關節 pivot 推斷接 keyframe(最高槓桿:接 S5 接觸縫 pivot,件繞真實關節轉,
可用 rig vs 非rig 縫撕裂量量化)**、(F) 更多主秀節拍(anticipate-hold / multi-hit combo / cascade,可續用顯式 cat 併入先驗)。
S5→L3 仍待 (D) 多 rig 真值(使用者資源)。
