# 2026-09-03 run 001 — S1 candidate 0g:big-win 連擊 / rollup 主秀 beat 模板(gen_combo)

分支:`claude/focused-dirac-dqhlc3`(harness 指定的固定開發分支;容器 clone 即在此分支、與最新研究線同 commit
`b2bbca0`=candidate 0f)。依 harness「Git Development Branch Requirements」在此分支累積並 push(取代 repo 內
`spine-main` 指示;此為固定分支,不會造成分支增生)。

## 這次做了什麼(一個有界工作塊)

從 STATE「下一步動作」建議 **(F) 更多主秀節拍** 挑 **multi-hit combo**。動手前先做定位分析,順帶界定另兩條建議路線:

- **(E) 主秀 beat 接進 genre 先驗庫**:實查發現 `slot_bigwin` 真值 `Award` 只有 `<Tier>_In/Loop/Out`
  (climax = 進場本身,**無獨立 hit 真值**);`slot_reveal` 的 `open`/`hit` beat 又**已**分別 route 到 gen_reveal/gen_hit
  (dict 順序 reveal 先於 intro,"open" 命中 reveal)。故「把 hit 加進已驗 slot_bigwin」= 注入無真值覆蓋的 beat、
  違反「勿動已驗先驗」。→ 結論:新主秀節拍**只接 UNVALIDATED genre**;(E) 收斂,不再視為待辦。
- **(G) 關節 pivot 接 keyframe**:`build_spine --rig --animate` 本就組合成立(生成 limb rotate 掛在關節骨 → 繞關節轉);
  但用 probe 實測「繞關節 vs 繞件中心」的位移比**與角度無關**(位移 ∝ 2sin(θ/2),ratio 中約掉)→ 等同
  `validate_rig_build` AC4 已證的 pivot 置放性質,新 gate 只是再證既有保證,價值低。→ 不優先。

→ 選定 **(F) combo**:一個真正新增、可客觀量化、且不動已驗先驗的能力。

## 交付(生成器 + 品質閘 + 負對照,依 RULES「每能力必配評估器」)

- **`tools/analyzer/beat_templates.py`**:新增 `gen_combo(role, side_sign, radial)` + `COMBO_KEYWORDS` + `DUR["combo"]=1.2`。
  N=`_COMBO_N`(3)次擊,每擊 `(peak-1)·_COMBO_DECAY^k`(0.62)→ **峰值遞減**(follow-through);
  每擊 `蓄力 dip(<1)→ peak_k → 回落 ~1.0`,擊間回 identity 附近(獨立擊),**首尾皆 setup identity**(可插 Loop 間/串接)。
  role-aware:limb 每擊甩鞭(遞減、首尾 0)、特效 每擊亮度閃(暗蓄→亮、遞減)、body/head 只 scale。
- **`tools/analyzer/gen_animations.py`**:註冊 `_DISPATCH["combo"]` + `_CAT_KEYWORDS`(主秀類別置前)。
- **`tools/analyzer/genre_priors.py`**:只在 **UNVALIDATED 的 `slot_symbol`** 加一個 `combo` beat
  → `build_spine --animate --genre slot_symbol` 端到端輸出 `land/idle/win/**combo**`。**未動已驗 slot_bigwin/slot_reveal**。
- **`tools/analyzer/validate_combo_template.py`**(新):C1–C6 閘,對**真實 robot 5 拆件+role** 端到端經 `build_animations`。
- **`tools/check_readiness.py`**:`spine-anim-forge` 區塊新增 cap `storyboard_combo_beat`(L2)。

## 自我驗收(C1–C6 全 PASS)

| AC | 內容 | 結果 |
|---|---|---|
| C1 well-formed | finite / 時間嚴格遞增 / round-trip | PASS |
| C2 chainable IF | combo 首尾皆 setup identity | PASS |
| C3 multi-peak | 每 bone scale ≥2 獨立正峰(5 件皆 **3 峰**) | PASS |
| C4 decaying peaks | 峰值嚴格遞減(身體 [1.28,1.174,1.108]) | PASS |
| C5 separated+antic | 擊間 troughs 回落 identity 附近 + 每擊 anticipation 蓄力 | PASS |
| **C6 negative control** | 單擊 hit·對稱脈衝→FAIL C3;**等幅重複·遞增→FAIL C4**;真 combo 具簽章 | **PASS(5/5)** |

**關鍵**:結構簽章 = **多峰 ∧ 遞減**。「等幅重複脈衝」通過 C3(多峰)卻被 C4(遞減)攔下 —— 正是「遞減」這個
follow-through 簽章的鑑別價值。峰偵測 `peak_segments`:`(scale-1)` 連續超過 PROM 的每段取極大為一峰、段間須回落
→ 保證峰**獨立**(長平台只算 1 峰,使「等幅重複」也能被抓到多峰再由遞減過濾)。

## 回歸(全 PASS)

- 0f `validate_beat_templates`(B1–B6)PASS
- 0d `validate_anim --selftest`(AC1–4 + 負對照)PASS
- 0e `validate_deform_gen`(OVERALL_PASS)PASS
- `validate_priors`(overall_pass)PASS(slot_symbol 屬 unvalidated 略過;prior_beats_unused 本非 fail 條件)
- 端到端 `build_spine --animate --genre slot_symbol` → combo 5 bones/1 slot 正常生成

## 誠實界定 / 下一步

- 連擊次數、衰減率、峰值、緩動幅度屬手感(A 類,留使用者微調);閘只驗結構簽章。
- `spine-anim-forge` 第 4 個運動基元(0d/0e/0f/**0g**);區塊**仍 HOLD**(運動基元皆先驗、單一真值資產 robot,防固化)。
- 續:cascade(**跨件錯開起始相位**的波浪擴散,屬 build_animations 層 per-part 相位偏移,非單模板)、anticipate-hold(長蓄力單放)。

## 產出檔

- 新:`tools/analyzer/beat_templates.py::gen_combo`、`tools/analyzer/validate_combo_template.py`、
  `knowledge/s1-combo-template.md`、`knowledge/figures/s1_combo_template.png`。
- 改:`gen_animations.py`、`genre_priors.py`(slot_symbol)、`check_readiness.py`、`knowledge/README.md`、`STATE.md`。
