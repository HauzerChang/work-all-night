# 2026-08-27 — session 001

## 目標 (bounded chunk)
承 STATE 候選 2(❗最高優先:S3 weighted mesh + BBW)。依 RULES「每能力必配評估器」,
先建**前置品質閘**:一支能量化 weighted(骨綁)mesh 在真實 bone 動畫下變形品質的評估器。
既有 `deform_eval.py` 只驗 unweighted mesh(deform timeline),對真實生產角色的 weighted mesh 無能為力。

## AC(動手前設定)
1. 由 weighted 綁定重建 setup pose → 拓樸乾淨(證數學正確)。
2. 藝術家自己的 weighted mesh 用真實動畫驅動 → 不透明結構件全幀 si=0(證閘不誤報)。
3. 破壞綁定(負對照)→ 必產生自交/翻面(證閘不漏報)。

## 做了什麼
- 新增 `tools/mesh_gen/weighted_deform_eval.py`:Python 重現 Spine 3.8
  bone world transform(transform=normal 仿射 + parent 鏈)+ weighted skinning
  (`Σ weight·boneWorld.transformPoint(bind)`)+ timeline 取樣(緊湊 bezier / stepped / linear)。
  沿用 `deform_eval` 的 self-intersection/flip 幾何檢查(單一真相來源)。
- 新增 `tools/mesh_gen/validate_weighted_deform.py`:三道校驗 + PASS/FAIL,對 Award 3 機器人 mesh。
- 圖 `knowledge/figures/s3_weighted_deform_eval.png`(視覺確認,matplotlib)。

## 自我驗證結果
`validate_weighted_deform.py` → **OVERALL: PASS ✅**
- ①setup 自一致:身體/左手/光暈 3 件重建 0 自交/0 塌陷。
- ②藝術家真值:身體(98v)、左手(80v)在 Legend_In/Loop/Out **全幀 si=0/flip=0/degen=0**。
- ③鑑別力:左手打亂權重 si=21/flip=3;身體近剛體(area 1.0)用 **amp=4** 分離:藝術家 si=0 vs 打亂 si=54。

## 過程中揪出並修的 bug(誠實紀錄)
1. **scale timeline 缺 channel 預設值**:Spine 省略 == 預設的 channel,translate/rotate 預設 0 但
   **scale 預設是 1**。原本一律回 0 → mesh 塌陷成一線 → 假性自交(光暈 In si=115、左手 Loop si=3)。
   改 per-channel default 後,左手/身體全乾淨。
2. **big-win『scale 從 0 彈入』被誤判 degenerate**:改用相對面積 degeneracy(< 1e-4×同姿勢平均;
   整體收合時判 0)。self-intersection/flip 本就 scale-invariant。

## 重要發現 / 誠實界定
- **軟性加成件(光暈)容許自我重疊**:reveal 期真實自交 si=71 且在 t=0 **精確 keyframe**(非內插假象)
  → additive halo 折疊在混合下視覺無害。**pass/fail 門檻須依 attachment 語意分類**(不透明件 si=0;軟件只記錄)。
- 只有 Legend 三支動畫驅動機器人骨;目前只支援 transform="normal"(Award 全部如此)。

## 產出
- `tools/mesh_gen/weighted_deform_eval.py`、`tools/mesh_gen/validate_weighted_deform.py`
- `knowledge/s3-weighted-deform-evaluator.md` + 索引;圖 `figures/s3_weighted_deform_eval.png`
- `requirements.txt` 加 matplotlib;更新 STATE(候選 2 前置閘打勾,下一步定為 BBW 生成)

## 下一步(見 STATE 候選 2)
**BBW(或 heat-diffusion 近似)權重生成 + 內部取樣密度控制**:對機器人件自動產 weighted mesh,
用本閘量測不透明件 si=0 + 變形平滑度(area_ratio 波動/邊長變異)≈ 藝術家 + 頂點預算合理。純 CPU 可自驅。
