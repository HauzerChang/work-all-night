# 2026-08-27 — session 001

## 這次做了什麼(S1 候選 0d:分鏡 → 動畫 keyframe)

承接上一 session(build_spine 打通「規格→可載入素材」但 `animations` 留空)。本次補上「會動」維度。

### 新工具
- `tools/analyzer/storyboard_to_anim.py` — `build_animations(spec, part_bone, part_slot)`:
  把 analyze_target 的 `3_motion_storyboard`(role-based beats)**確定性**轉成 Spine 3.8 `animations`。
  三支 beat **In / Loop / Out**;角色模板:特效(緩轉一圈+脈動+alpha)/ body(呼吸)/ head(微點頭)/
  limb(末梢擺盪,左右相位錯開)。無 ML、無隨機。
- `tools/analyzer/validate_anim.py` — 5 條量化 AC 自我驗收閘 + 簡易 FK 軌跡:
  AC1 結構合法 / AC2 Loop 無縫 / AC3 有動作 / AC4 Loop 有界 / AC5 FK 軌跡(root-child 前向運動學)。

### 整合 + 驗收
- `build_spine.py`:原 `"animations": {}` → 改呼叫 `build_animations(...)`(順帶回傳 part_bone/part_slot 映射)。
- 對 2 份真實 PSD 建構 + 驗收:**robot(5件)/ Symbol_Ww(18件)動畫 AC 全 PASS**
  (5/5 AC、每 beat 全件有動)。round-trip setup pose 不受影響(validate_build 仍 MAE 0.031、0 孤兒)。
- **評估器可信度(負對照)**:破無縫(首尾差 0.05)→AC2 fail;Loop rotate 25°→AC4 fail;
  未知 bone→AC1 fail;全 no-op→AC3 fail;time 非單調→AC1 fail。5 種缺陷各觸發對應 AC,baseline PASS。
- 實測確認 Spine 3.8 動畫格式(rotate `angle`、slot alpha `color` hex、time==0 省略)自 `assets/main_draw.json`。

## 產出
- 新工具 2 支;`build_spine.py` 整合動畫;`knowledge/s1-storyboard-to-animation.md`(+索引)。
- 更新 STATE(里程碑 + 下一步改為 (e) 關節 pivot 推斷 / (f) mesh deform timeline)。

## 誠實界定 / 限制
- **bone 原點在件中心(非關節 pivot)** → limb 繞中心轉;大幅甩動不自然。**pivot 推斷=S5**,未做。
- 緩動手感(緩急/重量)屬主觀,留使用者在 spine_inspector 目視(客觀/主觀分工符合 SOP)。
- beat 時長/幅度為類型先驗(slot_bigwin),未對真值影片校準;mesh 件尚未加 deform timeline。

## 下一步(見 STATE.md 候選 (e)/(f) 與候選 2)
最高優先:(e) 關節 pivot 推斷(解掉本步驟 limb 繞中心限制,供 S5;對 Award 骨架真值驗收)。
或候選 2:S3 weighted mesh + BBW 權重(補「weighted mesh 骨骼變形平滑度」未驗維度)。
