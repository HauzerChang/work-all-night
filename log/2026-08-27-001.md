# 2026-08-27 — session 001

## 目標(bounded chunk)
接續 STATE 候選 0d(log 004 指定的最高優先):**分鏡 storyboard → Spine 動畫 keyframe**,
讓 `build_spine.py` 產出的靜態素材「會動」。純 CPU 可自驅。

## AC(動手前定,可自我量測)
1. 生成器輸出合法 Spine 3.8 `animations`(bone translate/scale/rotate + 特效 slot color),引用 bone 皆存在。
2. Loop 嚴格週期:每條 timeline 值 t=0 == t=T(gap ≤ 1e-3)。
3. 有界非零振幅(呼吸 scale ≤±6% / rotate ≤±8° / translate ≤±12px)。
4. 相位錯開:各 bone 極值時間離散(破「全身同步紙板感」)。
5. 純 CPU 驗證器 + 負對照(flat/synced 必須 FAIL 對應閘)→ 證明閘有鑑別力。

## 這次做了什麼
### 新工具
- `tools/analyzer/build_animations.py` — 生成器。讀 build_spine 產出的 skeleton.json + 跑 analyze_target
  取每件 struct_role,依 genre 先驗 beats 生成 timeline 寫回。
  - Loop:正弦繞一圈取樣(週期=長度)保週期閉合;body/effect scale x 反相 y(squash/stretch);
    head/limb rotate;特效 slot alpha 脈動;每件 φ_i=i/N 相位錯開;段間緊湊 bezier `{"curve":0.25,"c3":0.75}`。
  - In:縮放彈入 + overshoot(時間相位錯開),末梢從偏角甩入,特效由 0 alpha 亮起(非週期)。
  - Out:縮出 + 特效淡出(非週期)。
- `tools/analyzer/validate_animations.py` — 純 CPU 5 檢查閘:schema / loop_cyclic(僅 loop) /
  amplitude(整組非零 + 分量上限) / phase_div(僅 loop 硬閘,綜合活動訊號極值時間 std ≥ 0.10) /
  world_motion(root→bone 世界變換重現)。含 Spine 緊湊 bezier 取樣(二分解 X→Y)。
  `--neg` 內建負對照(flat 死圖 / synced 同步紙板)。

### 自我驗證(全部量化,不靠肉眼)
- robot_parts(5件,slot_bigwin):In/Loop/Out 全 PASS;Loop gap=0.0、phase std=0.187。
- Symbol_Ww(18件,slot_symbol):land/idle 全 PASS;idle gap=0.0、phase std=0.184。
- setup-pose round-trip(`validate_build.py`)加動畫後**仍 PASS** → 不破壞靜態幾何/貼圖。
- 負對照:flat 在 amplitude 被抓、synced 在 phase_div 被抓 → `neg_control_pass: true`(閘可信)。
- bezier 取樣自驗:對稱 ease p=0.5→0.5、端點 0/1、單段單調有界、stepped/linear 正確。

## 修正
- amplitude 非零判定改看「整組」而非每分量 → 純垂直 bob(x=0,y≠0)不再誤判。
- phase_div 只硬閘 loop 類(同步進退場合法);相位代表改用「綜合活動訊號」極值時間 →
  純旋轉件(世界原點不動)也能取到正確相位。

## 產出
- 新工具 `build_animations.py`、`validate_animations.py`
- 新知識 `knowledge/s1-storyboard-to-animation.md` + 索引
- 更新 STATE(里程碑 + 候選 0d 標完成 + 續補候選 d1/d2)

## 誠實界定
- 只驗客觀結構;手感(緩動曲線/重量感)是主觀項,依 RULES 留給使用者目視。
- `slot_symbol` genre 仍 UNVALIDATED;未接實機渲染(spine-webgl CDN 政策 blocker 未解)。

## 下一步(見 STATE 候選)
d1 補 hit/open/reveal beat 模板;或 d2 量測真實 idle 振幅/相位校準預設幅度帶;
或 STATE 候選 2(weighted mesh + BBW,唯一未驗維度)。
