# 2026-08-30-001 — S5:pivot→bone 父子樹寫入 build_spine(`--rig`)

## 目標(AC-first)
延續 2026-08-29 的 S5 接觸縫 pivot 推斷,推 S5 脫離 HOLD 的**第二個 L3 要件**:
把 pivot→bone 父子樹**接進 `build_spine`**,產出帶真正骨骼關節鏈的可載入 Spine,並客觀驗:
① 骨樹結構正確;② setup pose 不因改骨原點而位移;③ 接觸縫 pivot 忠實安裝成子骨世界原點;
④ 關節語意正確(轉子骨繞接觸縫,非件中心)。

## 做了什麼
- `build_spine.py` 新增 `--rig`:`rig_layout()`(body=結構最大件/註記;head/limb 用 `infer_pivots.contact_seam_joint`
  對 body 取縫;effect 掛 body 下非關節)+ bone 階層序輸出(父先於子)+ attachment delta 位移保 setup pose
  (region 設 att.x/y、unweighted mesh 頂點加 delta)。`build_meta` 補 bone_parent/joint/role;summary 回 rig_root/rig_joints。
  `--rig`×`--weighted` 直接 SystemExit 拒絕(整合列後續)。
- 新增 `validate_rig_build.py`:以 rig 版 vs 非 rig 版同時 build,4 AC 客觀比對(真相來源=infer_pivots + weighted_deform_eval)。
- `check_readiness.py`:`spine-rig-pivot` 的 `pivot_end2end` L0→L2(接上新 gate);區塊仍 HOLD。
- 產圖 `figures/s5_rig_build.png`(骨樹 + 關節語意 rig vs 非rig 縫撕裂長條)。

## 結果(自我驗證)
- `python3 tools/analyzer/validate_rig_build.py` → **OVERALL PASS ✅**(exit 0):
  AC1 結構 PASS;AC2 setup 不位移 max **0.0000px**;AC3 pivot 往返 max **0.042px**;
  AC4 關節語意 頭 3.2× / 左手 2.8× / 右手 2.1× 縫撕裂減量,min ratio 2.1 > 2.0 PASS。
- 迴歸全綠:非 rig round-trip(validate_build)overall_pass、weighted build gate PASS、S5 pivot gate PASS、
  rig+weighted 正確拒絕。`check_readiness` spine-rig-pivot 3 caps L2 GREEN、區塊 HOLD(無 L3 cap)。

## 決策 / 發現
- **AC4 用真實 art 輪廓**(非 region bbox 4 角):右手 region 大框 alpha 僅 16% 填滿,corner 版失真(ratio→1.0)。
- **AC4 門檻 2.0×(縫運動至少減半)**為原則性上界:右手接觸縫解剖上本就寬,改善天然低於頭/左手,3 件全清。
- **關鍵發現**:Award 只有機器人(4_LEG)是可拆肢體 rig;OMG/SUP/MEG 為單圖+特效無接觸縫 →
  多 rig 真值屬**使用者資源**(C/資源類),故 pivot_end2end 定 L2 非 L3、區塊維持 HOLD(遵守防固化)。

## 下一步
擇一(皆自主):(1) 肢體父子樹自動推斷(補 rig 唯一「取自先驗」環節);(2) `--rig`×`--weighted` + 多層關節鏈;
(3) weighted-forge/rig 併入 spine-asset-forge skill(C 類需使用者拍板)。多 rig 真值待使用者提供分層/rig 檔。
見 `knowledge/s5-rig-build-integration.md`。
