# 2026-08-30-001 — S5:pivot 寫入 build_spine 關節骨樹(端到端)

## 這次做了什麼(bounded chunk)

接續 2026-08-29 的接觸縫 pivot 推斷器,把「推斷出的關節 pivot」**真的寫進可載入的 Spine 骨樹**,
補上 `spine-rig-pivot` 區塊 `pivot_end2end`(原 L0、無閘)的空白 —— 即 STATE「下一步 5」的
「pivot→bone 父子樹寫入 build_spine」半。

## 產出

- `tools/analyzer/build_spine.py` 新增 `--rig`:
  - `_structural_tree(spec)`:由分析器 #4 note(body/head/limb/特效)推運動學父子樹(誠實:tree 由分析器/先驗給)。
  - **中介 joint 骨**模式:`root → b_身體 →(j_子件 在接觸縫 pivot)→ b_子件 在件中心`;
    attachment 相對件中心**原封不動** → setup pose 逐件 d=0.0000px 與非 rig 相同(免費繼承 round-trip),
    而 joint 骨落 pivot → 轉 joint 帶動整條肢體繞正確關節旋轉。
  - pivot 由 `infer_pivots.contact_seam_joint(父件世界邊界, 子件世界邊界)` 算得;多產 `rig_meta.json`。
  - 相容:不帶 `--rig` 時輸出與改動前逐件 d=0 相同;`--rig` 與 `--weighted` 暫不同時支援(走 unweighted/region)。
- `tools/rig/validate_rig_build.py`(新閘):用 `weighted_deform_eval` 的 Spine 3.8 FK 實算世界座標,
  對 robot_parts 跑 4 AC + 負對照語意。
- `knowledge/s5-rig-build-integration.md`(+ 索引一行)。
- `tools/check_readiness.py`:`pivot_end2end` L0→L2(掛新閘);`skills/READINESS.md` 快照更新。

## 驗證結果(自我驗證,非肉眼)

`python3 tools/rig/validate_rig_build.py` → **OVERALL PASS ✅**:
- AC-R1 setup 不變:每 render 骨 FK 世界 == 非 rig build 件中心,**max d=0.0000px**。
- AC-R2 joint 落 pivot:每 joint 骨 FK 世界 == 推斷 pivot,**max d=0.005px**(2dp 骨座標捨入)。
- AC-R3 關節帶動肢體:轉 joint 30° → 子件繞 pivot **30.00°±0.003**、半徑 err<0.003px、**父件位移 0**。
- AC-R4 pivot 非天真:med|pivot−件中心|/body尺度=**0.255**、med 末梢(繞 pivot vs 繞件中心)位移/尺度=**0.132**(皆>0.10)。

無回歸(改動後實跑):非 rig `validate_build`(premult 0.031、0 孤兒)、`validate_weighted_build`(PASS)、
`validate_pivots`(4 AC PASS)全綠。

## 決策 / 發現

- **中介 joint 骨** > 直接把 render 骨移到 pivot:前者讓 attachment 一格不動、round-trip byte 級繼承,
  驗證極乾淨;是 Spine 標準 rig 模式。
- **多 rig L3 卡在資產(重要,影響下一步方向)**:實查 Award 內另三角色鏈 `1_OMG`/`2_SUP`/`3_MEG` ——
  各只是掛在鏈根骨上的**單一整片 weighted mesh**(slot `OMG角色`/`superwin_角色`/`megawin角色1,2`),
  **非**像機器人拆成多件、件間有接觸縫關節 → **無法充當第二個拆件 rig 的 pivot 真值**。
  達 L3 需**第二個 multi-part 且有藝術家 pivot 真值的 rig 資產**(使用者層級 / A 類外部資源)。

## 下一步(擇一,見 STATE「下一步動作」)

- 完全自主可做:**S2 骨架閘**(補 S2 樞紐最後一塊);或 **S5 肢體父子樹自動推斷**(目前取自分析器先驗);
  或 rig 與 weighted 合流。
- 需使用者:**第二個拆件 rig 資產**(才能證 S5 多 rig 泛化、脫離 HOLD);weighted-forge 併 skill 拍板(C 類)。
