# 2026-08-30-001 — S5 rig pivot 寫入 build_spine 骨鏈(整合閘 L2)

## 目標(本次 bounded chunk)

延續 2026-08-29 的 S5 pivot 推斷器(對 Award 真值 4 AC 全 PASS,但 `pivot_end2end` L0 HOLD),
推進 L3 兩條件之 **(b):把 pivot→bone 父子樹寫入 `build_spine`**(先前每件平掛 root,無關節鏈)。

## AC(動手前定的可檢查目標)

對 `robot_parts.psd`(star 先驗:身體=root,頭/左手/右手=子):
- AC1 素材不位移:pivot 從件中心搬到關節後,靜態外觀完全不變(< 1px)。
- AC2 pivot 落關節:每子件骨世界位置 == 重算接觸縫關節,父子掛接正確(< 1px)。
- AC3 繞關節旋轉:旋轉子骨,接觸縫位移 rig(繞關節)<< centroid(繞件中心),末梢確有位移。
- AC4 負對照:centroid pivot / 關節互換 皆爆 AC2/AC3。

## 做了什麼

1. **`tools/analyzer/build_spine.py --rig`**:
   - `_alpha_area`(不透明像素面積,避免「大框稀疏件」被誤判)+ `infer_rig_tree`(star 先驗:面積最大結構件=root)。
   - 每結構子件用 `contact_seam_joint`(沿用 infer_pivots)算關節 → 子骨掛父件骨下、pivot=關節、
     region `att.x/y` / unweighted mesh 頂點平移 補償(素材不位移);weighted mesh 走控制骨不套;effect 平掛 root。
   - rig 重親後拓樸排序 bones(parent 先於 child)。加 `--rig` CLI、summary["rig"]、build_meta["rig_parent"]。
2. **`tools/rig/validate_rig_build.py`**:整合閘,4 AC + 用 `weighted_deform_eval` 的 world transform 解算旋轉。

## 結果

- **robot:4 AC 全 PASS**(AC1 max 0.0px、AC2 max 0.005px、AC3 seam_ratio 0.31–0.47 末梢 0.09–0.47、
  AC4 centroid 0.33/swap 破)。`python3 tools/rig/validate_rig_build.py` exit 0。
- **鑑別力驗證**:同 `--rig` + 閘跑 `Symbol_Ww.psd`(18 層裝飾符號)→ AC1/2/4 PASS 但 **AC3 FAIL**
  (16 子件僅 6 過,seam_ratio 0.62–0.93)。**正確**:Symbol_Ww 非 articulated rig,star 先驗硬掛無真接觸縫。
  → 能力界定為**單角色 articulated rig**;閘不橡皮圖章。
- `check_readiness` 實跑:`pivot_end2end` L0→**L2 GREEN**;`spine-rig-pivot` 三 cap 全 L2 GREEN。
- regressions:validate_build(robot/Symbol_Ww)PASS、validate_pivots PASS。

## 決策 / 誠實界定

- 完成 (b)。**(a)「多 rig 真值」被素材阻擋**:Award 只有 robot 一組已拆多件 rig;
  `1_OMG`/`2_SUP`/`3_MEG` 經查為單 slot 角色(子骨僅驅動小燈/光暈,無 per-part 美術件)→ 無接觸縫真值。
  故 `pivot_end2end` 停在 L2、`spine-rig-pivot` 續 **HOLD**;達 L3 需使用者提供更多已拆件 articulated rig(A 類/資源)。

## 下一步(見 STATE.md「下一步動作」)

- 建議 (1):把 `--rig` 併入 `--animate`,讓 keyframe 骨旋轉繞正確關節,量化 vs 平掛 root 的差異(純自主)。
- 或 (2) 肢體父子樹自動推斷(脫離 star 先驗);或 (3) weighted-forge 併 skill(C 類)。
- 待使用者:更多 articulated rig 素材以推 pivot_end2end → L3。
