# 2026-07-30 — session 001

## 這次做了什麼（bounded chunk：S3⇄S4 端到端對照 Award 真實 mesh）

推進 STATE.md「下一步」候選 #1（最高優先、有真值）：生成 mesh 對照真實生產 spine `Award`
的藝術家手做 mesh。

1. **校驗 uv 慣例**（動手前先確認真值可用）：發現 Award 3 個 mesh 件的 `uvs` 是
   **region-local 正規化 [0,1]**（非先前 s4 筆記謹慎假設的 atlas-page UV），且對
   `atlas_crop.extract()` 的 CW-derotate region alpha **v 軸不需翻轉**
   （flip_v=False 藝術家 IoU 0.97–0.98；True 掉到 0.44–0.61）。
2. **新工具 `tools/mesh_gen/compare_to_award.py`**：extract 真值 region alpha → 藝術家 mesh IoU
   基準 → 生成 mesh 對同一 alpha → evaluate_mesh 全條 + IoU → 對照 4 條 AC。
3. **首跑（固定 epsilon=0.008）2/3 過**：身體/左手 PASS；**光暈 FAIL**（hull 只 14 點、IoU 0.9316
   vs 藝術家 0.9795）。掃描證實 IoU 由邊界取樣（epsilon）決定，內部點不影響覆蓋率。
4. **自我收斂修法（Build-Verify 落地）**：`generate_mesh.generate_adaptive()` 由粗到細掃
   epsilon、用內嵌 IoU 閘驅動，取「第一個 IoU≥target 且 v≤budget」。
5. **再跑 3/3 PASS**：光暈自動降到 eps=0.004（IoU 0.9677/37v），身體/左手 0.008 即過。
   全部達藝術家級覆蓋率（過 0.96、與藝術家差 ≤0.02），頂點只用藝術家的 ~40–50%。
6. **回歸**：curtain_left/right 用 v2 strip 仍 overall_pass（只加函式、未改既有 `generate`）。

## 關鍵結論

- 端到端「真實件 alpha → 生成 mesh ≈ 藝術家 mesh」對真實生產標的成立（有真值）。
- **固定 epsilon 對大而彎的件會欠取樣** → 生成器需用自己的 IoU 閘自適應（已實作）。
- 生成器比藝術家省頂點（~半）卻追平覆蓋率 → runtime 友善；但純邊界環（光暈）若要逐頂點
  deform，取樣密度可能仍需再加（留痕）。
- **方法論**：有真值就用真值——藝術家 mesh 給了「覆蓋率天花板 + 頂點預算」的客觀對照。

## 產出

- 新增 `tools/mesh_gen/compare_to_award.py`、`generate_mesh.generate_adaptive()`（+ 內嵌 `_iou`）
- 新增 `knowledge/s3-award-mesh-groundtruth.md`（+ 索引）
- 更新 `STATE.md`（里程碑 + 下一步改為 SkelToJson 組裝）、本 log

## 下一步（見 STATE.md）

切圖→Spine JSON 組裝（SkelToJson）：固化 `<PSD名>/<圖層名>`、size+2px、mesh/region 分配、
mesh 用 `generate_adaptive`，端到端產可載入的 Spine JSON。
