# 2026-07-30 — session 001

## 這次做了什麼(S3×S4 端到端 — PSD 件 → 生成 mesh → 對照 Award 真實 mesh)

推進 STATE.md 最高優先 bounded chunk(候選 1):把 S4 切圖與 S3 生成串成端到端,對真實生產
mesh(有真值)驗收。

1. **AC-first**:定 4 條可機讀 AC — 覆蓋率(vs 藝術家)、頂點經濟度、setup 幾何合法(含 0 自交)、
   外來真實場穩健度。
2. **工具** `tools/mesh_gen/benchmark_psd_mesh.py`:`psd_slice` 切 `robot_parts.psd` 的 3 個 mesh 件
   (光暈/身體/左手)→ `generate_mesh_v2` 生成 mesh → 對同一件 alpha 比覆蓋率、對 Award 藝術家 mesh 對照。
3. **抓修第四次 evaluator miscalibration**:初版用藝術家 local `vertices`(`v/wh+0.5`)算覆蓋率,
   藝術家 IoU 假性掉到 0.47~0.62(荒謬)。查因:Award mesh local vertices 非置中、非跨滿 wh
   (光暈跨 803×781 vs wh 708×685)。**改用 `uvs` 基準**(region-normalized [0,1],與 generate 同慣例)
   → 藝術家 IoU 校正為 0.949/0.948/0.977(合理)。
4. **deform 閘 honest 設計**:3 件在 Award 無自身 deform → 轉移 curtain_left 真實場(315px),
   **scale-normalize** 到單位框(消 px 尺度、兩 mesh 同場),標記為外來場探針。
5. **雙負對照確認鑑別力**:粗糙 3 頂點 mesh IoU 0.565(會 fail);左手 mesh 對身體 alpha 塌到 0.521。

## 結果(3 件全 overall_pass)

| 件 | 生成 IoU | 藝術家 IoU | 生成頂點 | 藝術家頂點 | 省 | setup 自交 | deform gen/artist(si) |
|---|---|---|---|---|---|---|---|
| 光暈 | 0.933 | 0.949 | 35 | 78 | 55% | 0 | 0 / 0 |
| 身體 | 0.966 | 0.948 | 60 | 98 | 39% | 0 | 0 / 0 |
| 左手 | 0.964 | 0.977 | 59 | 80 | 26% | 0 | 0 / 0 |

## 關鍵結論 / 決策

- **端到端「PSD→件→mesh」對真實生產標的成立**:生成 mesh 用少 26~55% 頂點,達到與藝術家相當的
  紋理覆蓋率,且 setup + 強外來真實場下 0 自交 → S3 對真實 blob 件可用且更精簡。
- **座標教訓(第四次)**:跨資產幾何比對先用獨立真值把「參照側」校準到合理值再信;藝術家 mesh
  的 local vertices 帶 attachment transform,件內比對走 `uvs`。
- 3 件皆走 Delaunay-v1(aspect<1.2 非 strip);strip(v2)專屬高瘦 warp 件。

## 產出

- `tools/mesh_gen/benchmark_psd_mesh.py`
- `knowledge/s3-psd-to-real-mesh.md`(+索引)
- 更新 `STATE.md`(里程碑 + 下一步改為 SkelToJson)、本 log

## 下一步(見 STATE.md)

(2) **SkelToJson**:把「件(命名/size+2px/uvs/vertices) + 生成 mesh」固化成寫出完整 Spine JSON 的工具,
打通「PSD → 可用 skeleton」。純 CPU 可自驅。
