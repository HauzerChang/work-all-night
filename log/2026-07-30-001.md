# 2026-07-30 — session 001

## 這次做了什麼(bounded chunk:STATE 候選 1 — 端到端 PSD件→S3 mesh 對照 Award 真實 mesh)

接手續跑。讀 RULES/PLAN/STATE/CLAUDE + knowledge 索引 + 最近 log。從 STATE「下一步」挑
**最高優先且有真值可比**的候選 1 推進。

### AC(先定可量測目標)
- **E1 端到端貫通**:`robot_parts.psd` 的 3 個 mesh 件(光暈/身體/左手)都能 PSD→件→v2 mesh 產出合法 unweighted mesh。
- **E2 靜態覆蓋**:對件 alpha 的 hull IoU(既有 `evaluate_mesh`)。
- **E3 輪廓對真值**:對 Award 真實 mesh 的輪廓 IoU ≥ 0.85 且為判別矩陣列/行最大(對角>非對角)。
- **E4 頂點預算**:生成頂點數 ≤ 藝術家。

### 實作
- 新增 `tools/mesh_gen/compare_to_award_mesh.py`(可重跑閘):psd_slice → generate_mesh_v2 →
  evaluate_mesh → 對照 `Award.json` 真實 mesh。
- **對真值方法(關鍵)**:Award 這 3 件是 weighted mesh,取 setup 世界座標需骨頭變換;改用
  **uv layout 即輪廓嵌入** + 各自 bbox 正規化 + **best-of-8 dihedral IoU**,免骨頭變換/免 uv 反解旋轉,
  自動吸收 ~0.704 打包縮小、atlas rotate、flip。
- 先做**判別力自驗**再信判定(遵 RULES:評估器要可信)。

### 結果(自我驗證)
| 件 | 靜態 IoU | 輪廓 IoU | 非對角 max | gen v/h | 藝術家 v/h |
|---|---|---|---|---|---|
| 光暈 | 0.933 ✗ | 0.888 | 0.635 | 35/16 | 78/78 |
| 身體 | 0.966 ✓ | 0.938 | 0.710 | 60/20 | 98/40 |
| 左手 | 0.964 ✓ | 0.967 | 0.710 | 59/19 | 80/42 |

- 對角 0.89–0.97 顯著壓過非對角 0.60–0.71 → **判別力確認**,評估器可信。
- **OVERALL PASS**(輪廓對真值 + 頂點預算全過)。
- 圖:`knowledge/figures/s3-psd-to-award-hull.png`(紅=生成、藍=藝術家 正規化疊圖)。

## 關鍵結論
- **S3×S4 端到端里程碑達成**:PSD件→自動 mesh 的輪廓與藝術家手做 mesh 吻合 0.89–0.97,頂點更省(0.45–0.74×)。
- **藝術家光暈 mesh = 78v 全在 hull 的純周界環**(無內部三角);glow 當單一柔性整體變形。
- **光暈靜態 0.933 < 0.95 是軟 alpha 已知限制**,對照證明非生成錯誤 → soft-alpha 件應放寬靜態門檻或改用動態基準。

## 產出
- 新增 `tools/mesh_gen/compare_to_award_mesh.py`、`knowledge/s3-psd-to-award-mesh.md`(+索引)、
  `knowledge/figures/s3-psd-to-award-hull.png`。
- 更新 `STATE.md`(標里程碑、改寫下一步)、本 log。

## 下一步(見 STATE.md)
候選 2:對 Award 真實 deform 做生成 mesh 的**耐變形對照**(本次僅 setup 輪廓);或候選 3 SkelToJson
把件+mesh 組成可載入 Spine JSON。
