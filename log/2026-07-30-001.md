# 2026-07-30 — session 001

## 這次做了什麼(自驅排程:S3+S4 端到端對照真實生產 mesh)

推進 STATE「下一步候選 1(最高優先,有真值)」:把 S3 生成器對 Award 機器人 big-win 的
3 個真實 mesh 件(光暈/左手/身體)做**端到端靜態覆蓋率對照**。

1. **標的定位**:`robot_parts.psd` 5 圖層 ⇄ Award slot `機器人拆件/<圖層名>`;其中
   光暈/左手/身體 在 Award 為 **weighted mesh**(78/80/98 頂點),右手/頭為 region。
2. **機制發現**:這 3 個 mesh **無任何 deform timeline** → 由骨頭 skinning 驅動(≠ main_draw
   的 unweighted+deform timeline)。既有 `deform_eval` 的 real_deform_field 對 weighted 直接爆
   (值/點數不符),證實不能沿用同一條變形閘。
3. **新工具** `tools/mesh_gen/validate_award_static.py`:atlas 切件 → `generate_mesh_v2` →
   ① 生成 mesh IoU ② 藝術家 mesh 覆蓋率基準(`artist_iou`,同 alpha frame)③ 頂點/三角/hull 對照。
4. **揪 bug + 校準(5 輪內)**:光暈初驗 IoU 0.929 < 藝術家 0.980 = FAIL。sweep epsilon 確認
   「覆蓋率由 hull 取樣密度決定」(舊發現複現);v1/Delaunay 回退預設 eps=0.008 對大型軟邊過疏。
   改 `generate_mesh_v2` 回退路徑用 `eps_fallback=0.002`(僅動 v2 回退,v1-standalone / strip 不變)。
5. **驗收**:3 件全過(0.9832/0.9913/0.9926,頂點 73/67/77 皆 < 藝術家),`validate_award_static.py`
   exit 0;main_draw v2(curtain_left strip)regression 仍 overall_pass → 無回歸。

## 關鍵結論

- **端到端(PSD件→S3 mesh→真實生產標的)靜態覆蓋率驗收通過**,且揪出+修掉真實 bug。
- **天然負對照**:修正前光暈是真 FAIL → 證 IoU 閘有鑑別力,藝術家自身覆蓋率當可信真值。
- **rig-requirement 洞見**:同資產庫混用兩種變形機制 —— weighted skinning(大柔性件)vs
  unweighted deform(窗簾/陰影)。生成器/評估器要兩者都覆蓋。

## 產出

- 新增 `tools/mesh_gen/validate_award_static.py`、`knowledge/s3-award-mesh-static.md`(+索引)。
- 改 `tools/mesh_gen/generate_mesh_v2.py`(回退 eps_fallback=0.002,含註解)。
- 更新 `STATE.md`(本次進度 / 下一步改為 weighted 變形閘 / 分支現況補記)、本 log。

## 下一步(見 STATE.md)

**候選 1(最高優先)**:weighted mesh 變形閘 = 對生成 mesh 算 BBW 骨權重 + 用 Award 動畫骨頭
變換模擬 skinning → 量自交/翻面。這是目前唯一還沒評估器覆蓋的 mesh 變形機制,純 CPU 可自驅。

## 備註(分支)

啟動分支 = `claude/vibrant-franklin-hwq7tf`,推回同分支。`prompts/run.md` 內文的舊分支名
(`zealous-noether`)已過時(排程改從 default clone),以啟動分支為準。
