# 2026-09-21 session 001 — S1 squash 接檔位差異化:體積守恆的耦合 amplify(candidate G-4''''')

## 目標(AC-first)

接續 STATE 建議 **(G-4''''')**:補 G-4'''' 留下的 honest boundary ——「squash 未接 tier(需**耦合
amplify**:scaleX/scaleY 一起以體積守恆放大,不破壞 scaleX·scaleY==1)」。讓 squash 的**擠壓幅度隨檔位
遞增**,同時**每檔位仍體積守恆**。可檢查 AC = `validate_squash_tier.py` 5 條(V1–V5)。

## 缺口診斷

(J) 的幅度增益 `_amp_scale(v,g)=1+g(v−1) if v≥1 else v` 是**各軸獨立**「只放大 identity 上方」規則。
squash 的 scale 是體積守恆對(scaleX=1+q>1 拉長、scaleY=1/(1+q)<1 壓扁)。各軸獨立套用 → 拉長軸放大
`1+g·q`、壓縮軸是樓地板保持不變 → `scaleX·scaleY≠1` **破壞面積守恆**。這正是 G-4'''' 把 squash 排除在
`MAIN_SHOW_CATS` 之外的原因。

## 做了什麼(全 additive)

1. **`tier_variants.py`**:
   - squash 併入 `MAIN_SHOW_CATS`;新增 `VOLUME_PRESERVING_CATS={"squash"}`。
   - **`_amp_squash_pair(sx,sy,g)`**(耦合 amplify):放大拉長軸(=max,沿用 `_amp_scale` 的 1+g(s−1))、
     **壓縮軸取拉長軸的倒數** → `scaleX'·scaleY'≡1`(對任意 g)、仍非均勻、identity (1,1)→(1,1)、阻尼耦合
     (q_i 同比放大 → 相繼 |scaleX−1| 仍嚴格遞減);`g==1.0` 精確 identity(不引倒數捨入誤差,base 逐位元不變)。
   - `amplify_bone_tl(b,g,coupled_scale=False)` / `amplify_anim(anim,g,coupled_scale=False)`:coupled 時 scale
     走 `_amp_squash_pair`,否則各軸獨立(rotate/translate/shear 兩者相同,shear 沿用 (G-4'') v'=g*v)。
2. **`gen_animations.py`**:`build_animations` 依 `cat∈VOLUME_PRESERVING_CATS` 決定 `amplify_anim(coupled_scale=)`。
   squash 非 count-aware(nosc 已備參數未接,honest,後續)。
3. **`validate_squash_tier.py`**(新,5 AC;復用 G-4' shear 判準 + G-4'''' `_sq3_eval`/`_interior_scale`)。
4. **`validate_tier_combo_count.py`** K5(c) 修正(見下)。
5. **`check_readiness.py`** 新增 cap `squash_tier_coupled_amplitude` L2 併入 `spine-anim-forge`;更新 `skills/READINESS.md`。
6. knowledge `s1-squash-tier-coupled-amplitude.md` + 索引;圖 `knowledge/figures/s1_squash_tier.png`。

## 產出 / 結果

- `validate_squash_tier.py` **OVERALL PASS**(V1–V5 全綠):
  - **V2 crux** 體積守恆耦合每檔位保持:每極值幀 |scaleX·scaleY−1|≤2e-2 + 非均勻 + squash 幅度遞減。
  - **V3 crux** 兩軸峰皆遞增:擠壓幅度 [0.16,0.216,0.272,0.336]、shear 峰 [16,21.6,27.2,33.6]°
    Super<Mega<Omg<Legend 嚴格遞增·Super==base·每檔位阻尼簽章保形。
  - **V5(a) crux 鑑別**:對真實 squash bone 套**樸素**各軸獨立增益(g=Legend)→ 體積守恆 FALSE
    (|scaleX·scaleY−1|=0.15);**耦合** → TRUE(4e-5)→ 證耦合 amplify 必要且閘可辨。
- 端到端 `build_spine --animate --tier-variants --shear-pivot`:直出 `squash__{Super,Mega,Omg,Legend}`;
  pivot 補償只加 translate → scale 通道不動 → 體積仍守恆(prod≈1.0)、幅度仍遞增。round-trip
  `validate_build` overall_pass。
- 回歸 **18 閘全綠**:squash_tier(新)/squash_gen/wobble_count/wobble_tier/tier_combo_count(K5c 修)/
  tier_variants/shear_gen/shear_pivot/scale_pivot/pivot_rotation/priors/priors_beats/priors_combo_charge/
  priors_cascade/cascade/more_beats/beat_templates/deform_gen。

## 決策 / 踩雷

- **連帶修正 `tier_combo_count` K5(c)**:squash 併入 `MAIN_SHOW_CATS` 後,原「非-combo 主秀 beat 各檔位
  `_min_peaks` 是否相同」的 count 隔離判準對 squash **假陽性** —— squash 的體積守恆 scaleX 幅度被檔位增益
  放大後,在較高檔位越過 impact-peak prominence 門檻 → 峰數各檔位不同([0,1,1,1]),**但那是幅度效應
  (amp_only 亦然)、非連擊數旋鈕(`tier_combo_hits`)外洩**。修法=count 隔離判準改**比對 full vs amp_only**
  (加上 `tier_combo_hits` 後非-combo 節拍須逐位元不變)。此為 (G-4'') 對 (J) J3 改 channel-aware 的**同類
  修法**:新節拍加入時,舊閘的量測維度可能對它假陽/假陰,須把判準收斂到真正的機制軸。

## 關鍵發現

**體積守恆通道的檔位增益是「耦合」變換(兩軸相依),不能沿用各軸獨立增益** —— 又一「檔位機制就緒 ≠ 每個
通道接上」實例(同 (E)/(H)/(I)/(J)/(G-4')/(G-4''))。耦合的要點:放大一軸、另一軸隨之取倒數以維持不變量
(scaleX·scaleY≡1),對比樸素獨立增益破壞不變量。此模式可推廣到任何帶**代數不變量**的多通道運動基元。

## honest boundary(仍在)

- 擠壓幅度階梯沿用 (J) 增益(PROPOSAL,手感留使用者 A 類);shearY≡0(單軸 shear)。
- squash **count-aware**(擠壓段數隨檔位,`gen_squash(nosc=)` 已備參數未接,比照 G-4''')為後續。
- 單一真值資產;`spine-anim-forge` 仍 **HOLD**(運動基元先驗、防固化)。

## 下一步(擇一,皆純自主)

- (G-4'''''') squash count-aware(擠壓段數隨檔位,nosc 已備參數;結構軸×幅度軸雙軸差異化再擴一通道)。
- (G-4''''''') 產 shearY(雙軸 shear)/ shear+scale+rotate 三通道同時的運動基元(塞滿一般仿射 M 全自由度)。
- (J-3) cascade 波速/散佈/件數隨檔位(cascade 的第三種檔位軸)。
- (G-1) `--rig`×pivot 系列 per-bone 語意去重;(G-2) 主秀 beat 下 limb 繞關節 AC。
- S5→L3 仍待 (D) 多 rig 真值(C/資源類,使用者提供)。
