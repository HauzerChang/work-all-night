# 2026-08-25 — session 001

## 這次做了什麼:S1 分鏡 → 動畫 keyframe(候選 0d,最高優先)

延續 log 2026-08-19-004 定的下一步:`build_spine` 產出的素材原本 `animations:{}`(不會動),
本次把 analyzer 的 #3 動作分鏡確定性轉成 Spine timeline,讓素材「會動」,並配可信自我閘。

### 產出
- `tools/analyzer/animate_spine.py`:#3 分鏡(role→action)→ Spine 3.8 `animations`。
  - 4 個確定性動作原型:`build_in`(enter 彈入)/`build_loop`(**無縫**待機呼吸)/
    `build_out`(exit 縮出)/`build_accent`(pop 強調)。
  - beat key 先經**語意類正規化**(BEAT_CLASS),讓 slot_bigwin(In/Loop/Out)、
    slot_symbol(land/idle/win)、slot_reveal(comeout/…/loop/close)等不同 genre 都能對映。
  - Loop 呼吸:body scaleY/transY raised-cosine、head/effect 平移正弦、limb **相位錯開**
    `sin(2πu+φᵢ)−sinφᵢ`(平移項讓首末仍=0 → 無縫且錨在 neutral)。
- `tools/analyzer/validate_animation.py`:6-AC 幾何自我閘(A1 結構 / A2 Loop 無縫 /
  A3 有動作 / A4 幅度有界 / A5 相位錯開 / A6 轉場連續)+ 線性軌跡取樣器。
- `knowledge/s1-storyboard-to-animation.md` + 索引;更新 PLAN 隱含、STATE、本 log。

### 驗收(AC-first,量化自驗)
- **robot(slot_bigwin,5件)**:loop_beat=Loop,A1–A6 全 pass。Loop 振幅 effect 14°/limb 10°/
  head 6°/body scale 0.03、transY 4px;A2 首末差 0.0。
- **Symbol_Ww(slot_symbol,18件)**:loop_beat=idle,A1–A6 全 pass(跨 genre 泛化確認)。

### 評估器可信度(per RULES 每能力必配可信評估器)
- **真值錨**:真實 `main_draw_loop / main_idle / main_idle3` bone timeline 首末差 = **0.0000**
  → 佐證 A2「Loop 嚴格無縫」是真實生產行為(非任意規則)。⚠️ `Award_*_Loop` 首末差達 22°
  (tier crossfade)→ 無縫錨取 main_draw 不取 Award。
- **幅度 band** 由真實幅度校準(rotate≤15°/scale≤0.15/trans≤35px)。
- **負對照 5/5 全抓**:非無縫 Loop→A2、超幅 180°→A4、同相位 limb→A5、In 末不回 neutral→A6、
  參照鬼 bone→A1。

### 過程抓到並修的 bug
- 評估器 `_neutral_at` 以 (name,prop) 為鍵 → scale.y 與 translate.y 互相覆蓋,誤報轉場不連續。
  改鍵為 (kind,name,prop) 修正(自己的 A6 先抓到,才發現)。
- effect rotate 幅度 8° → 峰對峰 16° 超 band → 降 7°(峰對峰 14°)。

## 誠實界定 / 限制(寫進 STATE 續作候選)
- easing 目前線性取樣(9 點近似正弦),未輸出 Spine 緊湊 bezier → 手感非藝術家級(候選 d2)。
- 未實機渲染對照(spine_inspector CDN 被政策擋,既有 blocker)。
- 動作是類型先驗 PROPOSAL;pivot 仍件中心(關節 pivot 屬 S5)→ limb 繞件中心轉非繞關節。

## 下一步(見 STATE.md 候選 d2–d4)
d2 bezier easing(最高:把手感做對,評估器已可續驗)/ d3 accent 爆發 / d4 併入 build_spine 一鍵產會動素材。
