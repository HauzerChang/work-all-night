# 2026-08-25 — session 001

## 這次做了什麼(S1 候選 0d:分鏡 → 動畫 keyframe)

接續 log 004 定的最高優先項:build_spine 產的素材 `animations:{}` 是靜態的,本次讓它「會動」。

### AC(動手前先定,可自我量測)
1. 由 analyzer #3 storyboard 的 **Loop** beat 確定性生成 Spine 3.8 `animations.loop`(可載入格式)。
2. 角色 → 運動原語:body 呼吸、head 點頭、limb 相位錯開擺盪、effect alpha 脈動。
3. 每條 timeline **loop 無縫**(t=0 值 == t=period 值)。
4. 有評估器反算量測(幅度合理/無縫/相位錯開/alpha∈[0,1]),對 ≥2 個真實資產 PASS。
5. 評估器附**負對照**證明鑑別力(靜態要 fail、同相位要 fail)。

### 產出
- `tools/analyzer/animate_spine.py` — storyboard Loop → `animations.loop`(dense-linear K=12,seam-safe,
  limb 相位 φ=i/n)。CLI:`animate_spine.py <skeleton.json> --psd <psd> --genre <g>`,原地注入。
- `tools/analyzer/validate_animation.py` — 重現 Spine linear 取樣,密集抽 200 點/週期量測 body.ty_range /
  rotate.range / limb.phase_spread / effect.alpha,逐條 AC pass/fail;`--neg` 跑負對照。
- `knowledge/s1-storyboard-to-animation.md`(+ 索引)。

### 自我驗證結果(全 PASS)
- robot_parts(5件):bones_animated=4 / slots=1;**12 檢查全過**,phase_spread=0.5。
- Symbol_Ww(18件):17 bones / 1 slot;**38 檢查全過**,phase_spread=0.915(12 肢體錯開)。
- 負對照:`static_fails=true`、`inphase_phase_fails=true` → `discriminating=true`。exit 0。

### 決策 / 誠實界定
- 用 **dense-linear** 而非 bezier:讓產生器與驗證器同一內插,閘驗到的就是 JSON 裡的(避免自欺)。
  bezier 緩動列為後續精修。
- **只做 Loop beat**:In/Out 需大位移+attachment 顯隱+overshoot,且無現成幾何真值可自驗 → 後續。
- limb 目前繞**件中心**旋轉(非關節 pivot);小幅待機可接受,大幅動作才露餡 → 待 S5 pivot。
- 節奏/幅度是主觀美術決定(RULES:主觀留使用者);本閘只驗客觀可量維度。

## 下一步(見 STATE.md 候選 0d2/0d3)
- In beat 彈入(translate/scale overshoot + attachment 顯隱),量化 overshoot 峰值/回穩;或
- Loop 升級 bezier 緩動(驗證器同步支援 3.8 compact-bezier 散鍵);或
- 候選 2:S3 weighted mesh + BBW(補「骨骼變形平滑度」唯一未驗維度)。
