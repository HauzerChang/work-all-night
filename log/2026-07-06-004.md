# 2026-07-06 — session 004

## 這次做了什麼(P1-B 作法具體化:CPU 粗補 → Claude 提示詞 → 生成型 AI 部位生成)

使用者補充 P1-B 的作法:先用目前初步補圖(填滿區塊)當底,**由 Claude 生成提示詞**
給其他生成型 AI 做「圖片完整化 / 部位生成」(例:耳機右罩→軀幹 = 生成其下層身體部件)。

角色分工就位:**Claude 出認知(看圖寫部位語意 + 把關),生成型 AI 出像素**。

### 產出:`tools/mesh_gen/genai_inpaint_pack.py`(B0,提示詞包產生器)
一鍵產出(給定 PSD + 上層 + 下層):
- `base_cpu_fill.png`:下層件全畫布,被遮區已 telea 粗補(生成 AI 的編輯底圖/位置提示)
- `mask.png`(白=重繪)+ `mask_alpha.png`(編輯區=透明,OpenAI images.edit 慣例)
- `context.png`:完整合成圖 + 紅框標補區(給生成 AI 看脈絡)
- `prompt.txt`:中英雙語結構模板 + 【部位語意】欄(Claude vision 看圖後填)
- `pack.json`:機讀 manifest(含回圖驗收指引:inpaint_eval 閘 + vision 自評 + 人審)

### 首例(已交使用者):dj_cat「頭→軀幹」2977px
- Claude vision 實際看圖寫語意:「DJ 貓被頭遮住的頸部與上胸;延續白色毛皮、左右肩線
  收攏至頸、深色剪影、紫粉色調;不要畫頭/耳機/外套(他層)」。
- 使用者可直接把 base+context+prompt 貼 ChatGPT 人工試效果(不需 API)。

### STATE 更新
P1-B 重排:B0 ✅ 提示詞包工具 → B1 試點回饋迴圈(使用者拿 pack 試生成,回圖過閘,
固化 prompt 模板)→ B2 調研選型文件(API 自動化可行性)。

## 下一步
- 等使用者 B1 試點結果(生成回圖 → 過閘 + 人審)→ 據此迭代 prompt 模板。
- 排程自驅則從 P1-A A1(修 psd_slice AC3)開始。

## 追加(同日 session 005 併入):Main_idle 合併層案例(使用者指定 3 步驟)

使用者指出 dj_cat 27 層**過度瑣碎化**,補圖目標易錯亂;改提供 `Main_idle_測試ai切圖.psd`
(**3 層:身體=整隻角色合併層(含耳機)**、DJ台範例圖、右手1)。這才是真實切圖→補圖場景:
**從合併層把件切出來,切完才有真洞**。

1. **切圖(僅耳機右罩)**:用 dj_cat「耳機右罩」層 alpha 當切割遮罩(兩檔對齊驗證:
   耳罩區 MAE 2.45,身體層該區 100% 有像素)→ 切出耳罩件 89×73 + 身體真洞 3188px。
2. **基本補圖**:telea 粗補,閘 PASS(hole_fill=1.0,seam=0.772)。
3. **限縮提示詞包**:`genai_inpaint_pack.py` 新增 `write_pack`(重構)+ `--crop MARGIN`:
   輸出限縮成洞 bbox+40px = **153×169 小圖**(pack.json 記回貼座標 [342,163])。
   vision 看圖寫語意:右側胸口+比基尼上緣+外套內緣/拉鍊+髮尾;交使用者給 ChatGPT。

發現:合併層案例的語意目標=「移除該件後自然露出的畫面」(非圖層歸屬);
切割遮罩可借其他版本 PSD 的分層真值(對齊即可用)。

## 追加(2026-07-08):B1 首試點人審通過 🏁 + 複數區域(4 件)切圖與提示詞包

- **B1 里程碑**:「耳機右罩」pack 經 ChatGPT 生成、**人為審查判定可行** —— 路線首次閉環。
  觀察:網頁版放大重繪整塊 crop(非嚴格遮罩)→ 回貼需 resize+邊緣融合;像素級控制需 API。
- **複數區域**:Main_idle 切 4 件(耳機 8295px/胸部 16758/眼鏡 7986/左手=掌∪臂 14274)。
  補區兩型:內部件整洞填;貼輪廓件(左手)occlusion_mask 只補窄帶(4093px)其餘留透明。
  4 洞 CPU 補全過閘(seam 0.53~1.09);4 個限縮 pack(語意含:生成雙眼/輪廓閉合/防金色滲染)
  + 5 件 PNG + overview 以 zip 交付。深疊區(耳機∩胸部)身體最底層仍空 → 逐層遞迴補圖(未解)。
