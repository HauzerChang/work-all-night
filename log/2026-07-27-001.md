# 2026-07-27 — session 001

## 這次做了什麼(S3×S4 端到端里程碑:PSD件 → mesh → 對照 Award 真實 mesh)

接續 `STATE.md` 最高優先 bounded chunk(選項 1):把 S4 切件與 S3 生成串成端到端,
對**有藝術家真值**的真實生產標的(Award「機器人拆件」)驗收。

1. **切件**:`psd_slice.py assets/robot_parts.psd -o /tmp/robot_parts` → 5 件緊湊 PNG + manifest(切圖無損)。
2. **定 AC + 校準評估器(先驗評估器可信)**:Award 有 3 個 mesh 件(光暈 78v / 身體 98v / 左手 80v,皆 weighted)。
   把**藝術家 mesh 的 uvs×[件W,件H]** 疊到 psd_slice 切件 alpha,覆蓋 IoU=0.948/0.948/0.977
   → ① 確立 Award uvs 是 **region-local**(非 atlas-page,更正舊 knowledge 措辭);② 評估器可信
   (藝術家自身即 ~0.95,設為 baseline)。
3. **生成 + 對照**:`generate_mesh_v2 (auto)` 對每件 → 覆蓋 IoU vs 件 alpha,對比 baseline(margin 0.02)。
   全 pass:光暈 0.933(−0.0155)、身體 0.966(**+0.0183**)、左手 0.964(−0.0126);
   生成頂點 35/60/59(藝術家 78/98/80,省 44~55%)、0 退化三角。
4. **固化 + 視覺確認**:寫獨立 AC 工具 `validate_against_award.py`(exit code 反映 pass);
   產藝術家 vs 生成 mesh 疊圖 `figures/s3-award-robot-mesh.png`(橘/綠皆貼合輪廓)。

## 關鍵結論 / 決策

- **S3 首次在 main_draw 以外、有藝術家真值的真實生產 spine 上驗收成功**:自動 mesh 覆蓋達手做水準、更省頂點。
- **端到端 PSD→件→mesh 打通**(純 CPU、可自驅、可重現)。
- **誠實範圍**:3 件在 Award **無 deform timeline**(骨骼權重變形)→ 本次只驗靜態覆蓋 + 頂點預算 + 拓樸;
  deform 耐受度另在 main_draw 4 mesh 以真實位移場驗過(互補)。3 件長寬比<1.2 → auto 回退 **v1 Delaunay**
  (本次驗的是 v1 靜態通用性,非 v2 strip)。
- 續行「評估器先校準/對真值自一致才下判定」的 SOP(這次用藝術家 mesh 當真值校準)。

## 產出

- `tools/mesh_gen/validate_against_award.py`
- `knowledge/s3-award-robot-e2e.md`(+ README 索引)、`knowledge/figures/s3-award-robot-mesh.png`
- 更新 `STATE.md`、本 log

## 下一步(見 STATE.md)

(1) SkelToJson 件→Spine mesh attachment 組裝工具(固化命名/padding/uvs/縮放慣例);
(2) S2 補圖閘 / 骨架閘;(3) S1 需 benchmark 影片(使用者層級)。
