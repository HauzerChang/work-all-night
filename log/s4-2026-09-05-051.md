# S4 chunk 51(2026-09-05):修正 head/fox_ears/hair_front/earrings/choker 決策檔框位置錯誤

## 背景

chunk 50 留下候選項:「對 `suggestions.json` 其餘部件(尤其 `low`/`medium` 信心項目)
逐一跑網格疊圖複核,盤點是否還有同類『框完全沒對準但沒被回報』的落差」。本次排程開始
無活人裁決(自動排程觸發),依既有慣例(chunk 26/49/50)執行零成本、不需授權的選項。

## 執行

1. 用 bash+PIL 把 `assets/jiuwei_yanlian_char_crop.suggestions.json` 20 個部件的
   `bbox_pct` 全部換算成像素、逐一裁出來檢視(全部 20 個都看過,不是只挑可疑的)。
2. 下半身(`leg_left`/`leg_right`/`boot_left`/`boot_right`/`hand_left`/`hand_right`)
   chunk 47-50 已驗證過,跳過;`sash_train`/`hair_main`/`tails_mass` 是已知本來就難的
   大範圍重疊材質,已被自動 heuristic 或分析階段標記低信心,跳過。
3. 剩下 9 個部件全部裁圖檢視,發現 **5 個(`head`/`fox_ears`/`hair_front`/`earrings`/
   `choker`)框全部落在畫面上方的標題文字「JIUWEI·YANLIAN」/引言「顛焰生蓮,狐影惑心」
   區域,完全或大部分沒框到自己的標籤內容**——這張圖本身是帶版面文字的角色設定圖,臉部
   要到畫面中段(y≈90px 起,總高898px)才開始,原始 bbox 卻普遍偏在 y<190px 的上方。
4. 用網格疊圖工具(bash+PIL,座標網格疊在放大裁圖上)在 x:100-420,y:0-280 範圍反覆定位
   五官/耳朵/耳環/頸飾的實際像素位置,改框:

   | id | 舊 bbox_pct(錯) | 新 bbox_pct | 新 bbox_px(460×898) |
   |---|---|---|---|
   | `head` | `[25,3,65,18]` | `[40,10,78,24]` | `(184,90,359,216)` |
   | `fox_ears` | `[30,0,75,8]` | `[38,1,78,19]` | `(175,9,359,171)` |
   | `hair_front` | `[28,5,62,15]` | `[35,2,80,20]` | `(161,18,368,180)` |
   | `earrings` | `[58,15,70,21]` | `[68,16,73,21]` | `(313,144,336,189)` |
   | `choker` | `[46,19,58,25]` | `[48,19,72,26]` | `(221,171,331,233)` |

   confidence 全部從 `high` 下修為 `medium`,`notes` 記錄修正原因。
5. **其餘 4 個(`bodice`/`sleeve_right`/`tag_pendant`/`skirt`)裁圖後有跟鄰近部件內容
   重疊的跡象,本次未處理**:`bodice`/`sleeve_right` 是 chunk 48 已知的「真正演算法
   歧異,點提示測試過仍解不了」案例;`tag_pendant`/`skirt` 是本次新觀察到的疑慮,還沒
   深入定位,不在本次「框完全落錯」的判定範圍內,留給後續 chunk。

## 自我驗證

從修正後的 `suggestions.json` 重建 20 部件 `bbox_px` 決策檔
(`tools/mesh_gen/s4_data/chunk51/decision_reconstructed.json`),跑 `s4_decompose_cut.py
--contour rect --eval`:

- `AC1_parts_produced`:20/20 部件全部產出,`overall_pass: true`。
- 逐格人工視覺複核修正後的 5 張裁圖(`tools/mesh_gen/s4_data/chunk51/cut_rect/`):
  `head` 完整臉部(額頭印記/雙眼/鼻/嘴/下巴)、`fox_ears` 兩隻完整狐耳、`hair_front`
  確實是頭髮內容(不再是文字)、`earrings` 清楚可見金色墜飾+紅色流蘇、`choker` 清楚
  可見紅色蝴蝶結+金色墜飾頸飾。

## 誠實限制

- `hair_front` 跟 `head`/`fox_ears` 新框大幅重疊——這個髮型(長直髮,雙耳外露,無明顯
  瀏海分界)本身沒有清楚的「瀏海」語意邊界,新框只保證框內是頭髮而非文字,精確範圍留待
  使用者用 assist viewer 確認。
- `earrings` 只框到畫面上唯一可見的單耳耳環(character 右耳),另一耳完全被長髮遮住,
  標籤寫「雙耳」但只能反映看得到的那一只。
- 本次盤點仍不是全部 20 個部件的完整驗證,`bodice`/`sleeve_right`/`tag_pendant`/
  `skirt` 留給後續。
- 驗證用的 20 部件決策檔延續 chunk 49/50 做法,從 `suggestions.json` 重新換算,不是
  chunk 46-48 使用者手動調整過的那份真實決策檔,不能延續其統計數字。
- 沒有重跑 `--contour sam`(容器未持久化 MobileSAM 權重)。

## 檔案

- 修改 `assets/jiuwei_yanlian_char_crop.suggestions.json`。
- 新增 `tools/mesh_gen/s4_data/chunk51/decision_reconstructed.json`、
  `tools/mesh_gen/s4_data/chunk51/cut_rect/`(5 張裁圖 + `manifest.json`,驗證證據)。
- 新增 `knowledge/s4-decompose-box-fix-face.md`。
- `knowledge/README.md` 尾端 append。
- 更新 `STATE_S4.md`。
- 未修改任何 production 代碼。

## 下一步(留給使用者/後續排程裁決,均未執行)

1. 對 `bodice`/`sleeve_right`/`tag_pendant`/`skirt` 這 4 個有鄰近部件重疊跡象的部件做
   進一步定位判定(前兩者是已知演算法歧異案例,後兩者是本次新觀察到的疑慮)。
2. `hair_front` 的精確語意邊界(跟 `head`/`fox_ears` 的重疊範圍)需要使用者用 assist
   viewer 手動確認,不建議用演算法猜。
3. 對 `bodice`/`sleeve_right` 接受人工處理(chunk 48 選項 b)或測試完整版 SAM(chunk 48
   選項 c,成本較高,需使用者先確認)仍然有效。
4. 若要對 `suggestions.json` 做徹底的信任度盤點,本次加上 chunk 47-50 已經覆蓋了 20
   個部件裡的 16 個(僅剩 `bodice`/`sleeve_right`/`tag_pendant`/`skirt` 待進一步定位),
   非「框完全落錯」類型的邊界/重疊問題不算在這個盤點範圍內。
