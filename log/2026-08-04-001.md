# 2026-08-04 — session 001

## 這次做了什麼(S3 端到端對照 Award 真實加權生產 mesh)

推進 STATE「下一個 bounded chunk」候選 #1(最高優先、有真值):
**PSD件 → S3 generate_mesh_v2 → 對照 Award 真實 mesh**。目標 3 件在 Award 為 weighted mesh:
`機器人拆件/光暈`、`/身體`、`/左手`。

1. **定 AC(靜態閘,誠實)**:這 3 件在 Award **無 deform timeline**(骨骼權重變形),故不套
   `transfer_deform_check`(無真實位移場,避免捏造)。AC = ① 覆蓋率 IoU ≥ 藝術家 self-IoU−0.02
   ② 頂點預算 ≤ 1.3×藝術家 ③ setup 拓樸 0 自交/0 退化 ④ 評估器自校驗(藝術家 mesh 映入
   裁切幀 self-IoU≥0.85)。
2. **寫 `validate_psd_to_award_mesh.py`**:atlas_crop 取 Award 貼圖裁切件 alpha → generate_mesh_v2
   → 對照。藝術家 mesh 參考幀用 region-local uv 直填(附自校驗)。含負對照 + PSD 端到端 sanity。
3. **抓修 2 個座標假設(自校驗發揮作用)**:
   - page_dims 誤收 region 的 size 行 → 改只取 page 標頭第一個 size。
   - 初版誤把 mesh uvs 當 atlas 頁面座標 → self-IoU 0.0 → 改用 **region-local uv**,self-IoU
     跳到 0.97–0.98(同時驗證 atlas_crop 對旋轉件 CW derotate 正確)。
4. **自我迭代達標(RULES 迴圈,≤5 輪)**:光暈預設 eps 覆蓋率 0.929<基準;依 eps schedule
   加密邊界 → eps=0.005 第 2 輪達 0.963 過關。身體/左手預設即過。
5. **回歸驗證**:4 mesh v2 validate 全 PASS(generate_mesh_v2 改動為 None 預設,向後相容;
   shadow2 需 `--name image/shadow` 共用 region,非回歸)。

## 結果

3 件 **overall_pass** + 負對照抓到(38 self-int)→ 驗證器 exit 0。
覆蓋率(光暈 0.963 / 身體 0.968 / 左手 0.960)均達藝術家基準,頂點數比藝術家更精簡;
PSD 切件跑同生成器 IoU ≈ atlas 件 → PSD→件→mesh 閉環。

## 關鍵結論

- S3 從「4 unweighted 窗簾/影子(strip)」**推廣到 3 weighted 生產件(Delaunay 靜態)**。
- **Spine mesh uvs = region-local 0..1**;覆蓋率 IoU 由邊界取樣密度(epsilon/hull)決定,
  軟邊界(光暈)需比預設 0.008 更細。詳見 `knowledge/s3-award-weighted-mesh.md`。

## 產出

- 新工具 `tools/mesh_gen/validate_psd_to_award_mesh.py`
- 改 `tools/mesh_gen/generate_mesh_v2.py`(epsilon_frac/max_interior 透傳,向後相容)
- 新 `knowledge/s3-award-weighted-mesh.md` + 更新索引;更新 STATE;本 log

## 下一步(見 STATE.md)

候選 #2:把件→Spine attachment 慣例固化成 SkelToJson 組裝工具(端到端產 Spine JSON);
或加權 mesh 的 BBW 權重生成(本次只驗拓樸/覆蓋,未做骨綁權重)。
