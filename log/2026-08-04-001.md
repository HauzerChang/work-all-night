# 2026-08-04 — session 001

## 這次做了什麼(bounded chunk:S3 推廣到真實生產 weighted mesh）

推進 STATE.md「下一步候選 #1」:把 S3 mesh 生成器從 main_draw 的 4 個簡單 unweighted mesh,
推到真實生產標的 —— `Award.json` 機器人 big win 的 3 個 **weighted** mesh(光暈/左手/身體,hull 40~78)。

1. **建對照閘** `tools/mesh_gen/validate_robot_mesh.py`:從 Award atlas + Award.png/Award2.png
   (atlas_crop 多頁+rotate)取真實 region → 生成 mesh → 靜態 IoU vs 藝術家覆蓋率基準 + 結構健全度。
   （這 3 件 weighted 且無 deform timeline → deform 轉移閘不適用,故只做靜態+結構。）
2. **診斷**:舊固定 `epsilon_frac=0.008` 對複雜輪廓取樣過疏,3 件 IoU 全低於藝術家基準
   (光暈 0.929<0.980、左手 0.960<0.968、身體 0.968<0.976),且光暈多 1 孤兒頂點。
3. **epsilon 掃描確認假設**:IoU 差距**完全來自邊界密度**(降 eps→hull 變密→IoU 逼近甚至超越藝術家)。
4. **修法固化**:`generate_mesh.generate` 加 `target_iou`/`vertex_budget` 的**自適應輪廓密度**
   (由疏到密掃 epsilon,`_recon_iou` 自量測到達標且最省頂點;`target_iou=None` 完全向後相容)。
   `generate_mesh_v2` 的 Delaunay 回退改用它(target 0.982/budget 110,`_mode=delaunay-v1-adaptive`)。

## 結果(自我驗證)

- `validate_robot_mesh.py --gen v2` **all_pass=True**:3 件全部 IoU ≥ 藝術家基準、0 孤兒、結構健全,
  且頂點數比藝術家**更少**(73<78 / 67<80 / 69<98)。光暈用 hull 38 就蓋過藝術家 hull 78 的覆蓋率。
- **無回歸**:main_draw 3 個 strip mesh(curtain_left/right/shadow)`validate_against_real --gen v2`
  仍 overall_pass(strip 路徑未動,只改 Delaunay 回退)。

## 關鍵結論

- 「簡單資產調的參數不泛化」再現(第 4 次校準,前有 rows/premultiplied/derotate)。正解:
  把**絕對量參數換成自量測回饋迴圈**(target IoU 自適應),讓生成器對形狀自我校準。
- 評估閘的頂點預算也非常數:真實生產 mesh 藝術家自身就用 78~98,不能套 main_draw 的 64。

## 產出

- 新增 `tools/mesh_gen/validate_robot_mesh.py`、`knowledge/s3-real-weighted-mesh.md`(+索引)。
- 改 `tools/mesh_gen/generate_mesh.py`(自適應輪廓密度)、`generate_mesh_v2.py`(回退改用之)。
- 更新 `STATE.md`(候選 #1 標完成 + 自適應密度里程碑)、本 log。

## 下一步(見 STATE.md)

- weighted mesh 的 **deform 級**驗證(需實作 skinning + 骨骼動畫套用)。
- **綁權重(BBW)**:把生成的 unweighted mesh 變成可替換生產的 weighted mesh(S3 下半段)。
- 或轉 S2 補圖閘 / 骨架閘(純 CPU 可續)。
