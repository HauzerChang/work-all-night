# 2026-09-01 — session 004(主研究)

## 本次有界工作塊:mesh deform timeline 生成器(candidate 0d 續 / STATE 候選 B)

補上 candidate 0d(分鏡→動畫)最後一個缺口:此前 `gen_animations.py` 只生成 bone TRS + slot
color alpha,**mesh 不會變形**(窗簾/軟體 deform 未生成)。本次讓 `build_spine --animate` 的
mesh 真正 deform。

> 開頭校正(分支):routine 又被指派隨機工作分支 `claude/focused-dirac-hhamoo`(SCHEDULE.md 警告的
> 分支增生現象)。但本 session 的 harness Git Development Branch Requirements **明確指定在
> `claude/focused-dirac-hhamoo` 開發並 push,且「NEVER push to a different branch without explicit
> permission」**。此分支已含完整 S5 歷史(tip aced739 == spine-main 內容),續跑無工作遺失。
> 依 harness 硬性要求在此分支開發/push,並於 log/STATE 記錄與 SCHEDULE.md(偏好 spine-main 線性累積)
> 的張力,供使用者裁決是否合流回 spine-main。

## AC(動手前定,可自量測)

- AC1 well-formed:deform 幀時間嚴格遞增、值有限、offset 長度==2·nv、可 JSON round-trip。
- AC2 loop seamless:loop 類別 `offset(t=0)==offset(t=dur)`,max_err ≤ 1e-6(可無縫接 TRS loop)。
- AC3 topology clean:全取樣幀(含相鄰內插)self_intersections==0 且 flips==0 且 degenerate==0。
- AC4 amplitude plausible:峰值 >0.5px(真的會動)且 ≤ 類別包絡 0.60·長軸;area_ratio∈[0.5,2.2]。
- AC5 negative control:(a) 灌爆/非平滑場 → AC3 必 FAIL;(b) 打斷 loop 無縫 → AC2 必 FAIL。

## 做了什麼

1. **量測藝術家真值定模型**:對 main_draw 4 個真實 mesh 跑 `real_deform_field` + 逐頂點分析,得
   「窗簾=頂錨定(|d|=0)、自由端(底)|d|=max、**水平掃**、幅度**線性於距錨距離**」(窗簾峰值 315px=59%
   長軸,shadow 102px=19%)。→ 確定性場 `offset_perp[i] = A · w_i · shape(τ)`,w_i=距錨正規化距離。
2. **`tools/mesh_gen/gen_deform.py`**(新):`gen_deform_timeline(setup,category,anchor,amp_frac,sway_sign)`
   + `build_deform_block(skeleton,category,...)`。beat 類別→時間 shape:loop=sin travelling-wave(無縫)、
   pulse=sin(πτ)、intro=(1-τ)阻尼(收在 identity)、outro=τ(生長)、hold=無。
3. **`tools/analyzer/spine_anim.py`**:新增 `sample_deform(frames,time)`(取樣 deform offset 向量,含
   bezier/stepped/linear)、`deform_frames_finite(frames)`(well-formedness)。
4. **`tools/analyzer/gen_animations.py`**:`build_animations(...,mesh_deform=True)` 依 beat 類別自動附
   deform 區塊;順手硬化 skeleton width/height 缺失時以 bone bbox fallback(main_draw 無 skeleton.width)。
5. **`tools/mesh_gen/validate_deform_gen.py`**(新):AC1–5 gate,對 4 mesh×4 beat 類別,exit 0=PASS。
6. **`tools/check_readiness.py`**:spine-asset-forge 加 cap `mesh_deform_gen` L2。
7. `knowledge/s1-mesh-deform-generator.md`(新)+ figure + knowledge/README 索引 + STATE 里程碑/進度/下一步。

## 自我驗證結果

- `validate_deform_gen.py`:**AC1–5 全 PASS**(overall_pass:true,exit 0)。4 mesh × 4 beat 類別:
  - AC2 loop seam_err = **0.0**(全 mesh);AC3 全 clean;AC4 全在包絡內(峰值 loop~31/pulse~54/intro~213/outro~187px)。
  - AC5:隨機非平滑場 → **si=62、flip=9**(偵測撕裂);打斷 loop 端點 → seam err>0(偵測)。
    附註:平滑 shear loop 幅度**灌爆 6×(~187px)仍乾淨**(呼應藝術家 315px strip)→ 鑑別力靠隨機場非加大幅度。
- **端到端**:build_animations 對 main_draw(idle→loop / open→intro / close→outro)自動附 4 mesh deform;
  經 `deform_eval.benchmark_real`(讀藝術家真實 deform 的同一支 Spine reader)round-trip **全 clean**。
- **回歸**:robot build --animate + `validate_anim --selftest` overall_pass True;`validate_against_real --gen v2`
  overall_pass True;`check_readiness` exit 0(既有區塊成熟度不變)。

## 關鍵發現 / 決策

- **位移場模型可從藝術家真值反推**(非臆測):4 個真實 mesh 一致呈現「錨定端→自由端線性掃動」
  → 確定性演算法有真值依據(符合 RULES「不用 ML 學美術決定」)。
- **平滑 shear 場極耐變形**:單向、幅度單調於 w 的剪切場本質不自交 → AC3 鑑別力**必須用隨機非平滑場**
  demo(si=62),不能靠加大幅度(這也解釋為何藝術家 315px strip 拓樸乾淨)。
- **類別包絡 vs 自身峰值**:AC4 上限用「跨所有藝術家 mesh 最大 deform≈59%長軸」當類別物理包絡,
  非「該 mesh 藝術家峰值」(後者是手感選擇,如 shadow 只掃 19%,非物理硬限)。

## 界定 / 下一步

- honest boundary:預設幅度窗簾級(intro/outro 對 shadow 會掃到 213>102px,在包絡內但語意偏大)→
  role-specific 幅度應由分析器/storyboard 傳 `amp_frac`(生成器已開放);reveal intro/outro 為**非零端點**
  設計(收在 identity 以可串接),大單向 open/close sweep 留 (B');weighted mesh 逐頂點 offset 不適用 → (B'')。
- 下一 bounded chunk 候選(皆可自主):(A) S1 keyframe 補主秀 beat 模板;(B') reveal 大單向掃動 +
  role-specific 幅度;(B'') deform-of-weighted。S5 自主面已飽和(再推需 (D) 多 rig 真值,使用者資源)。

## commit & push

`claude/focused-dirac-hhamoo`(依 harness 指定分支)。
