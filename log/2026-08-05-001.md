# 2026-08-05 — session 001

## 這次做了什麼(S3×S4 端到端:PSD件 → 生成 mesh → 對照 Award 真實生產 mesh)

推進 STATE.md 最高優先 bounded chunk(候選 1):對機器人 3 個 mesh 件做端到端真實驗收。

1. **對映驗證**:Award 中光暈/身體/左手為 mesh(右手/頭為 region)。這 3 件**無 deform timeline**
   → 判準改為靜態覆蓋率 + 頂點預算 + 拓樸有效性對照真實 mesh(不跑 deform 轉移閘)。
   以 8 種 uvs↔遮罩方位測試,**identity 全勝(0.97+)** → 確認 `uvs*(W,H)` 疊圖成立(atlas_crop de-rotate 後同向)。
2. **新工具** `tools/mesh_gen/compare_award_mesh.py`:extract 真實貼圖件 → 生成 mesh →
   ①生成覆蓋率 vs ②藝術家覆蓋率 vs ③cross_iou + 頂點預算 + 有效性,逐件 pass/fail。
3. **初跑 2/3**:身體/左手 pass,**光暈 fail**(覆蓋率 0.929<藝術家 0.979 + 1 孤兒頂點)。
4. **揪修 v1 兩缺陷**:
   - 孤兒頂點:`filter_triangles` 濾凹形三角後留下未引用頂點 → 加 `prune_orphans()`(移除+重編,保 hull-first)。
   - 軟邊欠取樣:v1 預設 `epsilon_frac=0.008` 對軟羽化光暈太粗;掃 epsilon 用真值校準 → 改 **0.0025**。
5. **重驗 3/3 pass**:光暈 0.980 / 身體 0.992 / 左手 0.989,頂點數 71/75/62 全 < 藝術家 78/98/80。
6. **回歸**:main_draw 4 mesh 走 v2 strip、不受 v1 預設變更影響,重驗全 overall_pass
   (註:shadow2 的 attachment/region 名為 `image/shadow`,與 slot 名不同)。
7. **視覺驗證**:`knowledge/figures/s3-award-mesh-compare.png`(橘=藝術家、綠=生成 wireframe)。

## 關鍵結論 / 決策

- **S3 首次對真實生產 mesh(有真值)端到端驗收通過** → S3 從 main_draw 推廣到第二個真實標的(機器人)。
- **真值是最好的 fidelity 校準器**:v1 epsilon 是無真值下的猜值;拿到真實 mesh 才知該取多細
  → 再次印證「改對評估/真值 > 硬調演算法」。
- 藝術家用「密 hull 全邊界」描軟邊;生成器用「疏 hull + 內部點」以更少頂點達同覆蓋率 → 策略不同但等價且更省。

## 產出

- `tools/mesh_gen/compare_award_mesh.py`(新)
- `tools/mesh_gen/generate_mesh.py`(加 `prune_orphans`;預設 epsilon 0.008→0.0025)
- `knowledge/s3-award-mesh-e2e.md`(+索引)、`knowledge/figures/s3-award-mesh-compare.png`
- 更新 `STATE.md`(里程碑 + 下一步改為 SkelToJson 組裝端)、本 log

## 下一步(見 STATE.md 候選 2)

**SkelToJson 組裝端**:把「切件 → mesh → Spine attachment JSON」固化成工具(slot 命名 `PSD名/圖層名`
+ size+2px padding + mesh/region 分配),端到端產可載入 Spine JSON,下游以重載對照 Award 命名/拓樸自驗。
