# 2026-08-05 — session 001

## 這次做了什麼(S3 泛化到 Award 機器人 mesh + epsilon 校正)

推進 STATE 下一步 #1:把 S3 mesh 生成器從 main_draw 推廣到**第二份生產資產**
(Award「機器人拆件」),用真實 mesh 當外部真值,防止「只在 main_draw 過擬合」。

### AC(先定可檢查目標)
對 Award 機器人 3 個真實 mesh(光暈/左手/身體),用同一 atlas region 為單一來源,
`IoU(generate_mesh_v2) ≥ IoU(藝術家 uvs) − 0.03`,且生成 mesh 全 AC(≤64v/0孤兒/0退化)乾淨,
且 main_draw 4 mesh 不回歸。

### 做的事
1. 新增 `tools/mesh_gen/validate_robot_mesh.py`:atlas region(自動選頁+CW derotate)→ mask;
   生成 mesh IoU vs 藝術家 uvs IoU;`best_artist_iou` 掃 4 種 uv 取向自動偵測 rotate 對齊。
2. 初測發現 3 mesh 皆走 **delaunay-v1**(blobby、非高瘦 → auto 正確回退),且**光暈 FAIL**
   (gen 0.929 vs art 0.980,gap −0.05)。
3. 根因定位(用漏覆蓋像素 + 掃描線):光暈 33% 半透明羽化邊、無洞;固定 `epsilon_frac=0.008`
   只給 14 hull 點 → 剪掉星形凸角、漏 6364px(藝術家 78 hull 點、漏 13)。
   **洞見:覆蓋率由 hull 密度決定,內部點不影響覆蓋。**
4. 校正 `generate_mesh.py` v1:`epsilon_frac` 0.008→0.003 + 頂點預算自適應
   (`interior = min(max_interior, budget − n_hull)`,內部點讓位給 hull)。

### 自我驗證(量化,非肉眼)
- Award 3 mesh:光暈 0.978(−0.002)/ 左手 0.987(+0.018)/ 身體 0.988(+0.012)→ **全 PASS**,
  全 ≤64v,`evaluate_mesh` 6 條 AC 全乾淨。
- main_draw 4 mesh:走 strip 路徑不受 v1 epsilon 影響 → `validate_against_real --gen v2`
  curtain_left/right/shadow 仍 overall_pass、mode=strip、deform 乾淨。**無回歸**。
- 副產驗證:rotate region(光暈/身體 rotate=true)的藝術家 uvs 全選 `y/xy`(原樣)且 art_iou 0.97–0.98
  → 確認 atlas_crop CW-derotate 後 Spine uvs 直接對齊裁切圖(補上 rotate uv↔pixel 對齊這一環)。

## 關鍵結論
- **S3 現跨兩份生產資產達藝術家覆蓋率**(main_draw strip + Award robot delaunay),泛化性確立。
- **固定 epsilon 對羽化/星形輪廓不夠**;覆蓋率是 hull 問題,校正後兼顧貼合與頂點預算。

## 誠實限制 / 下一步
- ⚠️ **只驗了靜態覆蓋率,未驗變形穩健**:Award 機器人 mesh 全 **weighted**,`deform_eval`
  的 `reshape(-1,2)` 只對 unweighted 正確 → real-deform 閘跑不了。
- 下一 chunk(STATE #1):讓 deform_eval 解析 weighted vertices,對 3 機器人 mesh 跑真實位移場
  轉移閘,把「PSD→件→mesh」對機器人完整驗收(覆蓋率 ≠ 耐變形,main_draw 已學過此教訓)。

## 產出
- 新增 `tools/mesh_gen/validate_robot_mesh.py`、`knowledge/s3-award-robot-generalization.md`(+索引)
- 改 `tools/mesh_gen/generate_mesh.py`(epsilon 0.003 + 頂點預算自適應 + docstring)
- 更新 `STATE.md`(S3 泛化里程碑 / 下一步改為 weighted deform 閘)、本 log
