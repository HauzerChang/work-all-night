# 2026-07-06 — session 001

## 這次做了什麼(S3 端到端對照 Award 真實生產 mesh — STATE 候選 #1)

推進 STATE「下一個 bounded chunk 候選 #1(最高優先,有真值可比)」:
用 Award(big win spine)機器人拆件 **3 個藝術家手做 mesh**(光暈/身體/左手)當**外部真值**,
端到端跑「atlas 生產貼圖切件 → `generate_mesh_v2` → 量化」並逐件對照。

### AC(動手前定義)
1. 生成 mesh 靜態 IoU(vs 生產貼圖 alpha)≥ 藝術家 mesh 自身 IoU(baseline)。
2. 格式/退化/孤兒/頂點預算(evaluate_mesh)全過。
3. deform 閘:確認 Award 這 5 件**無 deform timeline** → N/A(不用未校準合成壓力充數)。

### 結果:3 件全 overall_pass(eps=0.002)
| 件 | 藝術家 IoU | 生成 IoU | gap |
|---|---|---|---|
| 光暈 | 0.9795 | 0.9832 | +0.0037 |
| 身體 | 0.9760 | 0.9926 | +0.0166 |
| 左手 | 0.9681 | 0.9913 | +0.0232 |

## 關鍵發現 / 校準

- **mesh `uvs` = region 局部 0..1**(非整頁 atlas UV;log-006 筆記過度保守)。外部校驗:
  3 件 artist mesh 對 derotate 切件 silhouette IoU 0.968~0.980 → 一併確認 atlas_crop CW derotate 正確。
- **eps 預設 0.008 對細緻生產件太粗**(hull 14~21、光暈 IoU 差 -0.053);掃描出 **0.002 是甜蜜點**。
- **v1 孤兒頂點 bug**:凹形區三角被濾後留下未引用內部點 → 加 `prune_orphans()` 修復。
- 侷限:達同等 IoU 生成需 117~123v(藝術家 78~98v),頂點效率略遜(Delaunay 過採樣)。

## 產出
- 新工具 `tools/mesh_gen/compare_award_mesh.py`(端到端整合 AC,預設對 3 件真值)。
- 改 `generate_mesh.py`(`prune_orphans` + 接線)、`generate_mesh_v2.py`(`eps/max_interior/min_dist` 轉發)。
- `knowledge/s3-award-real-mesh.md` + 索引;本 log;STATE。
- 回歸:main_draw 4 mesh(v2)+ slicing 全 PASS(shadow2 attachment 名為 `image/shadow`,共用 region)。

## 下一步(見 STATE.md)
- 候選 #2:把對應慣例(`PSD名/圖層名`、mesh/region 分配、+2px、atlas 0.70 縮放、region-local uvs)
  固化成「件→Spine JSON 組裝」(SkelToJson)工具,端到端輸出可載入的 Spine mesh attachment。
- 頂點效率優化(可選):讓生成器更省頂點以逼近藝術家精簡度。
