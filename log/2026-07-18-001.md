# 2026-07-18 — session 001

## 這次做了什麼(自驅排程 session)

推進 STATE.md 最高優先 bounded chunk:**PSD件→S3 mesh→對照 Award 真實 mesh**。

1. **定 AC(AC-first)**:S3 v2 auto 對 Award 3 個機器人 mesh(光暈/左手/身體)
   - AC1 覆蓋:gen_IoU ≥ artist_IoU − 0.02(真值=藝術家 weighted mesh 覆蓋率,只吃 uvs/triangles → 可信)
   - AC2 預算:gen_nv ≤ artist_nv
   - AC3 靜態拓樸:0 退化 / 0 孤兒
2. **建工具** `tools/mesh_gen/validate_award_static.py`:atlas 依 region page 自動切頁(左手在 Award.png、
   光暈+身體在 Award2.png)→ 生成 mesh → IoU vs 藝術家基準 + 產對照圖(`knowledge/figures/award_static/`)。
3. **發現關鍵事實**:Award 3 mesh 全 **weighted**(main_draw 的 4 mesh 全 unweighted)。
   `deform_eval` 假設 unweighted → **weighted 的 deform 閘不可信**,故本次限定「靜態覆蓋」驗收。
4. **第一輪(auto)**:2/3 過;**光暈 fail**(delaunay-v1 覆蓋 0.929 < 目標,且 1 孤兒頂點)。
   看對照圖確認:光暈是尖刺凹形(射線),散點 Delaunay 切掉凹處;藝術家用全 hull 邊界環(hull==nv==78)。
5. **修正(在迭代預算內)**:於 `generate_mesh_v2.py` 加 **contour 模式** —— 外輪廓 loop + 受約束 PSLG
   三角化(`triangle` 'p',不加 Steiner 點),`nv==hull==邊界點數`,凹處正確填滿。
   auto 依 `solidity<0.9` 選 contour。
6. **驗證**:3/3 全過且**超越藝術家**(IoU 0.986/0.990/0.994 vs 0.980/0.968/0.976;頂點 45/42/45 vs 78/80/98);
   setup 0 自交/0 退化/0 孤兒、Spine 格式合法。**回歸**:main_draw 4 unweighted mesh 仍全過(strip 不受影響)。

## 關鍵結論

- **S3 對第二個生產骨架(Award)通用**(靜態覆蓋),且 contour 模式讓生成品質超越藝術家手做覆蓋率。
- **方法論再實踐**:AC-first → auto 跑 → 對照圖診斷 fail → 針對性加模式(不改既有預設)→ 回歸驗證。
- **contour vs delaunay-v1 分工**:凹形/尖刺(solidity<0.9)用 contour;高 solidity blobby 才留給 v1。

## 重要限制 / 誠實聲明

- **只驗了靜態覆蓋**;weighted mesh 的「耐變形」尚未驗(deform_eval 假設 unweighted)。
  → 下一 chunk 明確定為 **weighted-aware deform 閘**(見 STATE.md 下一步 #1)。

## 產出

- 新增 `tools/mesh_gen/validate_award_static.py`、`knowledge/s3-award-weighted-static.md`(+索引)、
  `knowledge/figures/award_static/{光暈,左手,身體}_static.png`。
- 修改 `tools/mesh_gen/generate_mesh_v2.py`(加 `gen_contour`/`solidity`/auto 選擇/CLI mode=contour)。
- 更新 `STATE.md`(里程碑+下一步)、本 log。

## 下一步(見 STATE.md)

weighted-aware deform 閘(把此閉環從靜態升到靜態+變形);之後 SkelToJson 寫端把 S3/S4 串出 Spine JSON。
