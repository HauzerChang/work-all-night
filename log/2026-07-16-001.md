# 2026-07-16 — session 001

## 這次做了什麼(bounded chunk:S3 端到端對照真實生產 mesh)

推進 STATE 的「最高優先」候選:**PSD/atlas 件 → S3 `generate_mesh` → 對照 Award 真實藝術家 mesh**。

1. **定 AC**:對 Award spine 中 3 個 `type=mesh` 的機器人件(`機器人拆件/光暈`、`左手`、`身體`),
   生成 mesh 的靜態覆蓋 IoU **≥ 藝術家自身 mesh 的覆蓋率 baseline**,且格式/預算/無孤兒/無退化三角全過。
   - 真值來源:直接從 Award atlas 切 region alpha(與藝術家 mesh 的 region-local uvs 同一貼圖幀,scale 一致可直接比)。
2. **新增** `tools/mesh_gen/validate_award_mesh.py`(重用 `atlas_crop.extract`、`evaluate_mesh.evaluate`、
   `validate_against_real.artist_iou`)。
3. **首跑(預設參數)3 件全 fail**,但只差 1~5%(gen IoU 0.929/0.960/0.968 vs baseline 0.980/0.968/0.976),
   光暈另有 1 孤兒頂點。
4. **診斷 + 迭代(2 輪,預算內)**:
   - 根因一:`epsilon_frac=0.008` 把輪廓簡化到 hull 只剩 14~21 點 → 切掉凹凸角欠覆蓋。掃描 eps → **0.002 甜蜜點**
     (hull 37~43、nv 67~77,貼齊藝術家 78~98 的精簡度且全過;更小則頂點爆量無意義)。
   - 根因二:`vertex_budget=64` 對真實大件太緊 —— **藝術家件本身就 78/80/98 頂點**。→ budget=100。
   - 調整後 **3 件 overall_pass(exit 0)**:覆蓋 IoU 0.983/0.991/0.993 全 ≥ baseline。
5. **未改全域生成器預設**;重驗 main_draw curtain_left/right(v2)仍 overall_pass、deform si=0 → 無回歸。

## 關鍵結論

- **S3+S4 端到端對真實生產標的驗收通過** — 生成器能達藝術家等級覆蓋。
- **mesh 密度是「標的相依」的**:單一固定 eps+budget 不通用;正確密度 = 貼齊該件藝術家精簡度。
- **Award mesh 件 weighted + 無 deform timeline**(骨骼權重 warp)→ 沒有位移場真值可轉移,
  只做靜態+拓樸 AC;**不硬套未校準合成壓力場**(守 STATE 的教訓)。
- 拓樸有趣點:光暈藝術家 mesh 是「純邊界」(hull=78=全 78 頂點、0 內部點)。

## 產出

- 新增 `tools/mesh_gen/validate_award_mesh.py`、`knowledge/s3-award-mesh-match.md`(+索引)。
- 更新 `STATE.md`(里程碑 + 下一步:自適應 epsilon)、本 log。

## 下一步(見 STATE.md)

- **自適應 epsilon**:依 arcLength / 目標頂點預算自動定 eps → auto 對任意件免手調貼齊藝術家。
- 或:切圖→Spine JSON 組裝(SkelToJson);S2 補圖/骨架閘。
