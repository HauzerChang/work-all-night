# 2026-08-10 — session 001

## 這次做了什麼(S3+S4 端到端:PSD件→mesh→對照 Award 真實 mesh)

推進 STATE.md 候選 #1(最高優先,有真值)。端到端「PSD 切件 → S3 生成 mesh → 對照 Award 生產 mesh」。

1. **切件**:`psd_slice.py assets/robot_parts.psd` → 光暈/身體/左手(+右手/頭 region 略)。
2. **座標框自檢**:先確認 Award 藝術家 mesh uvs(region-local 0..1,rotate 件亦然)填入 PSD 件 alpha
   IoU 0.95/0.95/0.98 → 座標框一致、baseline 可信,才下判定。
3. **對照生成**:`generate_mesh_v2` auto **正確把 3 件 blob 路由到 v1 Delaunay**(非 strip)。
4. **發現覆蓋不足**:v1 預設 eps=0.008(為窗簾調)對羽化/凹形件覆蓋率略低於藝術家
   —— 光暈 0.933<0.949、左手 0.964<0.977(身體 0.966>0.948 已過)。
5. **迭代(5 輪預算內)**:掃 epsilon 0.008→0.001,IoU 單調升(同 v2「密度決定覆蓋」機制);
   coarse 的 topo 失敗來源查明 = 孤兒內部點,加密即解。
6. **收斂解**:新增 `compare_to_award.py`,含 **adaptive** —— 沿 epsilon 階梯加密到「達標且拓樸全過的
   最精簡版」。3 件全 `overall_pass`:光暈 eps0.002 v64/78、身體 eps0.008 v60/98、左手 eps0.003 v75/80,
   IoU 全 ≥ 藝術家且頂點更少。

## 關鍵結論 / 教訓

- **S3 通用到第二類真實 mesh**:Award 這 3 件**無 deform timeline**(weighted-bone 變形),與 main_draw
  窗簾(strip、逐頂點 deform)不同類 → real-deform 閘誠實標 **N/A**,改用靜態幾何三 AC(覆蓋率/拓樸/預算)。
- **auto 路由選對**:blob 用 Delaunay、窗簾用 strip,與藝術家自身拓樸選擇一致。
- **覆蓋率是密度的函數**:v1 靠 epsilon、v2 靠 rows;adaptive 把「校準到目標」自動化,不必硬改全域預設。
- **不動既有工具 → 無回歸**(抽驗 main_draw curtain_left overall_pass)。

## 產出

- 新增 `tools/mesh_gen/compare_to_award.py`(端到端驗證器 + adaptive 加密)。
- 新增 `knowledge/s3-psd-to-award-mesh.md`;更新 `knowledge/README.md` 索引、`STATE.md`。
- 本 log。

## 下一步(見 STATE.md 候選 #2)

SkelToJson:把 `PSD名/圖層名` slot 命名 + size+2px padding + mesh/region 分配 + adaptive mesh 密度
固化成「件→Spine attachment」組裝工具,端到端產可用 Spine JSON。之後才做 weighted-bone 權重(BBW)。
