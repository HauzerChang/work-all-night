# 2026-08-10 — session 001

## 這次做了什麼(S3×S4 端到端:PSD件 → 生成 mesh → 對照 Award 真實 mesh)

推進 STATE.md 標記的最高優先 bounded chunk:把 S4(PSD 切件)與 S3(`generate_mesh_v2`)串成
端到端,對 **Award 生產 spine 的真實藝術家 mesh** 做驗收(有 ground truth)。

1. **切件**:`psd_slice.py assets/robot_parts.psd -o /tmp/robot_pieces` → 5 件 PNG + manifest。
2. **校驗 UV 對映**:先確認 Award mesh `uvs` 是 region-relative [0,1],`uv*W, uv*H`(**不 flip v**)
   對齊 PSD 切件像素空間 → 藝術家 mesh 覆蓋自身 alpha IoU 0.945/0.948/0.977(合理)。
   v-flip 版只有 0.426~0.604 → 確認方向。
3. **新工具** `tools/mesh_gen/validate_psd_to_award.py`:對 3 個 mesh 件(光暈/身體/左手)
   跑 generate_mesh_v2 → 3 AC:① IoU ≥ 藝術家 −0.03 ② 頂點 ≤ 藝術家 ×1.30 ③ 靜態 0 自交/0 退化。
4. **結果:3 件全 `overall_pass`(exit 0)**:
   - 光暈 gen 35v IoU 0.9343 (artist 78v 0.9445)
   - 身體 gen 60v IoU 0.9661 (artist 98v 0.9476) ← 生成覆蓋更高
   - 左手 gen 59v IoU 0.9643 (artist 80v 0.9766)
   全部用約藝術家一半的頂點達到等級覆蓋、0 自交。auto 模式對 3 件都選 delaunay-v1。
5. **負對照**:光暈 mesh 套身體 alpha(件錯配)IoU 0.488;光暈 v-flip 0.426 → 遠低於 ~0.94 通過區,
   確認 IoU 閘有鑑別力。

## 關鍵結論 / 決策

- **S3 生成器通用到第三個資產家族**(機器人 big win,與 main_draw 窗簾/陰影無關):真實生產藝術家
  mesh 的靜態覆蓋率被達到/超越,且頂點預算更省。這是「純 CPU 生成 mesh 可對真實標的用」的直接證據。
- **誠實聲明**:這 3 件在 Award 無 deform timeline(靠骨骼/權重),**無真實位移場**。依 RULES
  不捏造 stress_field,本次**不做變形閘**;變形穩健度已在 main_draw 4 mesh(真實場)證明。
- 再次先確認方向 + 負對照才信結論(延續前幾次「評估器要校準」的教訓)。

## 產出

- `tools/mesh_gen/validate_psd_to_award.py`(新工具,端到端驗收)
- `knowledge/s3-s4-psd-to-award-mesh.md`(+索引)
- 更新 `STATE.md`(里程碑 + 下一步改為 SkelToJson 組裝)、本 log

## 下一步(見 STATE.md)

(2) **SkelToJson 組裝工具**:把件 + 生成 mesh + 真實命名/尺寸/uv 慣例,寫成可載入的 Spine
attachment JSON,收口整條 PSD→spine pipeline(純 CPU 可自驅)。
