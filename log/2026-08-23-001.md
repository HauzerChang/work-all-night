# 2026-08-23 — session 001

## 這次做了什麼(bounded chunk:S3 weighted-mesh 變形評估器)

延續 `s3-robot-mesh-vs-award.md` 標記的**唯一未驗維度**:weighted mesh 的「骨骼變形平滑度」
(靜態 IoU 不涵蓋)。依 RULES「每能力必配評估器」,本 chunk 專注把**評估器**做可信,
生成器對照留待下一 chunk。

### 產出:`tools/mesh_gen/weighted_deform_eval.py`
- **FK**:每骨 world transform = parent.world ∘ local(setup + 動畫 delta);全骨依序。
  local 矩陣依 Spine 慣例(rotate+scale+shear);動畫 rotate/translate 加、scale 乘、shear 加。
  driving 骨全 transform=`normal`(已驗);其他 mode 拋 `NotImplementedError`(不默默算錯)。
- **LBS**:`Σ_b weight_b·(bone_b.world 套用 bind 座標)`,重現 Spine weighted mesh 世界頂點。
- **幾何閘**:重用 `deform_eval.eval_pose`(自交/翻面/退化/面積比)。
- **slot-alpha 可見度閘**:`slot_alpha()`(honor stepped);只判定 alpha>0.05 的可見幀。

### 自我驗證(三閘全 PASS,`_all_pass: true`)
- **AC1 中性**:空動畫 == setup,3 件 max_diff = 0.0(FK/LBS 正確)。
- **AC2 setup 乾淨**:3 件 setup 世界頂點 0 自交 / 0 退化。
- **AC3 負對照**:注入 +30/60/120° 假旋轉 → 抓到自交 23~81、翻面 1~3(有鑑別力)。

### 藝術家真實變形基準(visible frames)
- 硬邊件(左手/身體):In+Loop 全乾淨、面積比 ≈1.0(身體近剛體、左手手臂收放 0.78 但拓樸恆合法)。
- 軟邊光暈:Loop 全乾淨(面積 0.997–1.002);In 可見尾段容忍 4 自交(羽化半透明,肉眼無感)。

## 關鍵發現 / 踩坑
1. **變形判定必須做可見度閘**:`Award_Legend_In` 進場 slot alpha=`ffffff00`(不可見)+ 骨 stepped 1.667× 縮放
   → 未閘時光暈報 71 自交、面積 1.98,全在不渲染窗;加閘後 4 自交。**不可見的進場擠壓不是破圖。**
2. **初版 AC1 假性 FAIL**:「動畫 t=0 == setup」對 In/Out 進出場動畫不成立(t=0 本就偏離 setup)。
   改「空動畫 == setup」的中性測試才正確隔離 FK。
3. 軟邊 blob 的變形容忍度非 0 → 標竿應取 sustained `Loop`,而非「所有可見幀 0 自交」。

## 產出清單
- 新工具 `tools/mesh_gen/weighted_deform_eval.py`
- 新知識 `knowledge/s3-weighted-deform-evaluator.md` + 索引
- 圖 `knowledge/figures/robot_weighted_deform_loop.png`(setup vs Loop 最大變形疊圖)
- `requirements.txt` 加 matplotlib
- 更新 STATE.md(里程碑 + 下一步:生成器半 內部取樣+BBW)

## 下一步(見 STATE.md 候選 2 生成器半)
對 Award 3 件同一組骨,生成我方 mesh(內部取樣密度控制)+ heat-diffusion/BBW 權重,
用本評估器做同骨同動畫變形對照;AC:可見幀不劣於藝術家基準、權重和=1、權重過渡平滑。純 CPU 可自驅。
