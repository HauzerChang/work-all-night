# 2026-08-23 — session 001

## 這次做了什麼(bounded chunk:候選 2a — weighted mesh 變形評估器)

依 STATE 最高優先候選 #2「S3 weighted mesh + BBW」,遵 RULES「每能力必配評估器」,
先做**評估器**(BBW 能力的自我品質閘),補上 `compare_robot_mesh.py` 誠實界定的**唯一未驗維度**:
weighted mesh 靠骨骼+權重 LBS 變形時的**變形品質**(靜態 IoU 不涵蓋)。

### 新工具 `tools/mesh_gen/weighted_deform_eval.py`
- 最小 **Spine 3.8 FK 引擎**:bone setup local → 世界矩陣(transform=normal;標準 updateWorldTransform);
  動畫套用 rotate/translate/scale,支援 linear / stepped / **緊湊 bezier**(10 段查表 x→y)。
- **linear-blend skinning**:weighted 頂點格式 `[boneCount,(boneIdx,bindX,bindY,weight)*]`。
- 拿 Award 真實動畫(`Award_Legend_In/Loop/Out`)驅動機器人 3 weighted mesh 件(身體/左手/光暈)。

### AC(可檢查驗收目標)= 三項可信度閘,全過才算評估器可信
- **G1 rigid-invariance**:整體旋轉 40° → skinned == 解析旋轉 setup。**max 誤差 ~1e-13**、面積比 1.0、0 翻面 → FK+LBS 數學正確。
- **G2 positive control**:真實動畫逐幀(13幀)skinning。身體/左手 **3 動畫全乾淨**;3 件**穩態 loop 全乾淨**。
- **G3 negative control**:半數頂點強綁遠端骨 → 自交 38~2945/翻面出現 → **全件抓到**(有鑑別力)。
- **第三獨立校驗(絕對空間)**:setup skinned bbox vs `atlas region÷0.70`(STATE 記載 0.70 打包):
  左手 <2%、光暈 <5% → 證絕對世界座標也正確,非僅相對自洽。
- 結果:`evaluator_trustworthy=True`,exit 0。

### 關鍵發現
- 平滑度真正目標是**穩態 loop(呼吸)**,不是進出場暫態。身體/左手 In/Loop/Out 全乾淨;
  **光暈只在 In 自交**(面積 1.98x、位移 676px)—— 非評估器誤判,而是真實現象:光暈是**加法軟 blob**,
  進場被 4 骨扇形拉大近 2 倍,自疊在加法混色下**不可見**;同樣自交在不透明件(身體)才是撕裂缺陷。
- 故閘判定對齊 `STEADY_ANIM=Award_Legend_Loop`;In/Out 極端幀列 `non_clean_anims` 觀察值,不判失敗。

## 產出
- 新工具 `tools/mesh_gen/weighted_deform_eval.py`(FK+LBS+3閘+`real_bone_deform_field` 副產)。
- 圖 `knowledge/figures/weighted_deform_gates.png`(3件×setup/Loop max/In max,綠乾淨/紅自交)。
- 新知識 `knowledge/s3-weighted-deform-evaluator.md` + 索引;更新 `STATE.md`。

## 下一步(見 STATE 候選 2b,最高優先)
BBW 權重生成 + 內部取樣密度:(i) `generate_mesh_v2` 加內部取樣密度旋鈕;(ii) 對生成 mesh 算 BBW 權重
(純 CPU 拉普拉斯+有界二次規劃,或骨段 heat-diffusion 近似);(iii) 用本評估器對「生成 mesh+BBW」施
同一真實 pose,量化 loop 乾淨度 + 位移場 vs 美術件 RMS 差 → 變形平滑度對等。真值(權重/骨架)已在 `Award.json`。
