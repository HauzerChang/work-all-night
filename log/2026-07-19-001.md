# 2026-07-19 — session 001

## 這次做了什麼(S3×S4 端到端對真實生產 spine mesh 驗收)

延續 STATE.md 最高優先 chunk:把 S4(PSD 切件)與 S3(mesh 生成)串成端到端,
對**真實生產標的**(Award spine 的機器人 3 個 mesh 件)做 IoU 對照驗收。

### AC(動手前先定的可檢查目標)
- AC1:`robot_parts.psd` 的光暈/身體/左手 3 件經 `psd_slice`→`generate_mesh_v2(auto)`,
  各自對件 alpha mask 的靜態 IoU **≥ 藝術家真實 mesh(Award 同 slot)對同一 mask 的 IoU − 0.02**。
- AC2:生成 mesh 0 退化三角/0 孤兒/形心在遮罩內。
- AC3:報頂點數對照(是否在預算內)。
- 前提驗證:Award mesh uvs 的座標慣例(region-local vs atlas-global)、與 PSD 件框對齊(v 翻轉否)。

### 執行 + 量測
1. **前提量測**(不假設,直接測):
   - Award mesh uvs **region-local [0,1]**(光暈 u 跨 0.012–0.99;atlas-global 只會佔 ~0.24)→
     **修正 `s4-psd-to-spine-real.md` 先前「atlas UV 需轉 region」的錯誤推測**。
   - 與 PSD 件同框、**無 v 翻轉**(flipv=False 時 artist IoU 0.95/0.95/0.98;翻轉掉 0.43–0.60)。
2. **新工具** `tools/mesh_gen/validate_psd_mesh.py`:件 PNG → 生成 mesh → 生成 IoU vs 藝術家 baseline IoU。
3. **結果(`--gen v2`,exit 0,3/3 PASS)**:

   | 件 | 生成 v/tri | 生成 IoU | 藝術家 v/tri(weighted) | 藝術家 IoU | gap |
   |---|---|---|---|---|---|
   | 光暈 | 35/49 | 0.9331 | 78/76 | 0.9486 | −0.0155 |
   | 身體 | 60/97 | 0.9660 | 98/154 | 0.9477 | **+0.0183** |
   | 左手 | 59/97 | 0.9642 | 80/116 | 0.9768 | −0.0126 |

4. **負對照(確認閘鑑別力後才信 pass)**:跨件錯配 IoU 0.48–0.58;生成 uvs 平移 +0.15 → 0.43–0.60。
   margin 0.02 遠高於雜訊地板(~0.5)。

## 關鍵結論
- **兩能力(S4 切圖 + S3 mesh)首次串成端到端,對真實生產 mesh 驗收通過**,且**頂點更省**。
- `mode='auto'` 對這 3 緊湊團塊件全選 delaunay(非 strip)—— 印證選模邏輯(細長走 strip、緊湊走散點)。
- **重要區分**:Award 這批件是 **weighted 骨驅 mesh、無 deform timeline**,故可信閘=靜態覆蓋率;
  deform 轉移閘是 main_draw(unweighted+deform-timeline)情境的解,對這批 N/A。S3 v2 只到 unweighted 拓樸,
  權重綁定屬 S5 範疇。
- 方法論再實踐:先量測前提(座標慣例/翻轉)→ 再跑閘 → 再負對照,不靠假設。

## 產出
- 新增 `tools/mesh_gen/validate_psd_mesh.py`、`knowledge/s3-psd-to-real-mesh.md`(+索引)。
- 更正 `knowledge/s4-psd-to-spine-real.md` 待辦(標完成 + 修正 uv 慣例假設)。
- 更新 `STATE.md`(下一步定為 SkelToJson)、本 log。

## 下一步(見 STATE.md)
**SkelToJson**:把真實命名慣例 + padding + region-local uvs + 本次驗過的 mesh 生成,寫成
端到端「PSD → 可用 Spine JSON」組裝工具,用既有閘當出廠檢驗 → 把 S4+S3 升成「產出可用資產」。
