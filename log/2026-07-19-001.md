# 2026-07-19 — session 001

## 這次做了什麼(排程 session:S3×S4 端到端串接)

推進 STATE 下一步候選 #1(最高優先、有真值可比):**PSD 件 → S3 mesh → 對照 Award 真實 mesh**。

### AC(動手前定)
對 `robot_parts.psd` 中在 Award 為 mesh 的 3 件(光暈/身體/左手):
1. gen 靜態輪廓覆蓋 ≥ 0.90(main_draw 藝術家 mesh 基準 ~0.918)。
2. 頂點預算 ≤ 64,並報告 vs 藝術家 verts(78/98/80)。
3. 拓樸健全(0 孤兒 / 0 退化 / hull 有效)。
4. 認清 regime:Award 這 3 件無 deform timeline → 靜態閘,不套逐頂點 deform 閘。

### 做法與產出
- 新增 `tools/mesh_gen/award_mesh_compare.py`:slice PSD 件 alpha → `generate_mesh_v2` → `evaluate_mesh`
  靜態覆蓋 + 頂點預算閘;另算「藝術家 mesh 對件覆蓋」當對照(自校準 8 二面體擺放)。
- **結果:3 件全 PASS**(exit 0)。gen 覆蓋 0.933/0.966/0.964;頂點 35/60/59 = 藝術家 45%/61%/74%(更省)。
- 全走 **v1 Delaunay**(aspect <1.2 blob regime),非 strip → 印證 v1 適肢體/光暈、v2 strip 適窗簾。

### 自我驗證(誠實修正)
- **抓到並修掉一個假對照**:藝術家 mesh 對件覆蓋原本想當 pass/fail 基準,但 rotate:true 的身體卡在
  IoU 0.64。debug:recon 過覆蓋 35k px、overlay 見藝術家 mesh 相對真值旋轉/偏斜(uv 空間受 atlas
  90° 旋轉 + page 長寬比污染,uv bbox aspect 1.239 ≠ 件 1.121)。→ **降為 informational + reliable 旗標**
  (僅 rotate:false 的左手可信:0.974 vs gen 0.964,gen 用 74% 頂點做到同覆蓋),閘改用 gen 絕對覆蓋。
- **評估器負對照**:身體 mesh 向心縮 15%→0.709、平移 +25px→0.744(皆 <0.90)→ 覆蓋閘有鑑別力。

## 關鍵結論
1. S3+S4 端到端串通並對真實生產標的(Award 機器人 mesh)驗收:生成件更省頂點、覆蓋達標。
2. 兩拓樸各有適用域(v1 blob / v2 strip),auto 判別分對。
3. Award 這 3 件是 weighted+骨驅動(無 deform)→ 補上 S3 第二個 regime;逐頂點 deform 是另一 regime。
4. **第 4 次評估器校準教訓**:凡涉 atlas 幾何(旋轉/縮放/page)先用外部真值(PSD/原始 alpha)校驗。

## 產出
- 新增 `tools/mesh_gen/award_mesh_compare.py`、`knowledge/s3xs4-award-mesh-e2e.md`(+索引)
- 更新 `STATE.md`(里程碑 / 下一步改指向 SkelToJson 組裝)、本 log

## 下一步(見 STATE.md)
候選 2:**切圖→Spine JSON 組裝(SkelToJson)** — 把已驗慣例(`機器人拆件/<圖層名>`、size+2px、
mesh/region 分配、v1/v2 auto)固化成「件→Spine mesh attachment」寫出工具,端到端產可載入 JSON。
