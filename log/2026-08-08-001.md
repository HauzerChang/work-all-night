# 2026-08-08 — session 001

## 這次做了什麼(S3×S4 端到端跨資產推廣)

承 STATE「下一步 (1)」(有真值可比、純 CPU 可自驅):把 S4 前段(psd_slice)接到 S3(mesh 生成),
對第二份生產資產 **Award(機器人 big win)** 裡真正是 mesh 的件跑端到端,並以「藝術家自己的 mesh
對自身輪廓覆蓋率」為基準判定 —— S3 首次離開 main_draw。

1. **定 AC(可機讀)**:每件 ①生成 mesh 輪廓覆蓋 IoU ≥ 藝術家基準−margin ②mesh 合法性閘全過
   ③頂點數 ≤ 藝術家(精簡度)。
2. **新增 `tools/mesh_gen/validate_psd_to_mesh.py`**:PSD 圖層→緊湊 PNG→`generate_mesh_v2(auto)`
   →`evaluate_mesh` 閘 + 藝術家基準(用 `atlas_crop.extract` 從 Award 切同 region alpha,填藝術家
   uvs/triangles 算覆蓋率)。含 delaunay 路徑的 **5 輪 refine 迴圈**(IoU 不足時 epsilon 減半)。
3. **跑驗收**:光暈/左手/身體 **3 件全 pass,overall_pass=True(exit 0)**。
4. **負對照**:生成 mesh 平移 30px → IoU 0.966→0.703,確認 AC1 能鑑別錯位(非橡皮圖章)。

## 關鍵結論

| 件 | 模式 | 生成 IoU | 藝術家基準 | 生成/藝術家頂點 | refine |
|---|---|---|---|---|---|
| 光暈 | delaunay-refined(eps=0.002) | 0.9796 | 0.9795 | 65 / 78 | 3 輪 |
| 左手 | delaunay-v1 | 0.9642 | 0.9681 | 59 / 80 | 1 輪 |
| 身體 | delaunay-v1 | 0.9660 | 0.9760 | 60 / 98 | 1 輪 |

- **S3 跨資產成立(靜態)**:對真實生產件,生成 mesh 覆蓋率達/逼近手做,且頂點更省。
- **機器人件非 strip**(blob 狀)→ v2 auto 回退 Delaunay;strip 是窗簾類專用解。
- **軟邊/圓形件(光暈)需更細輪廓取樣**:預設 epsilon 0.008 undershoot(0.933),減半到 0.002 追平藝術家。
- **範圍界定(誠實)**:Award mesh 皆 weighted(骨驅),本閘只驗靜態輪廓,**未驗 deform**;
  weighted deform 重現列為下一步。

## 產出

- 新增 `tools/mesh_gen/validate_psd_to_mesh.py`、`knowledge/s3-psd-to-award-crossasset.md`(+索引)
- 更新 `STATE.md`(下一步/歷史/進度摘要)、本 log

## 下一步(見 STATE.md)

(1) weighted-mesh deform 重現(補齊本次留的範圍缺口) / (2) SkelToJson 件→Spine JSON 組裝 /
(3) S2 補圖閘·骨架閘。
