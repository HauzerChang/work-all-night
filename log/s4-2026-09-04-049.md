# S4 chunk 49 — 2026-09-04(修正 leg_left/boot_left 決策檔框位置錯誤)

## 觸發

chunk 48 留下 3 個未執行選項給使用者裁決,本次排程無活人可裁決。其中選項 (a)「先修
`leg_left`/`boot_left` 兩個錯誤框」被 chunk 48 明確標記為「零成本、唯一已知有效的
改進動作」,不像選項 (c)(測試完整版 SAM)需要使用者先確認投入成本才能做——依既有
慣例(chunk 26 无活人時執行不需授權的選項),本次執行選項 (a)。

## 執行

1. **追溯錯誤來源**:chunk 46-48 測試用的「使用者手動確認過的真實決策檔」是對話當次
   提供、未持久化進 repo。改查 repo 裡持久化的 `assets/jiuwei_yanlian_char_crop.
   suggestions.json`(chunk 43 我自己的原始 vision 草稿),換算 `leg_left`/`boot_left`
   的 `bbox_pct`→像素,結果跟 chunk 48 報告的錯誤框座標**完全吻合**——確認錯誤從
   chunk 43 就已存在,使用者手動調整決策檔時沒有改到這兩個框。
2. **視覺定位正確位置**:用 bash+PIL 疊座標網格、逐段放大檢視來源圖(460×898)腿/靴
   一帶,發現兩腿在畫面上緊貼重疊、肉眼分不出左右腿分界,靴子同理只有一個連續靴筒
   輪廓。比照已驗證正確的 `leg_right`/`boot_right`(框住連續膚色/靴筒的右半部),把
   `leg_left`/`boot_left` 改框到同一片區域的左半部:
   - `leg_left`:`bbox_pct` 從 `[27,57,46,73]`(錯,框到手+尾巴毛髮)改為
     `[41,51,55,61]`(對,框到大腿膚色左半)。
   - `boot_left`:`bbox_pct` 從 `[27,67,49,99]`(錯,框到布料+尾巴毛髮)改為
     `[41,61,56,98]`(對,框到靴筒左半)。
   - 兩者 `confidence` 從 `high` 下修為 `medium`,`notes` 記錄修正原因與材質限制。
3. **自我驗證**:從修正後的 `suggestions.json` 重建 20 部件 `bbox_px` 決策檔,跑
   `s4_decompose_cut.py --contour rect --eval`——AC1(20/20 部件產出)pass。逐格人工
   視覺複核修正後的 `leg_left`/`boot_left` 裁圖:確認畫面主體分別是裸露大腿膚色/黑色
   靴筒(邊緣夾帶少量鄰接布料,屬允許範圍內的 bleed),不再是手/布料+毛髮。

## 誠實限制

- 兩腿在來源圖裡本來就視覺重疊、不可分——框位置修好後,`leg_left`/`leg_right`
  (`boot_left`/`boot_right`)裁出來的內容仍是**同一團連續膚色/靴筒的左右兩半**,不是
  兩條真正獨立可辨的腿。這是美術圖姿勢本身的限制,不是分割演算法的問題,也不是這次
  修正能解決的範疇。
- 本次驗證用的 20 部件決策檔是從 `suggestions.json` 重新換算,**不是**
  chunk46-48 使用者親手調整過的那份真實決策檔(那份檔案的部分欄位如 `fox_ears` 曾被
  使用者手動修改,未持久化進 repo)——除 `leg_left`/`boot_left` 外的 18 個部件用的是
  未經使用者調整的原始草稿座標,跟 chunk 47「9 個明確正確」等統計數字不是同一份輸入,
  不能直接延續累計統計。
- 沒有重跑 `--contour sam`:本容器沒有持久化 chunk 47 下載的 MobileSAM 權重,矩形
  裁切的視覺驗證已足以證明框位置本身修好,SAM 精修屬於選項 (c) 範疇未執行。
- `hand_right` 框沒對準的問題本次未修正,留待後續(chunk48 已記錄,優先度低於本次
  明確點名的兩個框)。

## 檔案

- 修改 `assets/jiuwei_yanlian_char_crop.suggestions.json`(`leg_left`/`boot_left`
  的 `bbox_pct`/`confidence`/`notes`)。
- 新增 `tools/mesh_gen/s4_data/chunk49/decision_reconstructed.json`、
  `tools/mesh_gen/s4_data/chunk49/cut_rect/`(4 張腿/靴裁圖 + `manifest.json`,驗證
  證據)。
- 新增 `knowledge/s4-decompose-box-fix-legs.md`。
- `knowledge/README.md` 尾端 append。
- 更新 `STATE_S4.md`。
- 未修改任何 production 代碼(`s4_decompose_cut.py`/`s4_sam_segment.py`/
  `s4_decompose_assist.html` 均維持 chunk 47/48 版本)。

## 下一步(留給使用者/後續排程裁決,均未執行)

1. 對 `bodice`/`sleeve_right`/`hand_right` 三個真正演算法歧義案例接受人工處理
   (chunk48 選項 b)。
2. 測試完整版 SAM(ViT-H)是否對點提示更敏感(chunk48 選項 c,成本較高,需使用者先
   確認)。
3. 修正 `hand_right` 框本身沒對準的問題(現成、低成本,但本次未列入範圍)。
4. 若之後需要 `leg_left`/`leg_right` 在 spine 裡做成真正獨立擺動的兩個 slot,需另外
   處理「兩腿視覺重疊、內容幾乎相同」這個素材限制(生成式補全被遮擋腿部,或接受目前
   重疊內容),不是這次修正能解決的範疇。
