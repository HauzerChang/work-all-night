# 2026-08-31 — session 001(主研究)

## 本次有界工作塊:S5 (c) 肢體父子樹自動推斷

補上 rig pipeline **最後一個「取自先驗」的環節**。此前 `build_spine --rig` 的 `rig_layout` 直接
**假設星形**(每結構件直掛 body,root/父邊取自分析器 note),使 `--rig` 並非完全自決。

## AC(動手前定,可自量測)

- AC1 root 正確:幾何推得 root == body。
- AC2 拓樸精確:推得結構父子樹邊集合 == 真值 `ROBOT_TREE`。
- AC3 門檻穩定:root+tree 在一段 τ_frac band 內不變。
- AC4 多跳通用:合成鏈 recover 成鏈(非強制星形)、合成星形亦正確。
- 負對照:NC1 隨機父≈0;NC2 斷開肢體→父邊變(幾何驅動);NC3 天真納 effect→汙染(證 role 須輸入)。

## 做了什麼

1. **`tools/rig/infer_tree.py`**(新):
   - 幾何核心:多邊形加密(沿邊補點)→ 準確件間最近距離(scipy cKDTree);重疊(點在對方內)→ d=0。
   - `infer_root`:**area-primary**(軀幹=最大質量 trunk),degree 為 tiebreak/診斷。
   - `infer_tree`:完全圖(權重=接觸距離)自 root 跑 Dijkstra 最短路徑樹 → parent 邊(支援多跳鏈)。
   - `diagnose_include_effect`:天真納 effect 的汙染診斷(honest boundary)。
2. **`tools/rig/validate_tree.py`**(新):AC1–4 + NC1–3,一鍵 exit 0=PASS。
3. **`build_spine.py` `rig_layout` 重寫**:結構件集合仍由 note 分類(effect/structural,honest boundary),
   **root+父邊改由 `infer_tree` 幾何推斷**;子件 pivot 對**推得的父件**取接觸縫;bone 以拓樸序輸出(父先於子)。
   build 段改吃 `rig_order`(移除舊的單層 star 排序假設)。
4. **`check_readiness.py`**:新增 cap `limb_tree_infer`(L2 gen);更新 `pivot_end2end` note。
5. 重生 `skills/READINESS.md` 快照。

## 自我驗證結果

- `validate_tree.py`:**AC1–4 + NC1–3 全 PASS**(OVERALL PASS,exit 0)。
  - root=身體(area_max=身體);樹=頭/左手/右手→身體(==真值);τ_frac∈[0.008,0.20] 8 點不變;
    合成鏈 root=torso tree={upper→torso, lower→upper} OK、星形 OK;隨機父命中 1.4%;斷開左手父邊由身體→右手;
    天真納光暈 → root/邊全被光暈奪走。
- **回歸**:`validate_rig_build.py`(前一里程碑)接新推斷樹後 **4 AC 端到端全 PASS**
  (rig_root=b_身體 自動推得;setup 不位移 0.00px、pivot 往返 0.04px、關節語意 min ratio 2.1×)。
- `check_readiness.py`:`spine-rig-pivot` 維持 L2 HOLD(符合預期,防固化)。

## 關鍵發現 / 決策

- **root 須 area-primary 非 degree-hub**:初版 degree-hub 對星形對,但純肢體鏈「中間件」degree 最高卻非 root
  (合成鏈 AC4 抓到:root 誤選 upper)。真實軀幹穩健簽名=最大面積 → 改 area-primary,三情境全對。
- **重疊 composite 下 degree 飽和**:機器人 4 結構件在合成座標互重疊(接觸矩陣:body 對三肢皆 0,連
  head–右手=0),τ 內全 degree 3 → degree 無鑑別力,area 才是決定 root 的訊號;tree 幾乎 τ-無關。
- **honest boundary**:本器推拓樸,不做 effect vs structural 角色分類(NC3 證天真納光暈會奪 root)→ role
  仍為輸入(來自 note)。這是獨立子問題。

## 界定 / 下一步

- `spine-rig-pivot` **仍 HOLD**:L3 硬缺口=**多 rig 真值**(Award 僅機器人一件可拆肢體 rig)不變,屬使用者資源。
  本次補齊「拓樸自動化」使 rig 拓樸完全自決,但未新增真值 rig。
- 下一個 bounded chunk 候選(皆可自主):
  (1) `--rig`×`--weighted` 併用(weighted 控制骨併入關節鏈);
  (2) 多層關節鏈遞迴接觸縫(手→前臂→手掌;演算法已支援,可先在合成多跳件端到端 build 驗);
  (3) S1 keyframe 補 hit/open/reveal 主秀 beat 模板。
- C 類(需使用者拍板):weighted-forge / rig 併入 `spine-asset-forge` skill 的 sync 時機(READINESS 已達門檻者)。

## commit & push

`claude/spine-main`(固定分支)。
