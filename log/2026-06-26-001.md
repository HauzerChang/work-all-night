# 2026-06-26 — session 001

## 這次做了什麼(S3 推廣到全部 4 mesh)

承 STATE 的「下一步 (1)」:把整合 AC `validate_against_real.py` 推廣到其餘 3 個 mesh,確認 v1 是否通用。

1. **列出 4 個真實 mesh**:curtain_left/right(各 21v)、shadow/shadow2(各 12v,shadow2 共用 `image/shadow` region)。
2. **v1 全跑**:curtain_left 過(回歸確認);**curtain_right(19 si)、shadow/shadow2(64 si)真實 deform 自交失敗**
   —— v1 靜態 IoU 都高(0.90~0.98)但 deform-fragile。
3. **先驗評估器(關鍵)**:依上一輪教訓,先把藝術家自己的 mesh 餵進 `transfer_deform_check`
   → 4 個全 si=0/flip=0/乾淨 → 閘忠實重現真實變形,故失敗判定可信(非假性失敗)。
4. **排除座標系錯位**:確認藝術家 uvs 與生成 uvs 皆 region 內 0..1,griddata 線性覆蓋 40/41、39/39 → 轉移無錯位。
5. **v2 strip 全跑**:4 mesh 真實 deform **全乾淨**;IoU 在 shadow 過、curtain 略低(0.911 < ~0.916)。
6. **參數掃描**:發現 **IoU 由 rows 決定、cols 不影響覆蓋率**;`rows=10,cols=3`(30v)對 4 mesh 全過 IoU+deform。
7. **改 v2 預設 rows 8→10**;標準指令 `validate_against_real.py --gen v2` 對 4 mesh 全 `overall_pass=True`(exit 0)。

## 關鍵結論

- **v1 散點 Delaunay 不通用**:過 curtain_left 是部分運氣;散點拓樸在大單向拉伸下易 sliver 翻面/自交。
- **v2 strip(藝術家式直條)通用**:4 mesh 全 deform-robust。deform 是硬約束,v2 定為 deform-bearing mesh 預設。
- **可重用旋鈕**:rows=覆蓋率/IoU、cols=內部頂點密度(不影響 IoU)。
- 再次實踐「先驗評估器再下判定」,避免重蹈合成壓力 miscalibration 的覆轍。

## 產出

- 改 `tools/mesh_gen/generate_mesh_v2.py`(預設 rows 8→10 + 調校發現註記)
- 新增 `knowledge/s3-four-mesh-generalization.md`(含 v1/v2 對照表、可重現指令)、更新索引、STATE

## 下一步(見 STATE.md)

spine_inspector 實機 round-trip(headless 截圖)/ v2 auto 自適應 rows / 往上游 S1 反推分析器 / S4 切圖。
S3「生成 deform-robust mesh + 自評閘」對 4 mesh 已收斂達標。
