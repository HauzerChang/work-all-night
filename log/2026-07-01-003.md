# 2026-07-01 — session 003(多塊 run:今日剩餘工作塊)

> 新節奏(每 run 6–8 塊)首次實行:本檔一次 run、每塊一小節、逐塊 commit。
> 承 001(S3×S4 覆蓋率驗收)、002(SkelToJson)後,推進剩餘塊達今日目標。

## 塊 3 — auto-epsilon 沉澱進 `generate_mesh` 本體(重構,消重複)

- **AC**:覆蓋率驅動 epsilon 搜尋成為單一真相來源;`validate_award_mesh` 與 `skel_to_json` 改用之,兩者仍 exit 0。
- **做法**:`generate_mesh.py` 新增 `generate_auto(path, target_iou, vertex_budget)`(由粗到細掃
  `EPS_SCHEDULE`,回第一個 IoU≥target 且 nv≤budget 的 mesh + meta;否則覆蓋率最佳者)。
  `skel_to_json` 刪掉自己的 `gen_mesh_for_part` 改呼叫 `generate_auto`;`validate_award_mesh` 從
  `generate_mesh` import `EPS_SCHEDULE`(排程單一來源,保留其 instrumented 逐輪報告)。
- **驗證**:glow `generate_auto` → {iou 0.9779, eps 0.003, nv 68, hit_target}；
  `validate_award_mesh` exit 0、`skel_to_json` exit 0。
- **產出**:改 `generate_mesh.py`(+generate_auto/EPS_SCHEDULE)、`skel_to_json.py`、`validate_award_mesh.py`。

## 塊 4 — S2 骨架/rig 評估器(S5 自主收斂前置)

- **AC**:新 `evaluate_skeleton.py` 對真實/生成 skeleton 判 pass/fail;正對照(main_draw/Award/robot-gen)
  全 PASS,負對照(破壞 slot-bone/三角索引/權重和/bone-parent)各自 FAIL 於對的 criterion。
- **做法**:三條可機讀判準 — AC1 structure(必要鍵、slot→bone 存在、bone 樹合法:單一 root/parent 存在/無環、
  預設 attachment 存在於 skin;null attachment 允許)、AC2 attachments(mesh 三角索引/hull;region 尺寸;
  **clipping/boundingbox/path/point 依型別驗**)、AC3 rig_weights(weighted mesh:bone 索引在範圍、每頂點權重和≈1)。
- **先驗評估器(關鍵,又一次應驗方法論)**:初版對 **main_draw FAIL** → 查出把 `clipping`(root,CLAUDE.md
  記載的 clipping×1)誤當 region 要求 width/height。**這是評估器 bug,靠對真實真值驗證才抓到**(非 main_draw 有問題)。
  修:region 尺寸檢查只對 type==region;clipping 驗 vertexCount/vertices;boundingbox/path/point 結構有效即可。
- **驗證**:修後 main_draw/Award/robot-gen 全 PASS;4 負對照全 FAIL 於對的 criterion → 閘鑑別力確認、可信。
- **產出**:新增 `tools/mesh_gen/evaluate_skeleton.py`。

## 塊 5 — S5 骨架階層草案(可自動部分;pivot 待人)

- **AC**:由件 alpha 重疊自動推出合法骨架階層(單一 root/無環,過 evaluate_skeleton)+ 關節 pivot 草案;
  世界版面保真(父子鏈 local 位移還原件中心 ±0.5px);主觀決策明確 flag 待人。
- **做法**:新 `rig_draft.py` — 件畫布 alpha → 重疊圖 → 從 root BFS 生成樹(parent=通往 root 的重疊件)→
  每件關節 = 與 parent 的**重疊區質心**(pivot 草案)→ 重寫 bones 成父子鏈(local x/y 由 parent 世界位置反推
  → 世界位置不變)。`_needs_human_pivot` / needs_human 明列待人項。
- **驗證**:robot_parts skeleton_valid=True。**自動 root 誤選 光暈(背景件,重疊度 4 tie、面積最大勝出)**
  → 正是「root 需人確認」案例;`--root 身體` 覆寫得到合理階層(身體=root,頭/雙手/光暈為子,各帶 pivot 草案),
  世界位置保真 ±0.5px。
- **誠實界定**:自動=連接圖+生成樹+關節質心(確定性);**待人=root 確認、pivot 精確微調、mesh 權重綁定(BBW)**
  (PLAN 明示 pivot 是唯一卡死處)。attachment 目前仍 unweighted。
- **產出**:新增 `tools/mesh_gen/rig_draft.py`。

## 塊 6 — S2 補圖閘(evaluate_inpaint.py;S2 評估器套件補齊)

- **AC**:補圖品質閘沿「補圖能力梯」三態可分:強補圖→PASS、cv2 紋理區→升級訊號(AC3 MAE 高)、留洞→缺(AC1)。
- **做法**:新 `evaluate_inpaint.py` — 盲測 AC1_filled(hole 區仍透明比例=0)+ AC2_seam(邊界環帶梯度/內部基準,
  偵測接縫);有真值時 AC3_gt_mae(hole 區 premult-RGB MAE)。附 cv2 Telea baseline 補圖驗閘。
- **驗證(真實貼圖 身體件)**:強補圖(GT+微噪擬 LaMa,MAE 3.73)→ 全過;cv2 Telea(MAE 32.6,filled/seam 過)
  → AC3 FAIL = **升級訊號**(呼應 PLAN:cv2 是低階,紋理區需 LaMa/GPU);不補(unfilled 1.0)→ AC1 FAIL。
- **發現**:MAE 依 hole 所在**局部紋理**而非只看洞大小(8% 中心高細節洞 MAE 33.9 > 22% 洞 26.7)→
  補圖難度是「紋理複雜度」問題,閘正確反映此點作為升級依據。
- **里程碑**:**S2 四評估器全到位**(切圖 evaluate_slicing / mesh evaluate_mesh+deform_eval /
  骨架 evaluate_skeleton / 補圖 evaluate_inpaint)→ S2 樞紐(自主收斂前提)完成。
- **產出**:新增 `tools/mesh_gen/evaluate_inpaint.py`。
