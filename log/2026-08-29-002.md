# 2026-08-29 run 002 — S5(b) pivot→骨鏈,寫入 build_spine

分支:`claude/focused-dirac-06ezyo`(本次排程啟動所在分支;內容與 `claude/spine-main` 同基 `21060fe`)。

## 這次做了什麼(一個有界工作塊)

從 STATE「下一步」挑建議塊 (1):**S5 續推 → pivot→bone 父子樹寫入 build_spine**。
延續 run 001 的接觸縫 pivot 推斷,把 pivot 從一個座標接成**可載入的關節鏈**。
依 RULES「每能力必配評估器」,同塊內交付建構器 + 接合閘。

- **`tools/rig/build_rig.py`** —— 通用骨鏈建構器 + 姿勢求值器(確定性、純 CPU,無瀏覽器)。
  `build_bone_chain(parts_world, tree, pivots)` → Spine bones(x,y 相對父骨局部)+ 件→骨名 + 件局部多邊形;
  `part_world(part,…,pose)` 骨鏈合成算變形後世界點;`fixed_point_of_rotation` 由旋轉前後點雲解關節。
  世界變換重用 `weighted_deform_eval`,與真實 Spine 3.8 對齊。
- **`tools/rig/validate_rig_tree.py`** —— 接合閘(真值=Award 機器人 rig)。
- **`build_spine.py --rig-tree`** —— 端到端:子件骨落推斷關節 pivot、parent 到 body 骨;
  region 補 attachment x,y 偏移、unweighted-mesh 平移頂點 → setup pose 渲染不變。
- **升級 `validate_build.py`** —— round-trip 重建器改為**合成骨鏈世界變換 + 讀 region attachment 偏移**;
  對 flat rig 退化為舊行為(向後相容)。
- **`check_readiness.py`** —— `spine-rig-pivot` 區塊補「骨樹接合」pipeline gate(GREEN),多 rig 列如實標資產限。
- **知識** `knowledge/s5-rig-chain-build.md`(+ README 索引)。

## 結果(全 PASS)

`validate_rig_tree.py`(exit 0)4 AC:

| AC | 結果 |
|---|---|
| AC1 setup round-trip | jointed 接鏈不移件,max 位移 7.6e-4 px(捨入下限)PASS |
| AC2 轉子件骨繞真值關節 | max err/rig 0.051<0.10;中位 jointed 21.6px < flat 43.0px PASS |
| AC3 轉父件→子件跟動+剛體 | 子件位移 min 75.7px、全機件距變化 1.7e-13;flat 子件不動(脫節)PASS |
| AC4 泛化界定(誠實) | Award 僅機器人被拆件,OMG/SUP/MEG 單 slot → 無多 rig 真值可驗(資產限)PASS |

端到端(build_spine --rig-tree,robot_parts):
- 骨鏈 `b_身體`(root)← `b_頭`/`b_左手`/`b_右手`(落關節);`validate_build` round-trip **AC 全 PASS**
  (rgb_mae **0.031**、0 孤兒,與 flat **完全相同**)。
- 直接量測:轉 `b_頭` 30° 旋轉不動點 == 脖子 pivot(**0.0 px**);flat rig 會繞頭質心(偏 **50px**)。

Regression 全綠:`validate_pivots`(S5 pivot)PASS、`validate_weighted_build` OVERALL PASS、
`validate_anim` overall PASS、flat robot / Symbol_Ww round-trip 不變。

## 決策 / 發現

- **`validate_build` 原本假設「骨位==影像中心」且忽略 attachment 偏移** —— 接關節鏈前必須升級成合成骨鏈 +
  讀偏移;已做到向後相容(flat 數值分毫不變),是本塊隱藏成本也是通用化收穫。
- **AC2 用中位而非逐件**:緊湊肢體(右手)質心偶爾恰近其關節(17px),逐件宰制是灌水;
  中位才是誠實的集體宣稱(與 `validate_pivots` AC2 同基準)。
- **多 rig 真值是資產限,非方法限**:Award 僅機器人被拆件 → AC4 如實標注,不灌水。

## 下一步(留給下次排程,STATE 已更新)

建議自主塊:(1) **S5(c) 肢體父子樹自動推斷**(去掉 role 先驗依賴,純 CPU 可自主);
(2) **S5(d) weighted + jointed 整合**。多 rig 真值需使用者提供第二個拆件角色(A 類,非自主)。
