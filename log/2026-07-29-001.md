# 2026-07-29 — session 001

## 這次做了什麼(S3+S4 端到端:件 → 生成 mesh → 對照 Award 真實生產 mesh)

推進 STATE 的最高優先 bounded chunk(有真值可比)。

1. **定 AC**(可機讀):對機器人拆件 3 個在 Award 為 mesh 的件(光暈/身體/左手),
   ① 覆蓋 IoU ≥ 藝術家真實 mesh − 0.02;② setup 0 自交/翻面/退化/孤兒;③ 頂點 ≤ 藝術家×1.5;
   ④ 同一 stress 下自交/翻面 ≤ 藝術家(相對)。真值 = Award.json 的 mesh(uvs 為 region 局部正規化)。
2. **初測**:v2 auto 對 3 件皆判為非 strip → 回退 Delaunay。預設 epsilon=0.008 下光暈 IoU 0.930 < 藝術家 0.980(fail),身體/左手在 margin 內。
3. **迭代(掃參,預算內)**:發現 **IoU 由 hull 邊界密度(epsilon)決定,max_interior 完全不影響** —— 與先前 strip「rows 決定、cols 不影響」同源。epsilon 0.008→0.002 使 3 件 IoU 全超越藝術家,頂點數仍 ≤ 藝術家。
4. **工具**:`generate_mesh_v2.generate` 新增並**透傳 epsilon/max_interior 到 Delaunay 回退**(先前寫死,無法調 hull 密度);CLI 加 `--epsilon`/`--max-interior`。新寫端到端閘 `validate_psd_to_mesh.py`(針對 weighted+無 deform 的 Award mesh,變形項改用相對耐變形,延續 stress_field 不可當硬閘的教訓)。
5. **自我驗證**:`validate_psd_to_mesh.py` 3 件 all_pass exit 0;重跑 `validate_against_real.py --gen v2` 對 main_draw 3 mesh 全 overall_pass(預設未動,無回歸);v2 CLI epsilon smoke OK。

## 關鍵結論 / 決策

- **端到端「件→mesh」對真實生產標的驗收通過**(里程碑):不只自產自驗,而是對藝術家手做 mesh 對標且**達到/超越其品質**(覆蓋更高、頂點更少、耐變形不劣)。
- **普適規律**:mesh 覆蓋率是「邊界取樣密度」的函數,內部頂點只給變形自由度,不影響覆蓋 —— 跨 strip / Delaunay 兩條生成路徑一致。→ 下一步可做「自適應 epsilon」免逐件調參。
- 排除先前疑慮:Spine mesh uvs 為 region 局部正規化(main_draw uvX/uvY∈[0,1] 佐證),不需額外轉換。

## 產出

- `tools/mesh_gen/validate_psd_to_mesh.py`(新)、`tools/mesh_gen/generate_mesh_v2.py`(透傳 epsilon)
- `knowledge/s3-psd-to-award-mesh.md`(+索引)、更新 `STATE.md`、本 log

## 下一步(見 STATE.md)

(1) 自適應 epsilon(依曲率自動選,免調參);或 (2) SkelToJson 把 PSD→件→mesh→Spine JSON 串成可產檔 pipeline。
