# 2026-07-29-001 — 端到端 S4→S3:PSD 件 → 生成 mesh → 對照 Award 真實 mesh

## 本次工作塊
STATE 的下一步候選 #1(最高優先,有真值可比):把切圖(S4)與 mesh 生成(S3)串成端到端,
對真實生產標的 `Award` spine 中機器人拆件的 3 個 mesh 件(光暈/身體/左手),用美術手做 mesh
當真值基準做量化對照。

## AC(動手前定的可檢查目標)
每件:
- AC-coverage:生成 mesh IoU(對 PSD 切件 alpha)≥ 美術 mesh IoU − 0.02。
- AC-topology:格式有效(hull-first/索引範圍/無孤兒/無退化)+ 三角重心全在 mask。
- AC-parsimony:生成頂點 ≤ 美術頂點 × 1.6。
- 註:這些件無 deform timeline(weighted,骨骼變形)→ 不套逐頂點 deform 閘。

## 做了什麼
1. 讀齊 RULES/PLAN/STATE/CLAUDE + knowledge 索引 + 近期 log;確認分支狀態
   (本 session 在 `claude/vibrant-franklin-pg17w8`,與 `zealous-noether` 內容一致、與 origin 同步)。
2. **實測驗證 Award mesh UV 座標系**(不臆測):三 mesh uvs 範圍皆 ≈[0,1] → 確認是
   region-local 正規化,非先前 knowledge 記的「atlas UV 需轉換」。更正之。
3. 新增 `tools/mesh_gen/compare_psd_to_award_mesh.py`:psd_slice 切件 → generate_mesh_v2 →
   evaluate_mesh 量 IoU/拓樸,對照 Award mesh 柵格化基準。
4. 跑驗收:3 件全 overall_pass。
5. 負對照:各生成 mesh 對「錯誤件」alpha 覆蓋率崩至 0.50~0.58、重心 0.63~0.74 → 閘有鑑別力。

## 產出 / 結果
| 件 | 美術(真值) | 生成(受測) | 判定 |
|---|---|---|---|
| 光暈 | v78 IoU0.949 | v35 IoU0.933 [v1] | PASS |
| 身體 | v98 IoU0.948 | v60 IoU0.966 [v1] | PASS |
| 左手 | v80 IoU0.977 | v59 IoU0.964 [v1] | PASS |

- 生成 mesh 用更少頂點達相近覆蓋率。
- 這 3 件方正(aspect<1.2)→ auto 落回 Delaunay v1,**首次以外部真值驗證 v1 對真實生產件通用**。

## 決策 / 更正
- Award mesh uvs = region-local [0,1](更正 s4-psd-to-spine-real.md 舊記載)。
- 這批件的正確 AC = 靜態覆蓋率 + 拓樸(無 deform timeline)。

## 下一步
- 候選 #2 升為最高優先:**SkelToJson 組裝**(件→attachment 慣例固化,端到端產可載入 Spine JSON)。
- 候選 #3:S3 加 BBW 權重(生成件目前 unweighted,真實件 weighted)。

## 記錄
- knowledge:新增 `s3-s4-end-to-end-award-mesh.md` + 更新索引。
- STATE:更新下一步、進度摘要、分支註記。
