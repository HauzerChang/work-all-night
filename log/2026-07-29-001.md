# 2026-07-29 — session 001(排程自驅)

## 這次做了什麼(bounded chunk:STATE 候選 1 — PSD件→S3 mesh→對照 Award 真實 mesh)

接手續跑。讀完 RULES/PLAN/STATE/CLAUDE + knowledge 索引 + 最近 log,從 STATE「下一步」挑
**最高優先且有真值可比**的候選:把 Award 的 3 個真實 weighted mesh 件當 ground truth,
驗證 S3 生成器對同一素材的自動 mesh 是否達藝術家水準 → S3×S4 端到端閉環。

### 先定 AC(可自量測)
- AC_transform:藝術家 mesh 映射到共同畫布後,覆蓋率 oracle ≥0.80 且方向負對照可鑑別(否則 baseline 不可信)。
- AC_coverage:生成 mesh 覆蓋 IoU ≥ 藝術家 IoU − margin(margin=0.02,依 atlas 0.70 縮放+羽化邊)。
- AC_format:生成 mesh 格式合法(沿用 evaluate_mesh AC4)。
- (deform 閘 **N/A**:這 3 件無 deform timeline,靠骨骼/權重變形,非逐頂點 deform。)

### 實作
新增 `tools/mesh_gen/validate_psd_to_award.py`:
1. `atlas_crop.extract()` 取「直立去旋轉、atlas 縮放」件裁圖當**共同畫布**。
2. 生成 mesh(`generate_mesh_v2` auto)與藝術家 mesh **都柵格化到此畫布**比 IoU。
3. 裁圖 alpha 當獨立 oracle + **4 朝向負對照**(direct/vflip/hflip/rot180)確認轉換方向。

### 自我驗證迴圈(抓到並修正一個真 bug)
- **第一版轉換錯**:誤把 Award mesh uvs 當 atlas 頁面 UV(乘 pageW/H + CW 去旋轉)→ oracle
  IoU 崩到 0.0/0.49/0.54,`transform_valid=false`。差點被「gen ≥ baseline(=0.0)」假性通過
  (故 overall_pass 已加上 transform_valid 條件)。
- **查因/修正**:實測 uv 對 181px region 卻 span 滿 [0,1] → **Award mesh uvs 是 region-local**
  (Spine 匯出慣例);改 `px=(u·W, v·H)` 直接映射 → oracle direct 0.98/0.98/0.97,
  負對照 0.40~0.76 → 轉換可信。
- **調參迭代(1 輪)**:發現覆蓋率由 **邊界取樣密度(epsilon)** 決定、內部點無關;v2 Delaunay
  回退預設 epsilon 0.008 對 atlas 縮放+羽化件太粗 → 改 0.004 即達/超藝術家。

## 量化結果(全 PASS,exit 0)

| 件 | 生成 IoU | 藝術家 IoU | 生成頂點 | 藝術家頂點 | 輪廓一致 |
|---|---|---|---|---|---|
| 光暈 | 0.966 | 0.980 | 61 | 78 | 0.954 |
| 身體 | **0.986** | 0.976 | 69 | 98 | 0.968 |
| 左手 | **0.982** | 0.968 | 57 | 80 | 0.964 |

→ 身體/左手覆蓋率**超過藝術家**且頂點少 ~30%;光暈差 0.014(< margin)。

## 關鍵結論
- **S3×S4 端到端閉環達成**:PSD 件 → S3 自動 mesh 對真實生產標的達藝術家覆蓋水準(純 CPU、自驅)。
- **覆蓋率由邊界密度決定,內部點無關**(Delaunay 分支再證 strip「rows 決定」原理)。
- **Award mesh uvs = region-local**(重要格式知識,寫進 knowledge)。
- **方法論延續**:oracle + 負對照先確認 baseline 可信,才信 pass 判定(否則假性通過)。

## 未回歸驗證
- main_draw curtain_left/right + shadow(--gen v2)重跑全 OVERALL True(走 strip,不受 epsilon 改動影響)。
- shadow2 是既有資料 quirk(與 shadow 共用 region,`image/shadow2` 非有效 region 名),與本次改動無關。

## 產出
- 新增 `tools/mesh_gen/validate_psd_to_award.py`、`knowledge/s3-psd-to-award-mesh.md`(+索引)。
- 改 `tools/mesh_gen/generate_mesh_v2.py`(Delaunay 回退 epsilon 0.008→0.004,加 eps 參數 + 註解)。
- 更新 `STATE.md`(里程碑 / 下一步改為候選 2 SkelToJson 組裝器)、本 log。

## 下一步(見 STATE.md)
候選 2:件→Spine JSON 組裝器(SkelToJson),把生成 mesh + 命名/padding/region-local uv 慣例組出完整
Spine JSON,真正端到端 PSD→Spine。註:生成為 unweighted,真實件 weighted → 綁定對映需 S5(未做)。
