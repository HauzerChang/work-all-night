# 2026-09-02 · session 001 — S1 candidate 0g:slot_bigwin 主秀重擊 beat 接進先驗庫

## 選定的有界工作塊

STATE「下一步」候選 **(E) 主秀 beat 接進 genre 先驗庫**(3 個純自主候選 E/F/G 之一)。

## 做了什麼

1. **診斷可達性**(先量測再動手):
   - 0f 的 `gen_hit`/`gen_reveal` 已註冊到 `gen_animations._DISPATCH`,但端到端輸出需 storyboard 含對應 beat。
   - 實查兩 validated 先驗的 `beat_category` 路由:
     - `slot_reveal`(main_draw):`open→reveal`、`hit→hit` **已可達**(main_draw 生產檔本有獨立 open/hit 動畫)。
     - `slot_bigwin`(Award):只有 `In/Loop/Out`。Award 12 支 = In/Loop/Out×4 檔位,**無獨立 hit 動畫**(主秀折進 In)。
   - 結論:真缺口只在 `slot_bigwin` —— `build_spine --animate --genre slot_bigwin` 從不輸出主秀節拍。
2. **實作(最小、不動已驗先驗)**:`tools/analyzer/genre_priors.py`
   - slot_bigwin 的 `beats` 加 PROPOSAL beat `Hit`(kw `hit/win/bigwin/impact/…`,`beat_category→hit`→路由 `gen_hit`);
   - 補 `_BIGWIN_ROLES["Hit"]` 角色動作文字;已驗 In/Loop/Out 一字未動。
   - 端到端結果:`build_spine --animate` 輸出 `In, Hit, Loop, Out`。
3. **自驗閘**:`tools/analyzer/validate_bigwin_mainshow.py`(真實 robot_parts 端到端,沿用 0f 結構簽章判定子)。

## 產出

- `tools/analyzer/genre_priors.py`(+Hit beat/roles)
- `tools/analyzer/validate_bigwin_mainshow.py`(新,5 AC 閘)
- `knowledge/s1-bigwin-mainshow-beat.md` + `knowledge/README.md` 索引
- `STATE.md`(里程碑 + 候選更新 + progress log)

## 驗收結果(AC-first,量化)

`validate_bigwin_mainshow.py --json` → **overall_pass true**:
- G1 reachability:`In,Hit,Loop,Out`;Hit 5 bones ✅
- G2 hit signature:`b_身體` peak **1.279**(≥1.12)、命中前下蹲 **0.931**(<0.99)、(scale−1) 變號 **4**(≥3)✅
- G3 chainable:Hit 首尾幀皆 setup identity(bone identity + slot alpha=1)✅
- G4 neg-control:同一 build 的 `In`、`Loop` 皆**不**具 hit 簽章 ✅(證加的是真·獨立主秀)
- G5 regression:validate_priors slot_bigwin 覆蓋率 **1.0**、Award In/Loop/Out 歸類不動、`Hit`=`prior_beats_unused` ✅

**回歸(全綠)**:`validate_anim --selftest`(0d,AC1–4+負對照)、`validate_beat_templates`(0f,B1–B6)、
`validate_priors`(overall_pass)、`validate_deform_gen`(0e)。

## 決策 / 誠實界定

- `Hit` 對 Award 命名不命中 → 明標為 **PROPOSAL(先驗提案)非真值觀測**;Award 把主秀折進 In 是該資產美術選擇。
  本 chunk 提供的是「若要獨立主秀節拍」的端到端可達路徑,不宣稱 Award 有它。
- 這是**先驗庫接線**(把 0f 已驗模板接到 big-win 預設演出),非新演算法/新美學 → `spine-anim-forge` 仍 **HOLD**(防固化)。

## 分支註記(需使用者留意)

本次排程 harness 指定工作分支 `claude/focused-dirac-idq83r`(非專案慣用的 `claude/spine-main`),
且「NEVER push to a different branch without explicit permission」。故本 session 在該分支 commit/push。
該分支已含 `claude/spine-main` 全歷史(截至 0f)。**建議**:使用者把此分支合回 `claude/spine-main` 以維持單一 canonical,
或調整 routine 設定讓工作分支釘 `claude/spine-main`(見 `SCHEDULE.md` 分支策略)。

## 下一步(擇一,皆純自主)

- (F) 更多主秀節拍:anticipate-hold / multi-hit combo / cascade,各配結構簽章 AC。
- (F') 每檔位一支主秀:把 `Hit` 觸發接進 slot_bigwin 的 `tiers`(Super/Mega/Omg/Legend)。
- (G) S1 (e) 關節 pivot 推斷接 keyframe:S5 接觸縫 pivot 餵給 keyframe(件繞關節轉而非件中心)。
