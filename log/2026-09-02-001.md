# 2026-09-02 run 001 — S1 candidate 0g:主秀 payoff 節拍併入 genre 先驗庫

分支:`claude/focused-dirac-g1anf1`(本 session 容器起於此分支,HEAD 與研究線同 commit b2bbca0=0f;
依任務指示釘此固定分支累積,收尾 push 回同名)。

## 這次做了什麼(一個有界工作塊)

從 STATE「下一步動作」建議 **(E)**、及 0f log 明列的下一小步:**把 0f 的主秀 beat 併入 genre 先驗庫**,
讓 `build_spine --animate` 端到端直接輸出含主秀節拍的完整演出。

**先量化缺口**(動手前):實跑 `build_spine --animate --genre slot_bigwin` →
`anims = [In, Loop, Out]`。0f 的 `gen_hit`/`gen_reveal` 雖已驗過(6AC),但那份閘用**合成 storyboard**
(直接塞 beat key);真實 `slot_bigwin` 先驗只有 In/Loop/Out → 端到端**主秀 payoff 完全缺席**。
(對照 slot_reveal 先驗有 open/hit → 本來就吐主秀。)

- **`genre_priors.py`**:在 `slot_bigwin` beats 的 **In 與 Loop 之間**新增 beat `Hit`
  (kw=hit/impact/命中/重擊/…),補 `_BIGWIN_ROLES["Hit"]`。`gen_animations` **零改動**——0f 已把
  `hit`→`gen_hit` 註冊進 `_DISPATCH`/`_CAT_KEYWORDS`,`beat_category("Hit")→hit`。純先驗庫改動即接通。
  - **為何 Hit 非 Reveal**:gen_hit 首尾皆 setup identity → 可無縫夾在 In(尾 identity)與 Loop(首 identity)間;
    gen_reveal 首幀 collapsed(scale~0/alpha 0)只能當「從無到有」首拍,接在已現身的 In 後會 pop-to-invisible。
- **`validate_show_beat_wiring.py`**(新):**走真實組裝路徑**(`build_spine.build --animate` 寫 skeleton.json 讀回),
  6 AC 端到端閘;**復用 0f `validate_beat_templates._hit_signature`** 當可信簽章判定器。
- **`check_readiness.py`**:`spine-anim-forge` 新增 cap `show_beat_wiring`(L2,pipeline)。

## 結果(`validate_show_beat_wiring.py` 6 AC 全 PASS,exit 0)

| AC | 判準 | 結果 |
|---|---|---|
| W1 emit_show_beat | slot_bigwin 端到端吐主秀 beat;序列 == In→Hit→Loop→Out | ✅ |
| W2 signature | 吐出的 Hit 每 bone 具 hit 簽章(復用 0f 判定器) | ✅ 5/5 bone(peak 1.18–1.35、蓄力 0.931、變號 4) |
| W3 seamless_chain | In 尾/Hit 首尾/Loop 首尾/Out 首 皆 identity → 邊界位移=0 | ✅ max_disc **0.0** |
| W4 truth_regression | validate_priors overall_pass + slot_bigwin 覆蓋 1.0(Hit=unused) | ✅ |
| W5 negative_control | (a) 對稱脈衝取代 Hit→簽章 False;(b) 移除 Hit 先驗→端到端退回 [In,Loop,Out] 0 主秀 beat | ✅ |
| W6 both_genres | slot_reveal 端到端仍吐 reveal(open)+hit(hit) | ✅ |

回歸(未破壞既有):0d `validate_anim`(robot / robot --selftest / Symbol_Ww,重建含 Hit 後仍過)、
0e `validate_deform_gen`、0f `validate_beat_templates` 全 **PASS**。

**踩到並修好一個耦合**:`check_readiness` 抓到 `validate_analyzer_award.py` 的 `4_storyboard_structure`
由 GREEN→RED(加 Hit 後,proposed_beats 由 {In,Loop,Out} 變 {In,Hit,Loop,Out},原用**嚴格集合相等**判定 → fail;
連帶 spine-asset-forge / spine-target-analysis 兩區塊變 HOLD)。修法(非降門檻,修正過嚴等式):改為
`observed ⊆ proposed`(分析器須復現 Award 每一 beat)+ **extra 每個都必須是合法主秀類別(hit/reveal),否則仍 FAIL**
(保留反捏造鑑別力;實測 extra={wiggle}→FAIL)。修後 validate_analyzer_award overall_pass True。**教訓**:先驗庫是
多個下游閘的共用真值來源,動它要掃全域回歸——這正是 check_readiness 存在的價值。

## 關鍵發現

- **主秀節拍是「先驗庫」層而非「模板」層的事**:0f 把模板做好也驗好,但先驗庫沒引用它 → `build --animate`
  端到端等於沒有主秀。真正接通的是先驗 beat 表 +1 拍(gen 端零改動)。閘要走**真實組裝路徑**才抓得到這缺口。
- **介面契約(identity endpoints)決定哪個模板能插哪裡**:Hit(identity 兩端)可夾序列中段;
  Reveal(collapsed 起手)只能當首拍。0d 的「setup identity 介面」推廣到主秀節拍。
- **負對照 W5(b) 給出因果證明**:把 Hit 從先驗拿掉,端到端立刻退回 [In,Loop,Out] → 主秀出現 = 併入先驗。

## 誠實界定 / 下一步

- Award(slot_bigwin 真值)只有 `Award_<tier>_In/Loop/Out`,payoff **融進 In 動畫**,無單獨 hit 命名 →
  Hit 為**提案節拍**,`validate_priors` 標 `unused: ["Hit"]`(不占真值覆蓋,slot_bigwin 仍 1.0)。這是誠實的。
- 「勿動已驗先驗」達成:In/Loop/Out 的 key/kw 一字未改,只新增 Hit;slot_reveal 全未動。
- `spine-anim-forge` 仍 **HOLD**(運動基元先驗、單一真值資產,達 L3 前不打包)。
- 下一小步候選:(F) 更多主秀節拍(anticipate-hold / multi-hit combo / cascade,各配結構簽章 AC;
  沿用 0g 介面契約:identity 兩端可夾中段、collapsed 起手只能首拍);(G) S1(e) 關節 pivot 接 keyframe。

## 產出檔案

- 新增:`tools/analyzer/validate_show_beat_wiring.py`、`knowledge/s1-show-beat-wiring.md`、本 log。
- 更新:`tools/analyzer/genre_priors.py`(slot_bigwin +Hit beat / +role)、
  `tools/analyzer/validate_analyzer_award.py`(check-4 由嚴格相等改 observed⊆proposed + extra 須主秀類別)、
  `tools/check_readiness.py`(cap `show_beat_wiring`)、`skills/READINESS.md`(快照)、
  `STATE.md`、`knowledge/README.md`。

## 下一個 session 接手

從 STATE「下一步動作」:(F) 更多主秀節拍、(G) S1(e) 關節 pivot 接 keyframe。
S5→L3 仍待 (D) 多 rig 真值(使用者資源)。
