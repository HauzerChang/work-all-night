# 2026-08-07 — session 001

## 這次做了什麼(S3 端到端:PSD 切件 → mesh → 對照 Award 真實 mesh)

推進 `STATE.md` 最高優先候選(有真值可比):把「PSD→件→S3 mesh」整條接到**真實生產 mesh 真值**。

1. **切件**:`psd_slice.py assets/robot_parts.psd` → 5 件 PNG(其中光暈/身體/左手在 Award 為 mesh)。
2. **新工具** `tools/mesh_gen/compare_award_mesh.py`:對 3 件各跑 4 條靜態幾何 AC —
   - AC-A 藝術家基準(Award mesh uvs 映回件像素填三角,對件 alpha 求 IoU;v 方向自動判定)。
   - AC-B 生成覆蓋(`generate_mesh_v2 auto`)≥ 藝術家基準 − 0.03。
   - AC-C 生成 vs 藝術家 覆蓋一致性 IoU ≥ 0.80。
   - AC-D 頂點預算且與藝術家同數量級。
3. **視覺自驗**:`overlay.py` 產 `knowledge/figures/s3-award-mesh-overlay.png`(3 件 × 藝術家/生成 線框疊件 alpha)。

## 結果(3 件全 overall_pass)

| 件 | gen_mode | 藝術家 IoU | 生成 IoU | 一致性 | 生成v/藝術家v |
|---|---|---|---|---|---|
| 光暈 706×683 | delaunay-v1 | 0.949 | 0.933 | 0.918 | 35/78 |
| 身體 379×425 | delaunay-v1 | 0.948 | 0.966 | 0.928 | 60/98 |
| 左手 257×215 | delaunay-v1 | 0.977 | 0.964 | 0.957 | 59/80 |

## 關鍵結論 / 決策

- **uvs 解讀被外部真值證實**:Spine mesh `uvs`=region-local `[0,1]`、v 由上而下(3 件 flip 皆 False、
  藝術家覆蓋自身 IoU 0.95~0.98)。延續「用真值校準評估器」的紀律(前三次 miscalibration 教訓)。
- **v1 Delaunay 對「團塊狀」真實件通用**:3 件 aspect<1.2 → auto 正確回退 v1;v1(團塊)與 v2-strip(條狀)
  **互補**,auto 選對模式。這補上了「v2 只在窗簾驗過、blob 件如何」的空白。
- **生成 mesh 更精簡**:少 25~55% 頂點達同等覆蓋。
- **誠實限制**:這 3 件在 Award 無 deform timeline → **只驗靜態幾何,未驗 deform 穩健**(deform 結論仍只
  來自 main_draw 窗簾/陰影)。要驗機器人件變形需骨骼/權重(S5 相關),超出本 chunk。

## 產出

- `tools/mesh_gen/compare_award_mesh.py`
- `knowledge/s3-award-mesh-endtoend.md`(+索引)、`knowledge/figures/s3-award-mesh-overlay.png`
- 更新 `STATE.md`、本 log

## 下一步(見 STATE.md)

候選1:**SkelToJson**(件→Spine mesh attachment 組裝,固化 uvs=region-local + `PSD名/圖層名` +
size+2px padding),把 S4→S3→JSON 寫出端補齊、端到端貫通。
