# 2026-08-07 — session 001

## 這次做了什麼(自驅排程:S3×S4 端到端對照真值)

推進 STATE 的 next-action #1(最高優先、有真值可比):把「PSD 件 → 自動生成 mesh」對真實生產
spine `Award` 的藝術家 weighted mesh 做量化對照。

1. **切件**:`psd_slice.py assets/robot_parts.psd` → 5 件;取 Award 中為 **mesh** 的三件
   光暈/左手/身體(右手/頭是 region,不列入)。
2. **新工具** `tools/mesh_gen/compare_against_award.py`:
   - 對每件跑 `generate_mesh_v2`(auto → 三件皆 Delaunay-v1,因非直條件)。
   - 在**正規化 UV 空間(512²)**疊合比對藝術家真值 mesh:cover_iou_gen / cover_iou_real /
     footprint_iou / 頂點數。免 atlas 反旋轉對位(uvs 正規化 + mesh W,H=原生藝術尺寸 ≈ PSD 件 bbox)。
3. **結果:三件全 PASS(exit 0)**:

   | slot | cover_gen | cover_real | parity | footprint | verts gen/real |
   |---|---|---|---|---|---|
   | 光暈 | 0.927 | 0.945 | 0.981 | 0.918 | 35/78 |
   | 左手 | 0.955 | 0.981 | 0.973 | 0.957 | 59/80 |
   | 身體 | 0.962 | 0.948 | 1.015 | 0.929 | 60/98 |

## 關鍵發現 / 決策

- **端到端里程碑**:S3(生成)+ S4(PSD 切圖)首次對**真實藝術家真值**閉環驗收通過。生成 mesh 覆蓋
  與藝術家相當(parity 0.97–1.02)、輪廓吻合(footprint 0.92–0.96),而**頂點數只有藝術家 45–75%**。
- **評估器校準(延續本專案主題)**:光暈是羽化光暈(**半透明佔內容 24.6%**,vs 手/身體 2–3%),
  絕對覆蓋 IoU≥0.95 **連藝術家(0.945)都達不到** → 對羽化件用絕對門檻是錯的。改法:**E1 只驗
  格式/拓樸**,覆蓋率交給 **E2(絕對下限 0.90)+ E3(parity-against-artist)**。有客觀羽化證據支持,
  非移動門柱。
- **可信度**:做了三向負對照(拿左手真值當身體真值 footprint→0.512;uv 翻轉 0.607;uv 平移
  cover→0.340)全被 E2/E3 擋下 → 比對器有鑑別力。

## 產出

- 新增 `tools/mesh_gen/compare_against_award.py`、`knowledge/s3-s4-end-to-end-award.md`(+索引)。
- `results/robot_award_compare/`:3 件生成 mesh JSON + `report.json`。
- 更新 `STATE.md`(里程碑 + 下一步 A/B/C)、本 log。

## 下一步(見 STATE.md)

擇一:**A** deform 對照(需先擴充 Python weighted 世界座標 + 骨變換);**B** BBW 權重生成
(生成 mesh 目前 unweighted);**C** SkelToJson(件→可載入 Spine JSON)。
