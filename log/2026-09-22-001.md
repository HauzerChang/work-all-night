# 2026-09-22 run 001 — S1 (G-4'''''') 生成器產出 shearY 通道(雙軸 shearX+shearY 正交扭)

## 目標(bounded chunk)
補 wobble(G-4',純 shearX)/ squash(G-4'''',shearX+耦合非均勻 scale)一路留到現在的**最後一條
shear honest boundary**(`shearY≡0`)—— 讓生成器實際產出 **shearY** 通道(雙軸 shear),把一般仿射 M
的所有 shear 自由度塞滿。

## AC(動手前定,可自我量測)
- TW1 present + 雙軸 crux:twist beat 直出、finite、有 bone,峰 |shearX|≥MIN_SHEAR **且**峰 |shearY|≥MIN_SHEAR。
- TW2 兩軸各自阻尼振盪:首尾 0 + 繞 0 變號≥3 + 相繼極值嚴格遞減。
- TW3 正交相位(crux):shearX 全域峰時 |shearY|/峰≤QUAD_TOL、shearY 峰時 |shearX|/峰≤QUAD_TOL。
- TW4 identity 介面:sample(0)/sample(dur) identity 且兩 shear 軸首尾 0。
- TW5 端到端 --shear-pivot 含 shearY 補償:pivot 殘差 <TOL_FIX vs 未補償負對照 ≥MIN_NEG 且 >NEG_RATIO×。
- TW6 負對照:純 shearX→雙軸 FALSE、同相 shear→正交 FALSE、shear 隔離、加性零回歸。

## 做了什麼
- `tools/analyzer/beat_templates.py`:新增 `gen_twist`/`_twist_env`/`_TWIST_SHEAR`/`TWIST_KEYWORDS`。
  運動基元=雙軸阻尼 shear 於正交相位:於 [WOBBLE_LEAD,WOBBLE_TAIL] 均勻布 2·nosc 個交錯事件
  (偶事件放 shearX 極值+shearY 顯式 0、奇事件放 shearY 極值+shearX 顯式 0),兩軸各自阻尼 `A·rⁱ`/`B·rⁱ`。
  兩軸包絡構於同一時間點,直接 zip 成單一 `shear` timeline `{time,x,y}` → 精確 quadrature 由建構保證。
- `tools/analyzer/gen_animations.py`:註冊 `_DISPATCH["twist"]` + `_CAT_KEYWORDS` twist。
- `tools/analyzer/tier_variants.py`:`SHEAR_CATS` 加 twist(第三個 shear 產出者;shear-isolation 閘認得)。
- `tools/analyzer/genre_priors.py`:slot_bigwin 加 twist beat(additive)+ `_BIGWIN_ROLES["twist"]`。
- `tools/analyzer/validate_twist_gen.py`(**新**):6 AC 整合閘(先驗庫→真實 build_spine robot 骨架→build_animations)。
- `tools/check_readiness.py`:anim-forge 加 cap `twist_dual_axis_shear` L2(gen)。
- 文件:`knowledge/s1-twist-dual-axis-shear.md`(新)+ 索引、`skills/READINESS.md`、`STATE.md`。

## 產出/驗證結果
- `validate_twist_gen.py` **6 AC 全 PASS**:峰 shearX=16°、shearY=12°;TW3 正交比 0.0(同相退化=1.0 分離);
  TW5 pivot 殘差 0.0099–0.0354px vs 負對照 8.03–23.19px(>500×,shy_peak 至 8.87°)。
- **回歸 20 閘全綠**(19 既有:shear_gen/wobble_tier/wobble_count/squash_gen/squash_tier/squash_count/
  tier_combo_count/tier_variants/shear_pivot/scale_pivot/pivot_rotation/cascade/priors/priors_beats/
  priors_combo_charge/priors_cascade/more_beats/beat_templates/deform_gen + 新 twist_gen)。
- 端到端 `build_spine --animate --shear-pivot` 直出 `twist`(shearY peak 12° at 光暈)+ `validate_build`
  round-trip overall_pass(premult MAE 0.031)。
- `check_readiness.py`:cap GREEN;anim-forge 仍 HOLD(運動基元先驗、單一真值資產)。

## 決策 / 關鍵發現
- 一般仿射 M 的 2×2 local basis 兩角度自由度(`θ+shearX`、`θ+90+shearY`)已分別由 shearX(wobble/squash)
  與 shearY(twist)驅動 → **所有 shear 自由度已被生成器塞滿**。
- **正交相位是「真雙軸」的簽章**:同相加第二通道只是斜拉放大 √2 倍,非扭;「峰時另一軸過 0」才使斜拉方向旋轉。
- twist 刻意只產 shear、不接檔位差異化 → regression surface 最小、專注補 shearY 邊界。

## 下一步(擇一,皆自主)
- (G-4'''''''') shear+scale+rotate 三通道同時的單一運動基元;
- (G-4''''''-tier) twist 接檔位幅度/count-aware(shearY 通道差異化);
- (J-3) cascade count-aware(波速/散佈/件數隨檔位);(G-4'''''-charge) charge 蓄力段數。
- S5→L3 仍待 (D) 多 rig 真值(C/資源類,使用者提供)。
