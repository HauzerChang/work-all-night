# 2026-08-27 — session 003

## 目標(使用者選項 3)
續推候選 2:實作 weighted mesh 生成(內部取樣密度 + BBW 權重),用上一里程碑的閘驗收,
讓 `spine-weighted-forge` 脫離 HOLD。

## AC(動手前)
AC1 生成 mesh 重建 setup 乾淨;AC2 真實 Legend 動畫 si=0/flip=0;AC3 變形平滑度≈藝術家;AC4 頂點預算≤1.4×。

## 做了什麼
- `weighted_deform_eval.py` 重構:抽出共用核心 `eval_pv`(JSON 與生成 mesh 共用),
  加 `inverse_transform_point`(算 bind)、`edge_length_cv`(平滑度指標)。閘回歸 PASS(無迴歸)。
- 新 `generate_weighted_mesh.py`:輪廓 → `triangle` 三角化(max-area 控內部密度)→
  **heat-diffusion 權重**(`(L+H)W=HP`,cotangent Laplacian + 到最近骨線段距離熱貢獻,天然 partition of unity)→
  剪 top-k → Spine weighted 格式(bind 經逆骨變換)。
- 新 `validate_weighted_gen.py`:對 Award 3 機器人件跑 AC;opaque 硬性、soft 只記錄。

## 自我驗證結果
`validate_weighted_gen.py` → **OPAQUE 件 OVERALL: PASS ✅**
- 身體:生成 nv=98(==藝術家)、si=0、cv增幅 0.000(藝術 0.000)、ar_std 0.146(藝術 0.146)→ 4 AC 全過。
- 左手:生成 nv=104、si=0、**cv增幅 0.000 < 藝術 0.004(更平滑)**、ar_std 0.150 → 4 AC 全過。
- 光暈(soft):生成 si=368 vs 藝術家 si=71 → 未追平,但 additive 無害、閘歸 si-tolerant,記為已知限制。
- 圖 `knowledge/figures/s3_weighted_mesh_gen.png`(藝術家 vs 生成 setup + 變形幀)。

## 完成度機制連動(如設計運作)
- `check_readiness.py`:weighted-forge 的 `bbw_weights`/`interior_sampling` L0/L1 → **L2 GREEN**。
- 區塊**仍 HOLD**,但理由由「生成器未做」→「待端到端 L3(接 build_spine)」= 健康的部分進度。
- 防固化規則生效:沒有因為「生成器能跑」就宣稱可 skill 化 —— 缺 L3 pipeline 就 HOLD。
- 同步更新 skill 資產副本 `skills/spine-mesh-doctor/assets/weighted_deform_eval.py`(我改過 tools 版),
  自 assets 獨立跑仍 PASS;重生 `skills/READINESS.md`。

## 產出
- `tools/mesh_gen/generate_weighted_mesh.py`、`validate_weighted_gen.py`;重構 `weighted_deform_eval.py`
- `knowledge/s3-weighted-mesh-generator.md` + 索引;圖 `s3_weighted_mesh_gen.png`
- 更新 `check_readiness.py` / `skills/READINESS.md` / 同步 skill 資產 / STATE

## 下一步
1. **端到端(→ weighted-forge L3)**:weighted mesh 寫回 `build_spine` 產完整可載入 spine,round-trip 驗。
2. 軟件非均勻拓樸追平光暈(次要)。
3. 達 L3 後 weighted-forge 併入 `spine-asset-forge` skill(C 類回報使用者拍板)。
