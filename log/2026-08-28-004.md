# 2026-08-28 — session 004

## 這次做了什麼(使用者決策 B:跨分支研究成果「乾淨合流」到單一 canonical 分支)

### 背景
排查分支增生時發現:remote 的 200+ 條 `claude/*` 不是雜訊,而是**平行且重複**的研究線——
每次 routine run 從同一份 default clone、讀到同一個 STATE「下一步」,於是各自重做同一 chunk 再 push 到隨機新分支,
彼此發散、從未合流。結果:~6 條重複的 weighted 變形評估器、~5 條重複的 S1 keyframe 生成器,
且沒有任何單一分支同時擁有全部成果。使用者選 **B(最乾淨)**:盤點各線最佳成果,合併成一條 canonical。

### 合流決策(以實跑/實測為據,非臆測)
- **canonical 底 = `claude/vibrant-franklin-3r9ey4`**(最完整:weighted 變形評估器 + weighted 生成器
  + `build_spine --weighted` 端到端 + **skill 化完成度機制**(`check_readiness.py`、`skills/`、spine-mesh-doctor)。
  實跑 `validate_weighted_deform.py` / `check_readiness.py` 全 PASS。
- **S1 keyframe:5 條平行實作擇一**——比對各自 knowledge 文件的 AC/架構後選 **zjze4k**
  (可重用取樣器 `spine_anim.py` + `build_spine --animate` 一鍵整合 + intro/loop/outro 皆落 setup identity 的乾淨串接)。
  其餘 4 條(animate_spine/build_animations/storyboard_to_anim…)**去重丟棄**。
- **去重**:丟棄本 session 早先的近重複評估器 `weighted_deform.py`(3r9ey4 已有 `weighted_deform_eval.py`)。
- **併入**:S4 交接(`handoff_S4.md`/`run_s4.md`/`STATE_S4.md`)+ 分支釘定(`run.md`/`SCHEDULE.md`)。

### 動手(合流機制)
- build_spine.py:手動合併 zjze4k 的 `--animate` 進 3r9ey4 的 build()(與 `--weighted` 並存,無衝突)。
- 解 log 檔名衝突:3r9ey4 與 zjze4k 都有 `log/2026-08-27-001.md` → 保留 3r9ey4 原檔,zjze4k 的改名 `-005.md`。
- run.md:在 3r9ey4 版(含步驟 4.5 skill readiness)上再加步驟 0「釘 `claude/spine-main`」+ 收尾改固定 push。
- knowledge/README 補 keyframe 索引;STATE/PLAN 補 S1 keyframe 里程碑 + S4 交接 + 分支策略,並更正 S2/S3/S4 狀態行。

### 驗證(合流後單一 tree 全綠)
- `build_spine --animate` + `validate_anim --selftest` → **overall_pass: true**(4 AC + 負對照)。
- `check_readiness.py` → spine-mesh-doctor / spine-asset-forge / **spine-weighted-forge READY**;
  spine-slicing / spine-target-analysis HOLD(防半成品)。全區塊 validator 實跑通過。

## 產出
- canonical 分支 `claude/spine-main` = 3r9ey4 底 + zjze4k keyframe + S4 交接 + 分支釘定 + 去重。
- 保留兩線全部 log(3r9ey4 的 08-27-001~004 + zjze4k 改名的 -005)。

## 使用者待辦(GitHub / web UI)
1. 把 repo **default branch 改成 `claude/spine-main`**(Settings → General → Default branch)。
2. 更新主研究 Routine 的 Prompt 為 SCHEDULE「① 主研究 Routine」那段(自帶 checkout `claude/spine-main`)。
3. (選配)清理 200+ 條舊 `claude/vibrant-franklin-*` / `zealous-noether-*`;確認成果已在 spine-main 後可安全刪。

## 主排程下一步(見 STATE 候選)
S5 rig pivot(唯一卡死環節,槓桿最高)或 weighted-forge 併入 `spine-asset-forge` skill(C 類需拍板)。
