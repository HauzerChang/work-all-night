# 2026-07-31 — session 001

## 這次做了什麼(S3×S4 端到端對真實生產 spine 驗收 — 里程碑)

接 STATE 最高優先 bounded chunk:**PSD件 → S3 mesh → 對照 Award 真實 mesh**。

### AC(先定可檢查目標)
1. 對 `robot_parts.psd` 中在 Award 為 mesh 的部位(光暈/身體/左手),自動生成 mesh 的
   **覆蓋率 IoU ≥ 藝術家生產 mesh 基準**(margin 0)。
2. 生成 mesh setup-pose 幾何乾淨(0 自交/0 翻面/0 退化)。
3. 評估器可信:負對照(縮小的 mesh)必須被判 fail(有鑑別力)。

### 做了
1. **端到端 harness** `tools/mesh_gen/psd_to_award_mesh.py`:PSD 切件(reuse `psd_slice`)→
   `generate_mesh_v2`(auto)→ 覆蓋率 IoU(生成 vs 藝術家,同一件 alpha 量測)+ setup 幾何閘 +
   負對照(0.85× 縮)。映射 PSD 圖層 → slot `機器人拆件/<名>`,只挑生產 spine 為 mesh 的 slot。
2. **epsilon 校準**:掃描 v1 hull 的 `epsilon_frac` 0.008→0.001。舊預設 0.008 對羽化軟邊
   (光暈)覆蓋率低藝術家 **4.5%**;**0.004 為甜蜜點**(3 件全 match/beat、頂點 ≤ 藝術家)。
   → 把 `generate_mesh_v2.generate` 的 v1 後備預設改 `epsilon_frac=0.004`。
3. **回歸驗證**:main_draw 4 mesh 以 `validate_against_real --gen v2` 全 overall_pass(strip 路徑不受影響)。

## 關鍵結論 / 發現

- **端到端通過(真值)**:光暈 +0.012 / 身體 +0.0351 / 左手 +0.0028(全 ≥ 藝術家),頂點數 44/69/70
  對 78/98/80(藝術家),即**用約半數頂點達到 ≥ 藝術家覆蓋率**,setup 幾何全乾淨,負對照 IoU 掉到 0.69~0.72。
- **deform 閘對此資產 N/A**:Award 機器人 mesh 是 weighted 骨骼蒙皮,枚舉全 12 動畫**無任何 deform
  timeline** → 此資產真值是靜態覆蓋率;`transfer_deform_check` 只適用 unweighted+有 deform 的 main_draw。
- **座標對齊確立**:Award region `orig==size`、`offset 0,0`(僅 ~0.70 打包縮放,無 trim),
  mesh `width/height` = 件 +2px → 藝術家 UV(region 正規化)可直接對齊件 alpha(誤差 <0.5%)。
- **小坑**:`image/shadow2` slot 的 mesh attachment 名為 `image/shadow`(共用 region),
  驗證需 `--slot image/shadow2 --name image/shadow`(已記進 knowledge)。

## 產出

- `tools/mesh_gen/psd_to_award_mesh.py`(端到端整合 AC,exit code = pass/fail)
- `tools/mesh_gen/generate_mesh_v2.py`(v1 後備 epsilon 0.008→0.004 校準 + 說明)
- `knowledge/s3-psd-to-award-mesh.md`(+ 索引)
- 更新 `STATE.md`、本 log

## 下一步(見 STATE.md)

(1) **SkelToJson**:把生成 mesh 組裝成真正可載入的 Spine JSON attachment/skin(產檔),完成
「PSD→件→mesh→Spine 檔」全鏈;(2) S2 補圖/骨架閘;(3) weighted mesh deform 支援(若要對其他 weighted 生產檔做變形級驗收)。
