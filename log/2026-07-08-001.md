# 2026-07-08 — session 001

## 這次做了什麼(S3 端到端對照 Award 真實生產 mesh — 里程碑)

推進 STATE 下一步 #1(最高優先、有真值可比):把機器人拆件 3 個 mesh 件的真實 alpha
餵給 S3,對照 Award(big win spine)藝術家真實 mesh 做端到端靜態驗收。

1. **探查 Award 3 mesh 件**:光暈(78v/hull78 純邊界)、身體(98v/hull40)、左手(80v/hull42),
   **全部 weighted**(vertices!=uvs);且 **12 支動畫全無 deform timeline 觸及這 3 slot**(程式確認)。
2. **uv 對齊確認**:Award mesh uvs 是件內 region-local 0..1,與 atlas_crop(CW derotate+0.70 縮)crop 方向一致;
   4 向翻轉負對照(none 0.97~0.98、其餘 0.4~0.76)確認方向無誤。
3. **建端到端驗收工具** `tools/mesh_gen/validate_award_mesh.py`:切件 → generate_mesh_v2 → 3 條 AC
   (覆蓋率 IoU ≥ 藝術家自身 −0.03 / evaluate_mesh 全條 / 頂點預算對齊藝術家量級)。
4. **首跑抓到 2 問題**:光暈 cover 0.929<藝術家 0.980,且有孤兒頂點。
   - **修 A(bug)**:`filter_triangles` 濾三角後留孤兒頂點 → 加 `prune_orphans()`(丟未用內部頂點+重編,hull 前綴保留)。
   - **修 B(參數)**:epsilon 掃描證實覆蓋率由邊界密度主導;v1 預設 0.008 對大件太粗 → **改 0.003**。
5. **重驗全過**:3 件 overall_pass=True(gen IoU 0.978/0.988/0.988 vs 藝術家 0.980/0.976/0.968,nv 61~71 vs 78~98)。
   回歸:main_draw v2 strip 4 mesh 不受影響仍過;main_draw v1 curtain_left 仍過(IoU 0.992、deform 乾淨)。

## 關鍵結論 / 教訓

- **S3 首次對「跨資產、真實生產標的」端到端驗收成功**(先前只對 main_draw 自身 4 mesh)。
- **參數對小件過擬合的盲點**:0.008 只在 v2-strip 小窗簾驗過,對大件/軟邊嚴重欠採樣 → 用真實大件才暴露。
- **deform 閘誠實標記 N/A**:weighted+無 deform timeline,S3 尚無 BBW 權重 → 只驗靜態;不套未校準壓力(遵 RULES)。

## 產出

- 新增 `tools/mesh_gen/validate_award_mesh.py`
- 改 `tools/mesh_gen/generate_mesh.py`(prune_orphans + 預設 epsilon 0.008→0.003)
- 新增 `knowledge/s3-award-mesh-end-to-end.md`;更新 knowledge 索引、STATE
- 本 log

## 下一步(見 STATE.md)

BBW 權重生成(把靜態幾何對齊升級為可動對齊,替代 weighted mesh 的關鍵缺口)或
件→Spine JSON 組裝工具(SkelToJson 寫出,固化 `<PSD名>/<圖層名>` 命名慣例)。
