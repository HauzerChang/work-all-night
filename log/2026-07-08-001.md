# 2026-07-08 — session 001

## 這次做了什麼(S3×S4 端到端:對照 Award 真實藝術家 mesh)

推進 STATE 的最高優先候選 #1:PSD/atlas 件 → S3 `generate_mesh_v2` → **對照 Award 真實生產藝術家 mesh**。
首次以真實藝術家 mesh 當 ground truth(先前只有 main_draw 窗簾與合成/自資產)。

1. **對齊驗證**:確認 Award 機器人 mesh 的 `uvs` 實測為 **region-local 0..1**(非全頁 atlas UV),
   直接 ×crop(W,H) 對齊即高 IoU(0.968~0.980,noflip)→ 修正先前「需轉 region 局部」的假設。
   同時**再次確認 `atlas_crop` CW derotate 正確**(光暈/身體 rotate=true 對齊後 IoU 高)。
2. **端到端量化**(`compare_robot_mesh.py`,3 件:光暈/身體/左手):
   - auto 模式全回退 **v1 Delaunay**(3 件近方形,aspect<1.2 → 非 strip)。
   - 輪廓 IoU vs 藝術家:身體 Δ−0.008、左手 Δ−0.008(**幾乎追平**)、光暈 Δ−0.050(**落後**)。
   - 頂點經濟度:生成用藝術家 **54%~62% 頂點**(48–61 vs 78–98)仍近乎追平剛體件。
   - 全件 AC(拓樸/格式/預算)過、靜態 0 自交 0 退化。
3. **修 bug — 內部孤兒頂點**:光暈生成 mesh 有 1 內部頂點無三角參照(AC2c fail)。
   加 `prune_orphans()`(只剪內部孤兒、保 hull-first)→ nv 54→53、IoU 不變、AC2c PASS。
   main_draw 窗簾 v2 回歸未受影響(overall_pass、IoU 0.9338≥0.9176、deform clean)。

## 關鍵結論 / 教訓

- **v1 Delaunay 對緊湊剛體件通用性佳**:以更少頂點追平藝術家輪廓(身體/左手 Δ−0.008)。
- **柔性放射狀件(光暈)是唯一落後項**:hull 簡化太粗(epsilon 0.008 → hull=14)貼不住圓潤羽化邊
  → 明確改善方向:低 solidity 件降 epsilon 加密 hull。
- **Award 機器人 mesh 無 deform timeline**(骨骼/權重變形)→ 無真實位移場,**AC5 真實 deform 轉移 N/A**;
  這些柔性件在 v1 下的耐變形性未被生產資料驗證(誠實標註)。
- 評估器方法沿用真值自一致(藝術家 mesh 自身 si=0)+ 對照,結論可信。

## 產出

- 新增 `tools/mesh_gen/compare_robot_mesh.py`(可重現端到端對照)。
- 改 `tools/mesh_gen/generate_mesh.py`(`prune_orphans` 修內部孤兒頂點)。
- 新增 `knowledge/s3-robot-mesh-vs-artist.md`;更新 `knowledge/README.md`、`STATE.md`。

## 下一步(見 STATE.md)

(2) 柔性件 hull 加密(改善光暈落後項,有真值可比,純 CPU)——最高優先;
或 (3) 把命名/尺寸慣例固化成 SkelToJson 組裝工具,串起「切圖→mesh→Spine JSON」。
