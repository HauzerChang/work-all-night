# 2026-07-03 — session 008

## 這次做了什麼(驗收升級:AI 自主切圖 — 從「執行」到「規劃」)

使用者出題:給粗分件 PSD(DJ 貓 3 層),要 AI 自己從動畫需求反推切圖規則、輸出細分件 PSD,
之後用美術完成版交叉比對(測 AI 切圖能否達到/超越人類)。

### 做法
1. **動作清單反推件清單**(運動決定拆件):頭律動/耳擺/髮甩/左手揮拍/右臂刷碟/耳機彈跳/
   胸腔律動/轉盤旋轉 → 13 件。
2. **切線工藝規則**:切在關節藏在遮擋下、重疊帶 14px 補繪(不整區亂補)、旋轉自覆蓋件
   copy 不挖洞、切邊羽化 2px、像素歸屬優先權、z 序=遮擋序。
3. 新工具 `psd_resegment.py`(通用:規格 JSON 驅動)+ `assets/dj_cat_reseg_spec.json`(AI 決策)。
4. 逐件視覺板迭代 v1→v4(耳機吞胸口→雙橢圓;耳偷帽側;轉盤重定位偏 60px;頭頂補到 y0)。

### 結果
- `acceptance/dj_cat_ai_sliced.psd`:13 層、中文層名正確、切圖閘 MAE **0.007**/0 孤兒 PASS;
  四份 PSD(dj_cat 原檔/robot/Symbol/AI 重切)閘全過。
- 修工具鏈 bug×2:psd-tools 中文層名(luni 區塊)、程式生成 PSD 的 composite preview 無 alpha
  (`ignore_preview=True`)。

### 待續
- **等使用者提供美術切圖版 → 交叉比對**(件數/切線位置/命名/補繪品質;見 s4-ai-slicing-rules.md 待比對項)。
