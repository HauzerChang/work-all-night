# 2026-08-28 — session 003

## 這次做了什麼(使用者決策:釘固定分支,根治分支增生)

### 診斷
- remote 上累積 **207 條 `claude/vibrant-franklin-*` + 12 條 `claude/zealous-noether-*`** 分支。
- 根因:Routine 每 run 從 default clone + Claude Code 自動開隨機名 `claude/<家族>-<隨機碼>` 工作分支;
  而舊 `prompts/run.md` 收尾用「push 回啟動時所在的分支」(`git rev-parse --abbrev-ref HEAD`,動態偵測)
  → 每 run 的啟動分支都是新隨機名 → **每 run 增生一條分支**,彼此發散(非線性累積)。
- 對照:S4 排程 prompt 明確 `git checkout claude/spine-s4-inpainting`(固定名)→ remote 只有 1 條,正確。

### 修正(① 我改 repo / ② 使用者改 GitHub)
- **`prompts/run.md`**:新增步驟 0「釘固定分支 `claude/spine-main`」(開頭 fetch+checkout);
  收尾步驟 6 改為明確 `git push -u origin claude/spine-main`,移除動態偵測與過時的 vibrant-franklin-3urh59 註記。
- **`SCHEDULE.md`**:改寫頂部「🔀 排程規劃」— 兩條 Routine 各釘固定分支表
  (主 `claude/spine-main` / S4 `claude/spine-s4-inpainting`)、為何要釘(否則增生)、
  **更正官方最新 branch push 機制**(「Allow unrestricted branch pushes」開關已不存在;`claude/` 一律接受、
  非 `claude/` 走 3 條件檢查)、兩條完整 web-UI Prompt、建議把 default branch 設 `claude/spine-main`、舊分支清理。
  刪除過時的「二選一/方案A/方案B(zealous-noether)」段。
- **`handoff_S4.md` / `prompts/run_s4.md`**:主排程分支引用 `vibrant-franklin-bykdbi` → `claude/spine-main`。
- 建立並推送固定分支 **`claude/spine-main`**(承載當前最新主線)。

### 使用者待辦(GitHub / web UI)
1. **改 default branch**:GitHub → repo → Settings → General → Default branch → 選 `claude/spine-main` → Update。
2. **更新主研究 Routine 的 Prompt**:改成 SCHEDULE「① 主研究 Routine」那段(自帶 checkout `claude/spine-main`)。
3. (選配)清理 200+ 條舊 `claude/vibrant-franklin-*` / `zealous-noether-*` 分支。

## 產出
- 改 `prompts/run.md`、`SCHEDULE.md`、`handoff_S4.md`、`prompts/run_s4.md`、`STATE.md`;新分支 `claude/spine-main`。

## 主排程研究下一步(不變)
S3 候選 2:自產 mesh 配 BBW/骨距權重 → 套 `weighted_deform.py` 變形閘,對照美術平滑度。
