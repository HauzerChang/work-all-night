# 2026-07-05 — session 001

## 這次做了什麼(S3 端到端對照 Award 生產真值)

推進 STATE 候選 #1(最高優先、有真值):`robot_parts.psd` 三個「Award 中為 mesh」的圖層
(光暈 / 左手 / 身體)→ 抽 alpha 件 → `generate_mesh_v2` 產 mesh → `evaluate_mesh` 量靜態 AC,
並把 Award **藝術家 mesh** 的 `uvs`(region-local)光柵化成同尺度覆蓋當基準對照。

1. 抽件:psd_tools 抽三層 alpha PNG 到 `scratch_robot/`。
2. 對照:三件長寬比 <1.2(blob)→ v2 auto **回退 v1 Delaunay**(符合預期)。
3. 量化:預設 eps=0.008 三件全過絕對閘(IoU≥0.90),頂點 35/59/60 vs 藝術家 78/80/98(省 2.3×);
   身體 IoU 已勝藝術家。掃 `epsilon_frac`:eps=0.004 時三件 **IoU 全 ≥ 藝術家且頂點仍 ≤ 藝術家**。
4. 視覺證據:`knowledge/figures/s3-robot-vs-award.png`(藝術家橘 / 我方綠 線框疊 alpha)。

## 關鍵結論 / 教訓

- **IoU 差距純由邊界密度(頂點預算)決定**,非能力缺口:eps 一調即 ≥ 藝術家。
- **UV 慣例確認**:Award mesh uvs = region-local 0..1、v 向下;藝術家覆蓋 IoU 0.95~0.98 自洽驗證正確。
- **重大發現:機器人 mesh 為 weighted 且 12 支動畫全無 deform timeline** → 靠骨骼 skinning 驅動,
  S3 變形閘(real_deform_field)**不適用**,本輪只驗靜態。**weighted/BBW 權重是 S3 未建缺口。**
- v2 auto 分流(blob→v1、高瘦→strip)在真實件上成立。

## 產出

- `knowledge/s3-robot-award-validation.md`(新)+ `knowledge/README.md` 索引 + 圖 `figures/s3-robot-vs-award.png`
- `scratch_robot/`(抽件 + `compare.py`,scratch)
- 更新 `STATE.md`(S3 端到端條目 + 重排下一步:#2 SkelToJson 升為最高優先)

## 決策

- 只驗靜態(而非硬套變形閘):因真值 mesh 無 deform timeline,套未校準的合成場會假性失敗(RULES/AC.md 明訓)。
- 把 weighted/BBW 標為未建缺口而非本輪硬做:牽涉骨架(S5),屬較大工作,建議先確認需求(可能 A 類岔路)。

## 下一步(與 STATE 對齊)

#2 SkelToJson:把 S4 切件 + S3 生成 mesh + `機器人拆件/<層>`+size+2px 慣例串成「PSD→Spine JSON」自動組裝,
對照 Award slot 結構驗。blob 件採本輪平衡點 eps≈0.004。
