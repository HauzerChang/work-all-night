# 2026-09-05 run 001 — S1 (G) 關節 pivot 感知 keyframe(candidate 0i)

## 目標(bounded chunk)

STATE「建議下一個」**(G) S1 (e) 關節 pivot 推斷接 keyframe**:把 S5 的接觸縫 pivot 餵給 keyframe
生成器,讓結構子件(頭/肢體)**繞解剖關節旋轉而非繞件中心** —— 純 keyframe 補償路徑(不動 --rig)。

## AC(動手前定)

1. formula:Δ=(R(θ)−I)(O−P) 補償後 P 為精確不動點(閉式)。
2. fixed_point:端到端 build --animate --pivot,Loop 肢體 pivot 世界點跨全幀位移 ≤ 1px。
3. neg_control:無補償(build --animate)pivot 位移**吻合繞件中心閉式**且被補償壓掉(鑑別力)。
4. still_rotates:件遠端點確有位移(沒把旋轉凍結)。
5. interface:--pivot 產物仍過 `validate_anim` 全 AC(補償未破壞介面契約)。
6. discriminative:隨機 pivot → 真 P 會動(P 專屬);P==O → Δ≡0 no-op。

## 做了什麼

- **`tools/analyzer/pivot_keyframe.py`(新)**:`rotate_about_pivot_delta`(閉式 Δ)+
  `compensate_bone`(讀 rotate、新建/合併 translate;**細分 8 段**取樣角度使 Δ 非線性以線性內插逼近,
  幀間殘差 sub-0.001px)+ `compensate_animation`。
- **`gen_animations.build_animations(..., pivots=None)`**:給 pivots 後,凡結構子件有 rotate 即套補償;
  未給則原行為(繞件中心),**完全向後相容**。
- **`build_spine.py --pivot`**(需 --animate、非 --rig):`compute_part_pivots` 復用 `rig_layout`
  的 joint 判定(infer_tree + contact_seam_joint,與 --rig 一致),只取 joint 件;pivot 記入 build_meta。
- **`tools/analyzer/validate_pivot_keyframe.py`(新)**:6 AC + 扁平骨架世界取樣器。
- 註冊 cap `pivot_aware_keyframe` L2 於 `check_readiness`(spine-anim-forge);更新 knowledge 索引 + READINESS。

## 結果

**`validate_pivot_keyframe.py` 6 AC 全 PASS**(robot_parts):
- AC1 formula err < 1e-9;θ=0→Δ=0。
- AC2 fixed_point:頭/左手/右手 pivot 位移 **0.0001px**。
- AC3 neg_control:無補償位移 **14.326 / 2.62 / 10.998px 精確等於閉式 2|P-O|sin(θpk/2)**
  (半徑 164/50/126px、峰角 5/3/5°)→ 證確為繞件中心;補償後壓掉 >20000×;max 位移 14.3px≥5 實質性閘過。
- AC4 far_disp 13~32px(仍旋轉);AC5 validate_anim 4/4;AC6 隨機 pivot 真 P 動 18.97px、P==O no-op。

**回歸全綠**:validate_anim(+selftest)/ cascade / more_beats / beat_templates / priors_beats / priors /
rig_build 皆 overall PASS(--pivot 為選用旗標,預設路徑不變)。

## 關鍵發現

- **無補償位移逐 bone 精確等於閉式**(14.326==14.326…)→ 一次同時證了世界取樣模型正確 + 無補償=繞件中心。
- **頭位移小(2.62px)是忠實反映非弱點**(nod 僅 3°、頸關節距頭心 50px)→ AC3 改用**閉式吻合 + 相對壓縮比**
  的原理化判準,而非會誤殺小運動件的絕對門檻。
- **θ=0→Δ=0** 讓補償對 In/Loop/Out 介面契約**天然中性**(AC5 免調參即過)。
- honest boundary:不動點對**旋轉分量**成立;scale/徑向 translate 仍搬 P(intro 尤甚)→ 驗於 loop 純 rotate 段。
  **--rig(結構搬骨)與 --pivot(keyframe 補償)互補非併用**。

## 下一步(擇一,皆自主)

- pivot 補償推廣到 **pulse/hit 等主秀 beat**(需「分離旋轉分量」的更嚴 AC,因並存 scale/徑向)。
- **cascade × pivot**:跨件錯開波 + 各件繞自身關節旋轉。
- (H) combo/charge 接進 genre 先驗庫(如 (E) 對 hit/reveal 所做)。
