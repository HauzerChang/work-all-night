# 2026-09-01 run 002 — S1 candidate 0f:big-win 主秀 beat 模板(anticipation + settle)

分支:`claude/spine-main`(依 `prompts/run.md` 步驟 0 釘固定分支;本 session 容器起於隨機
`claude/focused-dirac-e5oida`,與 spine-main 同 commit,已 checkout spine-main 累積)。

## 這次做了什麼(一個有界工作塊)

從 STATE「下一步動作」建議 (A):**S1 keyframe 補主秀 beat 模板**。candidate 0d(`gen_animations.py`)
的主秀節拍只有 `gen_pulse` 的**對稱三角脈衝**(identity→peak→identity),缺兩個經典動畫原理:
**anticipation(反向預備)** 與 **settle / follow-through(阻尼回擺)**。依 RULES「每能力必配評估器」,
同塊交付生成器 + 自我品質閘 + 負對照。

- **`tools/analyzer/beat_templates.py`**(新):兩個 role-aware 主秀模板。
  - `gen_hit`:蓄力(scale 0.93)→ 命中(peak)→ 阻尼回擺(0.965/1.015/…)。**首尾皆 setup identity**(可插 Loop 間)。
    role 加成:limb whip、head 點頭衝擊、特效 亮度閃+旋轉甩。
  - `gen_reveal`:藏(0.02)→ 蓄勢 hold → 炸開 overshoot → 回穩。**首 collapsed(scale~0/alpha 0)、尾 identity**。
- **wire 進 `gen_animations.py`**:新增類別 `hit`/`reveal`(中英關鍵字),`hit`/`burst` 移出泛用 `pulse`;
  註冊放檔尾避 import 迴圈(beat_templates 只需上方已定義的 DUR/_rot/_xy/_color)。
- **`tools/analyzer/validate_beat_templates.py`**(新):6 AC 閘,對 **真實 robot 5 拆件+role** 端到端經 `build_animations`。
- 在 `check_readiness.py` 的 `spine-anim-forge` 區塊新增 cap `storyboard_beat_templates`(L2)。

## 結果(`validate_beat_templates.py` 6 AC 全 PASS,exit 0)

| AC | 判準 | 結果 |
|---|---|---|
| B1 well-formed | finite / 時間嚴格遞增 / JSON round-trip | ✅ |
| B2 可串接介面 | hit 首尾 identity;reveal 首 collapsed 尾 identity | ✅ |
| B3 真峰 | scale overshoot ≥1.12(hit 1.348 / reveal 1.35) | ✅ |
| B4 anticipation | hit 命中前下蹲 <0.99(實測 0.931);reveal burst 前蓄勢 hold | ✅ |
| B5 settle | hit `(scale-1)` 變號 ≥3(阻尼回擺);reveal 峰後穿越 ≥2 | ✅ |
| B6 負對照 | 對稱脈衝(gen_pulse)判非主秀 / 不歸位 FAIL B2 / 無峰 FAIL B3 / 真 hit 具簽章 | ✅ |

回歸(未破壞既有):0d `validate_anim`(robot/Symbol_Ww)+`--selftest`、0e `validate_anim(--animate --deform)`、
Symbol_Ww `slot_reveal` 全 **PASS**(pulse 關鍵字調整不影響 In/Loop/Out 類別)。

## 關鍵發現

- **`(scale-1)` 的符號變化數是分辨「主秀 hit」與「天真脈衝」的強鑑別子**:真 hit(反向蓄力+阻尼回擺)≥3 次變號,
  對稱三角脈衝只有單正峰(0 次負向偏移)→ 負對照 `symmetric_pulse_not_main_show` 乾淨分離。
- **anticipation 用「峰前是否下蹲 <1」判定**最穩(與峰值大小解耦、對取樣密度不敏感)。
- 可串接性沿用 0d 的 identity 介面契約(hit 首尾 identity / reveal 首 collapsed 尾 identity)。

## 誠實限制 / 下一步

- 主秀 beat **無唯一正確運動**(先驗手感),閘驗**客觀結構簽章非美感**;緩動幅度手感留使用者(A 類)。
- 目前 hit/reveal 由 fixture storyboard 驅動(真實拆件 role 端到端);**接進 `genre_priors` 先驗庫**(讓
  `build_spine --animate` 直接輸出主秀節拍)為後續小步(需同步 `validate_priors` 真值覆蓋,勿動已驗先驗)。
- `spine-anim-forge` 定 L2 → **仍 HOLD**(運動基元先驗、單一真值資產,防固化;達 L3 前不打包)。

## 產出檔案

- 新增:`tools/analyzer/beat_templates.py`、`tools/analyzer/validate_beat_templates.py`、
  `knowledge/s1-beat-templates.md`、`knowledge/figures/s1_beat_templates.png`。
- 更新:`tools/analyzer/gen_animations.py`(hit/reveal 類別+檔尾註冊、pulse 關鍵字)、
  `tools/check_readiness.py`(cap `storyboard_beat_templates`)、`STATE.md`、`knowledge/README.md`。

## 下一個 session 接手

從 STATE「下一步動作」:(E) 主秀 beat 接進 genre 先驗庫、(F) 更多主秀節拍(combo/cascade)、
(G) S1 (e) 關節 pivot 接 keyframe。S5→L3 仍待 (D) 多 rig 真值(使用者資源)。
