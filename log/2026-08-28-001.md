# 2026-08-28 — session 001

## 這次做了什麼(推進一個有界工作塊)

**S3 weighted-mesh 骨綁變形評估器 —— 真值端(里程碑)。**

補上 `s3-robot-mesh-vs-award.md` 標註的唯一未驗維度:Award 機器人 3 件(光暈/左手/身體)是 weighted mesh、
靠骨骼+權重(LBS)變形且**無 deform timeline**,故逐頂點 `deform_eval.py` 不適用 →「靜態 IoU PASS」
不代表「骨綁變形平滑度對等」。先把**真值端**(美術 mesh 在真實動畫下如何變形)量化建起來。

### 資料事實(從 `assets/Award.json` 實測)
- 7 個 weighted mesh;機器人 3 件綁定骨:光暈=4_LEG3–6、左手=4_LEG5/9、身體=4_LEG3/7/8。
- **唯一驅動這 3 件的動畫 = `Award_Legend_Loop`**(其餘 11 動畫都不動這些骨)。
- 綁定骨鏈 `4_LEG7→4_LEG3→4_LEG2→4_LEG→root`,**全骨 transform=normal** → FK 為單純仿射鏈合成。

### 工具:`tools/mesh_gen/weighted_deform.py`(純 CPU)
- Spine 3.8 bone FK:local 仿射鏈合成;timeline rotate(加)/translate(加)/scale(乘);
  內插支援 compact bezier(`curve=cx1,c2=cy1,c3=cx2,c4=cy2`)/stepped/linear。
- weighted mesh LBS:`worldV = Σ w_j·(boneWorld_j ⊗ bind_j)`。
- 幾何閘重用 `deform_eval`(signed_area/_seg_cross/tri_edges)→ flips/degen/self_intersections。

### 結果:3 件 × AC0–AC4 全 PASS(exit 0)
- **AC0 reproducer 自信任(關鍵)**:每頂點 `Σw=1`(≤1e-5)、動畫 t=0 逐頂點重合 setup pose(**0.0000px**)。
- AC1 setup 幾何非退化;AC2 真實變形乾淨(25 幀 0 翻面/0 自交/0 退化);
  AC3 非平凡(頂點位移 12~36px = diag 的 **2.4%~10.8%**,左手最靈活);
  AC4 負對照(30× 放大 pose)抓到大量翻面(2~26)+ 自交(23~270)。

## 產出
- 新工具 `tools/mesh_gen/weighted_deform.py`
- 新知識 `knowledge/s3-weighted-deform-evaluator.md` + 索引
- 更新 STATE(里程碑 + 候選 2 改寫為「真值端已完成,下一半=自產 mesh 配權重對照」)

## 誠實界定 / 下一步(見 STATE 候選 2)
- ✅ 建成量化真值端(任一 pose 下美術 mesh 世界頂點 + 拓樸乾淨度可算);量化了真實變形量級。
- ⏳ 尚缺:自產 mesh(boundary-dense/strip)+ 內部取樣密度控制 + BBW/骨距權重 → 套同一 driver anim
  與美術逐點比對「變形後表面差 + 平滑度」。真值端已就緒可直接接。量級提醒:對比要在 ~diag×10% 尺度上做。
