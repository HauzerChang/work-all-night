# 2026-07-03 — session 005

## 這次做了什麼(權重 → 資產動起來,S3 最後一塊 × S5 綁定交會)

使用者「進行下一步」→ STATE 最高優先「weighted mesh + 權重」。

### 先挖 Award 權重真值
- 三個 weighted mesh:權重和恰 1、稀疏(≤3 骨)、約半數頂點單骨、接縫次要權重最高 0.84。
- **關鍵發現**:藝術家混的不是 parent,而是**子件級變形骨**(身體+肩部輔助 4_LEG7/8、
  左手+前臂 4_LEG9);光暈綁 **4 根部位骨**(跟著全身動)。「件內再分幾節」需運動資訊
  (S1 反推的事)→ 誠實列範疇外;v1 做 own+parent envelope(關節混合,語義不同但 pattern 同型)。

### 工具
1. `weights.py`:envelope 綁定(關節 R 內 smoothstep 混向 parent,wmax=0.85 錨自藝術家;
   R=2.5×重疊 RMS 半徑)+ weighted 格式編出 + LBS(旋轉沿骨階層)。
2. `skel_to_json --weights`:有 parent 的 mesh 件升級 weighted;eval 對 weighted 用 LBS 反推
   影像框、格式檢查用 uv 重建座標+權重和複核。
3. `validate_weights.py`:格式/±40° 變形掃描/**錨定 AC**/pattern 對照 + **pose 渲染器**
   (逐三角 affine warp 貼圖,region 剛性、unweighted mesh 剛性、weighted LBS)。

### 結果(全 PASS)
- 格式:和 ∈[1,1]、≤2 影響。變形掃描 ±15/25/40°:全 0 自交/0 翻面。
- **錨定 AC**(權重的存在意義):高 parent 權重頂點位移 = 剛性綁定的 **0.395**(剛性=1.0 天然負對照)。
- 組裝:weighted 位置 0.001px、光柵 0.031;骨架閘過。回歸 12 項 ALL PASS。
- **pose 渲染**:雙臂 ±22°+頭 ±8° 三幀,肩部連接不脫離 → `knowledge/figures/robot_pose_strip.png`,
  已傳使用者。**「PSD→可動 Spine 資產」端到端閉環。**

## 關鍵決策 / 教訓
- 範疇外要依證據標記:子件變形骨(需運動)、光暈跨件綁定(A 類)、BBW(等件內多骨才有意義)。
- pose 渲染器是**未來 S1 影片逼近迴圈的渲染引擎雛形**(影片幀 ↔ pose 幀比對)。
- weighted 格式滲透到既有工具的每個假設(eval 影像框、evaluate_mesh 頂點格式)— 逐一補齊而非繞過。

## 產出
- `tools/mesh_gen/weights.py`、`validate_weights.py`(新);`skel_to_json.py`(+--weights)
- `knowledge/s5-weights.md`、`knowledge/figures/robot_pose_strip.png`(+索引)
- 更新 `STATE.md`、本 log

## 下一步(見 STATE.md)
第 2 階段四能力全有工具+閘,端到端已通。建議:**多資產推廣**(Symbol_Ww 18 層全流程,
驗啟發式通用性)→ 觸發里程碑審查(C 類);S1 可用 pose 渲染器自造合成影片 bootstrap。
