# 2026-08-19 — session 005

## 這次做了什麼(bounded chunk:S1 分鏡 → 動畫 keyframe)

接續 session 004 定的最高優先(STATE 候選 0d):把 build_spine 產出的靜態素材補上「運動」,
讓它「會動」。純 CPU、確定性、幅度校準自真實 Award,並先有量化自驗閘再下判定。

### 產出
- **`tools/analyzer/gen_animation.py`**:analyze_target #3 storyboard → Spine 3.8 `animations`(In/Loop/Out)。
  - role→timeline:body 呼吸縮放、head 微點頭、limb 末梢反相擺盪、特效 脈動+alpha。
  - Loop 無縫(首尾==setup,呼吸走上行形狀,與 Award `大標` Loop 一致);In 收斂到 setup → 接 Loop 連續;Out→0。
  - 同檔導出取樣器 `sample_bone/sample_slot_alpha`(含緊湊 bezier 解算)= validator 單一真相。
- **`tools/analyzer/validate_animation.py`**:8 條量化 AC 閘 + 負對照(`--selfcheck`)+ 真值 sanity(`--award`)。
- **`build_spine.py --animate`**:嵌入動畫;robot 5 件 build → In/Loop/Out。
- 圖 `knowledge/figures/s1_animation_curves.png`(三段曲線);知識 `knowledge/s1-storyboard-to-animation.md`。

### 自我驗證(不靠肉眼)
- 幅度真值:Award 12 支 *_Loop 實測 scale ppk∈[0.05,3.75]、rot ppk∈[0.44°,22.4°];我方 body 0.05 / limb 6° 全落區間。
- **robot(slot_bigwin)8/8 AC PASS**;經 build_spine `--animate` 的 built skeleton 重跑亦 8/8;
  setup-pose round-trip 不受影響(MAE 0.031、0 孤兒)。
- **負對照 5/5**:破壞無縫/零運動/limb 同相/In 不收斂/Out 不退場 → 對應 AC 全由 PASS 翻 FAIL → 閘有鑑別力。
- 跨資產:Symbol_Ww(slot_reveal,18 件)build+validate 亦 OVERALL PASS(結構穩健)。

### 誠實界定 / 限制(寫入 knowledge)
1. 僅 slot_bigwin In/Loop/Out 有真值校準;其他 genre 目前套通用三段,未對映專屬 beats。
2. 真值另見:Award Loop 僅 82/120 timeline 嚴格無縫 → 我方把嚴格無縫設為比真值更嚴的品質目標(已記錄)。
3. 無關節鏈 pivot(limb 繞件中心非繞肩,pivot 屬 S5);無 mesh deform;緩動 bezier 手感留給使用者。

## 分支
啟動即在 `claude/vibrant-franklin-6uia8u`(HEAD 已含最新 S1 SkelToJson 工作),直接推進、push 回同分支。

## 下一步(見 STATE 候選)
d2 In/Out 緩動 bezier 化;d3 genre-specific beats(需真值);e 關節鏈 pivot;或候選 2 S3 weighted+BBW。
