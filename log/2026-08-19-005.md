# 2026-08-19 — session 005

## 這次做了什麼

### 研究:S3 weighted mesh 骨綁權重 + 變形平滑度真值閘(里程碑)
補上 `s3-robot-mesh-vs-award.md` 誠實標記的**唯一未驗維度**:靜態 IoU PASS ≠ weighted mesh 骨骼變形平滑度對等。

- **`tools/mesh_gen/weighted_mesh.py`**(能力):
  - `compute_bone_world`:Spine 3.8 bones 樹 → setup-pose 世界變換(FK,2×2+原點)。
  - `parse_weighted` / `reconstruct_setup`:解析藝術家 weighted vertices,重建每頂點 setup 世界座標。
  - `cotangent_laplacian` + `harmonic_weights`:**有界調和權重**(BBW 純 CPU 可證有界+單位分解主幹)。
    由最大值原理 → w∈[0,1];調和唯一性 → Σw≡1;調和 = 最小 Dirichlet 內插(最平滑)。
  - `assign_seeds`(auto,最近骨原點)/ `artist_anchor_seeds`(方法驗證,藝術家純區)。
  - `deform`(LBS)/ `bone_delta_xform` / `dirichlet_energy`。
- **`tools/mesh_gen/validate_weights.py`**(真值閘):對 Award 3 真實藝術家 weighted mesh
  (光暈/左手/身體)5 AC × 3 件**全 PASS**(exit 0):
  - AC1 自一致 5.7e-14;AC2 有界+單位分解 |Σw−1|<1e-16;
  - AC3 平滑度 Dirichlet 相當或更優(身體 −30%、左手 −34%、光暈 +4% 在 10% 容差內);
  - AC4 變形一致 anchor RMS/diag 0.018~0.038(auto 0.051~0.087);
  - AC5 負對照 隨機權重 RMS 為我方 5~8 倍。

### 迭代修正(自我驗證迴圈)
1. 初版負對照失效 → 合成 pose 太均勻(各骨近剛體同動)。改**鑑別性 pose**(每骨不同角度+平移)。
2. AC3 嚴格「harmonic≤artist」對軟邊件(光暈)不成立(藝術家錨點 w≈0.9 非全純,非同 BC 可行解)→
   改判「相當或更平滑,容差 10%」並文件化。
3. 順帶驗 FK:重建 span ≈ 宣告 region w/h(左手 257×216 vs 259×217)。

## 產出
- 新工具 `weighted_mesh.py`、`validate_weights.py`
- 圖 `knowledge/figures/s3_weighted_bbw.png`(setup/藝術家/harmonic/random 變形 + 權重熱圖)
- 新知識 `knowledge/s3-weighted-mesh-bbw.md` + 索引;更新 STATE(候選 2 標完成)

## 下一步(見 STATE.md)
候選 2-接續:(a) 把 auto 權重接上 `generate_mesh_v2` + `build_spine`,端到端產出**帶權重的可載入 weighted mesh**;
(b) 量化「內部取樣密度 vs 變形誤差」曲線,給 generate_mesh_v2 內部密度旋鈕。或候選 0d 分鏡→動畫 keyframe。
