# 2026-08-31 — session 002(主研究)

## 本次有界工作塊:S5 (d) `--rig` × `--weighted` 併用

移除 `build_spine` 的 `--rig`(pivot→bone 關節鏈)與 `--weighted`(骨綁 mesh + 自動控制骨)
**互斥限制**(此前併用直接 `SystemExit`),讓 weighted mesh 接進 rig 關節鏈。

> 開頭校正:routine 被指派隨機工作分支 `claude/focused-dirac-2ax80a`(又一次分支增生);依 SCHEDULE.md
> ① 主研究 Routine + prompts/run.md 步驟 0,切到固定分支 `claude/spine-main`(== 隨機分支同 commit,無工作遺失)推進並 push。

## AC(動手前定,可自量測)

- AC1 結構:每個 weighted 件控制骨 parent == 該件關節骨 `b_{nm}`(接進關節鏈);rig 樹完好;骨 index 合法。
- AC2 setup 不位移:rig×weighted 逐頂點世界座標 == weighted-only 版(證重掛 + 座標轉局部後重建不變)。
- AC3 關節articulate:(a) 轉件關節骨 `b_{nm}` → rig 動 / weighted-only 不動;(b) 轉 rig 根 → 子件隨動 / weighted-only 脫鉤。
- AC4 變形乾淨:關節旋轉逐幀結構件 si=0/flip=0(effect 容忍)。

## 做了什麼

1. **`build_spine.py`**:
   - 移除 `if rig and weighted: SystemExit` 互斥。
   - `_weighted_attachment` 加 `parent_bone`/`parent_world`:控制骨改掛任意關節骨、座標轉相對局部
     (`x,y = 控制骨世界 − parent_world`);**weighted bind 偏移不動**(setup 精確)。
   - 主迴圈:weighted mesh 件在 `rig` 時以 `parent_bone=b_{nm}, parent_world=wo[nm]` 呼叫(掛該件關節骨),
     否則沿用舊行為(掛 root)。非 mesh 結構件仍走 rig 的 delta 路徑。
2. **`tools/analyzer/validate_rig_weighted_build.py`**(新):AC1–4,以 rig×weighted vs weighted-only 對照,exit 0=PASS。
3. **`check_readiness.py`**:`spine-rig-pivot` 新增 cap `rig_weighted_combo`(L2 pipeline)。
4. `knowledge/s5-rig-weighted-combo.md`(新)+ knowledge/README 索引 + STATE 里程碑/進度/下一步。

## 自我驗證結果

- `validate_rig_weighted_build.py`:**AC1–4 全 PASS**(OVERALL PASS,exit 0)。
  - AC1:光暈控制骨→b_光暈、身體控制骨→b_身體;rig 樹 body→root、子件→body。
  - AC2:max_dev = **0.0000px**。
  - AC3a:轉關節骨 rig 光暈 72.0 / 身體 53.1px vs weighted-only **0.0px**;AC3b:轉 rig 根 b_身體 光暈隨動 73.9px vs **0.0px**。
  - AC4:身體(structural) si=0/flip=0;光暈(effect) si=0。
- **回歸**:`validate_rig_build.py`(rig-only)4 AC 全 PASS(min ratio 2.1);
  `validate_weighted_build.py`(weighted-only)身體/光暈皆 PASS。→ 既有兩條路徑未受影響。

## 關鍵發現 / 決策

- **併用是純座標問題,非演算法衝突**:setup 下整條父鏈皆純平移 → 控制骨世界位置不變 → weighted bind
  偏移**完全不用改**;只有骨的父子關係 + 局部座標變。故 setup 逐頂點 0.00px、動起來才沿鏈帶動。
- **內建負對照 = weighted-only 版**:同 mesh 掛 root 時,轉關節骨/根骨對它位移=0 → 證 rig 版位移是真的接進鏈,非度量假象。
- **honest boundary**:robot_parts 的 weighted mesh 只有身體(rig 根)+ 光暈(effect);結構肢體子件(頭/左手/右手)
  是 region 件 → 沒有「weighted mesh 當中段肢體」真實案例。AC3b 以 effect 光暈驗父→子耦合;更深的多跳
  weighted 肢體鏈需新分層素材(使用者資源,同多 rig 真值缺口)。

## 界定 / 下一步

- `spine-rig-pivot` **仍 HOLD**:L3 硬缺口=多 rig 真值(Award 僅機器人一件可拆肢體 rig)不變;本次補齊
  「rig 與 weighted 兩條線整合」但未新增真值 rig,遵守防固化。
- 下一個 bounded chunk 候選(皆可自主):
  (d') 多層 weighted 肢體鏈(2+ 跳遞迴接觸縫;演算法已支援,可先用 `make_test_psd` 造合成多跳件端到端 build 驗);
  (e) S1 keyframe 補 hit/open/reveal 主秀 beat 模板;
  (f) weighted-forge / rig 併入 `spine-asset-forge` skill(C 類使用者拍板 sync)。

## commit & push

`claude/spine-main`(固定分支)。
