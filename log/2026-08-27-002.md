# 2026-08-27 — session 002

## 目標(使用者指定)
進行 skill 規劃:研究成果分區塊、建立完成度機制(防固化半成品)、階段性成果 skill 化、維護/版本更新;
分類好區塊與對應 skill 後,列出目前完成度與可 skill 化項目。

## 做了什麼
1. **盤點真實狀態**:實跑所有 validator 確認 GREEN/RED(非憑印象),含 build_spine 端到端(build+validate)。
2. **完成度機制** `tools/check_readiness.py`:成熟度階梯 L0–L4;每能力宣告成熟度須由 validator 實跑佐證;
   skill 化門檻「核心能力全 ≥L2 GREEN 且 ≥1 條 L3」;**防固化關鍵規則:評估器就緒≠生成器就緒**。
   含指令結果快取(同一閘多能力共用不重跑)。
3. **策略文件** `skills/README.md`:區塊→目標 skill 地圖、與既有 spine-ai-editor 分工、SemVer 維護政策、
   人機邊界(repo 只備套件,sync 由使用者拍板)。快照 `skills/READINESS.md`(由工具產出)。
4. **固化首個 skill** `skills/spine-mesh-doctor/`(v0.1.0):自含 deform_eval / weighted_deform_eval /
   validate_weighted_deform / evaluate_mesh + SKILL.md(觸發詞/能力邊界/changelog)+ references(變形數學)。
   自 assets 目錄獨立跑 `validate_weighted_deform.py` → PASS(證明打包自洽)。
5. **掛鉤自驅迴圈**:`prompts/run.md` 加步驟 4.5(里程碑跑 readiness + 達門檻打包升版 + C 類回報);
   `PLAN.md` 加 skill 化章節;`STATE.md` 記錄。

## 完成度矩陣(check_readiness.py 實跑結果)
- **spine-mesh-doctor** L3 → **READY ✅**(靜態 IoU/unweighted/weighted 變形閘全 GREEN + 整合 AC L3)
- **spine-asset-forge** L3 → **READY ✅**(analyze_target/psd_slice/generate_mesh_v2 全 L2 GREEN + build_spine L3)
- **spine-slicing** L2 → HOLD(無 L3 pipeline;併入 forge)
- **spine-target-analysis** L2 → HOLD(依分層 PSD 契約;平圖 L1 負結果、影片 L0)
- **spine-weighted-forge** L2 → HOLD(**閘 L2 但 BBW 生成器 L0**;防固化示範)

## 產出
- `tools/check_readiness.py`、`skills/README.md`、`skills/READINESS.md`
- `skills/spine-mesh-doctor/`(SKILL.md + assets×4 + references×1)
- 更新 `PLAN.md` / `prompts/run.md` / `STATE.md`

## 下一步(待使用者拍板 / 或自驅接續)
- 使用者拍板:是否把 spine-mesh-doctor 套件 sync 到帳號 skill;forge 命名/範圍是否照此打包。
- 自驅可做:打包 `spine-asset-forge` v0.x(明標「靜態,不含動畫」);續推候選 2(BBW 生成)讓 weighted-forge 脫離 HOLD。
