# S4 chunk 50 — 2026-09-04(修正 hand_left/hand_right 決策檔框位置錯誤)

## 觸發

chunk 49 完成 `leg_left`/`boot_left` 兩個零成本框修正後,留下「下一步」候選項之一:
「修正 `hand_right` 框本身沒對準的問題(現成、低成本,但本次未列入範圍)」。本次排程
無活人可裁決,依既有慣例(chunk 26/49,無活人時執行不需授權的零成本選項)承接執行,
並在複查過程中額外發現 `hand_left` 也有同類問題。

## 執行

1. **`hand_right`**:換算 `suggestions.json` 的 `bbox_pct=[75,47,89,57]` → 像素
   `(345,422,409,511)`,跟 chunk 48(`knowledge/s4-sam-point-prompt-investigation.md`)
   報告的錯誤座標 `(345,422,409,512)` 幾乎完全吻合(1px 誤差屬四捨五入)——確認跟
   `leg_left`/`boot_left` 同一種情況,錯誤從 chunk 43 草稿就存在,可在源頭直接修正。
2. **`hand_left`(額外發現)**:核對 `suggestions.json` 的 `bbox_pct=[13,50,26,59]` →
   像素 `(59,449,119,529)`,用網格疊圖工具放大檢視,發現這個框完全落在角色左側
   九尾狐尾/翅膀狀白色毛髮束上,附近完全沒有手的內容——跟 chunk 47
   (`knowledge/s4-sam-segment.md`)用「使用者手動確認過的真實決策檔」把 `hand_left`
   列為「9 個明確正確」矛盾。推斷:使用者當次應該手動調整過 `hand_left` 的框,但那次
   調整沒有持久化進 repo,所以 `suggestions.json` 草稿裡的座標其實一直是錯的,只是先前
   沒人拿它跟真實決策檔逐一比對過。
3. **視覺定位正確位置**:用 bash+PIL 疊座標網格、擴大搜尋視野找兩隻手實際位置:
   - `hand_left`:實際左手(手腕纏紅色絲帶、五指自然下垂含粉色指甲)在
     `(132,423,218,558)`,跟原框 `(59,449,119,529)` 完全不重疊。
   - `hand_right`:實際右手(手腕纏紅色絲帶、五指微彎)完整範圍在
     `(378,443,450,588)`,原框只有右下角一小塊擦到手腕(呼應 chunk 48 原始描述),
     手掌主體在原框下緣 y=511 之外。
   改框:
   - `hand_left`:`bbox_pct` 從 `[13,50,26,59]`(錯,框到毛髮紋理)改為
     `[29,47,47,62]`(對,框到左手)。
   - `hand_right`:`bbox_pct` 從 `[75,47,89,57]`(錯,大部分框到符紙標籤+白紗)改為
     `[82,49,98,65]`(對,框到右手完整範圍)。
   - 兩者 `confidence` 從 `high` 下修為 `medium`,`notes` 記錄修正原因;`hand_left`
     額外記錄「chunk47/48『明確正確』判定用的是未持久化的真實決策檔,跟這份草稿不是
     同一份輸入」。
4. **自我驗證**:從修正後的 `suggestions.json`(承接 chunk 49 已修正的
   `leg_left`/`boot_left`)重建 20 部件 `bbox_px` 決策檔,跑 `s4_decompose_cut.py
   --contour rect --eval`——AC1(20/20 部件產出)pass,`overall_pass: true`。逐格人工
   視覺複核修正後的 `hand_left`/`hand_right` 裁圖:確認畫面主體分別是完整左手/右手
   (手腕紅色纏繞絲帶+五指),邊緣夾帶少量白紗/符紙標籤邊角,屬允許範圍內的 bleed,
   不再是毛髮紋理/符紙標籤+白紗。

## 誠實限制

- 本次驗證用的 20 部件決策檔是從修正後的 `suggestions.json` 重新換算,**不是**
  chunk46-48 使用者手動調整過的那份真實決策檔(未持久化進 repo),不能拿來延續 chunk 47
  的統計數字。
- `hand_left` 這次修正**提高一個疑慮**:`suggestions.json` 草稿至少已確認有 3 個部件
  (`leg_left`/`boot_left`/`hand_left`)存在「使用者當次手動修好但未持久化」的落差,
  只是前兩個先前已被 chunk 48 的目視複核發現,`hand_left` 是本次才發現。這代表草稿裡
  目前「沒被回報過問題」的其餘部件(如標 `low`/`medium` 信心的 `earrings`/
  `sash_train`/`tails_mass` 等)**不能因為沒被回報過就假設框是對的**——只是還沒被逐一
  複查。若後續要對這份草稿做更全面的信任度盤點,建議把剩餘部件也逐一跑一次網格疊圖
  複核,而非只等下游回報才修,留為候選給後續排程。
- 沒有重跑 `--contour sam`:本容器沒有持久化 chunk 47 下載的 MobileSAM 權重,矩形裁切
  的視覺驗證已足以證明框位置本身修好,SAM 精修屬於 chunk 48 選項 (c) 範疇未執行。
- 兩隻手在畫面上本身是分離、可清楚辨識的獨立內容(不像 `leg_left`/`boot_left` 有腿部
  本身重疊不可分的素材限制),修正後的框沒有已知的殘留視覺歧義。

## 檔案

- 修改 `assets/jiuwei_yanlian_char_crop.suggestions.json`(`hand_left`/`hand_right`
  的 `bbox_pct`/`confidence`/`notes`)。
- 新增 `tools/mesh_gen/s4_data/chunk50/decision_reconstructed.json`、
  `tools/mesh_gen/s4_data/chunk50/cut_rect/`(2 張手部裁圖 + `manifest.json`,驗證
  證據)。
- 新增 `knowledge/s4-decompose-box-fix-hands.md`。
- `knowledge/README.md` 尾端 append。
- 更新 `STATE_S4.md`。
- 未修改任何 production 代碼(`s4_decompose_cut.py`/`s4_sam_segment.py`/
  `s4_decompose_assist.html` 均維持 chunk 47/48 版本)。

## 下一步(留給使用者/後續排程裁決,均未執行)

1. 對 `bodice`/`sleeve_right` 兩個真正演算法歧義案例接受人工處理(chunk48 選項 b,
   `hand_right` 已隨本次修正排除,不再屬於這個歧義清單)。
2. 測試完整版 SAM(ViT-H)是否對點提示更敏感(chunk48 選項 c,成本較高,需使用者先
   確認)。
3. 對 `suggestions.json` 其餘部件(尤其是 `low`/`medium` 信心項目)逐一跑網格疊圖
   複核,盤點是否還有同類「框完全沒對準但沒被回報」的落差(本次 chunk 50 的
   `hand_left` 發現提高了這個疑慮)。
4. 若之後需要 `leg_left`/`leg_right` 在 spine 裡做成真正獨立擺動的兩個 slot,需另外
   處理「兩腿視覺重疊、內容幾乎相同」這個素材限制,不是這次修正能解決的範疇。
