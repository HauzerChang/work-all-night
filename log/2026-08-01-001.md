# 2026-08-01 — session 001

## 這次做了什麼(bounded chunk:candidate #1 — 生成 mesh 對照 Award 真實 mesh)

依 STATE.md 最高優先候選:把 S3 `generate_mesh_v2` 跑在**真實生產件**上,對照 Award 真實 mesh
做端到端「PSD/atlas 件 → S3 mesh」驗收(有真值、純 CPU 可自驅)。

1. 新建 `tools/mesh_gen/compare_award_mesh.py`:
   - 從 Award atlas 抽出機器人 3 mesh 件(光暈/左手/身體)的真實貼圖(derotate 對正)。
   - 對該 alpha 跑 `generate_mesh_v2(auto)`,量覆蓋 IoU / 格式 / 頂點預算。
   - 解 Award 真實 mesh 的 **weighted 頂點 → 設定姿勢世界座標**(bone world transform),以 dihedral+等向縮放
     對正到件輪廓,量藝術家覆蓋 + gen-vs-artist mesh IoU。

## 關鍵結論(見 knowledge/s3-award-mesh-e2e.md)

- **三件全走 v1 Delaunay**(aspect 不夠瘦長)→ 實測確認 **strip 是窗簾類專用、團塊件用 Delaunay**。
- 生成 mesh 對真實件 alpha **覆蓋 IoU 0.929/0.960/0.968 全達標、格式合法、頂點 48–61 ≤ 64、比藝術家精簡 20–40%**。
- **發現 Award.json uvs 跨滿整頁 ≠ 縮小重打包的 Award.atlas**(json 為原始打包 uv,atlas ~0.70 重打包)
  → 形狀對照**不能用 uv→atlas**(會全 0),須用解 weighted 幾何。教訓入 knowledge。
- decoder 自驗:左手解出世界 bbox 257×216 ≈ attachment wh 259×217 → 解碼正確。
- 左手/光暈 gen-vs-artist mesh IoU **0.95 / 0.85** → 生成 mesh ≈ 藝術家輪廓。
- 身體 0.61(self_check flag=fail):主骨設定 rot 87.81°+多骨混成,90° 對正**量測局限,非生成缺陷**
  (身體對自身件覆蓋 0.968 才是可靠品質值)。

## 方法論再實踐

- 自我驗證抓錯:第一版用 json uvs→atlas 對照,藝術家覆蓋全 0 → `self_check_artist_mapping_ok` flag
  正確擋下 → 查因發現 uvs/atlas 不一致 → 改用解幾何。**評估器先有自我驗證旗標才可信**。
- 不過度迭代:身體對正殘差已定性為量測局限(留 Procrustes 待後續),不硬湊 IoU、不誇大結論。

## 產出

- 新增 `tools/mesh_gen/compare_award_mesh.py`、`knowledge/s3-award-mesh-e2e.md`(+索引)。
- 更新 `STATE.md`(本次結果 + 更新候選:下一步建議 SkelToJson 組裝 / 身體 Procrustes 對正)、本 log。

## 下一步(見 STATE.md)

candidate #2 切圖→Spine JSON 組裝(SkelToJson,固化 v1/strip 分流 + 命名 + padding),或 #4 身體嚴謹對照。
