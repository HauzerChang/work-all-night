# 2026-08-01 — session 001

## 這次做了什麼(S3+S4 端到端對真實生產 mesh 驗收 — 里程碑)

推進 STATE 的最高優先 bounded chunk:用 Award 生產 spine 裡機器人 3 個 mesh 件的真實 atlas alpha
跑 `generate_mesh_v2`,對照 Award **藝術家 ground-truth mesh** 做端到端驗收。

1. **AC-first**:定 4 條可量測 AC(生成成功 / IoU ≥ 藝術家 baseline−0.02 / 靜態拓樸乾淨 / 頂點預算)。
2. **確認無 deform 真值**:查證機器人 3 mesh 件在 Award **無 deform timeline**(靠骨骼/權重)→
   誠實地**不套未校準合成壓力場**(RULES 禁),閘改為靜態 IoU + 靜態拓樸乾淨。
3. **第一輪**:身體/左手直接 PASS;**光暈 FAIL**(固定 epsilon=0.008 對大柔塊太粗:hull 14、
   IoU 0.929、掉 1 孤兒點)。
4. **工具升級(通用,非 overfit)**:
   - `generate_mesh.boundary_points` 加**自適應**模式(`epsilon_frac=None` 為新預設):由粗到細掃
     DP epsilon,取「hull 多邊形覆蓋率 ≥ 0.97」的最粗結果 → 保真足夠且頂點精簡。
   - 加 `drop_orphans`:過濾凹三角後清除未用頂點並重編索引(hull 全保留)。
5. **重驗全 PASS**:光暈/身體/左手 IoU 0.978/0.983/0.976(達/超越藝術家 0.980/0.976/0.968),
   頂點 68/68/53(< 藝術家 78/98/80),0 自交/退化/孤兒。
6. **無回歸**:main_draw 4 mesh 全走 strip,不碰 v1 邊界;重驗 curtain_left/right + shadow 仍全 PASS。
7. 落成可重現工具 `tools/mesh_gen/compare_award_mesh.py`(ALL_PASS)。

## 關鍵結論 / 教訓

- **auto 路由成立**:團塊(aspect<1.2)自動回退 v1 Delaunay,窗簾類走 strip —— 與藝術家選擇一致。
- **藝術家覆蓋率(~0.97–0.98)是自然的 IoU 目標**;固定 epsilon 對不同尺度/柔邊不通用 → 自適應才對。
- **無 deform 的件不硬湊變形閘**(誠實):有真值就比真值,沒有就只做能站得住的靜態閘。

## 產出

- 改 `tools/mesh_gen/generate_mesh.py`(自適應邊界 + drop_orphans;CLI epsilon 預設改 None)
- 新增 `tools/mesh_gen/compare_award_mesh.py`(端到端對照工具)
- 新增 `knowledge/s3-award-mesh-end-to-end.md`;更新索引 / STATE / 本 log

## 下一步(見 STATE.md)

切圖→Spine JSON 組裝(SkelToJson):固化 `<PSD名>/<圖層名>` 命名、mesh/region 分配、+2px、
atlas 縮放,端到端從 PSD 件輸出可載入的 Spine attachment(mesh 用本次已驗生成器)。
