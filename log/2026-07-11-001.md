# 2026-07-11 — session 001

## 這次做了什麼(bounded chunk:S3×S4 端到端對真實 mesh 驗收)

從 `STATE.md` 下一步候選 1(最高優先、有真值可比)推進:
**PSD 件 → `generate_mesh_v2` → 對照 Award 真實藝術家 mesh**。

### AC(動手前定的可檢查目標)

1. 正確讀出 Award 3 個 mesh 件(光暈/身體/左手)的真值覆蓋(uvs→region 局部 → 填三角 → vs 件 alpha)。
2. 生成 mesh 覆蓋 IoU ≥ 藝術家真值 IoU − 0.03(同一 alpha、同一 fill,只換拓樸)。
3. 生成 mesh 通過格式閘(unweighted / hull 排前 / 索引合法)+ 頂點預算 ≤64。
4. 判定可信:flip 校準明確、且對「不可達」case 有量化解釋(不硬套過嚴閾值)。

### 做法與結果

- `psd_slice.py` 切 `robot_parts.psd` → 5 件;取 3 個 mesh 件。
- 新工具 `tools/mesh_gen/compare_award_mesh.py`:真值 mesh 以 uvs 還原覆蓋,對比生成 mesh 覆蓋。

| 件 | 真值頂點/IoU | 生成 mode/頂點/IoU | 判定 |
|---|---|---|---|
| 光暈 706×683 | 78 / 0.949 | delaunay-v1 / **35** / 0.933 | PASS |
| 身體 379×425 | 98 / 0.948 | delaunay-v1 / **60** / 0.966(反超) | PASS |
| 左手 257×215 | 80 / 0.977 | delaunay-v1 / **59** / 0.964 | PASS |

→ `compare_award_mesh.py` **overall PASS**;`evaluate_mesh` 對身體/左手 overall_pass=True。

## 關鍵結論

1. **端到端「PSD→件→mesh」對真實生產標的驗收通過**:生成覆蓋與藝術家真值相當(≤0.016 差),
   且用 **45–77% 的頂點**(身體甚至反超)。S3 生成器對真實件與藝術家等級、更省。
2. **auto 模式對 blob 狀件選 Delaunay-v1**(aspect<1.2),strip 是窗簾(高瘦單向拉伸)專屬。
   這些件在 Award 無 deform timeline(骨骼權重變形)→ 對的軸是靜態覆蓋 + 頂點經濟,非 deform 耐受。
3. **★ 評估器校準(第 4 次洞見)**:固定 0.95 靜態 IoU 對**軟邊發光件**不可達 —— 光暈 soft_ratio=0.236,
   連藝術家 78 頂點真值也只 0.949。判準改以**藝術家真值 IoU 為可達上限**(或 recall);
   `evaluate_mesh` AC1 本體暫不改(避免動搖既有 4-mesh 結論),先在 compare 工具規避。

## 產出

- 新增 `tools/mesh_gen/compare_award_mesh.py`、`knowledge/s3-vs-award-real-mesh.md`(+索引)。
- 更新 `STATE.md`(候選 1 完成、進度摘要、下一步指向 SkelToJson 組裝)、本 log。

## 下一步(見 STATE.md)

候選 2:**切圖→Spine JSON 組裝(SkelToJson)** — 把件→attachment 慣例(`PSD名/圖層名`、size+2px、
mesh/region 分配、生成 mesh uv 佈局)固化成寫出工具,端到端產可載入 Spine JSON。純 CPU 可自驅。
