# 2026-08-21 — session 001

## 這次做了什麼(S1 候選 0d:分鏡 → 動畫 keyframe)

接續上一 log 指定的最高優先塊:讓 `build_spine` 的靜態產物「會動」——把 #3 storyboard 的
**待機/呼吸循環**節拍轉成 Spine `animations` timeline。

### 生成:`tools/analyzer/storyboard_anim.py`
- `build_loop_animation(spec, safe)`:由 `analyze_target` 規格的結構角色驅動,生一支**無縫循環**待機動畫:
  - body → `scale` 呼吸(半餘弦 0→3%→0)+ `translate.y` 胸口微移
  - head → `rotate` ±2° 微傾
  - limb → `rotate` ±4°,**逐件相位錯開** φ=i·2π/n_limb(右手/左手正好反相,對稱擺動)
  - effect → slot `color` alpha 脈動(ff→~c6→ff)
- 無縫保證:對完整正弦/餘弦週期 K=8 過取樣 → 首幀(t=0)== 末幀(T=1.5s);不寫 curve 鍵=Spine 3.8 預設 linear。
- 已整合進 `build_spine.py`(`animations` 不再是空);summary 多回 `animation` meta。

### 評估器:`tools/analyzer/validate_anim.py`(純 Python Spine 3.8 timeline 取樣器)
- 實作 linear/stepped/緊湊 bezier 取樣 bone(rotate/translate/scale)+ slot color(不需瀏覽器,CDN 被擋)。
- 5 條可機讀 AC:loop 閉合 / 有動(非 no-op)/ 幅度有界 / limb 相位錯開 / 格式合法。
- **可信度**:取樣器對真實 `main_draw.json` 386 keyframe 逐點回放 `error=0`;4 項負對照(注入壞動畫)全部 pass→fail 鑑別。

## 結果(自我驗證)
- robot(5件,slot_bigwin/Loop)、Symbol_Ww(18件,slot_symbol/idle):**5 AC 全 PASS,overall_pass=true**。
- `validate_build` round-trip 不受影響(setup pose MAE 0.031 不變)——加動畫不動 setup pose。
- 修 2 個評估器 bug:
  1. per-field 判 no-op 會誤殺 body 的恆 0 `translate.x` → 改**逐 timeline 聚合**。
  2. 用 `|angle|` 抓峰值時間對 π 相位差不敏感(|sin| 週期 T/2)→ 改**帶號峰值時間**(spread 0→0.75,正確判錯開)。

## 產出
- 新工具 `storyboard_anim.py`、`validate_anim.py`;`build_spine.py` 整合動畫輸出。
- 新知識 `knowledge/s1-storyboard-to-animation.md` + 索引;更新 STATE(里程碑/下一步/progress log)。

## 下一步(見 STATE.md 候選 f/g)
- (f) In/Out 非循環節拍(入場 overshoot / 退場淡出;驗證起訖≠、末幀==setup)。
- (g) 關節父子鏈 + pivot 推斷(limb parent=body,繞關節而非繞件中心自轉),供 S5。
