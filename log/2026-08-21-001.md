# 2026-08-21 — session 001

## 這次做了什麼(bounded chunk:候選 2 的評估器半)

補上 `s3-robot-mesh-vs-award.md` 標記的**唯一未驗維度**:weighted mesh 骨骼變形平滑度
(靜態 IoU 不涵蓋)。建 weighted mesh 的**真實骨動**變形評估器。

### AC(動手前定的可檢查目標)
1. 能對 Award 3 美術 weighted mesh(光暈/左手/身體)在其真實動畫下算變形後世界頂點(skinning)。
2. 拓樸閘(複用 `deform_eval` 的自交/翻面/退化/面積)判定,美術真值應**自洽乾淨**(_checker_validated)。
3. 負對照證明閘有鑑別力(壞 mesh / 壞骨動要抓得到)。

### 實作 `tools/mesh_gen/weighted_deform_eval.py`
- 解析 weighted `vertices`(`[n,(boneIdx,bindX,bindY,w)×n]`);建 77 骨階層。
- **骨 world affine**:normal transform 閉式(a=cos·sx, b=-sin·sy, c=sin·sx, d=cos·sy)+ parent 組合。
- **linear-blend skinning**:`worldV=Σ w·(boneWorld 施於 bind)`。
- **timeline 套用** + **緊湊 bezier easing**(CLAUDE.md #7,二分解 X(s)=a→Y(s))。
- **slot alpha 可見度遮罩** `slot_alpha()`:只在可見幀(alpha≥0.03)判定。
- 相位分閘:`*_Loop`=穩態硬閘、`*_In/*_Out`=進退場診斷。
- `--negctrl`:bind-shuffle + glow In 未遮罩,雙向鑑別。

## 過程中的關鍵發現(兩個)

1. ⭐ **更正 STATE 舊假設**「目前資產未含這 3 件的變形動畫」→ **錯**。腿骨 4_LEG3..9 在
   `Award_Legend_In/Loop` 有 bone timeline(rotate/translate/scale)→ 能建**真實** deform 閘。
   (僅 Legend 家族拉腿骨;Mega/Omg/Super 未動到。)
2. ⭐⭐ **weighted 變形閘必須做 alpha 可見度遮罩**(CLAUDE.md #2/#3 的 weighted 版):
   glow `Legend_In` 全幀 si=71、area 衝到 1.98 → 看似會壞;但該 slot alpha=00(全透明)於 t=0~0.5,
   逐幀對照顯示**自交全發生在 alpha≈0 進場段**,alpha 一開就乾淨。遮罩後 71→穩態 0。
   進場的折疊是看不見的編排,非拓樸缺陷。
   - 追加校準:懷疑 In 殘留 4 自交(t=0.63)是線性內插假象 → 實作 bezier 後仍在 →
     確認是**美術半透明 glow 在 squash 極端的真實微折**(~1 幀,前後皆 0),列 `_transient` 不作 pass/fail。

## 結果(自我驗證)

- **硬閘 PASS**:`_checker_validated=True` — setup + 3 件所有 `*_Loop` 可見幀 **0 自交/0 翻面**
  (area 0.975~1.003)。`python3 tools/mesh_gen/weighted_deform_eval.py` → exit 0。
- **鑑別力 PASS**:`--negctrl` → bind-shuffle setup **6804 自交**、glow In 未遮罩 **71 自交**,
  `discriminative=True` → exit 0。
- 視覺:`knowledge/figures/s3_weighted_deform_glow.png`(setup / Loop 穩態乾淨 / In t0.63 微折,紅=交叉邊)。

## 產出

- 新工具 `tools/mesh_gen/weighted_deform_eval.py`(含 `--negctrl`)。
- 新知識 `knowledge/s3-weighted-mesh-deform-eval.md` + 索引;圖 `s3_weighted_deform_glow.png`。
- 更新 `STATE.md`(里程碑 + 更正舊假設 + 候選 2 refine)。

## 下一步(見 STATE.md 候選 2 生成器半)

評估器已就位。下一 chunk:**我方生成 weighted mesh 的對照** — 加 (a) 內部取樣密度控制、
(b) BBW/heat 骨綁權重生成(把 generate_mesh_v2 的件綁到 Award 這 3 件的骨結構),
再用本評估器量「我方 vs 美術」在同一真實骨動下的變形品質。純 CPU 可自驅。
