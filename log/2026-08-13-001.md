# 2026-08-13 — session 001

## 這次做了什麼(bounded chunk:S3 端到端對 Award 真實生產 mesh 驗收)

延續 STATE 既定「下一步 #1」:把 S4(PSD 切圖)→ S3(mesh 生成)串成端到端,並用 Award 生產 spine
的**藝術家 mesh 當 ground truth** 對照。

1. **確認標的**:`robot_parts.psd` 5 件 ⇄ Award slot `機器人拆件/<圖層名>`;其中 3 件是
   **weighted mesh**:光暈(78v,全 hull)、左手(80v)、身體(98v);皆無 deform timeline(靠骨骼 warp)。
2. **寫端到端真值閘** `tools/mesh_gen/robot_mesh_gt.py`:atlas 切件 alpha → `generate_mesh_v2`(auto)
   → 對照藝術家 mesh 覆蓋率;含**自我品質閘(AC-A)**:藝術家 mesh 映射後高覆蓋 + v-flip 負對照。
3. **AC-A 先抓到我的映射 bug**(方法論奏效):初版假設 uvs 是 atlas 頁 uv → 覆蓋 0.49/0.54/0.0。
   實測發現 **uvs 其實是 region 局部 0..1(v top-down)**(推翻 log 006 推測)→ 改 `col=u·cropW,row=v·cropH`
   後藝術家覆蓋 0.976~0.980、v-flip 崩到 0.44~0.61(負對照)。副產:高覆蓋**再次獨立確認 atlas_crop 的
   CW derotate 正確**。
4. **AC-B 抓到 v1 生成器兩缺陷並修掉**:光暈(大平滑圓)初判 fail —
   - **孤兒頂點**:filter_triangles 刪凹外三角後留孤兒 → 加 `drop_orphans()`(重映射、保 hull-first)。
   - **平滑曲邊覆蓋不足**:周長比例 epsilon 對大曲邊過粗(glow 14 邊形 IoU 0.93)→ `boundary_points()`
     改**自適應 epsilon**(達覆蓋目標的最粗解,受 `hull_cap=budget-8` 上限);`generate()` 改吃 `vertex_budget`,
     interior 依 hull 大小配額。
5. **最終全過**:3 件生成 IoU 0.987/0.990/0.988 ≥ 藝術家 0.980/0.968/0.976;頂點 64 = 藝術家的 82/80/65%;
   0 孤兒/0 退化/format 合法。`robot_mesh_gt.py` exit 0。

## 自我驗證 / 無回歸

- main_draw 4 mesh(v2 strip,不走 v1 邊界)`validate_against_real --gen v2` 四件全 `overall_pass`
  (含 shadow2:`--slot image/shadow2 --name image/shadow`,共用 region)。
- 合成 curtain gen+eval 全過(IoU 0.99、0 孤兒);v1 直呼 curtain_left 不 crash。
- 修改僅限 `generate_mesh.py`;main_draw 走 strip 故不受影響(事前已驗 4 件 strip=True)。

## 關鍵結論

- **S3+S4 端到端對真實生產標的通過**(里程碑):PSD 切件 → 生成 mesh 覆蓋率達生產水準且更省頂點。
- **方法論再驗**:自我品質閘(正對照高 + 負對照崩)先抓到 UV 映射 bug,才信後續判定 —— 完全符合 RULES。
- **下一缺口明確**:真值皆 weighted mesh；S3 拓樸已足,還缺 **BBW 骨權重生成**(用 Award 綁骨當真值)。

## 產出

- 新增 `tools/mesh_gen/robot_mesh_gt.py`、`knowledge/s3-psd-to-award-mesh.md`(+索引)
- 改 `tools/mesh_gen/generate_mesh.py`(自適應邊界 + drop_orphans + vertex_budget)
- 更新 `STATE.md`(里程碑 + 下一步改為 S3 BBW)、本 log

## 下一步(見 STATE.md)

S3 **BBW 骨權重生成**:先寫權重評估器(和=1/平滑/對比藝術家綁骨),再實作,用 Award weighted mesh 當真值。
