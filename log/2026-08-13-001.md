# 2026-08-13 — session 001

## 這次做了什麼(bounded chunk:S3×S4 端到端對照 Award 真實 mesh)

接手續跑。讀 RULES/PLAN/STATE/CLAUDE + knowledge 索引 + 最近 log。STATE 的最高優先候選是
「件→S3 mesh→對照 Award 真實 mesh」(有真值、純 CPU 可自驅)。推進之。

### AC(動手前定的可檢查目標)
對 Award 中為 mesh 的 3 件(機器人拆件 光暈/身體/左手):
1. 覆蓋率 IoU:生成 mesh >= 藝術家真值 mesh 基準 − margin(0.03)。
2. 頂點預算:生成 nv <= 64。
3. 靜置拓樸:self-intersection / degenerate = 0。
4. 評估器可信度先驗:藝術家自身 3 件 mesh 靜置拓樸須全乾淨(閘不誤殺真值)。

### 實作 + 驗證
- 新工具 `tools/mesh_gen/compare_award_mesh.py`:`atlas_crop.extract`(多頁+CW)切件 alpha →
  `generate_mesh_v2(auto)` → `evaluate`(IoU)+ `deform_eval.check`(靜置拓樸)+ 藝術家 mesh 對照。
- 首跑:2/3 過,**光暈 fail**(IoU 0.929 < 基準 0.9795−0.03)。
- 診斷:epsilon 掃描確認 —— 光暈是大面積軟羽化邊,預設 `epsilon_frac=0.008` 把外框砍成 14 hull 點欠取樣;
  降 epsilon → hull 變密 → IoU 上升(0.006→0.950、0.004→0.966、0.002→0.983)。
  **這是 Delaunay 的覆蓋率旋鈕(hull 密度),與 strip 的 rows 同理。**
- 修正:在 compare 工具加**確定性 epsilon 階梯自動收斂**(粗→細,取第一個過門檻且在預算內者),
  符合 RULES 的「迭代預算內自我修正」。重跑:**3 件全 overall_pass、exit 0**;
  `artist_topology_all_clean=True`(閘可信)。

## 關鍵結論
1. **S3×S4 端到端對真實生產 mesh 真值打通**(先前 4-mesh 全是 strip;這 3 件補上 blob/Delaunay 半邊)。
2. **auto 對 blob 正確回退 v1 Delaunay**(長寬比<1.2、非 row-convex)。
3. **Delaunay 覆蓋率 = hull 密度(epsilon_frac)**;預設對大軟 blob 欠取樣 → 確定性自動收斂補上。
4. 效率:生成用藝術家 ~60–75% 頂點達覆蓋率基準。
5. ⚠️ 誠實聲明:這 3 件無 deform timeline → 未套 deform 閘;blob 變形穩健性驗收為待補缺口。

## 產出
- 新增 `tools/mesh_gen/compare_award_mesh.py`、`knowledge/s3-award-mesh-endtoend.md`(+索引)。
- 更新 `STATE.md`(端到端里程碑、下一步候選、進度 log)、本 log。

## 下一步(見 STATE.md)
SkelToJson 組裝工具(件→Spine JSON)/ weight-based deform 閘 / S2 補圖・骨架閘。
