# 2026-08-12 — session 001

## 這次做了什麼(S3 端到端對真實生產件驗收 + 修好 v1 兩處)

推進 STATE next-action #1:**機器人拆件 PSD → S3 v2 mesh → 對照 Award 生產 mesh**(端到端、純 CPU、可自驅)。

### AC(先定可檢查目標)
對 3 個 mesh 件(光暈/身體/左手):v2 生成 mesh 的 ① 靜態覆蓋 IoU ≥ 藝術家基準(或無可信基準時 ≥0.90)
② 拓樸全過(無退化/無孤兒/頂點≤64/格式)③ 頂點經濟度對照藝術家。deform 閘視資料而定。

### 做的事
1. **新工具 `tools/mesh_gen/compare_award_mesh.py`**:atlas 抽件(生產貼圖)→ `generate_mesh_v2` →
   `evaluate_mesh` 靜態 AC;藝術家真值基準用「uv-bbox→region 影像 bbox」自校準(枚舉 8 二面體方位取 IoU 高者)。
2. **抓到並處理 3 個真實課題**:
   - **weighted + 無 deform**:Award 3 mesh 件是 weighted mesh 且無 deform timeline(骨骼驅動)→
     真實位移場 deform 閘 N/A。可套用「靜態覆蓋+拓樸」閘。
   - **uvs 座標系不一致**:body 藝術家基準怎麼疊都只有 0.64 → 查出 Award.json uvs 在**原始 atlas 座標系**,
     shipped PNG 是 ~0.70 repack,直接 page-normalize 對不上(u_max·1780=1351 ≠ region col 1460)。
     誠實處理:基準自校準 IoU<0.85 即判不可信,**退回 AC.md 絕對門檻 0.90**(延續 log 006「要外部真值」紀律)。
   - **凹形孤兒頂點**:光暈(星芒凹形)v1 濾凹陷三角後留 1 孤兒頂點(違反 AC2c)。
3. **連帶修好生成器兩處**(v1,v2-auto 對 blobby 件會回退 v1):
   - 加 `prune_orphans()`:濾三角後移除未引用頂點並重編索引,保住 hull-first 不變式 → 光暈 orphans 1→0。
   - epsilon 預設 **0.008→0.006**:0.008 對細長突起(左手手指)過度簡化,漏 ~1% 覆蓋;0.006 讓左手
     IoU 0.960→0.974(勝藝術家 0.970),52 頂點仍 <<64。

### 自我驗證(量化,非肉眼)
- `compare_award_mesh.py` → **all_pass=true(exit 0)**:光暈 56v/0.950、身體 64v/0.971、左手 52v/0.974,拓樸全過;
  頂點經濟度全勝藝術家(56/64/52 vs 78/98/80)。
- **回歸**:main_draw 4 mesh(curtain_left/right + shadow/shadow2)v2 全 **overall_pass**(30v,IoU 0.93~0.96,deform 乾淨)
  → epsilon 改動乾淨。v1 curtain_right/shadow deform 仍 fail = **既有已知 v1 限制**(STATE 早記,故 v2 為預設),非本次回歸。

## 關鍵結論
- **S3 v2 端到端推廣到真實生產 PSD 件成功(里程碑)**:靜態拓樸/覆蓋層面對生產標的可用且經濟度更佳。
- **S3 缺塊明確化**:生產 warp 件有兩種變形機制(unweighted+deform / weighted+骨骼);S3 目前只產 unweighted,
  要真正取代 Award 的 weighted mesh 需補 **BBW 骨骼權重 + 骨骼驅動變形閘**。
- 方法論再實踐:基準來源不可信時誠實退回絕對門檻,不假裝有藝術家參照(第 N 次「閘要先校準/可信才採信」)。

## 產出
- 新增 `tools/mesh_gen/compare_award_mesh.py`、`knowledge/s3-psd-to-award-mesh.md`(+索引)
- 改 `tools/mesh_gen/generate_mesh.py`(`prune_orphans` + epsilon 0.008→0.006)
- 更新 `STATE.md`(里程碑 + 下一步候選改為 BBW/SkelToJson)、本 log

## 下一步(見 STATE.md)
BBW 骨骼權重生成(S3 最後缺塊)/ 「件→Spine JSON」SkelToJson 組裝 / S2 補圖閘、骨架閘。
