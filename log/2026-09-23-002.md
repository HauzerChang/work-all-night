# 2026-09-23 run 002 — S1 (G-4''''''-tier) twist 反相雙軸 shear 峰隨檔位遞增(兩軸同比 φ 保形)

## 這次做了什麼(bounded chunk)

延續 run(2026-09-23 001,G-4'''''')在 log/STATE 尾列出的候選 **(G-4''''''-tier)**:補上 (G-4'''''') 明列的
honest boundary —— **「twist 未接 tier 幅度(`gen_twist(nosc=)` 已備參數未接;比照 wobble G-4''/squash G-4''''')」**。
twist(反相雙軸阻尼 shear,產線第一個驅動 shearY 的節拍)是主秀節拍,強度理應隨大獎檔位遞增,但幅度軸在**兩條**
shear 軸,尚未被檔位放大 —— 又一「檔位機制就緒 ≠ 每個新通道接上」缺口(同 wobble G-4''、squash G-4''''')。
本次把 twist 接上 tier 幅度差異化,與 wobble/squash 的推進順序一致(先 tier 幅度,count-aware 為後續)。

## AC(先定,再驗)

新閘 `tools/analyzer/validate_twist_tier.py` 6 條(客觀、可量測),真值界定同 E/H/I/J/G-4'/G-4''''/G-4'''''':
從先驗庫(slot_bigwin)→ **真實 build_spine robot 骨架** → build_animations(tier_gains) 端到端量。

- TT1 present + backward-compat:每檔位 `twist__{tier}` 產出/finite/有 bone/帶雙軸 shear/名經 `beat_category` 仍路由回 twist;base(含 In/Loop/Out + base twist)帶/不帶 tier_gains 逐位元不變。
- TT2 **crux** dual-axis peak monotone:峰 |shearX| **與** 峰 |shearY| 皆 Super<Mega<Omg<Legend 嚴格遞增;Super(g=1)==base。
- TT3 **crux** φ 比值 tier-invariant:每 bone 每檔位 shearY 峰 / shearX 峰 ≈ TWIST_PHI(0.7,≤2e-3)。
- TT4 兩軸阻尼簽章逐檔保形:每檔位每 twist bone 的 shearX 與 shearY 各自 (a)首尾 0 (b)繞 0 變號≥3 (c)相繼極值遞減。
- TT5 反相逐檔保形 + 偏離峰遞增:每檔位每內部極值幀 shearX·shearY<0;夾角偏離峰 |shearY−shearX| 隨檔位嚴格遞增。
- TT6 負對照/隔離:(a)平增益守衛全 1.0→兩軸遞增 FALSE 且==base;(b)單一-g 兩軸同比單元測 + 獨立軸增益負對照破 φ;(c)shear 隔離到 SHEAR_CATS。

## 產出 / 實作

- `tools/analyzer/tier_variants.py`:`MAIN_SHOW_CATS` 加入 `twist`(+ 說明註解)。**關鍵:twist 純 shear(無 scale 通道,
  故不在 `COUPLED_SCALE_CATS`)** → `amplify_bone_tl` 既有 shear 迴圈(`v'=g*v`)以同一 g 同時放大 shearX 與 shearY,
  無須新增任何放大邏輯;twist 亦不在 `COUNT_AWARE_CATS`(走 amplify base、非重生成)。
- `tools/analyzer/validate_twist_tier.py`:新閘(6 AC)。復用 `validate_shear_gen`(阻尼簽章)+ `validate_twist_gen`
  (`_shear_y`/`_shear_xy`/`_interior_shear`/`_tw3_eval`/`_has_shear_y`,雙軸讀取與反相判準)。
- `tools/check_readiness.py`:註冊 cap `twist_tier_amplitude` L2(pipeline)。
- `knowledge/s1-twist-tier-amplitude.md`(+ README 索引)。

## 驗證結果

- `validate_twist_tier.py` **6 AC 全 PASS**。TT2 峰:shearX [16,21.6,27.2,33.6]°、shearY [11.2,15.12,19.04,23.52]°;
  TT3 φ 比值逐檔恆 0.7;TT5 反相夾角偏離峰 [27.2,36.72,46.24,57.12]°。
- 端到端 `build_spine --animate --tier-variants --shear-pivot` 直出 `twist__{Super,Mega,Omg,Legend}`,shearY 經關節
  pivot 補償仍存活且隨檔位遞增(11.2→23.52°)。
- **21 回歸閘全綠**(20 既有 + 新 twist_tier);`check_readiness.py` 退出 0(無 GREEN→RED);新 cap GREEN;
  `spine-anim-forge` 區塊仍 HOLD(防固化)。(`spine-target-analysis` 的「分層 PSD→規格」RED 為既有狀態,與本次無關。)

## 決策 / 關鍵發現

- **幅度軸的檔位差異化至此在全數運動通道成立**:scale(J)、rotate(J)、shearX(wobble G-4'')、
  shear+耦合非均勻 scale(squash G-4''''')、**反相雙軸 shear(twist 本次)**。
- **帶跨軸關係約束的類別,tier 幅度要多驗一層「約束在放大後仍成立」**:twist 的 φ 比值(兩軸同比 → φ 不變)、
  squash 的體積守恆(耦合 amplify → scaleX·scaleY≡1)。**通則:用單一 g 同時作用相關通道 = 關係保形**
  (獨立軸增益會破壞關係 —— TT6b 的獨立軸增益負對照證此)。

## 分支說明(給使用者)

harness 指派本 session 工作分支為 `claude/focused-dirac-pb8z0w`(啟動時與 `origin/claude/spine-main` 同 commit
`6921f83`,0 ahead/0 behind)。SCHEDULE.md ① 主研究 Routine 指定固定分支 `claude/spine-main` 線性累積。為兼顧兩者
(session 顯式指定分支 + 排程續跑需讀到更新後 STATE),本次收尾**同時 push 到兩條 claude/ 分支**(spine-main 與
focused-dirac-pb8z0w,同一 commit),延續 run 2026-09-21 / 09-23 001 的做法。兩者皆 `claude/` 前綴 → 皆被接受。
若要根治分支指派飄移,建議把 repo default branch 設為 `claude/spine-main`(見 SCHEDULE.md)。

## 下一步(擇一,皆純自主)

- **(G-4''''''-count)** twist 接 count-aware(扭轉段數 nosc 隨檔位遞增,`gen_twist(nosc=)` 已備參數未接;比照 wobble
  G-4'''、squash G-4'''''-c)。crux:段數重生成後兩軸仍須同比保 φ → count × tier 幅度 × φ 保形三效正交。
- **(G-4''''''-vol)** volume-conserving twist:反相雙軸 shear 接體積守恆耦合 scale(`det=cos(shearY−shearX)≠1` →
  加耦合 scale 使 `sx·sy=1/cos(shearY−shearX)` → 擰而不變面積;shear+scale+rotate 三通道同時且守恆)。
- **(J-3)** cascade 波速/散佈/件數隨檔位(cascade 的 count-aware,簽章在件之間)。
- **(G-4'''''-charge)** charge 蓄力段數等第四個通道的 count-aware。

## honest boundary(仍在)

- twist 未接 count-aware(`gen_twist(nosc=)` 已備參數未接)。
- 反相雙軸未接體積守恆(`det=cos(shearY−shearX)≠1`,volume-conserving twist 為後續)。
- 幅度 / φ 皆為 PROPOSAL(手感 A 類);單一真值資產(robot_parts);anim-forge 仍 HOLD;S5→L3 仍待多 rig 真值(C/資源類)。
