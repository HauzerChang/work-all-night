# 2026-08-19 — session 002

## 這次做了什麼(使用者新增研究項目:S1 目標圖反推分析器)

使用者(live)新增研究項目:分析目標 2D 圖,反推 spine 動畫開發前置需求 —— 五項:
① 運動構件 ② 周邊特效 ③ 動作腳本/分鏡 ④ 拆圖策略 ⑤ 補圖項目。目標:更精準拆圖/補圖。
這具體化了 PLAN 的 **S1 反推分析器**(核心信念「運動決定一切」),差異:輸入是**靜態分層 PSD**(非影片)。

1. **建 `tools/analyzer/analyze_target.py`**:分層 PSD → 五段規格(JSON + markdown)。
   - #2 特效分類用確定性加權訊號(命名關鍵字最強 + 羽化/佔比/包覆/低細節/z 極端);
   - #3 分鏡走類型先驗庫(slot 大獎 = In/Loop/Out × 檔位),標 PROPOSAL;
   - #4 沿用 S4 慣例(`<檔名>/<層名>`、2px、mesh vs region);
   - #5 誠實拆兩塊:5A 露出區(下層已完整→不需補)、5B 封閉破洞補圖候選。
2. **建 `tools/analyzer/validate_analyzer_award.py`**:用 `robot_parts.psd ⇄ Award` 真實 spine 當真值校驗。
3. **修 2 個實作/語意 bug**:(a) 遮擋分析誤用第一件切片形狀當畫布 → 傳真畫布尺寸;
   (b) 露出合理性只查遮擋者運動 → 改「遮擋者移開 或 被遮件自己移出」二者之一(手移出身體即露出)。
4. **重定義 #5(關鍵誠實界定)**:分層 PSD 各層本就完整 → 遮擋≠缺像素;robot 得 **0 封閉破洞**,
   判定「PSD-first 契約成立、補圖需求極低」。平圖(未分層)結論相反 → 屬後續。

## 校驗結果(對 Award 真值,全 PASS)

| 校驗 | 結果 |
|---|---|
| ① 件召回 | 1.0(5/5) |
| ② 特效分類 | 5/5(光暈→特效,餘結構) |
| ③ mesh/region vs Award | 無 mismatch(光暈/身體 mesh match;右手/頭 region 對;左手 partial) |
| ④ 分鏡結構 | In/Loop/Out 全中 + 4 檔位全中 |
| ⑤ 露出合理性 | 4/4(真值骨運動佐證) |

## 產出

- 新工具 `tools/analyzer/analyze_target.py`、`validate_analyzer_award.py`
- 新知識 `knowledge/s1-target-image-analyzer.md` + 索引;範例 `knowledge/s1-example-robot-spec.md`、`specs/robot_parts.spec.json`
- 圖 `knowledge/figures/s1_robot_analysis.png`
- 更新 PLAN(S1 狀態 🟡)、STATE(里程碑 + 下一步)、session-start hook(PYTHONPATH 加 tools/analyzer)

## 下一步(見 STATE.md 候選 0)

(a) 規格→實際素材:mesh 件串 `generate_mesh_v2`、region 件串 `psd_slice`,端到端「圖→spine 素材」;
(b) 平圖(未分層)流程:rembg/SAM 粗切 → 同分析器(補圖需求上升,接補圖分級);
(c) 分鏡先驗庫擴充(更多角色/場景類型)。
