# 2026-06-24 — session 002

## 這次做了什麼

收到真實 `main_draw.json` + `.atlas`(使用者提供),執行「deform-aware mesh 評估器」課題。

1. **收進資產**:`assets/main_draw.json`(4 unweighted mesh + 9 anim deform)、`assets/main_draw.atlas`。
   解析確認與 `main_draw_解析報告.md` 一致。
2. **實作 Spine deform(Python)**:`tools/mesh_gen/deform_eval.py` —
   unweighted 逐頂點加 offset(sparse offset 段補零對齊),attachment-local 空間,不需 PNG。
3. **幾何品質閘**:自交(非相鄰邊交叉)、翻面(signed-area 變號)、退化、面積比、bbox。
4. **真實 benchmark**:4 mesh × 各動畫,逐 keyframe + 相鄰幀內插取樣評估。
5. **耐變形對比 + 負對照**驗證閘門可信且有鑑別力。

## 產出

- `assets/main_draw.{json,atlas}`
- `tools/mesh_gen/deform_eval.py`
- `knowledge/s3-deform-evaluator.md`(+ 索引)

## 結果

- **真實 4 mesh 全乾淨**(0 自交/0 翻面/0 退化,across 9 anim)→ `_checker_validated: true`。
- 面積比:窗簾 1.0–1.14、idle2 0.97–1.01;真實最大頂點位移 ~315px。
- 耐變形:真實 curtain_left 與我生成的合成 mesh **都撐過 400px(>315 上限)0 自交**。
- 負對照:故意自交 → 抓到;頂點翻面 → 抓到。檢查器非永遠 pass。

## 決策

- deform 用 local 空間判定拓樸正確性(不需骨世界變換/不需 PNG),範圍最小卻足以驗證撕裂類問題。
- 線性內插取樣(暫不精確重現 3.8 bezier easing),對「找瞬時極端」足夠。

## 下一步(見 STATE.md)

(a) 把「stress_field @315px 0 自交」納入 S3 正式 AC + 內部點密度甜蜜點掃描(純 CPU);或
(b) 待 `main_draw.png` 檔 → texture IoU / UV 撕裂;或
(c) spine_inspector 實機 round-trip(需 headless 瀏覽器)。
