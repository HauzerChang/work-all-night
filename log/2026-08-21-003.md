# 2026-08-21 — session 003

## 這次做了什麼(物理主線 P1:分鏡 → 動畫 keyframe)

使用者定案「先研究物理世界、P1→P2 優先」後,直接推進 **P1** 第一個 bounded chunk:
讓 `build_spine` 的產物(原本 `animations:{}`)會動。

### AC(動手前定,可機讀)
1. loop 閉合:_sample(t=0)==_sample(t=period)(bezier-aware),gap≈0。
2. 有界運動:有動且旋轉幅度在合理呼吸範圍(非零、不過大)。
3. 物理簽名:`motion_physics` 對生成動畫 inertia_index>0。
4. round-trip:輸出 JSON 可 parse、時長正確。

### 實作 `tools/analyzer/storyboard_to_anim.py`
- 把分鏡 idle/loop beat → Spine `animations`(v1 = 無縫**待機呼吸 Loop**)。
- **校準自真實 `main_draw.main_idle2`**:呼吸 = body scaleY 主導脈動(±~15%)、sx 幾乎不變
  (非體積守恆,與 motion_physics 結論一致)。
- bezier **ease-in-out**(端點速度≈0,呼吸極端有停頓感)→ inertia_index=1.0。
- `auto_roles`:body/head/limbs 分派;body 選取①指定②名稱關鍵字③後代最多④退回。
- CLI `--body/--period/--sy-amp/--save`;`--save` 才寫出加了 Loop 的 skeleton。

## 過程關鍵事件

- **測真實產物揪出結構限制**:build_spine 產出為**扁平 rig**(各件 root 直屬,無父子)→
  auto_roles 靠「後代最多」誤選 body(機器人選到 `b_頭`)。**加名稱關鍵字優先**後正確選 `b_身體`。
  誠實記:扁平 rig 無父子鏈 → follow-through 相位傳遞須待階層 rig(S5/重綁)或 P2 以相鄰件相位差近似。

## 結果(自我驗證)

- main_draw(真實骨架,自動選 body=main)+ robot(build_spine 產物,自動選 b_身體):
  **4 AC 全 PASS**(loop gap=0 / 有界 / inertia_index=1.0 / round-trip ok),exit 0。
- 產出會動的機器人資產 `specs/robot_spine/skeleton_idle.json`(gitignore,可重生)。
- 圖 `knowledge/figures/p1_breathing_curve.png`:eased(綠,物理)vs linear(紅,機械);峰值 |Δ|≈0.0003=速度≈0。

## 產出

- 新工具 `tools/analyzer/storyboard_to_anim.py`;圖 `p1_breathing_curve.png`。
- 新知識 `knowledge/p2-storyboard-to-keyframe.md` + 索引;更新 PLAN/STATE(P1 完成→P2 最高)+ log。

## 下一步(P2,見 STATE 排序框)

**物理注入生成端 v1**:把「機械」keyframe(或非呼吸動作)自動加 ease(依質量)+ overshoot(依阻尼)
+ 末梢 follow-through lag;用 `motion_physics.py` 量注入後 inertia_index/overshoot 上升、
`deform_eval`/`weighted_deform_eval` 確認拓樸不壞。呼吸已平滑 → P2 價值在非呼吸動作(彈跳落地/材質反應)。
