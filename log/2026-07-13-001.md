# 2026-07-13 — session 001

## 這次做了什麼(S3+S4 端到端:PSD 件 → mesh → 對照 Award 真值)

推進 STATE 最高優先候選:把 S4 切件與 S3 生成器串起來,對真實生產標的驗收。

1. **環境**:`pip install -r requirements.txt`(numpy/cv2/scipy/psd-tools 皆就緒)。
2. **AC**(先定):三件(光暈/身體/左手)—— 座標系須可校準一致、生成 mesh 靜態 IoU 達 0.9 且不落後藝術家太多、
   拓樸(重心在內/無退化/無孤兒)乾淨。deform 閘因標的無 timeline 而排除(誠實界定範圍)。
3. **實作** `tools/mesh_gen/validate_psd_to_award.py`:slice_psd 取全解析度件 alpha → generate_mesh_v2 →
   evaluate_mesh 靜態 AC;另對藝術家 mesh 做 4 朝向變體校準,挑最佳 IoU 當基準。
4. **自我驗證**:三件全 exit 0。座標校準結果全為 `identity` → 座標系一致(非假設)。

## 關鍵結論 / 發現

- **座標系 identity 一致**:Award 藝術家 mesh uvs = 件本地 0..1,與 PSD 切件同框。
  從**幾何**面再確認 s4 的 texture-IoU 閉環;atlas 的 rotate/0.70 縮放**不改** JSON mesh uvs 邏輯座標。
- **v2 auto 對三件全回退 v1 Delaunay**(團塊件不符 strip 的細長+row-convex 條件)。
  → strip 專給細長、承載逐頂點 deform 的件(窗簾/陰影);角色部件走 v1。auto 分流對真實件判斷正確。
- **輪廓覆蓋率貼近藝術家、頂點更省**:IoU 差 ≤1.55%(身體 0.966 > 藝術家 0.9477),頂點少 30–55%,全過 0.9。

## 誠實的範圍限制

- Award 這 5 件無 deform timeline(骨/權重變形,非逐頂點 deform)→ 真實位移場轉移閘無真值可跑。
  本輪只宣稱**靜態輪廓 + 拓樸**達標;逐頂點 deform 穩健仍唯 main_draw 4 mesh 被真值驗過。
- 合成 stress 場已知未校準,不拿來當這些件的 pass/fail。

## 產出

- `tools/mesh_gen/validate_psd_to_award.py`(可重現,exit 0)
- `knowledge/s3-s4-end-to-end-robot.md`(+ 索引)
- 更新 `STATE.md`、本 log

## 下一步(見 STATE.md)

(1) SkelToJson:把命名慣例 + size+2px + mesh/region 分配固化,把切件+mesh 組裝成完整 Spine JSON。
