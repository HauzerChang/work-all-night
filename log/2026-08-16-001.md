# 2026-08-16 — session 001

## 這次做了什麼(S3×S4 端到端對真實生產 mesh 驗收 + v3 生成器)

推進 STATE 最高優先 bounded chunk:**PSD 切件 → S3 生成 mesh → 對照 Award 真實藝術家 mesh**。

### AC(動手前定的可量測目標)
每個 mesh 件(光暈/身體/左手,Award 中為 weighted mesh):
- AC1 覆蓋率:生成 mesh 對件 alpha 的 IoU ≥ 藝術家 mesh 覆蓋率 − 0.03。
- AC2 頂點預算:生成頂點數 ≤ 藝術家頂點數。
- AC3 變形穩健:轉移 main_draw curtain_left 真實位移場後 0 自交 / 0 翻面。

### 做的事
1. **新閘 `tools/mesh_gen/validate_psd_to_award.py`**(端到端整合 AC,`--gen v1|v2|v3`)。
2. **先確保評估器可信**(遵 RULES「閘要先校準」):
   - 踩雷並修正:Award mesh `uvs` 是 **region-local [0,1]**(非 atlas-page 正規化)。
     初版誤判 → 身體 artist_iou=0.0 假象。改對 atlas_crop 還原(de-rotate upright)的 region 量測、
     v-origin 自校準後,藝術家基準恢復合理(0.968~0.980)。
3. **跑 v1/v2 → 發現雙缺口**:機器人三件皆 row/col-convex 緊湊件(aspect 0.84~1.12),
   **v2 strip gate(aspect≥1.2)從不觸發 → 一律退回 v1 Delaunay**。v1:光暈覆蓋 0.933(<0.98)不足、
   左手轉移場 10 自交/3 翻面。強制 strip:變形乾淨但曲面 blob 覆蓋封頂 ~0.93。→ 兩者對緊湊件都不足。
4. **實作 `tools/mesh_gen/generate_mesh_v3.py`**(輪廓 hull + 規則內部格 + 約束 Delaunay,triangle 'pYY'):
   `target_hull=40` 三件全過 —— 覆蓋率 0.979/0.992/0.988(≈或勝藝術家)、頂點 51/54/58(< 藝術家 78/98/80)、
   轉移真實場 0 自交/0 翻面。`validate_psd_to_award.py` overall_pass=True。

## 關鍵結論
- **S3×S4 端到端對真實生產標的驗收通過(里程碑)**:PSD 切件 → 生成 mesh 品質 ≈/勝生產藝術家 mesh。
- **v3 補上 v1/v2 對緊湊凸形件的缺口**:v1 覆蓋不足+變形自交、strip 只適合高瘦沿拉伸軸件。
  生成器選擇政策:高瘦 row-convex→v2 strip;緊湊凸形→v3 contour-grid。
- **評估器第四次校準經驗**:uvs 座標系(region-local vs page)先數值確認再判定,避免假性失敗
  (前三次:stress_field、composite 白底、atlas derotate 方向)。

## 產出
- 新增 `tools/mesh_gen/generate_mesh_v3.py`、`tools/mesh_gen/validate_psd_to_award.py`
- 新增 `knowledge/s3-v3-contour-grid.md`;更新 `knowledge/README.md` 索引、`STATE.md`、本 log

## 下一步(見 STATE.md)
1. **SkelToJson**:件 + v3 mesh + `<PSD檔名>/<圖層名>` 命名慣例 → 組出可載入的完整 Spine JSON。
2. v3 泛化驗證(高瘦窗簾 vs v2、凹形/多連通件),定統一生成器選擇政策。
