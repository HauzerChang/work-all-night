# 2026-08-16 — session 001

## 這次做了什麼

推進 STATE.md「最高優先」bounded chunk:**PSD 件 → S3 生成 mesh → 對照 Award 真實 mesh**,
首次把 S4(切圖)與 S3(mesh 生成)串成端到端,並對真實生產標的(Award 藝術家 mesh)做
ground-truth 對照。

### AC(動手前先定的可檢查目標)
對 Award 中為 mesh 的 3 件(光暈/身體/左手):
- ① 覆蓋率 IoU:生成 mesh vs 件 alpha,基準 = 藝術家 mesh 對同 mask 的 IoU(margin 0.02)。
- ② 拓樸乾淨:三角重心全在 mask、0 退化、0 孤兒。
- ③ 頂點預算:不超過藝術家 2 倍。

### 實作
- 新增 `tools/mesh_gen/validate_psd_to_award.py`:psd_slice 切件 → generate_mesh_v2(auto)→
  對件 alpha mask 量 IoU,對照 Award.json 藝術家 mesh(weighted;artist_iou 只用 uvs+triangles)。
- 座標系一致性先驗:藝術家 mesh 對件 mask IoU 0.948~0.977 → 確認 uvs(region 正規化)與 PSD 緊湊件對齊。

## 產出

- `tools/mesh_gen/validate_psd_to_award.py`(端到端整合驗收工具)
- `knowledge/s3-s4-psd-to-award-mesh.md`(+ README 索引)
- `knowledge/figures/psd2award_wireframe.png`(藝術家橙 / 生成綠 wireframe 視覺對照)

## 結果(全過,exit 0)

| 件 | 藝術家 v | 生成 v | 藝術家 IoU | 生成 IoU | 判定 |
|---|---|---|---|---|---|
| 光暈 | 78(全 hull) | 35 | 0.949 | 0.933 | ✅ −0.016 |
| 身體 | 98 | 60 | 0.948 | 0.966 | ✅ **+0.018** |
| 左手 | 80 | 59 | 0.977 | 0.964 | ✅ −0.013 |

- **首次驗證「非 strip blob」regime**:3 件 aspect<1.2 → auto 全回退 v1 Delaunay(先前 v1 只有合成
  資料 + curtain_left 單件)。
- 生成 mesh **頂點數約藝術家一半**,覆蓋率仍達標 → runtime 記憶體/計算優勢。
- 藝術家 mesh 覆蓋率非 1.0(0.95~0.98)→ 確認「對齊藝術家」為正確標竿(延續 curtain 校準教訓)。

## 決策

- **deform 閘標 N/A(誠實)**:這 3 件在 Award 無 deform timeline(靠骨骼/權重變形),無真實位移場
  可轉移;依 RULES「不用未校準 stress_field」,本次只做靜態覆蓋率 + 拓樸 + 預算對照(皆有真值)。
- IoU 標竿用「≥ 藝術家 − 0.02」而非武斷 0.95。

## 下一步(見 STATE.md)

最高優先:**切圖→Spine JSON 組裝(SkelToJson)** — 把已固化慣例(`PSD名/圖層名`、size+2px、
mesh/region 分配)寫成工具,輸入 PSD → 輸出可載入的 Spine JSON。純 CPU 可自驅。
