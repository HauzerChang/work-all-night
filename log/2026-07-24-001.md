# 2026-07-24 — session 001

## 這次做了什麼(bounded chunk:S3×S4 端到端 → 對 Award 真實 mesh 驗收)

推進 STATE 最高優先候選 #1:把 S4(PSD 切件)與 S3(mesh 生成)串成端到端,
第一次以**真實生產 mesh(Award)當真值**驗收。

### AC(動手前定)
每件(光暈/身體/左手):PSD 切件 alpha → `generate_mesh_v2(auto)` →
① 生成 mesh 覆蓋率 IoU ≥ Award 藝術家 mesh 對同一 alpha 的覆蓋率 IoU − 0.03；
② 格式合法(unweighted、hull 有效、無孤兒、無退化三角);③ 頂點 ≤ 預算 64。

### 產出
- 新增 `tools/mesh_gen/validate_psd_to_award.py`(端到端閘,exit 0/1 可機讀)。
- 新增 `knowledge/s3-psd-to-award-endtoend.md`(+ 索引一行)。

### 結果(overall PASS)
| 件 | 生成 mode/v | 藝術家 v | 生成 IoU | 藝術家基準 | 判定 |
|---|---|---|---|---|---|
| 光暈 | v1 / 35 | 78 | 0.933 | 0.949 | PASS |
| 身體 | v1 / 60 | 98 | 0.966 | 0.948 | **勝過藝術家** |
| 左手 | v1 / 59 | 80 | 0.964 | 0.977 | PASS |

## 關鍵發現 / 決策
- **auto-mode 路由正確**:3 件皆 blob(aspect<1.2)→ 全落 v1 Delaunay(strip 只給窗簾類單軸件)。
- **生成 mesh 更精簡卻覆蓋相當**(頂點數約藝術家一半)。
- **藝術家多餘頂點是給骨骼權重變形用的自由度**,靜態覆蓋率用不到 → 純覆蓋率 AC 對「拓樸可用性」已足夠。
- Award 這批件**無 deform timeline**(weighted,骨骼變形)→ 正確地**不套真實位移場 deform 閘**(避開 miscalibration 陷阱)。
- **閘可信度**:負對照(uvs 縮0.7→0.46~0.50、平移+12%→0.60~0.72)全被抓到,遠低於門檻 → 有鑑別力、未過頭。

## 局限(誠實留痕)
- 生成件仍 **unweighted**;真實件靠 BBW 骨骼權重變形。要真正取代藝術家 mesh 尚缺**權重綁定**(S3 路線圖後半)。
- 耐變形無法在此資產驗(無 deform timeline);已在 main_draw 窗簾(有 deform)驗過 → 兩資產互補。

## 下一步(見 STATE.md)
二選一皆純 CPU、皆有 Award 真值:
(2) SkelToJson:把件 manifest 組裝成完整 Spine JSON(最接近可交付);
(3) S3 BBW 權重綁定:讓生成 mesh 能真正骨骼變形。
