# 2026-07-24 — session 001

## 這次做了什麼(自驅排程:S3×S4 端到端驗收)

推進 STATE.md「下一步」中最高優先的 bounded chunk:**PSD 件 → S3 mesh → 對照 Award 真實藝術家 mesh**。

先定 AC(可機讀,不靠肉眼):S3 自動生成的 mesh 對照藝術家真值,要在
(A) 輪廓形狀吻合、(B) 覆蓋 IoU、(C) 頂點預算 ≤ 藝術家、(D) 拓樸乾淨 四項全過,
並要有負對照證明形狀指標有鑑別力。

1. `psd_slice.py` 切 `robot_parts.psd` → 5 件 PNG(取 光暈/身體/左手 3 件,在 Award 中為 mesh)。
2. 解析 `Award.json`:確認 3 件為 **weighted mesh**(78v/98v/80v),無 deform timeline。
   讀 atlas 旗標:offset 全 0、orig==size(無 trim),~0.70 縮放;rotate 左手=false、光暈/身體=true。
3. 新增 `tools/mesh_gen/compare_to_award_mesh.py`:
   - A 形狀:藝術家 hull vs S3 hull,各正規化單位方形,取 **8 種二面體對稱最佳 IoU**
     (刻意旋轉/翻轉不變 → 只問形狀、避開 atlas 旋轉方向雷點)。
   - B 覆蓋:`evaluate_mesh` 三角填充 IoU vs PSD alpha。
   - C 預算:S3 頂點數 ≤ 藝術家。 D 拓樸:退化=0/孤兒=0/重心落 alpha 內。
   - 負對照:跨件形狀 IoU。
4. 產視覺 overlay `knowledge/figures/s3-vs-award-robot-mesh.png` 確認 hull 貼合輪廓。

## 結果(exit 0,overall_pass)

| 件 | 模式 | A 形狀 | B 覆蓋 | C 頂點 S3/藝 | D |
|---|---|---|---|---|---|
| 光暈 | delaunay-v1 | 0.886 | 0.933 | 35/78 | ✅ |
| 身體 | delaunay-v1 | 0.937 | 0.966 | 60/98 | ✅ |
| 左手 | delaunay-v1 | 0.968 | 0.964 | 59/80 | ✅ |

負對照:同件形狀 IoU 最低 0.886 **>** 跨件最高 0.711(gap≈0.18)→ 指標有鑑別力。

## 關鍵結論

- **S3 在真實生產件達 ≈ 藝術家品質且更精簡**(頂點數三件全少於藝術家,覆蓋不輸)。
- **v1/v2 分工獲印證**:不規則團塊件 auto 自動走 delaunay-v1(貼合更好);窗簾類單向拉伸長條走 v2-strip(耐變形)。
- **誠實侷限**:本次只驗**靜態**(覆蓋/形狀/拓樸)。3 件是 weighted mesh、無 deform timeline,
  沒有逐頂點位移場可轉移;deform 面向需先做骨骼權重轉移(列為下一課題)。
- 方法論再實踐:形狀指標先跑跨件負對照確認鑑別力才信其 pass。

## 產出

- 新增 `tools/mesh_gen/compare_to_award_mesh.py`、`knowledge/s3-psd-to-award-mesh.md`、
  `knowledge/figures/s3-vs-award-robot-mesh.png`(+索引)。
- 更新 `STATE.md`(里程碑 + 下一步候選重排)、本 log。

## 下一步(見 STATE.md)

(2) SkelToJson 件→Spine JSON 組裝(把命名/padding/mesh-region 分配/0.70 縮放慣例固化成工具,對照 Award 結構),
或 (3) 補 weighted mesh 骨骼權重轉移的 deform 面向。
