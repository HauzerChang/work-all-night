# 2026-08-24 — session 001

## 這次做了什麼(推進一個有界工作塊)
接續 session 004 定的最高優先 STATE 候選 0d:**分鏡 → 動畫 keyframe(讓 build_spine 產出的素材「會動」)**。

### AC(動手前定的可檢查目標)
1. 由 `analyze_target` 的 #3 分鏡 + 已建 skeleton.json,自動生成 Spine 3.8 `animations`(In/Loop/Out)。
2. Loop 可 seamless 循環、幅度落在真實 slot loop 的合理帶、兩肢相位錯開。
3. 配套評估器閘,且用負對照證明閘有鑑別力(非恆 PASS)。
4. 不破壞既有 build round-trip。

### 產出
- `tools/analyzer/build_animation.py`:角色參數化(body/head/limb/特效)確定性規則 → bone
  rotate/translate/scale + slot color alpha timeline,寫回 skeleton.json。曲線用 ease-in-out 緊湊 bezier。
- `tools/analyzer/validate_animation.py`:9 檢查(完整/Loop無縫/角色幅度/相位錯開/In收斂/Out塌陷/
  非退化/世界位移FK/時長)+ `--selftest` 4 負對照。校準真值取自真實 `main_draw_loop`。
- `knowledge/s1-storyboard-to-animation.md` + 索引;更新 STATE/PLAN 進度。

### 結果(自我驗證)
- robot_parts(5件):9/9 PASS + selftest 4/4 抓到(零運動→C3/C4/C8;斷縫→C2/C8;中段scale=0→C7;缺件→C1)。
- Symbol_Ww(18件):9/9 PASS(泛化)。
- build round-trip 回歸:仍 PASS(加動畫不影響 setup pose)。
- 世界位移 FK:robot Loop 40px / In 491px;Symbol_Ww Loop 12px / In 121px(Loop 細微、In 顯著,符預期)。

### 迭代修正(1 輪,在預算內)
- 閘首跑抓到 In 特效 scale=0(炸開起點)被誤判退化。判定這是合法「不可見邊界」→ 精修 C7:
  scale==0 僅在首/末 keyframe 合法,中段 scale≤0 才判塌陷、負 scale 判翻面。修後全 PASS 且 selftest(c) 仍抓中段塌陷。

## 誠實界定 / 限制
- 閘驗**幾何/結構品質**非藝術手感(緩動、重量感留使用者)。
- role 由 genre-prior 對件名推定:大獎主角合理;非角色符號(墨鏡/框被歸 limb)僅保證結構有效會動。
- FK 為剛體單骨變換(件綁單骨),確定性正確,不涉 mesh deform → 不受 stress_field 雷點影響。
- 未做:真機回放(CDN 擋)、mesh deform 動畫、tier 變體展開。

## 下一步(見 STATE 候選 e/f)
(e) 關節 pivot 推斷(件中心→相鄰件關節,如 hand↔body;真值 Award.json)——目前 limb 繞件中心轉不自然。
(f) mesh deform 動畫(窗簾式 deform timeline)。
