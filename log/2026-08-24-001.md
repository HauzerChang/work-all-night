# 2026-08-24 — session 001

## 這次做了什麼(bounded chunk:候選 2 的評估器端 — weighted mesh 變形閘)

推進 STATE 候選 2 最高優先項的**評估器端**(依 RULES「每能力必配評估器」,先做閘再做生成):
補上 `compare_robot_mesh.py` 標註的唯一未驗維度 —— **weighted mesh 骨骼變形平滑度**。

### 新工具 `tools/mesh_gen/weighted_deform.py`
- **Spine 3.8 normal-mode 骨架世界變換**:local matrix + parent 複合;Award 全 77 骨皆 normal(已確認)。
- **linear-blend skinning**:weighted mesh `[骨數,(boneIdx,bindX,bindY,weight)×N]` →
  `worldPos = Σ weight_b·M_b(bind_b)`。
- **真實動畫套用**:讀 bone timeline(rotate/translate/scale),線性取樣 keyframe 時間聯集+中點
  (keyframe 為精確值),重算世界變換 → 變形後世界頂點 → 幾何閘(重用 `deform_eval`)。

### AC 與自我驗證(全部量化,不靠肉眼)
1. **評估器可信(核心 gate)**:多骨頂點的每根骨各自把 bind 座標映到 setup 世界,偏差
   光暈 0.019 / 左手 0.007 / 身體 0.008 px（匯出浮點捨入級）→ 獨立於權重驗證 world-transform 數學。✅
2. **藝術家基準**:身體/左手(structural)在 In/Loop/Out 全程 clean(0 自交/翻面/退化)→ PASS。✅
3. **負對照(鑑別力)**:身體平滑權重換 naive 硬綁,施合成相對旋轉 → 平滑耐 60° 仍 clean、
   硬綁 30° 即 si=7、60° si=38。`--negctrl` 可重跑,discriminates=True。✅
4. **回歸**:`compare_robot_mesh.py` 仍 exit 0。✅

### ⭐ 關鍵發現(誠實、可追溯)
- **`si=0` 不是 weighted mesh 的通用有效性判準**。光暈(effect 件,hull==nv 純邊界軟邊 blob)在
  **自家 `Award_Legend_In`** 就自交(t=0 即 si=71、area 1.16),成因是驅動骨 `4_LEG6` 起手 scale
  **1.667×**、兄弟骨 `4_LEG5` 同時 0.86× 的非均勻縮放把 blob 折疊。這是真實出貨美術的性質:
  三重鐵證(同骨系 structural 件全 clean、bind 一致性 <0.02px、自交落在精確 keyframe)排除評估器 bug。
- → 評估器分類:**structural 件(hull<nv)硬閘 si=0;effect 件(hull==nv)只記錄基準,比「不比藝術家更糟」**。

## 產出
- 新工具 `tools/mesh_gen/weighted_deform.py`(baseline + `--negctrl`,皆 exit 0)
- 新知識 `knowledge/s3-weighted-mesh-deform.md` + 索引
- 更新 `STATE.md`(里程碑 + 進度 log + 修正候選 2 為「評估器已完成,下一步生成端」)

## 下一步(見 STATE.md 候選 2 生成端)
S3 **生成 weighted mesh**:對 structural 件加**內部取樣密度控制**(補足 boundary-dense 幾乎無內部點)
+ **BBW/heat-diffusion 權重**,用 `weighted_deform.py` 跑同一組真實/合成 pose,要求
`structural clean 且 worst ≤ 藝術家基準+margin`。真值(權重+骨架)已在 `Award.json`,純 CPU 可自驅。
