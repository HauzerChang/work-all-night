# 2026-07-20 — session 001

## 這次做了什麼(S3 端到端對照 Award 真實生產 mesh — 里程碑)

推進 STATE 的最高優先候選 #1:第一次拿**藝術家手做 mesh** 當 ground truth 驗收 S3。

1. **對齊地基先實測**:確認 Spine 3.8 mesh `uvs` 是 region 局部正規化座標(件正立邏輯座標),
   `(u*W,v*H)` 直接映到 PSD 切件像素。光暈藝術家 mesh vs PSD alpha:flip=False IoU=0.943、
   flip=True 0.426 → 對齊成立、不需 v-flip。**先驗地基再比較,避免自欺。**
2. **切件**:`psd_slice assets/robot_parts.psd` → 5 件 PNG + manifest(光暈/右手/頭/身體/左手)。
3. **新工具** `tools/mesh_gen/compare_to_award.py`:對 3 件 mesh(光暈/身體/左手;右手/頭為 region 排除)
   跑 `generate_mesh_v2(auto)`,對照 Award 真實 mesh 量 ① 覆蓋率 vs 藝術家基準 ② 外周輪廓 IoU
   ③ 頂點/三角預算 ④ 校準 deform(自交/翻面)。
4. **結果(auto 模式,全過)**:
   - 光暈 v1 35v/78,覆蓋 0.933 vs 0.949(−0.016),外周 IoU 0.910,deform si0/fl0 ✅
   - 身體 v1 60v/98,覆蓋 0.966 vs 0.948(**+0.018**),外周 IoU 0.932,deform si0/fl0 ✅
   - 左手 v1 59v/80,覆蓋 0.964 vs 0.977(−0.013),外周 IoU 0.957,deform si0/fl0 ✅
5. **視覺驗證**:`knowledge/figures/s3-vs-award-hull-overlay.png`(紅=gen 外周貼著綠=藝術家外周,3 件皆吻合)。

## 關鍵結論 / 決策

- **S3 首次對真實外部 ground truth 收斂**:覆蓋、輪廓、精簡度(頂點更少)、耐變形四項全過。
- **auto 拓樸選對**:3 件 aspect < 1.2 → 回退 v1 Delaunay;forced-strip 覆蓋掉到 0.878 →
  證實 strip 只適高瘦窗簾、v1 適團狀件,aspect 門檻正確。
- **第 4 次評估器 miscalibration + 修正**:窗簾位移場是絕對像素(313px),跨件直接轉移對小件(左手 257px)
  = >100% 相對形變 → 假性 si10/fl3。依 `part_diag/curtain_diag` 尺度正規化(area_ratio 回 1.12–1.24)
  → 左手轉 si0/fl0。教訓:跨資產量化閘先對齊量綱/尺度再判定。

## 產出

- `tools/mesh_gen/compare_to_award.py`(可重跑端到端對照工具)
- `knowledge/s3-vs-award-real-mesh.md`(+ README 索引)、`knowledge/figures/s3-vs-award-hull-overlay.png`
- 更新 `STATE.md`(里程碑 + 下一步改為 SkelToJson 組裝)、本 log

## 下一步(見 STATE.md)

候選 #2 升為最高優先:**SkelToJson 組裝工具** — 把「件→Spine attachment」慣例
(slot 名 `PSD名/圖層名`、size+2px、mesh/region 依 aspect 分配、寫入 uvs/vertices/hull/triangles)
固化成工具,吃 psd_slice manifest 產出可載入的 Spine 3.8 JSON,把 S4+S3 串成完整「PSD→Spine」產線。
