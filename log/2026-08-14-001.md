# 2026-08-14 — session 001

## 這次做了什麼(S3×S4 端到端:機器人件 → mesh → 對照 Award 真實 weighted mesh)

接續 STATE 候選 #1(最高優先、有生產真值、純 CPU 可自驅)。

1. **端到端跑 3 件**:`機器人拆件/{光暈,身體,左手}`(Award 中為 mesh)。atlas 切件(Award.png/Award2.png
   雙頁)→ `generate_mesh_v2`(auto → 有機件走 Delaunay-v1 回退)→ IoU 對照藝術家 mesh uvs 覆蓋率。
2. **發現真實件是 weighted / 骨骼驅動**:`vertices.length != uvs.length`(光暈 570/78、身體 738/98、
   左手 556/80),掃描 12 支動畫確認**這 3 slot 全部 0 deform timeline**。對比 main_draw 4 mesh 是
   unweighted + deform timeline 驅動 → 這是新 regime,現有位移場轉移閘不適用。
3. **修 v1 過度簡化**:初測(eps=0.008)3 件 IoU 全落在藝術家基準下(hull 只 14/21/18)。epsilon 掃描確認
   **0.002 為 binding**(光暈最嚴)→ 3 件全過(0.983/0.993/0.991,頂點 73/77/67 比藝術家精簡)。
   改 `generate_mesh.py` 預設 0.008→0.002。
4. **回歸驗證**:curtain_left v1 在 0.002 下 deform 仍乾淨(si=0)、IoU↑;curtain_right v1 兩 eps 都自交
   (v1 已知不通用,v2 strip 覆蓋,非回歸);main_draw 4 mesh 走 strip 不受影響(IoU/deform 全不變)。
5. **誠實化評估器**:`validate_against_real.py` 對 weighted/無 deform timeline 標的標 `AC_real_deform:n/a`
   (補零場會得假性 clean),`overall_pass` 僅取 IoU,新增 `target` 欄回報 weighted/藝術家頂點/hull。

## 關鍵結論

- **靜態端到端過關**(里程碑):S3 生成器對真實生產 mesh 件(切圖→mesh)靜態覆蓋率 ≥ 藝術家。
- **揭示缺口**:真實件靠**骨骼權重**變形(非 deform timeline)→ 要判定變形穩健性需**新的 bone-driven
  deform 閘**;且 S3 產 unweighted mesh,生產替換還需**權重指派(BBW)**。已把 bone-driven 閘分解成
  3 步寫入 STATE「下一步」(setup-pose skinning → 動畫 TRS 求值 → weighted 轉移閘)。
- **再次印證**:別讓靜態 IoU 過就宣稱完成(「靜態≠變形穩健」),缺 deform 閘就誠實標 n/a,不假性 pass。

## 產出

- 改 `tools/mesh_gen/generate_mesh.py`(epsilon_frac 預設 0.008→0.002 + 註解/argparse)
- 改 `tools/mesh_gen/validate_against_real.py`(weighted 標的誠實標 n/a + target 欄)
- 新增 `knowledge/s3-award-weighted-parts.md`;更新索引/STATE
- 本 log

## 下一步(見 STATE.md)

建 bone-driven deform 閘 step 1:**setup-pose weighted skinning + 自驗**(純靜態,無 bezier,可自駛)。
