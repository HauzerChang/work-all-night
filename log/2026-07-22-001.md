# 2026-07-22 — session 001

## 這次做了什麼(S3+S4 端到端:PSD件 → 生成 mesh → 對照 Award 真實 mesh)

推進 STATE 的最高優先候選 #1:把 S4 切圖與 S3 mesh 生成串成端到端,並對**真實生產標的 + 藝術家真值**驗收。

1. **探查 ground truth**:確認 Award(機器人生產 spine)的 `機器人拆件/{光暈,身體,左手}` 是 **mesh**
   (verts 78/98/80),`{右手,頭}` 是 region;且這 5 件**皆無 deform timeline**、mesh 為 **weighted(骨骼驅動)**。
   → 定調本 chunk 只做**靜態幾何 AC**(deform 轉移閘不適用於 weighted 無 deform 的件,誠實排除)。
2. **寫 `tools/mesh_gen/validate_psd_to_mesh.py`**:PSD 圖層名 → Award slot 對映;PSD 切件 alpha 生成 v2 mesh;
   量測 4 條 AC:① 覆蓋率 IoU vs 藝術家(同 PSD frame 公平比,另附 atlas frame 供參)② 頂點預算 ≤ 藝術家
   ③ setup 0 自交/翻面/退化 ④ format 合法。
3. **執行(exit 0,3 件全 overall_pass)**:
   - 光暈 gen 0.9331 / 藝術家 0.9486(PSD)/ 頂點 35 vs 78 / 0 si —— margin 內過(羽化邊界最難)。
   - 身體 gen 0.9660 / 藝術家 0.9477 / 頂點 60 vs 98 / 0 si —— **優於**藝術家。
   - 左手 gen 0.9642 / 藝術家 0.9768 / 頂點 59 vs 80 / 0 si —— margin 內過。

## 關鍵結論

- **S3+S4 首次對真實生產標的端到端驗收通過**(里程碑):生成 mesh 覆蓋率 ≈/優於藝術家、**頂點少 25–55%**、setup 乾淨。
- 3 件長寬比 < 1.2 → v2 auto 全回退 **v1 Delaunay** → 實質驗的是 v1 在真實團塊形狀的靜態表現。
- **誠實限制**(寫進 knowledge,避免被「全過」誤導):Award mesh 為 weighted 無 deform,**未涵蓋骨骼旋轉 regime**;
  光暈羽化邊界僅靠 margin 過(gen < 藝術家 1.55%)。

## 產出

- 新增 `tools/mesh_gen/validate_psd_to_mesh.py`
- 新增 `knowledge/s3-psd-to-mesh-real.md`;更新 `knowledge/README.md` 索引、`STATE.md`
- 本 log

## 下一步(見 STATE.md)

(2) **SkelToJson**:把「件→Spine attachment」(命名慣例 + size+2px + 生成 mesh + region/mesh 分配)固化成工具,
端到端產可進 Cocos 的 Spine JSON;或 (3) 光暈羽化邊界自適應取樣(對藝術家真值的可量化 gap)。
