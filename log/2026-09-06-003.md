# 2026-09-06 session 003 — S1 (J) 檔位主秀幅度差異化

## 目標(bounded chunk)

接續 log 002「下一步」建議 **(J)**:`slot_bigwin` 宣告 tiers `[Super,Mega,Omg,Legend]` 但一直只是
**metadata**(`build_animations` 對所有檔位產同幅度主秀,「檔位」形同虛設)。讓愈高檔位主秀**幅度單調遞增**,
用客觀結構簽章 AC 驗「Legend 幅度 > Super」且**保持介面契約**,並把它**接進產線**(`build_spine --animate --tiers`)。

## AC(動手前先定,可自我量測)

- T1 present+routing:tiers=True → 主秀節拍展開為 `<Tier>_<beat>`、路由回正確類別、真峰≥1.12;框架節拍不展開;tiers=False 無檔位前綴。
- T2 interface 保留:每檔變體端點(t=0/t=dur:bone TRS+特效 alpha)逐一==base 端點;base 尾端 setup identity。
- T3(crux)單調放大:每節拍每 bone scale 峰值嚴格遞增 Super<Mega<Omg<Legend;Legend/Super overshoot 比≈gain 比。
- T4 back-compat:`Super_<beat>`(tiers=True)逐位元==`<beat>`(tiers=False);validate_priors 覆蓋率仍 1.0。
- T5 負對照:slot_reveal(無 tiers)不展開;等 gain 令嚴格遞增判準失敗(鑑別力);框架節拍不展開。

## 做法(一條 gain 貫穿 + 端點恆等不變式)

1. `genre_priors.py`:`TIER_GAIN_STEP=0.2` + `tier_gains(genre)`→`{tier:gain}` 單調(Super1.0/…/Legend1.6),無 tiers 回 None。
2. `beat_templates.py`:`_tier_peak(base,intensity)=1+intensity*(base−1)`(**對 identity 恆回 1.0**);
   5 主秀產生器(hit/reveal/combo/anticipate_hold/cascade)各加 `intensity=1.0`,只把 peak 換成 `_tier_peak(...)`。
3. `analyze_target.build_storyboard`:輸出帶 `tier_gains`。
4. `gen_animations.build_animations(skeleton, storyboard, tiers=False)`:抽出 `_beat_timelines(...)`;
   tiers=True 且有 tier_gains 時對 `_MAIN_SHOW_TIERED={hit,reveal,combo,charge,cascade}` 依檔位展開 `<Tier>_<beat>`。
5. `build_spine.py`:`--tiers` flag → `build(...tiers=)` → `build_animations(...,tiers=)`。
6. 整合閘 `validate_priors_tiers.py`(從**先驗庫**經 build_storyboard→**真實 build_spine 骨架**→build_animations(tiers=True))。

## 結果:5 AC 全 PASS

- T3(crux)實測 effect/halo bone:hit [1.348,1.418,1.488,1.558]、combo [1.347,1.417,1.486,1.556]、
  cascade [1.336,1.404,1.472,1.539] —— overshoot 0.348→0.418→0.488→0.558 恰 ×1.2/1.4/1.6,Legend/Super=1.6=gain 比。
- T4:`Super_<beat>` 對 5 主秀節拍逐位元 == 未分檔輸出(Super gain=1.0,`_tier_peak(base,1.0)` 經 round(4) 相等)。
- T5(b):等 gain(全 1.0)下**任何 bone** 都無法通過嚴格遞增 → 判準能被證偽(非恆真)。

## 關鍵發現:放大與介面正交

因新自由度掛在**對介面點恆為零的變換**上(`_tier_peak` 對 identity 恆回 1.0),「主秀更大」與「可串接介面」
**不再互相牽制** —— 端點逐位元不變(T2)、Super 逐位元回退未分檔(T4a),放大只發生在曲線內部。這與 0i/G-3
的「補償在 setup=identity 時為 0」是**同一種設計手法**:把新自由度掛在對介面點恆為零的變換上,即可自由加料而不動契約。
又一「模板/metadata 就緒 ≠ 生成器接上」的補上(tiers 欄位早在,生成連線這次才接)。

## 回歸(全 PASS)

validate_priors_tiers(新,5AC)、validate_priors、validate_priors_cascade(I)、validate_priors_combo_charge(H)、
validate_priors_beats(E)、validate_more_beats(0g)、validate_beat_templates(0f)、validate_cascade(0h)、
validate_anim、validate_pivot_rotation(0i)、validate_scale_pivot(G-3)、round-trip validate_build(對 --tiers build)。
check_readiness:`spine-anim-forge` 新增 cap `tier_amplitude_differentiation` L2(pipeline)→ 區塊仍 HOLD。

## 產出

- `tools/analyzer/beat_templates.py`(+`_tier_peak`,5 主秀產生器加 intensity)、`genre_priors.py`(+tier_gains)、
  `analyze_target.py`(storyboard 帶 tier_gains)、`gen_animations.py`(build_animations(tiers=)+_beat_timelines+_MAIN_SHOW_TIERED)、
  `build_spine.py`(--tiers)、`validate_priors_tiers.py`(新)、`tools/check_readiness.py`(+cap)、
  `knowledge/s1-tier-amplitude-differentiation.md`(新)、`knowledge/figures/s1_tier_priors.png`(新)、
  `knowledge/README.md`(索引)、`skills/READINESS.md`(快照)、`STATE.md`、本 log。

## 下一步(擇一,皆純自主)

- **(J-2) 連擊數隨檔位遞增**:現只放大 combo 三峰幅度;讓 Legend 4 連擊 vs Super 3(改 combo 包絡結構,
  簽章加「峰數隨檔位遞增」且保介面);**(J-3) 檔位差異也套 rotate/whip 幅度**(現只放大 scale 峰)。
- (G-1) `--rig`×`--pivot-rotate`/`--scale-pivot` per-bone 語意去重;(G-2) 主秀 beat 下 limb 繞關節 AC;
  (G-4) shear/非均勻 scale 仿射保形 AC。
- S5→L3 仍待 **(D) 多 rig 真值**(C/資源類,使用者提供)。
