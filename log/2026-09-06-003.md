# 2026-09-06 session 003 — S1 candidate J:tier(檔位)變體幅度差異化

> 分支:`claude/focused-dirac-mzum4e`(本 routine 啟動分支;推進主研究 `prompts/run.md`)。

## 本次目標(bounded chunk)

推進 STATE.md「建議下一個 bounded chunk」列表首位 **(J) tier 變體幅度差異化**:
`genre_priors.slot_bigwin` 宣告 `tiers=[Super,Mega,Omg,Legend]` 但生成器一直不看它 —— 所有檔位共用同一組
主秀幅度。讓 `build_spine --animate --tiers` 對主秀 beat 依檔位序位產**差異化幅度**變體,愈高檔位愈誇張。

## AC(動手前定)

- J1 present+routing:每主秀 beat 恰產 `len(tiers)` 支 `<beat>_<Tier>`、類別正確;In/Loop/Out 仍單支。
- J2 幅度單調遞增:每主秀 beat pop overshoot 幅度依檔位嚴格遞增、量化增益吻合 g(Legend/Super=2.05)。
- J3 介面契約(逐檔位):首尾 identity(reveal 首 collapsed 尾 identity)。
- J4 結構簽章(逐檔位):放大不跨越簽章(combo 遞增峰/charge squash/cascade 跨件波/hit・burst 非 combo,charge)。
- J5 regression + 負對照:Super==base 逐位元、In/Loop/Out 不變;tiers=None 的 genre 產 0 支 _Tier clip;各檔位峰值全相異遞增。

## 做法

- `tools/analyzer/beat_templates.py`:新增 `TIER_STEP=0.35` / `tier_amp_gain(idx)=1+STEP·idx` /
  `tier_gain_for(tier,tiers)` / **`apply_tier_gain(b,s,g)`** —— 對單件 timeline 把幾何 excursion 依 g 一致縮放:
  scale 繞 identity=1(`v→max(0,1+(v−1)g)`,clamp≥0 保 collapsed 合法)、rotate/translate 繞 0(×g)、
  color/alpha 不動(與幅度正交、保 reveal alpha 介面)。`g==1.0` 直接回傳(逐位元 no-op)。
- `tools/analyzer/gen_animations.py`:`build_animations(skeleton, storyboard, tiers=False)`(**opt-in,default off**);
  抽出 `_build_beat_clip`(帶 gain)/`_pack`;`tiers=True` 且 storyboard 有 tier_variants 時,對
  `_TIERABLE={hit,reveal,combo,charge,cascade}` 的 beat 逐檔位產 `<beat>_<Tier>`(帶各檔位 g),其餘 beat 單支不變。
- `tools/analyzer/build_spine.py`:`build(...,tiers=False)` + `--tiers` CLI(需 --animate)。
- `tools/analyzer/validate_tier_variants.py`:5 AC 整合閘(先驗庫→真實 build_spine 骨架→build_animations tiers=True)。
- `tools/check_readiness.py`:註冊 cap `tier_amplitude_variants` L2 於 `spine-anim-forge` 區塊;`skills/READINESS.md` 快照重生。

## 自我驗證結果

- `validate_tier_variants.py` **5 AC 全 PASS**。J2:hit/burst/combo/charge/cascade 的 pop overshoot 各檔位
  [Super,Mega,Omg,Legend] 嚴格遞增,Legend/Super excess 比 = **2.05** = g_Legend(逐 beat 吻合)。
- **踩雷並修正(度量陷阱)**:初版 J2/J5 對 `burst`(reveal)失敗——`_scale_amp` 用 `max|scaleX−1|` 被 reveal 的
  **collapsed 起點**(scale~0.02,結構性「藏」的**向下** excursion,放大後 clamp 飽和到 1.0)主宰,而非 pop 峰。
  改度量為**正向 pop overshoot**(`max scaleX − 1`)即量到真正隨檔位放大的主秀幅度 → 5 AC 全 PASS。
- **回歸全綠**:validate_priors / priors_beats / priors_combo_charge / priors_cascade / beat_templates /
  more_beats / cascade / pivot_rotation / scale_pivot 全 PASS(default 路徑逐位元不變,g=1.0 no-op 保證;
  另量測 default build_animations clip 名無 _Tier 後綴)。
- **端到端**:`build_spine --animate --tiers`(robot_parts)產 23 支 clip(In/Loop/Out + 5 主秀×4 檔位);
  round-trip `validate_build` PASS、`validate_anim`(+selftest)overall PASS。
- `check_readiness.py` 全跑 exit 0,新 cap `tier_amplitude_variants` **GREEN**;anim-forge 區塊仍 **HOLD**(符合預期)。

## 產出

- 新檔:`tools/analyzer/validate_tier_variants.py`、`knowledge/s1-tier-amplitude-variants.md`、`knowledge/figures/s1_tier_variants.png`。
- 改檔:`beat_templates.py`、`gen_animations.py`、`build_spine.py`、`check_readiness.py`、`skills/READINESS.md`、
  `STATE.md`、`knowledge/README.md`。

## 關鍵發現

1. **「先驗宣告 ≠ 生成器使用」** —— `tiers` 欄位宣告已久卻從未驅動幾何,與 (E)/(H)/(I) 的「模板就緒 ≠ 生成器接上」同型。
2. **保結構的一致縮放** —— excursion 一致縮放讓幅度成單旋鈕,identity 為不動點使介面/簽章自動保留、g=1.0 no-op 零風險。
3. **度量陷阱** —— reveal collapsed 是結構性向下 excursion,量主秀幅度必須用正向 pop overshoot。

## 誠實界定 / 下一步

- 檔位→幅度增益曲線(STEP=0.35 線性)為**提案**:Award 各檔位僅名字帶檔位,無「Legend 比 Super 大多少」量化真值,
  故驗客觀性質(單調/量化增益/介面/簽章保結構)非美感。單一真值資產;與 anim-forge 同 HOLD。
- 下一步候選(見 STATE.md):(J-2) tier 連擊數/節拍數遞增(改峰結構的 per-tier 生成參數)、(J-3) In/Out 檔位化
  (對齊真實 Award_<Tier>_In)、(G-1)(G-2)(G-4)。S5→L3 仍待 (D) 多 rig 真值(資源類,使用者提供)。
