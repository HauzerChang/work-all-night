# 2026-09-24 run 001 — S1 (G-4''''''-count) twist 扭轉段數隨檔位遞增(count-aware × 幅度 × φ 保形三效正交)

## 這次做了什麼(bounded chunk)

延續 run(2026-09-23 002,G-4''''''-tier)在 STATE 尾列出的候選 **(G-4''''''-count)**:補上 (G-4''''''-tier) 明列的
honest boundary —— **「twist 未接 count-aware(扭轉段數隨檔位,`gen_twist(nosc=)` 已備參數未接;比照 wobble
G-4'''/squash G-4'''''-c)」**。(G-4''''''-tier) 讓 twist 兩軸 shear 峰**幅度**隨檔位遞增(兩軸同一 g 同比 → φ 不變),
但各檔位仍是**同樣 4 段**反相雙軸阻尼擺(有「擰多狠」沒「擰幾下」)。本次補上扭轉**段數** nosc 隨檔位嚴格遞增
—— 結構(拓樸)軸檔位差異化的**第四個通道**(前三:combo J-2、wobble G-4'''、squash G-4'''''-c)。

## AC(先定,再驗)

新閘 `tools/analyzer/validate_twist_count.py` 5 條(客觀、可量測),真值界定同 E/H/I/J/J-2/G-4'/G-4''/G-4''''/G-4'''''':
從先驗庫(slot_bigwin)→ **真實 build_spine robot 骨架** → build_animations(tier_gains, tier_twist_cycles) 端到端量。

- TC1 present + backward-compat:每檔位 `twist__{tier}` 產出/finite/有 bone/帶雙軸 shear;base twist 恆 4 段逐位元同無 count;`ttc=None` 逐位元同 (G-4''''''-tier) 幅度-only。
- TC2 **crux** count↑ ∧ φ 不變:段數 [4,5,6,7]==宣告嚴格遞增且 Super==base;每檔位每 bone φ 比值 ≈0.7(max_phi_err=0.0)。
- TC3 兩軸阻尼簽章逐檔保形:每檔位每 twist bone 的 shearX 與 shearY 各自首尾 0+變號≥3+極值遞減;兩軸峰仍遞增+每內部極值反相(段數軸不抵消幅度軸/不破反相)。
- TC4 正交:段數+平增益→段數遞增·兩軸峰不遞增·φ 仍不變;增益+無段數→段數恆 4·兩軸峰遞增。
- TC5 負對照:平段數(全 4)→單調 FALSE;slot_reveal twist_cycles_for None+gains None→不產段數變體;段數只作用 twist(wobble/squash 仍 4 段)。

## 產出 / 實作(全 additive)

- `tools/analyzer/tier_variants.py`:`twist` 併入 `COUNT_AWARE_CATS`;新增 `TIER_TWIST_CYCLES`
  (slot_bigwin Super4→Legend7,上界 7 論證同 wobble/squash)+ `twist_cycles_for(genre)`(+ 說明註解)。
- `tools/analyzer/gen_animations.py`:`build_animations` 加 `tier_twist_cycles=None`;`_count_maps` 加
  `"twist": tier_twist_cycles`。既有 `_build_beat(count=)` 路由已通用(count→`gen_twist` 的 `nosc` 第 4 位置參數)。
- `tools/analyzer/build_spine.py`:`--tier-variants` 時 `twist_cycles_for(genre)`→`tier_twist_cycles=ttc`。
- `tools/analyzer/validate_twist_count.py`:新閘(5 AC)。復用 `validate_shear_gen`(阻尼簽章)+ `validate_twist_gen`
  (`_shear_y`/`_shear_xy`/`_interior_shear`/`_tw3_eval`/`_has_shear_y`,雙軸讀取與反相判準)。
- `tools/check_readiness.py`:註冊 cap `twist_tier_count_aware` L2(pipeline)。
- `knowledge/s1-twist-count-generation.md`(+ README 索引)。
- `gen_twist(nosc=)` 本身不改(G-4'''''' 已備 `nosc` 參數;`_twist_env(A, nosc)` 早已通用)。

## crux(twist 獨有,與 wobble count 的差異)

twist 有**兩條** shear 軸(反相雙軸)。段數重生成後兩軸各多長 nosc 個阻尼極值,每個新極值(含低幅 A·rⁱ)
仍由 `_twist_env` 建構 `shearY=−TWIST_PHI·shearX` → **φ 比值由建構保證、與段數無關**;故段數 × 幅度 × φ 保形
三效必須同時成立、每檔位不論扭幾段皆:段數==宣告、φ≈0.7、每內部極值反相。同構於 squash count 的「新擠壓極值
也體積守恆」——**帶跨通道關係約束的類別 count-aware,要多驗一層「約束在段數增多後仍成立」**;約束由 `_env`
建構保證則段數增多天然不破(沿約束流形生成)。

## 驗證結果

- `validate_twist_count.py` **5 AC 全 PASS**。TC2 段數 [4,5,6,7]、max_phi_err=0.0;TC4(a) 平增益下 peak_x [16,16,16,16](段數軸與幅度軸正交)。
- 端到端 `build_spine --animate --tier-variants --shear-pivot` 直出 `twist__{Super,Mega,Omg,Legend}`,兩軸峰隨檔位遞增
  (shearX 16→33.6°)、φ 逐檔=0.7000(pivot 補償後仍守恆)。
- **22 回歸閘全綠**(21 既有 + 新 twist_count);`check_readiness.py` 退出 0(0 RED,無 GREEN→RED);新 cap GREEN;
  `spine-anim-forge` 區塊仍 HOLD(防固化)。

## 下一步(擇一,皆純自主)

- **(G-4''''''-vol)** volume-conserving twist:反相雙軸 shear 接體積守恆耦合 scale(`det=cos(shearY−shearX)≠1` →
  加耦合 scale 使 `sx·sy=1/cos(shearY−shearX)` → 擰而不變面積;shear+scale+rotate 三通道同時=塞滿一般仿射四自由度且守恆)。
- **(J-3)** cascade 波速/散佈/件數隨檔位(cascade 的 count-aware:跨件波的第三種檔位軸,簽章在件之間)。
- **(G-4'''''-charge)** charge 蓄力段數 / 其他 count-aware 節拍(把 count-aware 推到第五個通道)。
- **(G-1)/(G-2)** `--rig`×pivot 語意去重 / 主秀 beat 下 limb 繞關節 AC。
- S5→L3 仍待 **(D) 多 rig 真值**(C/資源類,使用者提供)。
