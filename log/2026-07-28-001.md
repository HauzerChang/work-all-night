# 2026-07-28 — session 001

## 這次做了什麼(S3×S4 端到端:PSD 件 → 生成 mesh → 對照 Award 真實藝術家 mesh)

推進 STATE 最高優先 bounded chunk #1(有真值可比、純 CPU 可自驅):把 S4(PSD 切件)串上 S3
(mesh 生成器),對 `Award` 生產 spine 的真實藝術家 mesh 做端到端覆蓋率對照。

1. **驗證環境**:numpy/cv2/scipy/triangle/psd_tools 全 import OK(SessionStart hook 已裝)。
2. **解析 Award mesh 件**:確認 3 個 mesh 件(光暈 78v / 左手 80v / 身體 98v),皆 **weighted**、
   **無 deform timeline**。
3. **實測 UV 慣例**:Award mesh uvs = region-local(0..1);對 PSD 件 alpha 直接 uv×(W,H) 得高 IoU
   (0.949/0.977/0.948),沿用此解讀。身體 uv x-max 0.759 是件本身透明邊距,非慣例問題。
4. **寫端到端驗收工具** `tools/mesh_gen/validate_psd_to_award.py`:psd_slice → generate_mesh_v2 →
   evaluate_mesh(IoU+拓樸閘)+ 對照藝術家 mesh IoU 基準。
5. **跑驗收 + 產圖**:3 件全 `overall_pass`;產 `knowledge/figures/s3-psd-to-award-mesh.png` 疊圖存證。

## 量化結果(AC 逐條)

| 件 | 生成 mode/頂點 | 生成 IoU | 藝術家頂點/IoU | AC-a 平價 | AC-b 拓樸 |
|---|---|---|---|---|---|
| 光暈 | v1 / 35 | 0.933 | 78 / 0.949 | ✅ 98.4% | ✅ 0孤兒0退化 |
| 左手 | v1 / 59 | 0.964 | 80 / 0.977 | ✅ 98.7% | ✅ |
| 身體 | v1 / 60 | 0.966 | 98 / 0.948 | ✅ 101.9%(勝) | ✅ |

## 關鍵結論 / 教訓

- **生成器對真實藝術家 mesh 達覆蓋率平價,且更精簡**(35–60v vs 78–98v)。藝術家頂點多為變形經濟,
  純靜態 IoU 上生成器不吃虧。
- **auto 路由判斷正確**:3 件皆團塊狀(aspect<1.2、非 row-convex)→ 全自動回退 v1 Delaunay(strip 是給窗簾用的)。
- **端到端閉環成立**:PSD→件→mesh 對真實生產標的可用。

## 誠實邊界(寫進 knowledge/STATE)

- 只驗**靜態幾何/覆蓋率**,**未驗權重**(生成 unweighted,藝術家 weighted;S3 無 BBW)。
- deform 閘 **N/A**(這 3 件無 deform timeline;逐頂點 deform 穩健性已於 main_draw 4 mesh 驗過)。

## 產出

- 新工具 `tools/mesh_gen/validate_psd_to_award.py`(可重現,EXIT 0/1)
- 新 knowledge `knowledge/s3-psd-to-award-endtoend.md` + 索引一行 + 圖 `knowledge/figures/s3-psd-to-award-mesh.png`
- 更新 `STATE.md`(里程碑 + 更新後下一步候選)、本 log

## 下一步(見 STATE.md)

1. S3 權重生成(BBW)→ 對照 Award weighted `vertices` 驗權重分佈(才能真正取代藝術家 weighted mesh)。
2. 件→Spine JSON 組裝(SkelToJson)。
