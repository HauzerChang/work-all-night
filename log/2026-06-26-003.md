# 2026-06-26 — session 003

## 這次做了什麼(S4 走 PSD 契約 — pipeline 打通)

使用者:已 merge PR #1;策略拍板**走 PSD 契約**。據此開 S4。

1. **環境前置**:確認 `psd-tools 1.17.4`(+ Pillow)可在排程環境安裝;加進 `requirements.txt`。
   pytoshop build 失敗,但 psd-tools 1.17 自帶寫入 API(`new`/`create_pixel_layer`/`save`)→ 可純 CPU 造合成 PSD。
2. **合成測試 fixture** `make_test_psd.py`:造 4 圖層 PSD(bg/char__body/char__arm/fx__deco,含 alpha、重疊、`/` 命名)。
3. **PSD 切圖工具** `psd_slice.py`:讀 PSD → 可見 leaf 圖層 → 每件切出緊湊 PNG + manifest(offset/size/z)
   → 自驗閘:各件依 offset 由下而上 alpha-over 重組 == PSD composite。
4. **自驗 + 修 bug**:初版把圖層順序反轉 → MAE 15(錯);驗明 psd-tools `descendants()` 已是由下而上,
   移除反轉 → **重組 MAE=0.0104、0 孤兒、overall_pass**。
5. **負對照**:漏任一圖層 → MAE 1.97~11.4(抓到)。發現孤兒指標在有全幅 bg 時對「漏中間層」不敏感,
   由 MAE 補位 → 兩指標互補,閘可信。
6. **PSD 契約規格** `knowledge/s4-psd-contract.md`:給美術的交檔規範(一圖層=一部位、共用座標系、
   被遮處要畫全=補圖前移、命名對應 slot、不可合併/裁切、pivot 留待 S5)+ 驗收指令。

## 關鍵結論 / 決策

- **策略落地**:走 PSD 契約 = 把「平面圖自動拆件+補繪」這個無 GPU 做不到的難題,上移成對美術的交檔規範。
- pipeline(PSD→件→重組驗證)純 CPU 已通且自評;**最後一哩是拿到真實分層 PSD 做真實驗收**(使用者層級)。
- 再次實踐自驗 + 負對照才信閘;並當場抓修圖層順序 bug。

## 產出

- `tools/mesh_gen/make_test_psd.py`、`tools/mesh_gen/psd_slice.py`
- `knowledge/s4-psd-contract.md`(+索引)、`requirements.txt`(+psd-tools/Pillow)、`.gitignore`(忽略 .psd fixture)
- 更新 `STATE.md`、本 log

## 下一步(見 STATE.md)

(2) 用合成切件把下游「PSD→S3 mesh→Spine JSON」接通(純 CPU 可自驅);(1) 等使用者提供真實 PSD 做驗收。
