# 2026-08-31 — session 003(主研究)

## 本次有界工作塊:S5 (d') 多跳 weighted 肢體鏈端到端驗收

session 002 的 `--rig × --weighted` 併用留下唯一 honest-boundary 缺口:**weighted mesh 當作
鏈中段肢體(既是某件的子件、又是另一件的父件)**在真實資產 robot_parts **無樣本**
(其可拆肢體皆 region 件 + 星形單跳;weighted 只有 body=rig 根 + 光暈=effect)。本 session
依 session 002 提的 (d') 路徑,用合成鏈 fixture 端到端驗此案例。

> 開頭校正:routine 被指派隨機工作分支 `claude/focused-dirac-x94k6g`(又一次分支增生);依 SCHEDULE.md
> ① 主研究 Routine + prompts/run.md 步驟 0,切到固定分支 `claude/spine-main`(== 隨機分支同 commit,無工作遺失)推進並 push。

## AC(動手前定,可自量測)

- **AC0(fixture 有效性)**:合成 PSD 經 `infer_tree` 給出**鏈拓樸**(root=trunk,body→arm→forearm→hand,≥2 跳),
  ≥2 個相鄰件被判為**結構件(非 effect)且 geometry=mesh**(→ weighted);roles 正確(body=root、其餘 limb)。
- **AC1 鏈結構**:bone 父鏈為拓樸鏈(鏈深≥3、非星形);每個 weighted 件控制骨 parent==該件關節骨 b_{nm};index 合法。
- **AC2 setup 不位移**:rig×weighted setup 世界頂點 == weighted-only 逐頂點(<0.05px)。
- **AC3 遞迴帶動**:轉某關節→後代(含自身)weighted 件動、祖先/旁支不動;weighted-only 全脫鉤(負對照);
  **特別驗 2 跳**(轉 b_body→forearm 經 arm 隔一跳仍隨動)。
- **AC3R region 葉件隨鏈**;**AC4 變形乾淨**(結構件逐幀 si=0/flip=0)。

## 做了什麼

1. **`tools/mesh_gen/make_limb_chain_psd.py`**(新 fixture):4 件鏈 `body→arm→forearm→hand`,
   佈局刻意「只相鄰件重疊、非相鄰件分離」→ `infer_tree` recover 出鏈非星形;實心銳邊純色塊→結構件;
   ASCII 圖層名(避 PSD 寫檔 mac_roman CJK 雷)。
2. **AC0 probe**(scratchpad):實跑確認 `infer_tree` 給出 `body→arm→forearm→hand`、roles 正確、
   arm/forearm geometry=mesh(coverage 0.39/0.38)、hand=region。
3. **`tools/analyzer/validate_rig_weighted_chain.py`**(新閘,5 AC + 雙軌負對照):復用 combo 閘的
   `_load/_weighted_slots/_skin_setup/_pose_rotate` + `weighted_deform_eval` 的 world transform;
   對照 rig×weighted vs weighted-only build。
4. **`check_readiness.py`**:`spine-rig-pivot` 新增 cap `rig_weighted_chain`(L2 pipeline)。
5. `knowledge/s5-rig-weighted-chain.md`(新)+ knowledge/README 索引 + STATE 里程碑/進度/下一步
   + `skills/READINESS.md` 快照。

## 自我驗證結果

`python3 tools/analyzer/validate_rig_weighted_chain.py` → **OVERALL PASS(exit 0)**:

- AC1:bone 鏈 `root→b_body→b_arm→b_forearm→b_hand`(鏈深 4、非星形);
  arm/body/forearm 控制骨皆 parent 各自 `b_{nm}`。
- AC2:max_dev **0.0000px**。
- AC3(轉 25°,平均位移 px;D=後代):

  | 驅動 \ 件 | arm | body | forearm |
  |---|---|---|---|
  | b_body | 73.7(D) | 39.3(D) | **80.4(D,隔 arm 一跳)** |
  | b_arm | 48.0(D) | **0.0(祖先)** | 67.6(D) |
  | b_forearm | 0.0 | 0.0 | 48.2(D) |
  | b_hand | 0.0 | 0.0 | 0.0 |

  weighted-only 版**全欄 0.0**(脫鉤);多跳驗證=True。
- AC3R:region 葉件 hand 隨鏈(body 73.6/arm 73.4/forearm 70.4/自身 17.8px)。
- AC4:3 結構 weighted 件 si=0/flip=0。
- **回歸**:`validate_rig_weighted_build`(robot_parts combo)、`validate_rig_build`(rig-only)、
  `validate_tree`(Award 真值)皆仍 PASS(本次只新增檔案,未改 build_spine/既有閘)。
- `check_readiness`:新 cap `rig_weighted_chain` **GREEN (pipeline)**;`spine-rig-pivot` 仍 **L2 → HOLD**。

## 關鍵發現 / 決策

- **併用機制深度無關**:setup 下整條父鏈皆純平移 → 控制骨世界位置不變 → weighted bind 偏移不用改
  → **setup 逐頂點 0.00px**(與鏈深度無關);動起來時控制骨掛在該件關節骨上,沿**任意深度**關節鏈
  剛性帶動 → 遞迴耦合免額外處理。故這是**填補驗收覆蓋率**,不是新演算法。
- **雙軌負對照**:①weighted-only 全脫鉤(位移是真接進鏈,非度量假象);②rig 版**非後代不動**
  (證鏈**方向性**,非「全部一起動」)。二者同時成立才 PASS。
- 踩雷 2 則:(a) **PSD 寫檔 mac_roman 不吃 CJK 圖層名** → fixture 用 ASCII(分析器 role 關鍵字吃英文);
  (b) **旋轉某骨不移其自身原點**(只旋轉其子/attachment) → region 葉件要看 **attachment 世界點**
  (`transform_point(bone_world, local_offset)`),非骨原點。

## 界定 / 下一步

- `spine-rig-pivot` **仍 HOLD**:L3 硬缺口=**多 rig 真值**(Award 僅機器人一件可拆肢體 rig)不變;
  合成 fixture 驗的是「機制端到端成立」非「貼合藝術家意圖」,遵守防固化。
- **S5 的可自主子問題已收斂**(pivot 縫 + 樹推斷 + rig 組裝 + weighted 併用 + 多跳鏈);再推 S5→L3 需
  使用者提供第二個真值 rig(資源類)。
- 下一個 bounded chunk 候選(見 STATE 尾):(A) S1 keyframe 主秀 beat 模板;(B) mesh deform timeline 生成;
  (C) weighted-forge/rig 併入 forge skill(C 類拍板);(D) rig 真值資源(C/資源類,阻塞 S5→L3)。
  建議先做 (A)/(B)(純自主)。

## commit & push

`claude/spine-main`(固定分支)。
