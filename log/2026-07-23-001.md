# 2026-07-23 — session 001

## 這次做了什麼(S3×S4 端到端:PSD 件 → 生成 mesh → 對照 Award 真實 mesh)

推進 STATE.md 的最高優先 bounded chunk(候選 1):把 S4 切件與 S3 mesh 生成串成端到端,
並對**真實藝術家真值**(生產 spine `Award` 的機器人三件 mesh)驗收。

1. **座標系校驗(先解 log 006 的懸念)**:log 006 記「Award mesh uvs 為 atlas 頁 UV,需先轉
   region 局部」。實測:把 Award 三件 mesh 三角形以 `uv*[W,H]` 直接渲染到件 alpha →
   **v-top IoU 0.95~0.98、v-flip 0.43~0.61** → uvs 本就是 **region-local、v 由頂部量**,
   與生成器慣例一致,**無需轉換**。推翻假設(正/負對照都做了)。
2. **新工具 `tools/mesh_gen/compare_award_mesh.py`**:`robot_parts.psd`→psd_slice 切件(全解析度)
   → `generate_mesh_v2(auto)` → 三條 AC:COVERAGE(gen_iou ≥ artist_iou − 0.03)、
   PARSIMONY(gen 頂點 ≤ artist 頂點)、TOPOLOGY(格式/0孤兒/0退化/重心全在 mask)。
3. **結果:三件全 `overall_pass`(exit 0)**:

   | 件 | 生成 v/t/hull | 生成 IoU | 藝術家 v/t/hull | 藝術家 IoU | 覆蓋差 |
   |---|---|---|---|---|---|
   | 光暈 | 35/49/16 | 0.9331 | 78/76/78 | 0.9486 | −0.0155 |
   | 身體 | 60/97/20 | 0.9660 | 98/154/40 | 0.9477 | **+0.0183** |
   | 左手 | 59/97/19 | 0.9642 | 80/116/42 | 0.9768 | −0.0126 |

4. **視覺對照圖**:`knowledge/figures/award_mesh_compare.png`(每件左=生成綠線框、右=藝術家橘)。

## 關鍵結論 / 教訓

- **v1 Delaunay 首度在真實生產「團塊狀」mesh 對藝術家真值過驗**(三件 aspect 都 <1.2 → auto 走 v1,
  非 strip)。strip 之前只在高瘦窗簾驗過;auto 依形狀分流正確。
- **生成器比藝術家精簡**:同覆蓋率下頂點約藝術家的 45~75%。藝術家對「光暈」用純邊界多邊形
  (78 頂點全 hull、0 內部)密集描邊 → 邊緣覆蓋略高;v1 較粗 hull 少 ~1.5% 邊緣但在容差內。
  → 若要逼近藝術家邊緣貼合,可加「hull 取樣密度」參數(覆蓋率↔頂點數取捨)。
- **deform 閘對這五件不適用**:Award 中機器人五件無 deform timeline(靠骨骼/權重變形)。
  遵守 RULES:不套用不適用/未校準的閘;變形穩健性在 main_draw 四 mesh 已另驗。
- **log 006 的 uvs 假設是錯的** → 已在 STATE/knowledge 更正,避免下個 session 誤走轉換。

## 產出

- 新增 `tools/mesh_gen/compare_award_mesh.py`(修掉 artist 三角數重複除以 3 的報表 bug)。
- 新增 `knowledge/s3-s4-award-mesh-endtoend.md` + 索引;圖 `knowledge/figures/award_mesh_compare.png`。
- 更新 `STATE.md`(里程碑 + 下一步:SkelToJson 組裝工具)。

## 下一步(見 STATE.md)

1. **SkelToJson 組裝工具**:把已驗慣例固化,端到端由 PSD 產可載入 Spine JSON(把 S3+S4 變交付產物)。
2. (可選)v1 hull 取樣密度參數 → 對藝術家真值做覆蓋率↔頂點 Pareto。
3. S2 補圖閘 / 骨架閘;S1 需 benchmark 影片(repo 無)。
