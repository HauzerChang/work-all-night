# 2026-07-23 — session 001

## 這次做了什麼(排程 session:S3×S4 端到端串接)

推進 STATE.md「下一步動作」最高優先候選 1:**PSD件 → 自動 mesh → 對照 Award 真實 mesh**。

### AC(動手前定的可檢查目標)
對 Award 3 個 mesh 件(光暈/左手/身體),自動生成的 mesh 需:
1. **覆蓋率 IoU ≥ 藝術家 mesh 自身覆蓋率**(- margin 0.02;真值 = Award 藝術家 uvs/triangles)。
2. **頂點數 ≤ 藝術家頂點數**(vertex budget)。
3. **格式合法**(unweighted、hull-first、triangles 索引在範圍)。
- deform 穩健:因本資產 mesh 無 deform timeline(靠骨骼權重),**無逐頂點 deform 真值** →
  列為 informational 探針,不當 pass/fail 閘(依既有教訓,不用 OOD 位移場當閘)。

### 產出 / 結果
- 新工具 `tools/mesh_gen/validate_psd_to_mesh_award.py`(exit 0 = overall_pass)。
- 3 件全過:

  | 件 | 生成頂點 | 藝術家頂點 | 生成 IoU | 藝術家基準 |
  |---|---|---|---|---|
  | 光暈 | 73 | 78 | 0.9832 | 0.9795 |
  | 左手 | 67 | 80 | 0.9913 | 0.9681 |
  | 身體 | 77 | 98 | 0.9926 | 0.9760 |

- 圖 `knowledge/figures/s3-award-robot-mesh.png`(cv2 疊圖,線框 + hull 點)。
- knowledge `s3-psd-to-mesh-award.md` + 索引 + STATE 更新。

## 關鍵結論
1. **首次拿自動 mesh 對真實生產 mesh 正面比對且通過** —— S3+S4 端到端串接落地,3 件用更少頂點達 ≥ 藝術家覆蓋率。
2. **epsilon 需隨件像素尺度調**:v1 預設 0.008(窗簾校準)對大件(光暈 496px)過粗 → 覆蓋率不足;
   掃描定 0.002 為 blob 件預設。教訓:輪廓簡化的絕對容差不能一常數打天下(後續可自適應)。
3. **Award 機器人 mesh 皆 weighted 且無 deform timeline**(逐 12 動畫確認)→ 靠骨骼權重變形。
   deform 閘無本資產真值;跨資產轉窗簾場為探針:area_ratio ~1.2 時乾淨/近乾淨(身體 si=0、光暈 si=3),
   過拉伸(左手 area_ratio 2.05)才自交 → 再次印證 v1 散點 Delaunay 大拉伸弱點,也印證「OOD 場不可當閘」。

## 方法論再實踐
- AC-first、有真值才下 pass/fail;無真值的項目誠實列為探針,不硬湊過閘(避免第四次 miscalibration)。

## 分支
- 排程指定分支 `claude/vibrant-franklin-mbyret`(與舊 `claude/zealous-noether-y2ecwu` 內容一致,無 diff)。
  本次 commit/push 到 `claude/vibrant-franklin-mbyret`。

## 下一步(見 STATE.md)
1. **SkelToJson(切件→Spine JSON 組裝)** —— 最自然收斂點(S3/S4/真值都就緒)。
2. S2 補圖閘 / 骨架閘。
3. v1 epsilon 自適應(消除人工調參)。
