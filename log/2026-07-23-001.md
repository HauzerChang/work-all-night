# 2026-07-23 — session 001

## 這次做了什麼(S3×S4 端到端:PSD件→生成 mesh→對照 Award 真實 mesh)

推進 STATE 最高優先候選 #1(有真值可比、純 CPU 可自驅)。

1. **定 AC(誠實裁剪)**:機器人 3 mesh 件(光暈/身體/左手)在 Award 是**加權 mesh、無 deform timeline**
   (見 `s4-psd-to-spine-real.md`)→ 真相只在「靜態覆蓋 + setup 拓樸」,**不硬套 deform 閘**(無 ground-truth
   位移場)。AC = 生成 mesh 覆蓋率 IoU ≥ 藝術家基準 − margin 且拓樸乾淨。
2. **寫工具** `tools/mesh_gen/compare_generated_vs_artist.py`:PSD 切件 → `generate_mesh_v2(auto)` →
   `evaluate_mesh` 量化 → 從 Award.json 讀藝術家 region-local uvs/tris,柵格化進切件框架(自動判 y 翻轉)
   → 對照判定。
3. **跑驗收**:`overall_pass=true`,3 件全過。
   - 光暈 35v/49t IoU 0.933 vs 藝術家 78v(純 hull)IoU 0.949 ✅
   - 身體 60v/97t IoU **0.966**(優於)vs 藝術家 98v IoU 0.948 ✅
   - 左手 59v/97t IoU 0.964 vs 藝術家 80v IoU 0.977 ✅
   - 全部 `uv_flip_y=false` 一致 → 交叉確認座標對映。
4. **評估器可信度(負對照)**:對藝術家 mesh 三態柵格化 —— 正確 0.95~0.98 / y 翻轉 0.43~0.60 /
   uv 縮小 30% 0.46~0.50 → 覆蓋率 IoU 有明確鑑別力,通過不是假性。

## 關鍵結論 / 決策

- **v1 Delaunay 在真實 blob 形狀首次真值對照通過**:3 件低長寬比 → auto 正確回退 v1;先前 v1 只在 curtain_left。
- **生成器更省頂點**:同/更佳覆蓋率只用藝術家 45~74% 頂點。藝術家多的頂點是為「加權變形平滑度」,
  不是覆蓋 → **下一個要補的是 BBW 權重 + 骨動 deform 驗收**,不是覆蓋率。
- **誠實邊界**:明確標註「未驗 deform 穩健(這批件靠骨權重、無 deform timeline)」,不宣稱做到未做的事。

## 產出

- `tools/mesh_gen/compare_generated_vs_artist.py`(新工具,可重現)
- `knowledge/s3-psd-to-award-mesh.md`(+索引)
- 更新 `STATE.md`(標記 #1 完成、重排下一步為 BBW/deform、進度 log)、本 log

## 下一步(見 STATE.md)

(1) weighted mesh 的 BBW 權重生成 + 骨動 deform 驗收(用 Award 真骨綁定當真值,純 CPU);
(2) SkelToJson 組裝(件→Spine attachment,固化命名/padding 慣例)。
