# 2026-08-11 — session 001

## 這次做了什麼(S3×S4 端到端接縫,對真實生產 mesh 驗收)

推進 STATE「下一個 bounded chunk 首選 #1」:PSD 件 → S3 生成 mesh → 對照 `Award` 真實生產 mesh。

### AC(動手前定)
- 對 `robot_parts.psd` 的 3 個 mesh 件(光暈/身體/左手),生成 mesh 的 coverage IoU
  ≥ 同件在 Award 藝術家手做 mesh 的 IoU(外部真值),且通過 `evaluate()` 全 AC。

### 做法與發現
1. `psd_slice.py` 切 robot_parts → 3 件 alpha。確認 **Award mesh uvs 為 region-local**
   (藝術家重建 IoU≈0.95 合理)→ 可直接對切件原尺寸 alpha 比對。
2. 舊預設(epsilon=0.008):身體過(0.966>0.948),**光暈/左手輸**藝術家(0.933/0.964)。
3. epsilon 掃描:**coverage 由邊界取樣密度決定,非內部點**(與 v2「IoU 由 rows 決定」同構)。
   0.004 → 三件全 BEAT 且頂點更少;0.002 更高但頂點多。
4. 0.004 下光暈出現**孤兒頂點**(凹口 hull 頂點相鄰三角被 centroid 濾光)→ 加 `prune_orphans()`
   (移除未引用頂點 + 重編號 + hull 計數相應減 1,保 Spine hull 不變式)。

### 改動
- `tools/mesh_gen/generate_mesh.py`:加 `prune_orphans()`;`generate()` 內套用;
  預設 `epsilon_frac` 0.008→0.004(main+argparse)。
- 新增 `tools/mesh_gen/validate_psd_mesh.py`:端到端 PSD件→mesh→對照真實 spine mesh 的可重跑閘。
- `knowledge/s3-psd-to-real-mesh.md`(+索引)、`STATE.md`、本 log。

### 驗證(全綠)
- 端到端閘 `validate_psd_mesh.py`:光暈 0.9606>0.9486(42v/78v)、身體 0.9828>0.9477(69v/98v)、
  左手 0.9796>0.9768(70v/80v),**三件全 pass、全 AC 過、exit 0**。
- Regression:curtain_left v1 整合 AC 過(IoU 0.98→**0.99**,deform 乾淨);v2 strip 4-mesh 全過。

## 關鍵結論
- **S3 生成器對真實生產標的成立**:達同等/更好覆蓋率、用更少頂點 —— 印證「確定性演算法+評估器」路線。
- coverage 是輪廓貼合度問題(邊界密度),不是內部密度問題;凹形需孤兒清除。

## 下一步(見 STATE.md)
- **SkelToJson 組裝工具**:命名慣例 + padding + mesh/region 分配 + 本 mesh 生成 → PSD 產出可載入 Spine JSON。
