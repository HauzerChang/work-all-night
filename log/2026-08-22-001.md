# 2026-08-22 — session 001

## 這次做了什麼(S3 candidate 2:weighted-mesh 變形評估器)

依 STATE 最高優先候選 2(補上 `compare_robot_mesh.py` 唯一未驗的維度:bone-driven weighted
mesh 變形品質)。RULES「每能力必配評估器,且需先於生成器」→ 本 session 做**評估器半邊**。

### AC(動手前定的可檢查目標)
1. 純 CPU 重現 Spine 骨骼變形,能對 Award 真實 weighted mesh 算出變形後世界座標(不需 Spine runtime)。
2. 引擎用**外部真值**驗證(非自產):多骨頂點 bind-consistency < 1px。
3. 有負對照 + 剛體校準,證明指標有鑑別力且不受整體運動污染。

### 產出:`tools/mesh_gen/weighted_mesh.py`
- 重現 Spine 3.8 **normal-mode 骨世界變換**(本資產全 77 骨皆 normal)+ **Linear Blend Skinning**。
- 解析 flattened weighted 格式 → per-vertex influences [(bone,bindX,bindY,w)]。
- 指標:AC1 bind-consistency、AC2 baseline bend clean(復用 deform_eval 自交/翻面閘)、
  bend-tolerance(對抗式最大乾淨角)、strain-smoothness(相鄰三角變形梯度 F 的 Frobenius 差)。

### 結果:三軸驗收全 PASS(Award 身體/左手/光暈)
- **AC1 bind-consistency**:多骨頂點 setup 世界點離散度 max **0.0075 / 0.0071 / 0.0193 px**(次像素)
  → 同時驗證解析 + 骨世界變換合成。**用藝術家真值本身做非平凡交叉驗證**。
- **AC2 baseline bend**:三件容差內 pose 皆 0 自交/翻面/退化。
- **負對照**(斷 parent 階層):bind-spread 168px ✅。
- **剛體校準**:root 旋 40°(全骨剛體)→ strain-smoothness **0.00000** ✅(量測純微分變形)。

### 關鍵發現
- **光暈 0 internal → bend-tolerance 僅 18°(最脆)**:純邊界多骨 mesh 對差分骨旋轉容忍度低
  → 量化佐證「內部取樣密度 → 變形穩健度」論點(身體 58 internal / 左手 38)。
- bend-tolerance 是 pose 相依特徵值,正確用法是**同 driver 同角度**比較生成 vs 藝術家(下一步),非比絕對數。
- strain-smoothness 已校準(剛體=0),可直接作生成器 weighted mesh 的平滑度閘。

## 產出清單
- 新工具 `tools/mesh_gen/weighted_mesh.py`(可重現,`python3 ... weighted_mesh.py` → exit 0)
- 新知識 `knowledge/s3-weighted-mesh-evaluator.md` + 索引
- 更新 `STATE.md`(里程碑 + candidate 2 進度:評估器半邊✅,生成器半邊為下一 chunk)

## 誠實界定 / 限制
- 只驗 `TransformMode.Normal`;用合成骨 pose(未逐幀比對 Award 動畫實際驅動角度)。
- 這是評估器半邊;**尚缺 weighted mesh 生成器**(內部密度 + BBW/熱擴散權重)。

## 下一步(見 STATE.md 候選 2,最高優先)
**S3 weighted 生成器**:一件 alpha + 骨骼結構 → 自動放內部取樣點(密度可控)+ Delaunay +
BBW/熱擴散骨權重 → flattened weighted vertices;用 `weighted_mesh.py` 對照藝術家身體/左手件:
同 pose strain-smoothness ≤ baseline(+margin)、bend-tolerance ≥ 藝術家、頂點經濟。純 CPU 可自驅。
