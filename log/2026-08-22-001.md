# 2026-08-22 — session 001

## 這次做了什麼(推進 STATE 候選 2:S3 weighted mesh + BBW 的第一個有界塊)

依 STATE「❗最高優先」補上先前唯一未驗維度「**weighted mesh 骨骼變形品質**」。
依 RULES「每能力必配評估器」→ 先做**閘**(評分權重好壞),再做生成器。

### 產出:`tools/mesh_gen/weighted_deform_eval.py`(weighted-mesh LBS deform 評估器)

`deform_eval.py` 只處理 unweighted(per-vertex offset);weighted mesh 由**骨骼** LBS 驅動,故自建:
1. 骨 world transform:骨階層 + 動畫 rotate/translate/scale timeline → `(a,b,c,d,wx,wy)`
   (rotate/translate=setup 加量、scale=setup 乘數;無 shear 簡化矩陣)。
2. Spine 3.8 weighted `computeWorldVertices`(bind 座標在 bone-local)。
3. 幾何閘重用 `deform_eval.eval_pose`(自交/翻面/退化/面積比)。
4. 負對照 `degenerate_weights`(硬指派最近單骨)+ `joint_bend_stress`(合成關節彎折掃描)。

### 自我驗證(對 Award 機器人 3 件,`run_robot()` 自動判定 overall_pass=True)

- **AC1** LBS setup 幾何乾淨(3 件)✅
- **AC2** 結構件(身體/左手)全真實動畫(In/Loop)0 自交/0 翻面 ✅
- **AC3** 鑑別力:身體 bend LEG7 掃描到 90° — **smooth 80° vs hard 單骨 50°** 才首次撕裂 ✅
  (smooth worst si=14/flip=2;hard worst si=49/flip=6)→ 閘能為未來 BBW 生成器評分。

### 校準發現(評估器本身要可信,誠實界定)

1. **『0 自交』非通用閘 → 依件角色分級**。藝術家自己的**光暈(軟 glow)**在 `Award_Legend_In`
   streak 段自交 71/翻面 7(exact keyframe t=0.233):單骨 `4_LEG6` 刻意飛 ~470px、scale 1.67×,
   其餘 3 骨守 setup → 光暈拉成流光重疊(軟效果視覺無害)。故分 structural(嚴)/soft-effect(容許)。
2. **拓樸閘量撕裂/翻面,不量彎折平滑度**。骨區**可分離**件(左手)hard 剛體平移也不撕裂
   (bend LEG9 90° 全乾淨,反優於 smooth 85°)→ AC3 必須挑**骨區交錯**件(身體)才成立;
   「平滑 vs 剛體」需另配 seam 連續性/應變指標(後續)。
3. LBS 核心正確性佐證:身體+左手真實動畫全乾淨(同段程式碼);光暈 71 自交可逐幀回溯真實 keyframe
   極端骨位移 → 排除數學 bug。

## 下一步(見 STATE 候選 2b)
**BBW 權重生成器**:對件 mesh 頂點 + 給定骨骼算確定性平滑骨綁權重 → 寫 Spine weighted vertices;
用本次 joint-bend 掃描自動收斂(目標:smooth 首次撕裂彎角 ≥ 藝術家)。真值 Award 3 件已備,純 CPU 可自驅。

## commit
`weighted_deform_eval.py` + `knowledge/s3-weighted-deform-evaluator.md`(+索引)+ STATE + 本 log。
