# S4 chunk 33 — 2026-09-04(使用者對談中裁決)

## 觸發

使用者在對談中主動詢問:(1) S4 目前研究進度報告;(2) LaMa 相關知識;(3) 候選15/17 詳細內容;
(4) 要求看候選15的實際圖片範例;(5) 詢問「這個問題是否用 gpt-image-2 就能解決,無須太過追求
在 Claude 進行補圖」;最終對候選15做出裁決:**無限期擱置**。

## 過程

1. 用 `tools/mesh_gen/s4_alpha_taper_candidate14.py` 既有函式(`load_materials`/
   `estimate_alpha_taper`/`estimate_combined`/`mae_in_mask`)+ `inpaint_eval.py` 既有
   `punch_hole`/`fill_cv2_inpaint` 重新產生候選15的真實視覺範例(未修改任何 production 代碼,
   純讀取既有函式組出一次性視覺化腳本,存於 session 的 scratchpad,不進 repo):
   - 案例1(fixed):`robot::右手` edge circle frac=0.06 seed=1 —— alpha_mae 115.6→2.6,
     硬邊材質從「毛邊暈染」修成「俐落邊界」。
   - 案例2(newly-broken):`robot::光暈` edge circle frac=0.06 seed=1 —— alpha_mae
     13.3→23.3(PASS 診斷尺翻 FAIL),平滑材質從「柔和漸層」變成「邊界偏硬」。
   兩個案例並排送圖給使用者,直觀呈現 trade-off。
2. 使用者接著問 gpt-image-2(候選17)能否讓候選15的追求變得沒必要。回頭核對
   `inpaint_eval.py` 原始碼確認一個關鍵事實:候選14/15 用的 `alpha_mae>20` 是研究者自訂
   的診斷尺,**不是** `passes()`(1a:`premult_mae`/`ssim`/`seam_grad_diff`)或
   `passes_1b()`(1b:`alpha_gap`/`seam_ratio`/`tone_gap`)實際採用的生產判定門檻。兩個
   代表材質(右手/光暈)在真正的 1b 生產門檻下,現行 median 版本本來就已經 PASS——候選15
   接不接受都不改變任何真實上線判定,純粹是已達標案例的補丁邊緣視覺精度微調。
3. 據此建議候選15與候選17(gpt-image-2 授權)是獨立決策、不互相取代:候選15可以獨立於
   候選17的裁決結果先行擱置。使用者採納,裁決**無限期擱置**。

## 結論

候選15:**無限期擱置**(區別於「不採用」的永久否決——保留未來若情境改變〔如材質家族擴大、
1b 門檻本身被重新校準〕可重新評估的彈性,但目前不排入任何排程工作)。未修改
`inpaint_eval.py` production 代碼(`estimate_alpha_taper` 維持 `min_ring=20`/`median`),
`tools/mesh_gen/s4_alpha_taper_candidate14.py` 的候選函式(`estimate_percentile`/
`estimate_combined`)保留原地供未來參考,不刪除。

**剩餘待裁決點收斂為兩項**:候選17(API key + 費用授權)、本排程(`claude/spine-s4-inpainting`)
接下來走向(待命/降頻/轉資源)。

## 檔案

- 更新 `STATE_S4.md`:頂部狀態說明、候選15 條目(open questions 區)、進度摘要(本則)。
- 新增本檔。
- 未新增/修改任何 `tools/mesh_gen/` 檔案(視覺化腳本是一次性對談產物,存於 session
  scratchpad,依檔案隔離契約不納入 repo)。
