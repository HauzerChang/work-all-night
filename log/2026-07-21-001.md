# 2026-07-21 — session 001

## 這次做了什麼(S3×S4 端到端:PSD 件 → mesh → 對照 Award 真實 mesh)

推進 STATE 候選 #1(最高優先、有真值可比)。

1. **切件**:`psd_slice.py assets/robot_parts.psd` → 5 件(取 mesh 類 3 件:光暈/身體/左手)。
2. **對照工具**:新增 `tools/mesh_gen/compare_to_real_mesh.py` — 件 PNG → `generate_mesh_v2` →
   對照生產 spine 真實 mesh 的**靜態公平閘**(覆蓋率 IoU + 頂點預算 + 拓樸格式);
   自動判定真實 mesh 是否 weighted、有無 deform timeline → 標示 `deform_gate_applies`。
3. **驗收**:靜態公平閘三件全過藝術家基準:
   - 光暈 44v IoU 0.9606 ≥ 0.9486(real 78v)
   - 身體 69v IoU 0.9828 ≥ 0.9477(real 98v)
   - 左手 70v IoU 0.9796 ≥ 0.9768(real 80v)

## 關鍵發現 / 決策

- **真相一(類別差異)**:Award 機器人 3 mesh 全為 **weighted / 骨骼驅動、0 deform timeline**,
  與 main_draw 窗簾(unweighted / 逐頂點 deform)**不同類**。S3 目前只產 unweighted 幾何 →
  **缺 BBW 權重生成**(幾何 ✓、skinning ✗)。定為下一步最高優先。
- **真相二(第 4 次評估器校準教訓)**:把 curtain 位移場轉移到骨骼驅動件當 deform 閘**跨域不適用** —
  連 weighted 藝術家真值都無法套 `transfer_deform_check`(vertices 是變長 weighted 格式);
  且把窗簾垂直大拉伸套到手部不代表其生產變形。工具已自動關掉此閘。
- **拓樸權衡量化**:blobby compact 件用 strip 靜態 IoU 僅 0.85–0.94(平帽/線性邊界漏曲率);
  v2 `auto` 正確不選 strip 而回退 Delaunay。對「骨骼驅動、無 deform」件,追求靜態貼合是對的
  (變形安全交給骨骼權重,非拓樸)。
- **修正**:v2 Delaunay 回退 `epsilon_frac` 0.008→0.004(預設為窗簾調,對大型 compact 件邊界過疏);
  mode 標 `delaunay-v1-fine`。不動 v1 預設 → main_draw 4 mesh(走 strip)重驗零回歸。

## 產出

- `tools/mesh_gen/compare_to_real_mesh.py`(新)、`tools/mesh_gen/generate_mesh_v2.py`(回退 epsilon 修正)
- `knowledge/s3-award-robot-end-to-end.md`(+索引)
- 更新 `STATE.md`、本 log

## 自我驗證

- 3 件靜態公平閘皆過藝術家真值基準,且頂點在預算內、拓樸格式有效。
- 回歸:main_draw curtain_left/right + shadow + shadow2(`--gen v2`)全 `overall_pass`(走 strip 不受影響)。
- 評估器可信度:對 weighted 真值明確標示 deform 閘不適用,避免第 4 次 miscalibration 誤判。

## 下一步(見 STATE.md)

S3 **BBW 權重生成**:對 weighted/骨骼驅動件補 skinning(bind 骨、每頂點權重和=1),有 Award 骨架當真值。
