# 2026-09-24 run 002 — S1 體積守恆扭轉(candidate G-4''''''-vol)

## 這次做了什麼(一個有界工作塊)

補齊 twist(G-4'''''')一路留到現在的**最後一條 honest boundary**:反相雙軸 shear 會變面積
(真實 Spine local `det(M)=scaleX·scaleY·cos(shearX−shearY)`,twist 無 scale 且反相 shearX≠shearY →
det=cos((1+φ)shearX)<1,一般仿射非等積)。讓生成器在**每個 shear 極值**施耦合 **uniform** scale
`s=1/√cos(shearX−shearY)` 使 **det(M)≡1(真面積守恆)且 scaleX==scaleY**。

## AC(可檢查,先定後做)

從先驗庫 → 真實 build_spine robot 骨架 → build_animations,6 AC(見 `validate_twist_vol.py`):
TV1 present+dual-axis+uniform 補償 scale、TV2 兩軸阻尼、TV3 反相雙軸耦合、TV4 identity 介面、
TV5(crux)真面積守恆 |det−1|≤2e-3 + 兩內建負對照、TV6 端到端 --shear-pivot pivot 不動 + 隔離 + 加性。

## 產出 / 改動

- `tools/analyzer/beat_templates.py`:新增 `gen_twist_vol` + `_twist_vol_env` + `TWISTVOL_KEYWORDS`、`DUR["twistvol"]`。
- `tools/analyzer/gen_animations.py`:`_DISPATCH["twistvol"]`;`_CAT_KEYWORDS` 置最前(單一 token 精確命中)。
- `tools/analyzer/tier_variants.py`:`twistvol` 併入 `SHEAR_CATS`;新增 `DUAL_AXIS_CATS={twist,twistvol}`。
- `tools/analyzer/genre_priors.py`:`slot_bigwin` 加 `twistvol` beat + role 模板(additive,覆蓋率仍 1.0)。
- `tools/analyzer/validate_twist_vol.py`:**新增**閘(6 AC）。
- `tools/analyzer/validate_twist_gen.py`:TW6c shearY-isolation 改以 `DUAL_AXIS_CATS` 認定(唯一回歸修補)。
- `tools/check_readiness.py`:新增 cap `twist_volume_conserving` L2。
- `knowledge/s1-twist-volume-conserving.md`(+ 索引)、`STATE.md`。

## 結果

- `validate_twist_vol.py` 6AC 全 PASS:det 實測 ≤1e-4;負對照 無 scale / squash 式 sx·sy=1 皆 max|det−1|≈0.11;
  pivot 殘差 右手 0.013 / 頭 0.004 / 左手 0.016px vs 負對照 8.6–29.5px。
- `check_readiness.py` 退出 0、**23 閘全綠**(0 RED,無 GREEN→RED);spine-anim-forge 仍 HOLD(防固化)。

## 決策 / 關鍵發現

- **squash 的「體積守恆」實為 scale-積守恆**(sx·sy=1;shear 在時真幾何 det=cos≠1);**twistvol 才是真面積(det)守恆**
  —— 兩種正交守恆用不同 scale 結構(非均勻 vs uniform)。這是 TV5 兩個負對照的核心洞見。
- **一般仿射四自由度(旋轉·非均勻 scale·雙軸 shear·等積約束)至此全被真實 beat 驅動且守恆**(G-4→G-4''''''-vol 這條線收束)。

## 下一步(見 STATE.md「下一步動作」)

- **(G-4''''''-vol-tier)** volume-conserving twist 接 tier 幅度(耦合 recompute:shear 放大 ×g 後重算 s 使 det 逐檔仍≡1)。
- 或 (J-3) cascade count-aware、(G-4'''''-charge) charge 蓄力段數、(G-1) per-bone 去重、(G-2) 主秀 beat limb 繞關節 AC。

## 分支備註(重要)

本 run 在 `claude/focused-dirac-st57q9`(harness 指定分支,較 `claude/spine-main` 領先 2+ commit)。SCHEDULE.md ① 主研究
routine 原設計 push 回 `claude/spine-main`;此排程被指定固定在 focused-dirac-st57q9,故 push 於此。建議使用者後續把
focused-dirac-st57q9 合回 `claude/spine-main`(此線為主研究最新 head),或把 default 對齊,以免主線分岔。
