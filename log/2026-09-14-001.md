# 2026-09-14 run 001 — S1 candidate (G-4''''')：squash 接檔位差異化(體積守恆耦合 amplify)

## 這次做了什麼(一個有界工作塊)

補上 (G-4'''') 明列的 honest boundary:「squash 未接 tier 幅度(`_amp_scale` 只放大 identity 上方 →
破壞體積守恆,需**耦合 amplify**)」。讓 squash 節拍(shear + 體積守恆非均勻 scale)也隨檔位差異化,
而**面積恆守恆**。

- **AC-first**:動手前定客觀驗收目標 = 每檔位 squash 的 (1) shear/拉長/非均勻三軸峰**嚴格遞增**且
  Super==base;(2) **每個** scale 極值幀 `scaleX·scaleY≈1`(守恆)且仍非均勻;(3) 阻尼+identity 介面保形;
  (4) 負對照:舊逐軸放大**破壞守恆** vs 耦合放大守恆(判別子)。

## 改動(全 additive,程式)

1. `tools/analyzer/tier_variants.py`:`MAIN_SHOW_CATS` 加 `squash`;新增 `COUPLED_SCALE_CATS={"squash"}`
   與 `_amp_squash_pair(sx,sy,g)`(把 q=scaleX−1 放大 g、scaleY 重建 1/(1+g·q) → 積恆 1);
   `amplify_bone_tl`/`amplify_anim` 加 `coupled_scale` 旗標。
2. `tools/analyzer/gen_animations.py`:`build_animations` 對 `cat∈COUPLED_SCALE_CATS` 傳 `coupled=True`。
3. `tools/analyzer/validate_squash_tier.py`(新,5 AC)。
4. `tools/analyzer/validate_tier_combo_count.py`:K5(c) 隔離判準由「峰數跨檔位不變」改為「帶/不帶
   `tier_combo_hits` 逐位元相同」(full vs amp_only)—— squash 入 MAIN_SHOW 後 scaleX overshoot 隨檔位
   跨過 impact 門檻(1.10)使 `_min_peaks` 峰數變動被誤判為 count 外洩;逐位元對比更精確(非放寬)。
5. `tools/check_readiness.py`:新增 cap `squash_tier_coupled_amplitude` L2 → `spine-anim-forge`(仍 HOLD)。
6. 文件:`knowledge/s1-squash-tier-coupled-amplitude.md` + 索引;圖 `knowledge/figures/s1_squash_tier.png`;
   `STATE.md`、`skills/READINESS.md` 更新。

## 自我驗證(不靠肉眼)

- `validate_squash_tier.py` **5 AC 全 PASS**。峰值:shear [16,21.6,27.2,33.6]°、拉長
  [0.16,0.216,0.272,0.336]、非均勻 [0.298,0.394,0.486,0.588]。V5b 判別子:舊逐軸積達 1.15(破壞)、
  耦合積 ≈1.0000(守恆)。
- **全 18 閘回歸全綠**(squash_tier + squash_gen + wobble_tier/count + tier_variants + tier_combo_count +
  shear_gen + shear_pivot + scale_pivot + pivot_rotation + cascade + 全 priors 系列 + more_beats +
  beat_templates + deform_gen)。
- **端到端**:`build_spine --animate --tier-variants --shear-pivot` 直出 `squash__{Super..Legend}`,
  `validate_build` round-trip **overall_pass**(premult MAE 0.031、0 孤兒)。shear-pivot 補償走 translate
  通道 → scale 守恆存活。

## 關鍵發現 / 決策

- **體積守恆是「耦合約束」而非兩個獨立幅度**:檔位放大時,守恆(積=1)與非均勻(兩軸不等)須同時維持,
  唯一辦法是把兩軸綁成單一 q 的函數並重建 scaleY;這是**第一個需要耦合 amplify 的檔位軸**。
- **新主秀 beat 進 MAIN_SHOW → 掃全主秀的舊閘可能假陽性**(K5(c)):修法是把隔離判準換成更精確的
  逐位元對比(該旗標對此 beat 零影響),非放寬。與過往 (G-4'') J3→channel-aware、(G-4'''') shear-isolation
  →SHEAR_CATS 同一課:每加一新通道/新 beat,掃全主秀的閘都要重檢假設。

## 下一步(擇一,皆純自主)

- **(G-4'''''') squash count-aware**(擠壓段數 nosc 隨檔位;`gen_squash(nosc=)` 已備參數,比照 G-4''')——
  補完 squash 的「幅度軸 × 結構軸」雙軸(同 wobble 已 G-4''/G-4''' 雙軸齊備)。
- **(G-4''''''') 產 shearY(雙軸 shear)/ rotate+scale+shear 三通道同時的運動基元**(塞滿一般仿射 M)。
- **(J-3)** cascade 波速/散佈/件數隨檔位;**(G-1)** `--rig`×pivot per-bone 去重;**(G-2)** 主秀 beat limb 繞關節 AC。
- S5→L3 仍待 **(D) 多 rig 真值**(C/資源類,使用者提供)。

## 狀態

`ACTIVE`。無阻塞;`spine-anim-forge` 續 **HOLD**(運動基元先驗、單一真值資產,防固化)。
