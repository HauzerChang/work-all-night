# 2026-09-25 run 001 — S1 體積守恆斜扭 twistvol(candidate G-4''''''-vol)

## 目標(bounded chunk)

補 twist(G-4'''''')一路留到現在的 honest boundary:**反相雙軸 shear 未接體積守恆**。twist 純雙軸 shear 令 Spine
local 行列式 det=cos(shearY−shearX)<1 → 擰轉縮面積。本次讓某節拍(twistvol)加**各向同性**補償 scale 使**完整**
local det≡1(擰而不變面積),達成 STATE 目標「shear+scale+rotate 三通道同時 = 塞滿一般仿射四自由度且守恆」。

## AC(先定、可自我量測)—— 全 PASS(`validate_twist_volume.py` 7AC)

- VT1 present + dual-axis shear + scale 通道(crux):shearX 峰16°、shearY 峰11.2°、帶 scale。
- VT2 兩軸阻尼振盪(承 twist);VT3 反相雙軸耦合(承 twist,復用 `_tw3_eval`)。
- VT4 **crux**:完整 det=scaleX·scaleY·cos(shearY−shearX)≈1(|det−1|~1e-6)+ 各向同性 |sx−sy|~0 + 內部補償拉伸。
- VT5 identity 介面(shear 首尾0、scale 首尾(1,1));VT6 端到端 pivot 殘差 0.004–0.016px vs 負對照 8–29px(>1000×)。
- VT7 負對照/隔離:純 twist 守衛(identity scale→完整守恆 FALSE)、squash 式守衛(非均勻 sx·sy=1→完整 det<1 且非各向同性)、
  正對照單元、耦合隔離(唯 twistvol 帶 shearY+scale)、加性零回歸(含純 twist 逐位元不變證未改 gen_twist)。

## 產出 / 變更

- `tools/analyzer/beat_templates.py`:新增 `gen_twistvol` / `_twistvol_scale` / `TWISTVOL_KEYWORDS`;`gen_twist` 逐位元不變。
- `tools/analyzer/gen_animations.py`:`_DISPATCH["twistvol"]` + `_CAT_KEYWORDS`(twistvol 排 twist 前,避子字串爭用)。
- `tools/analyzer/tier_variants.py`:`SHEAR_CATS` 加 twistvol;新增 `SHEARY_CATS={twist,twistvol}`(集中 shearY-isolation)。
- `tools/analyzer/genre_priors.py`:slot_bigwin 加 twistvol beat(additive)+ `_BIGWIN_ROLES["twistvol"]`。
- `tools/analyzer/validate_twist_gen.py`:TW6c 改用 `TV.SHEARY_CATS`(取代硬編碼 'twist')。
- `tools/analyzer/validate_twist_volume.py`:**新**整合閘(7 AC)。
- `tools/check_readiness.py`:新增 cap `twist_volume_conserve` L2(spine-anim-forge,仍 HOLD)。
- `knowledge/s1-twist-volume-conserving.md`(新)+ 索引;`STATE.md` 里程碑/下一步/進度摘要。

## 決策 / 發現

- **det=sx·sy·cos(shearY−shearX)**:「守恆」有兩層次 —— scale 子塊守恆(squash:sx·sy=1)vs 完整矩陣守恆(twistvol:sx·sy·cos=1)。
  squash 過去的「體積守恆」是前者;twistvol 首度達後者。各向同性補償 vs 非均勻擠壓是兩條正交面積軸。
- **順帶修 pre-existing RED**(非本 chunk 引入):`validate_analyzer_award.py` `4_storyboard_structure` 用 exact-equality
  (`proposed_beats=={In,Loop,Out}`),但 slot_bigwin 刻意過度提案主秀節拍 → 自 burst/hit/… 加入起恆 FALSE(與本 repo
  「prior_beats_unused 誠實、覆蓋率=1.0」設計矛盾;check_readiness 全跑時揭露,先前 STATE「0 RED」不準確)。改為**召回**
  (`Award beats ⊆ proposed_beats`,同 1_parts_recall)+ 透明列 extras → analyze_target RED→GREEN、spine-asset-forge 恢復 READY。
  這是修正過嚴判準、非 gaming(判準本身與 repo 一貫設計對齊)。

## 回歸 / 驗證

- `check_readiness.py` 全跑:**45/45 validator GREEN、0 RED、無 GREEN→RED**(23 閘 = 22 既有 + 新 twist_volume,含修好的 analyze_target)。
- 端到端 `build_spine --animate --shear-pivot` 直出 twistvol,完整 det 逐幀 ≈1(即使 pivot 補償後,translate 不影響 2×2 線性部)。

## 下一步(擇一,皆自主;細節見 STATE「下一步動作」)

- (G-4''''''-vol2) 各向同性補償 + 非均勻擠壓結合成單一節拍(雙軸 shear + 非均勻 scale + 完整 det 守恆同時);
- (G-4''''''-vol-tier/count) twistvol 接 tier 幅度 / 段數(`gen_twistvol(nosc=)` 已備);
- (J-3) cascade count-aware;(G-4'''''-charge) charge 蓄力段數;(G-1)/(G-2) rig 去重 / limb 繞關節 AC。
- S5→L3 仍待 (D) 多 rig 真值(C/資源類,使用者提供)。
