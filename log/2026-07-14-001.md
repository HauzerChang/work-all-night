# 2026-07-14 — session 001

## 這次做了什麼(S3+S4 端到端對照 Award 真實 mesh)

推進 STATE.md 最高優先 bounded chunk:把 S4(PSD 切件)與 S3(mesh 生成)串成端到端,
並用真實生產真值(big win spine `Award` 的藝術家 weighted mesh)對照。

新工具 `tools/mesh_gen/e2e_psd_award.py`(標準指令 `python3 tools/mesh_gen/e2e_psd_award.py`):
- `psd_slice` 切 `robot_parts.psd` 三個 mesh 件(光暈/身體/左手)→ 件 alpha。
- 藝術家 mesh:Award.json 的 region 局部 uvs × 件影像 W/H 光柵化 → IoU(vs alpha)基準。
- 生成 mesh:`generate_mesh_v2` auto → `evaluate` IoU + 覆蓋光柵。
- AC1:生成 IoU >= 藝術家基準 - 0.02;並報告生成↔藝術家覆蓋 IoU 與頂點預算。

## 結果(AC)

三件 **AC1 全過**、overall exit 0:

| 件 | 藝術家 v/IoU | 生成 v/mode/IoU | gen↔artist cover |
|---|---|---|---|
| 光暈 | 78 / 0.9486 | 35 / v1 / 0.9331 | 0.9179 |
| 身體 | 98 / 0.9477 | 60 / v1 / 0.9660 | 0.9284 |
| 左手 | 80 / 0.9768 | 59 / v1 / 0.9642 | 0.9572 |

生成 mesh 用約 45–75% 頂點達到不輸藝術家的輪廓保真、覆蓋同一 footprint。三件近方形(aspect<1.2)
故 auto 走 v1 Delaunay。

## 關鍵確認 / 教訓

- **uv→region 局部座標方向確定不翻 y**(解掉 log 006 開放問題):direct vs flip-y IoU 差 0.34–0.52,
  毫無歧義,與 `validate_against_real.artist_iou` 既有慣例一致。
- 件尺寸 ↔ attachment size 差 +2px(s4 已知 padding);本工具用件影像自身 W/H 對映 uvs 規避位移。
- 光暈藝術家 mesh 為純外框環(78 頂點全 hull、無內部)。

## 限制(誠實記錄)

- **只做靜態輪廓**:Award 三件皆 weighted(骨驅動,無 deform timeline),無法用
  `transfer_deform_check`(unweighted 位移場)對照變形 → weighted deform 對照列下一 chunk。
- 生成 mesh 仍 unweighted;要真正取代需補權重(BBW/heat)並綁 Award 骨架。

## 產出

- `tools/mesh_gen/e2e_psd_award.py`
- `knowledge/s3-s4-e2e-award.md`、`knowledge/figures/e2e-psd-award-mesh.png`、更新 knowledge 索引
- 更新 `STATE.md`(next action + 進度摘要)、本 log

## 下一步(見 STATE.md)

(1) weighted deform 對照(補足本次靜態限制,需重現 bone skinning);或
(2) SkelToJson 把生成 mesh 寫回 Spine JSON,做完整「PSD→件→mesh→JSON」。
