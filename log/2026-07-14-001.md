# 2026-07-14 — session 001

## 這次做了什麼(S3+S4 端到端對真實 mesh 驗收 — 里程碑)

推進 STATE.md 最高優先塊:**「PSD件 → S3 mesh → 對照 Award 真實 mesh」端到端驗收**。
第一次讓 S4(切件)+ S3(mesh 生成)這條 CPU 鏈,對**有 ground truth 的真實生產標的**收斂。

1. **定 AC(可量測)**:AC1 切件成功 / AC2 mesh 幾何乾淨(evaluate_mesh)/
   AC3 覆蓋率 IoU ≥ 藝術家 mesh 在同一 alpha 上的覆蓋率(真值閘)/ AC4 頂點數 ≤ 藝術家。
   deform 閘判定 **N/A**(這 5 件在 Award 無 deform timeline,骨骼/weighted 驅動,非逐頂點 deform)。
2. **新工具 `compare_to_award.py`**:`robot_parts.psd` → `slice_psd` → 3 個 mesh 件
   (光暈/身體/左手)→ `generate_mesh_v2(auto)` → 對照 Award.json 同名 slot 藝術家 mesh。
3. **第一輪(舊預設)**:2/3 fail AC3 — 光暈 0.933 < 0.949、左手 0.964 < 0.977。查因:
   我的 hull(approxPolyDP eps 0.008)僅 16~19 點,藝術家 42~78 點 → 平滑曲邊覆蓋不足。
4. **迭代(掃 epsilon)**:0.004 是甜蜜點 — 3 件覆蓋率全過**且**頂點數仍 ≤ 藝術家;
   0.002 會讓左手爆預算(84 > 80)。→ 改 `generate_mesh.py` 預設 0.008→0.004 + `generate_mesh_v2`
   加 `epsilon` 參數傳給 delaunay fallback。
5. **修負向發現(孤兒頂點)**:細 hull 後光暈有 2 個內部孤兒頂點(三角被 centroid 過濾)→ AC2c fail。
   新增 `prune_unused()`:過濾後移除未用**內部**頂點並重編號,hull 全保留(維持 hull-first 不變量)。
   光暈 44→42v、0 孤兒。
6. **回歸驗證**:4 個 main_draw mesh(皆 strip 模式,不走 delaunay)重驗全過 → epsilon 改動無回歸。
   （shadow2 slot 共用 shadow 的 atlas region,需 `--name image/shadow` 驗。)
7. **最終**:`compare_to_award.py` overall_pass=True(EXIT 0),3 件全過。

## 關鍵結論 / 決策

- **真值閘逼出兩個確定性校準**(非 ML):hull 精度(epsilon)與孤兒修剪 —— 都是可解釋、可重現的。
- 生成 mesh **覆蓋不劣於藝術家,且更精簡**(42/69/70 vs 78/98/80 頂點)。
- 座標學要點:Award mesh uvs 是 region-local [0,1](非整頁),可直接光柵化到切件遮罩;
  +2px padding / 0.70 縮放對 IoU 影響 ~0.3%(可忽略)。
- 再次實踐:AC-first → 量化 → 迭代(2 輪內)→ 負對照/回歸,才下「通過」判定。

## 產出

- `tools/mesh_gen/compare_to_award.py`(新)
- `tools/mesh_gen/generate_mesh.py`(epsilon 預設 0.008→0.004 + `prune_unused()`)
- `tools/mesh_gen/generate_mesh_v2.py`(`generate(epsilon=0.004)` 傳給 delaunay fallback)
- `knowledge/s3-psd-to-mesh-award.md`(+索引)、更新 `STATE.md`、本 log

## 下一步(見 STATE.md)

(2) **SkelToJson**:把生成 mesh 寫回可載入的 Spine JSON(命名/padding/縮放/旋轉件),
從「驗證」推到「產出可用資產」;(4) weighted 綁定進入 S5。
