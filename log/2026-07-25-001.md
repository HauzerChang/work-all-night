# 2026-07-25 — session 001

## 這次做了什麼(S3 端到端對照 Award 真實藝術家 mesh — 里程碑)

推進 STATE.md「最高優先」bounded chunk:**PSD 件 → S3 生成 mesh → 對照 Award 生產檔的真實藝術家 mesh**。
這是 S3 第一次拿到「有藝術家真值可比」的真實生產標的做整合驗收。

1. **定 AC**:對 3 個機器人 mesh 件,量(a) 生成 mesh IoU ≥ 藝術家 mesh 在同幀重建的 IoU − margin;
   (b) 靜態全乾淨(AC2/3/4);(c) PSD↔atlas 幀一致性佐證同素材。
2. **勘查**:Award.json 有 7 個 mesh,其中 3 個是機器人件(`機器人拆件/光暈,身體,左手`),全 **weighted**。
   逐 anim 掃描 → 這 3 件**無 deform timeline**(靠骨骼 skinning 變形)。實測 uvs 範圍 ≈ [0,1]
   → **region-local**,更正 log006「uvs 需轉」的警告。
3. **寫工具** `tools/mesh_gen/compare_to_award.py`(自含:切件不在就自動 `psd_slice`):
   PSD 件 alpha → `generate_mesh_v2(auto)` → `evaluate_mesh` + 藝術家 uvs 同幀重建 IoU 基準。
4. **跑驗收**:3 件 `all_pass`(exit 0)。
5. **視覺確認**:`knowledge/figures/s3-award-compare.png`(綠=生成 / 橘=藝術家 / 灰=alpha)。

## 量化結果

| 件 | 生成 IoU | 藝術家 IoU | 生成頂點 | 藝術家頂點 | 幀一致性 |
|---|---|---|---|---|---|
| 光暈 | 0.933 | 0.949 | 35 | 78 | 0.936 |
| 身體 | 0.966 | 0.948 | 60 | 98 | 0.972 |
| 左手 | 0.964 | 0.977 | 59 | 80 | 0.987 |

覆蓋率對齊藝術家(差≤1.5%,身體略高),頂點省 ~40–55%,靜態全乾淨。`generate_mesh_v2` auto 對這 3 件
(非高瘦 blob)自動回退 v1 Delaunay —— 驗證 auto 選路合理。

## 關鍵結論 / 邊界(誠實記錄)

- **僅靜態**:weighted + 無 deform timeline → 既有 deform 閘無真實場;變形對照未做。
  下一 chunk = 實作 weighted 骨骼位移場(setup vs 動畫幀世界頂點差)當真值,補上變形閘。
- 對照在 PSD 幀,幀一致性 0.94–0.99 非像素完美,~1–6% IoU 差可能含幀雜訊。
- Award mesh uvs 為 region-local(更正舊警告);atlas region ~0.70 縮小 + 部分 rotate,`atlas_crop` 已正確處理。

## 產出

- `tools/mesh_gen/compare_to_award.py`(新,自含可自驅重現)
- `knowledge/s3-award-mesh-compare.md`(新)+ 索引更新
- `knowledge/figures/s3-award-compare.png`(新)
- 更新 `STATE.md`(里程碑 + 下一步定為 weighted 變形閘)、本 log

## 下一步(見 STATE.md)

實作 **weighted mesh 骨骼位移場變形閘** → 把 Award 3 件的變形也對照;之後串「件→Spine JSON 組裝(SkelToJson)」。
