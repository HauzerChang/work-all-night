# 2026-07-25 — session 001

## 這次做了什麼(S3×S4 端到端:PSD件→生成mesh→對照 Award 藝術家 mesh)

承接 STATE 最高優先 bounded chunk(有真值可比、純 CPU 可自驅)。

1. **切件**:`psd_slice` 對 `assets/robot_parts.psd` 切出 5 件;取 Award 中為 mesh 的 3 件
   (光暈/身體/左手;右手/頭為 region 略過)。
2. **對照真值定位**:Award 3 mesh 皆 **weighted**(光暈 78v/左手 80v/身體 98v)、hull/tris 具全,
   查 12 支動畫 **無任何 robot slot 的 deform timeline** → 變形靠骨權重。
3. **座標校驗(關鍵、修正舊 note)**:Award mesh `uvs` 實測為 **region-local 0..1、v top-down**
   (vtop IoU 0.95~0.98 遠高於 vflip 0.43~0.60;當 atlas-page 正規化時 uv 落框比例≈0 → 排除)。
   修正 sess006 open note「Award mesh uvs 為 atlas UV,需先轉 region 局部」為誤判。
   期間先抓到自寫評估器的第一版用「bbox 正規化 + 8 朝向 max」對 身體 給出假性 0.64 → 換直接 region-local
   映射後校正為 0.949(RULES:評估器先校準再下判定)。
4. **產出整合 AC 工具 + 驗收**:`tools/mesh_gen/compare_to_award.py`,generate_mesh_v2(auto) vs 藝術家覆蓋率:

   | 件 | 藝術家 IoU/頂點 | 生成 IoU/頂點/mode | 差 | pass |
   |---|---|---|---|---|
   | 光暈 | 0.949/78 | 0.930/35/delaunay-v1 | −0.019 | ✅ |
   | 身體 | 0.949/98 | 0.961/60/delaunay-v1 | +0.012 | ✅ |
   | 左手 | 0.981/80 | 0.956/59/delaunay-v1 | −0.025 | ✅ |

   `overall_pass=true`(margin 0.03)。

## 關鍵結論 / 教訓

- **pipeline 首次對真實生產標的(有藝術家真值)整合驗收通過**(靜態):PSD→件→mesh 覆蓋率達藝術家水準、
  頂點少 40~55%。
- **v2 auto 對 3 blobby 件全正確回退 v1 Delaunay**(aspect<1.2 非-strip)—— strip 判準對真實非-strip 首驗可用。
- **deform 真值缺口(誠實)**:robot mesh 為 weighted 無 deform timeline,`validate_against_real` 位移場閘不適用;
  變形對等對照須先做 **BBW 骨權重**(S3 roadmap 未完項)→ 定為下一步。
- 再次印證「評估器先校準+對真值自驗」(bbox 正規化假性 0.64 被抓修)。

## 產出

- 新增 `tools/mesh_gen/compare_to_award.py`(整合 AC,overall_pass gate)
- 新增 `knowledge/s3-psd-to-award-mesh.md`;更新 knowledge/README 索引、STATE
- 本 log

## 下一步(見 STATE.md)

(1) BBW 自動骨權重 → 綁 Award 對應骨 → deform 對等對照(補本次唯一未覆蓋維度);或
(2) SkelToJson 件→Spine JSON 組裝(命名/尺寸/uv 慣例已就緒)。
