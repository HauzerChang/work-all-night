# 2026-07-25 — session 001

## 本次 bounded chunk

推進 STATE.md 最高優先候選 #1:**S3×S4 端到端 —— PSD 件 → `generate_mesh_v2` → 對照
Award 真實生產 mesh**(有藝術家真值可比,純 CPU 自驅)。

## AC(動手前定)

- AC1:自動生成 mesh 的輪廓覆蓋 IoU ≥ Award 藝術家自身 mesh 覆蓋(同 mask 同法量測)。
- AC2:deform 穩健(0 自交/翻面)—— **執行中發現不適用**(見下)。
- AC3:頂點數在藝術家預算內。

## 做了什麼

1. `psd_slice.py` 切 `robot_parts.psd` → 5 件;取 Award 中為 mesh 的 3 件:光暈/身體/左手。
2. 新增 `tools/mesh_gen/validate_psd_to_award.py`,兩路徑驗證:
   - **Path A(PSD 出身)**:PSD 件 PNG → mesh → 對自身 alpha 靜態 IoU + 格式/0孤兒/0退化。
   - **Path B(藝術家真值)**:atlas region → mesh → IoU 對照 `artist_iou` + 頂點預算。
3. 首跑 coverage 三件皆略低於藝術家(gap 0.008~0.050)→ 追因。

## 關鍵發現

- **`epsilon_frac`(hull 追蹤密度)是不規則/柔邊件的覆蓋槓桿**。掃描證實:glow 在預設 0.008 下
  hull 僅 14 點、IoU 0.929(gap +0.050 fail);eps 0.008→0.001 時 IoU 單調升 0.929→0.992。
  校準 **0.002**:3 件皆 IoU ≥ 藝術家、頂點皆 < 藝術家(73/78、77/98、67/80)→ 用更少頂點達 ≥ 生產覆蓋。
  `min_dist`(內點)幾乎不影響覆蓋 → 再證「覆蓋由邊界/hull 決定」。
- **結構性**:Award 3 件機器人 mesh 皆 **weighted(骨驅動)、9/12 動畫無 deform timeline** →
  真實位移場轉移閘(針對 main_draw unweighted+deform 窗簾)**結構上不適用**。工具明確標
  `deform_gate.applicable=false`(非零位移假性通過)。→ **S3 下一缺口 = 權重指派(BBW)**。

## 結果

- `validate_psd_to_award.py` **exit 0,3/3 overall_pass**。報告存 `knowledge/reports/psd-to-award-e2e.json`。
- 把 `epsilon_frac` 穿進 `generate_mesh_v2.generate`(**預設仍 0.008**,不動既有行為);
  回歸驗證 main_draw curtain_left v2 仍 overall_pass(strip、deform clean)。

## 決策 / 誠實標注

- 未擅改 v1 全域 eps 預設(避免影響 main_draw);校準值只在生產件驗證處使用 + 文件記錄。
- deform 閘標 N/A 而非假通過:骨驅動件的 deform 穩健需 BBW 權重,屬 S3 未建能力。

## 產出

- 新增 `tools/mesh_gen/validate_psd_to_award.py`、`knowledge/s3-psd-to-award-e2e.md`、
  `knowledge/reports/psd-to-award-e2e.json`;`generate_mesh_v2.py` 加參數。
- 更新 `knowledge/README.md` 索引、`STATE.md`(next action / 候選 / 進度摘要)、本 log。

## 下一步(見 STATE.md)

**S3 權重指派(BBW)** — 對骨驅動件輸出 weighted mesh,對照 Award 藝術家權重(有真值、純 CPU)。
或 `epsilon_frac` 依形狀複雜度(周長²/面積)自適應。
