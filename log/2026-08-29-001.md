# 2026-08-29 run 001 — S5 rig pivot 推斷器(首個能力)+ 真值閘

分支:`claude/spine-main`(依 `prompts/run.md` 步驟 0 釘固定分支)。

## 這次做了什麼(一個有界工作塊)

從 STATE「下一步」挑最高槓桿的自主塊:**S5 rig pivot 推斷**(路線圖標記的「唯一卡死環節」)。
依 RULES「每能力必配評估器」,同一塊內同時交付推斷器 + 自我品質閘。

- **界定子問題**:S5 整體含美術決定,但「關節落在子件與父件的**接觸縫**」是可客觀驗證的一塊。
  只解這塊;軸向精修/手感明確歸美術(A 類,留使用者)。
- **`tools/rig/infer_pivots.py`**:contact-seam 推斷器(child 最靠近 parent 的 q=0.2 分位點質心,確定性純 CPU)
  + Award 機器人 rig 真值 loader(mesh→hull 世界頂點;region→從 atlas alpha 取真實輪廓,套 0.70 打包縮放)。
- **`tools/rig/validate_pivots.py`**:對 Award 3 關節藝術家真值(骨 `4_LEG3/4/5/6` 世界位置)四道校驗。
- **圖** `knowledge/figures/s5_pivot_inference.png`(alpha vs rect 兩面板,推斷紅點 vs 藝術家金星)。
- 在 `check_readiness.py` 註冊 `spine-rig-pivot` 區塊(L2 → HOLD,兩閘 GREEN)。

## 結果(4 AC 全 PASS,`validate_pivots.py` exit 0)

| AC | 結果 |
|---|---|
| AC1 準度(err/軀幹尺度<0.10) | 頭 0.044(22px)/左手 0.021(11px)/右手 0.051(25px);max 0.051 ✅ |
| AC2 勝質心 baseline | 中位 21.6px < 43.0px（左手 11 vs 110）✅ |
| AC3 負對照(random+swap 爆閘) | 0.476 / 0.276(皆 >>0.10)✅ |
| AC4 輸入保真依賴(rect 應爆) | rect max 0.820 vs alpha 0.051 ✅ |

## 關鍵發現

**pivot 推斷品質 = 件輪廓保真**。右手用 region bounding-rect 代理(597×486 大框但 alpha 僅 16% 填滿)
→ 誤差 **406px**;改從 atlas 頁裁真實 alpha 輪廓 → **25px**;頭 66→22px。左手本就是 mesh(真 hull)→ 直接 11px。
即接觸縫演算法本身正確,失敗全來自輸入輪廓粗糙 → **「改輸入契約比硬攻演算法划算」在 rig 階段再現**。

## 誠實限制 / 下一步

- 僅單一 rig(robot,3 關節)驗過 → `spine-rig-pivot` **L2 HOLD**(端到端 cap 仍 L0)。
- 達 L3 需:(a) 多 rig 真值(Award 其他角色鏈 `1_OMG`/`2_SUP`/`3_MEG`);
  (b) pivot→bone 父子樹**寫入 `build_spine`**(目前每件綁 root,無關節鏈);(c) 肢體父子樹自動推斷。
- 父子樹目前取自分析器/先驗(非本器推)。軸向精修=美術(A 類)。

## 產出檔案

- 新增:`tools/rig/infer_pivots.py`、`tools/rig/validate_pivots.py`、`knowledge/s5-rig-pivot-inference.md`、
  `knowledge/figures/s5_pivot_inference.png`。
- 更新:`STATE.md`、`PLAN.md`、`knowledge/README.md`、`tools/check_readiness.py`(註冊 S5 區塊)。

## 下一個 session 接手

從 STATE「下一步動作」的建議 (1):S5 續推 L3 —— 把 pivot→bone 父子樹寫進 build_spine + 多 rig 真值。
