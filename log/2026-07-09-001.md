# 2026-07-09 — session 001

## 這次做了什麼(S3×S4 端到端:對照 Award 真實 mesh — 里程碑)

推進 STATE.md 最高優先 chunk #1:把 S4(PSD 切件)與 S3(mesh 生成)串成端到端,並首次對
**真實生產 mesh 的 ground truth**(`Award.json` 機器人 3 件 weighted mesh)做覆蓋率對照。

### AC(動手前定的可檢查目標)
- 對 robot_parts.psd 的 mesh 件(光暈/身體/左手,Award 中為 mesh)跑 `generate_mesh_v2`。
- 我方生成 mesh 的三角覆蓋率(IoU vs 件 alpha)≥ 藝術家真實 mesh 覆蓋率 − margin(0.02)。
- 評估器須以負對照證明有鑑別力。

### 做法 / 產出
- 新增 `tools/mesh_gen/validate_robot_mesh.py`:PSD 切件 → `gen_v2(auto)` → 我方 IoU vs 藝術家 IoU。
  藝術家 baseline = Award mesh 的 region-local `uvs`×(件W,H) 光柵化覆蓋率。
- 新增 `knowledge/s3-robot-mesh-vs-award.md`、`knowledge/figures/robot_mesh_vs_award.png`(疊圖)。

### 結果:3 件全 PASS
| 件 | 我方 | 藝術家 | 我方 IoU | 藝術家 IoU |
|---|---|---|---|---|
| 光暈 | 35v (v1) | 78v (weighted) | 0.9331 | 0.9486 |
| 身體 | 60v (v1) | 98v (weighted) | 0.9660 | 0.9477(反超) |
| 左手 | 59v (v1) | 80v (weighted) | 0.9642 | 0.9768 |

### 關鍵發現
- **塊狀件正確回退 v1 Delaunay**:三件 aspect 0.84~1.12,非高瘦條狀 → auto 不觸發 strip。
  確認「條狀件用 strip、塊狀件用 Delaunay」的分派符合預期。
- 我方 mesh **頂點少一半以上**即達同級靜態覆蓋率(藝術家密頂點是為 weighted 變形平滑,非覆蓋)。
- 獨立佐證 **Award mesh uvs 為 region-local 且方向正確**(uvs×件尺寸疊件 alpha IoU 0.95~0.98;整頁 UV 會近 0)。

### 負對照(評估器可信)
- 交叉配對:對角 0.949/0.948/0.977,離對角掉到 0.48~0.58。
- UV 垂直翻轉:Δ 0.34~0.52。

### 誠實限制
- Award 這 5 件**無 deform timeline**(靠骨骼權重變形)→ 本次只驗靜態覆蓋,未驗變形穩健。
- 我方 mesh 為 unweighted;藝術家為 weighted。本次證明的是**拓樸/覆蓋率**層級可行,不含權重。

## 分支
- 本 session 在 `claude/vibrant-franklin-i4hvsx`(harness 指定);與 `claude/zealous-noether-y2ecwu` 內容一致。
  推回同分支。

## 下一步(見 STATE.md)
1. ✅ 本 chunk 完成。
2. **SkelToJson**:把命名慣例 + size+2px + mesh/region 分派 + 生成 mesh 組裝成可載入 Spine JSON(端到端 PSD→Spine)。
3. BBW 權重把 S3 mesh 升成 weighted;或補塊狀件的 deform 閘(用 Award weighted 動作幀)。
