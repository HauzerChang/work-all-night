# 2026-07-09 — session 001

## 這次做了什麼(S3 端到端對第二個生產骨架 Award 驗收 — 候選 #1)

推進 STATE 的最高優先候選 #1:**PSD件 → S3 mesh → 對照 Award 真實 mesh**,把 S3+S4 串成端到端
並對「另一個生產骨架、另一類 mesh」做泛化驗收。

### AC(動手前定的可檢查目標)
- Award 的 3 個 mesh(光暈/左手/身體)用 v2 生成,IoU **達到或超過藝術家手做 mesh 基準**(逐件真值)。
- 從 `robot_parts.psd` 端到端切件→生成,確認 S4 輸出可無縫餵 S3(self-IoU 與 atlas 區塊一致)。

### 過程與發現
1. **探查 Award 3 mesh 性質**:光暈/左手/身體皆為 **weighted(多骨)**、**無 deform timeline**(靠骨骼剛體位移)
   的 blobby 團塊件 —— 與 main_draw 的 unweighted 直條窗簾/陰影**完全不同類型** → 真正的新泛化,非重複。
2. **變形閘不適用 → 修 validator**:直接跑 `validate_against_real` 崩潰(對 weighted 變長 `vertices` 硬跑
   `real_deform_field` shape mismatch)。加 `has_deform()` 偵測:無 deform 的 slot 標 `AC_real_deform=N/A
   (rigid/bone-driven)`,只用 IoU 判定。
3. **IoU 欠取樣 → 調 eps**:v2 `auto` **正確選 delaunay**(非 strip),但舊預設輪廓簡化 `eps=0.008`(為
   main_draw 簡單形狀所調)對真實/羽化輪廓欠取樣,3 件 IoU 全低於藝術家。掃描 eps:**0.002 是甜蜜點**
   —— 3 件全過且頂點數(73/67/77)≦ 藝術家(78/80/98),覆蓋率追平/勝出但更精簡。`epsilon_frac` 相對周長
   →尺度不變,設為 v2 delaunay 分支預設。
4. **端到端 PSD→件→mesh**:`psd_slice` 切 `robot_parts.psd`(件尺寸與 Award mesh 吻合,+2px),
   直接生成 self-IoU 0.98~0.99,與 atlas 區塊(0.70 縮小)一致 → S4 輸出可無縫餵 S3。

### 驗證結果(overall_pass 全 True)
| Award mesh | 藝術家基準 | v2 IoU (eps=.002) | nv (藝術家) | deform |
|---|---|---|---|---|
| 光暈(羽化) | 0.9795 | **0.9832** | 73 (78) | N/A |
| 左手 | 0.9681 | **0.9913** | 67 (80) | N/A |
| 身體 | 0.9760 | **0.9926** | 77 (98) | N/A |

main_draw 4 strip mesh 回歸重驗:不受影響(仍全過,deform si=0)。

## 關鍵結論
- **S3 v2 泛化到第二個生產骨架**(里程碑):對 weighted/剛體/blobby 件,auto 選模正確,IoU 追平藝術家且更精簡。
- **S3+S4 端到端打通**:從真實 PSD 切件即可生成合格 mesh。

## 產出
- 改 `tools/mesh_gen/validate_against_real.py`(`has_deform()` + rigid mesh 走 IoU-only 路徑)
- 改 `tools/mesh_gen/generate_mesh_v2.py`(delaunay 分支 `eps` 參數,預設 0.002,含 CLI `--eps`)
- 新增 `knowledge/s3-award-rigid-mesh.md`;更新索引/STATE;本 log

## 下一步(見 STATE.md)
最高槓桿:**S3 加權(BBW)+ SkelToJson 寫回** —— 目前生成 unweighted,Award 真實為 weighted;
要真正「生成能裝進真實骨架的 mesh」需權重指派 + 寫回 Spine JSON。純 CPU 可自驅(骨架資訊讀 Award.json)。
