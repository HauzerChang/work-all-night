# 2026-07-03 — session 007

## 這次做了什麼(驗收測試:能力 1 PSD 切圖,使用者提供新資產)

使用者上傳 `dj_cat.psd`(DJ 貓,772×427,3 扁平圖層:身體/DJ台範例圖/右手1)開始逐項人為驗收。

### 結果:PASS(修 1 個真實 bug 後)
- **抓到邊界 bug**:DJ台圖層 bbox 超出畫布(left=-1、bottom=529>427)→ `reassemble` 的
  numpy 覆蓋累計 shape 炸掉。先前兩份生產 PSD 圖層都在畫布內沒踩過。
  修:覆蓋累計裁到畫布(PIL paste 本來就會裁)。回歸 robot/Symbol_Ww 不受影響。
- 修後閘:重組 premult MAE **0.0021**、alpha MAE 0、孤兒 0 → overall PASS。
- 切件視覺確認:三件完整(身體含舉起的手掌、被 DJ 台遮住的下身有畫全;台;刷碟的右手)。
- 遮擋分析(供後續補圖/骨架測試):身體被上層遮 7.6%、DJ台 1.4%、右手 0%。
- 過程小教訓:視覺化面板 PIL(RGBA)/cv2(BGRA)通道序不一致 → 件的顏色 R/B 對調(視覺化 bug,
  非切圖 bug),修正後確認。

### 產出
- `assets/dj_cat.psd`(新測試資產)、`psd_slice.py` 畫布外圖層修正、
  `acceptance/renders/dj_cat_slice_check.png`(composite + 三件對照圖)、本 log。

### 驗收狀態
- ✅ 能力 1:PSD 切圖(dj_cat + robot + Symbol_Ww 三資產)
- 其餘 9 項待使用者續測。dj_cat 可一路測到能力 10(它有天然的「身體/道具/手」結構,
  適合走完整條 pipeline:骨架草案 → 組裝 → 權重 → pose)。
