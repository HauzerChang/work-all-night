# 2026-09-04 run 002 — S1 candidate 0h:cascade 跨件錯開 reveal(第一個跨件時序簽章)

分支:本 session 容器起於隨機工作分支 `claude/focused-dirac-cadjtz`,起點 commit `ca1a17f`(= candidate 0g,
與 `origin/claude/spine-main` tip 相同)。本 run 依 harness「Git Development Branch Requirements」在
**`claude/focused-dirac-cadjtz`** 累積並 push(此分支為 spine-main 的線性延續、strict superset,tip 一致 →
可 fast-forward 併回 spine-main)。⚠️ 與 `prompts/run.md` 步驟 0「釘 spine-main」的張力:本 session 的 harness
規則明確指定 focused-dirac-cadjtz 且「未經明確許可不得 push 到其他分支」,故遵之;續跑連續性靠「本分支 = spine-main
延續」保證,合流為 no-conflict fast-forward。

## 這次做了什麼(一個有界工作塊)

從 STATE「下一個自然小步」建議 **(cascade)**。0d–0g 的所有主秀 beat(pulse/hit/reveal/combo/anticipate_hold)
簽章都定義在**單一件**的時間包絡上;大獎主秀常見的「件依序 pop-in、掃出一道波」是**跨件(cross-part)時序**,
單件包絡簽章抓不到(同時 reveal 與錯開 reveal 的**每件**包絡可完全相同,只差件間相位)。cascade 補這個維度,
與 0g 的單件時序簽章互補。依 RULES「每能力必配評估器」同塊交付**生成器 + 品質閘 + 負對照**。

- **`tools/analyzer/beat_templates.py`**(擴充):
  - `gen_cascade_part(role, side, radial, onset, width, T)`:單件在給定 onset 的錯開 reveal 包絡
    (collapsed hold → burst overshoot → 阻尼回擺 → identity;首 collapsed 尾 identity)。
  - `CASCADE_KEYWORDS` + `DUR["cascade"]=1.4`。
- **`tools/analyzer/gen_animations.py`**:
  - `_cascade_beat`:件依空間序(bone x)排序,第 rank 件 onset=rank/(n−1)·0.5·T,逐件呼叫 `gen_cascade_part`。
  - `build_animations` 對 `cat=="cascade"` **特判**(不進 `_DISPATCH`,因需全 beat 件集合做件間相位編排);
    非 cascade 分支邏輯完全不變。cascade 加進 `_CAT_KEYWORDS`(置前)+ `DUR.update`。
- **`tools/analyzer/validate_cascade.py`**(新,6 AC):對真實 robot 5 拆件 role 端到端經 `build_animations`。
- `check_readiness.py`:`spine-anim-forge` 新增 cap `cascade_stagger`(L2)。

## 跨件簽章

一支 anim 是 cascade ⟺ ①**stagger spread**(件 scale 峰時間跨度 / dur)≥0.25,②**monotone sweep**
(峰時間沿空間序 bone x 嚴格遞增),二者兼備。

## 結果(`validate_cascade.py` 6 AC 全 PASS,exit 0)

| AC | 判準 | 結果 |
|---|---|---|
| C1 well-formed | cascade finite / 時間嚴格遞增 / JSON round-trip | ✅ |
| C2 reveal 介面 | 首幀所有件 collapsed(scale≈0.02/alpha 0)、尾幀所有件 identity | ✅ |
| C3 每件真峰 | 每件 burst 峰 ≥1.12(實測 1.177–1.344) | ✅ |
| C4 跨件簽章 | spread=**0.50**≥0.25 且峰時間 0.286→0.461→0.636→0.811→0.986 嚴格遞增 | ✅ |
| C5 負對照(5 條) | 見下 | ✅ |
| C6 回歸 reveal | 接上 cascade 特判後 reveal beat 仍為合法 reveal(collapse→identity+真峰) | ✅ |

**C5 負對照(鑑別力)**:正對照真 cascade 具簽章 ✅;**同時 reveal** spread=**0.0** 非遞增→非 cascade ✅
(核心內建負對照:件包絡相同、只差相位);**打亂 onset(逆空間序)** spread=**0.5** 仍大**但峰時間遞減→
monotone FAIL→非 cascade** ✅(**證「單調掃向」是必要條件,非只看跨度**);**單發 hit** identity 起非 collapsed→非 cascade ✅。

## 回歸(未破壞既有)

0f `validate_beat_templates`(6AC)、0g `validate_more_beats`(6AC+9 負對照)、(E) `validate_priors_beats`(5AC)、
0d `validate_anim`(robot bigwin)+ `--selftest`(AC5 負對照全偵測)、`validate_priors`(coverage)、
0e 系列全 **PASS(exit 0)**。`check_readiness` 實跑:`cascade_stagger` GREEN、`spine-anim-forge` 仍 **HOLD**(無誤觸發打包)。

## 關鍵發現

- **cascade 的鑑別子是「件間相位」而非「件包絡形狀」** —— 第一個必須看**多件關係**的簽章;同時/錯開 reveal 的
  單件包絡可完全相同,只有跨件峰時間分佈能分辨。
- **「跨度大」不足以判 cascade,還要「單調掃向」** —— 打亂 onset 的負對照 spread 一樣大卻被正確判否;
  monotone sweep 才鎖定「有向的掃動」而非「雜亂錯開」。
- **空間序(bone x)是 pivot** —— onset 依 x 指派 → 簽章可直接用 bone x 復現空間序驗證,無需額外標註。

## 誠實限制 / 下一步

- 主秀節奏無唯一正解(先驗手感),閘驗**客觀結構簽章非美感**;掃動方向/快慢手感留使用者(A 類)。
- cascade 目前由 fixture storyboard 驅動;若要 `build_spine --animate` 直出,需比照 (E) 併入 `genre_priors`
  → 新增 STATE 建議 **(H')**(與 (H) combo/charge 併入為同一收尾動作,可一併做)。
- `spine-anim-forge` 定 L2 → **仍 HOLD**(運動基元先驗、單一真值資產,防固化;達 L3 前不打包)。

## 產出檔案

- 新增:`tools/analyzer/validate_cascade.py`、`knowledge/s1-cascade-stagger.md`、`knowledge/figures/s1_cascade.png`。
- 更新:`tools/analyzer/beat_templates.py`(gen_cascade_part + CASCADE_KEYWORDS + DUR)、
  `tools/analyzer/gen_animations.py`(cascade 註冊 + _cascade_beat + build_animations 特判)、
  `tools/check_readiness.py`(cap)、`STATE.md`、`knowledge/README.md`。

## 下一個 session 接手

從 STATE「下一步動作」:(H)/(H') combo/charge/cascade 併入 genre 先驗庫讓 `build_spine --animate` 直出(同步
`validate_priors` 覆蓋、勿動已驗先驗)、(G) 關節 pivot 接 keyframe(rigid rotation-about-pivot)。
S5→L3 仍待多 rig 真值(使用者資源,C 類)。
