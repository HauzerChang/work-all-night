# 2026-08-20 — session 001

## 這次做了什麼(bounded chunk:STATE 候選 0d)
**分鏡 → 動畫 keyframe** —— 讓 `build_spine.py` 產出的素材從「只有 setup pose」變成「會動」。

### 產出工具
- `tools/analyzer/gen_animation.py`:把分析器 #3 動作分鏡的每個 beat(In/Loop/Out …)× 每件結構角色
  (body/head/limb/effect)**確定性**合成 Spine 3.8 `animations` 的 bone/slot timeline。
  - In:body scale overshoot+彈入、limb 大幅反向甩入、effect scale 峰值+slot alpha 0→ff。
  - Loop:呼吸/微點頭/末梢擺盪(相位錯開,sin 取樣保證非零+seamless)、effect 脈動;**首尾同值**。
  - Out:scale→0 + alpha→0(effect 收斂更快)。
  - 緊湊 bezier 散鍵 `{curve,c2,c3,c4}`(landmine #7)、末幀 linear 尾;bone/slot 名與 build_spine 一致。
- `tools/analyzer/validate_animation.py`:量化閘 AC1(結構合法)/AC2(語意幅度分帶+Loop seamless)/
  AC3(角色分化)/AC4(非平凡)。**含 `--selftest`:5 負對照 + 1 正對照**先確認閘可信。
- 整合進 `build_spine.py`:預設 `animate=True`(端到端 build 即含動畫),`--no-animate` 可關。

### 自我驗證(不靠肉眼)
- 閘 self-test:5 壞(非 seamless loop / time 逆序 / 壞 curve / In 無大幅 / 參照不存在 bone)全被抓、
  1 正對照通過 → 閘可信。
- robot_parts(5件)+ Symbol_Ww(18件)× slot_bigwin/slot_symbol:**動畫閘全 PASS**(14~15 檢查)。
- round-trip 迴歸:`validate_build.py` 靜態 setup pose 仍 PASS(premult MAE 0.031)→ 動畫不破壞靜態。
- 視覺證據:`knowledge/figures/s1_animation_curves.png`(In overshoot / In 左右反向甩 / Loop 相位錯開「X」/ Out 歸零)。

### 閘揪到並修的 2 個真實 bug(過程)
1. Loop 首個 limb(idx=0)因三角波在 0/½ 取樣點為 0 → 零幅不動 → 改 sin 相位取樣、末幀顯式對齊。
2. 多 limb 資產(Symbol_Ww 8 limb)In 中幀時刻 clamp 到 dur → time 非嚴格遞增(AC1 抓到)→ 改用剩餘空間比例分配。
教訓:看 metrics 範圍值(非只 pass/fail)才揪得出「僥倖過關」的退化 case。

## 產出檔案
- 新工具 `gen_animation.py`、`validate_animation.py`;改 `build_spine.py`(整合動畫)。
- 新知識 `knowledge/s1-storyboard-to-animation.md` + 索引;圖 `s1_animation_curves.png`。
- 更新 `STATE.md`(里程碑/下一步/進度)。

## 下一步(見 STATE.md)
二選一:候選 2(S3 weighted mesh + BBW 權重,補變形平滑度未驗維度)或
候選 0e(關節 pivot 推斷 → limb 繞真實關節旋轉 + 解鎖 S5)。皆純 CPU 可自驅。
誠實界定:動作幅度/相位是類型先驗參數(非影片觀測);實機播放視覺未驗(spine-webgl CDN 被網路政策擋)。
