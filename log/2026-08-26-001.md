# 2026-08-26 — session 001

## 這次做了什麼(候選 2 前置閘:weighted-mesh 骨骼變形評估器)

接手分支 `claude/vibrant-franklin-kgluel`。STATE 最高優先候選 2(S3 weighted mesh + BBW)體量大,
依 RULES 拆解,本 session 只做其**前置閘**(RULES「每能力必配評估器」):weighted mesh 在骨骼動畫下的
變形品質閘。這補上 `s3-robot-mesh-vs-award.md` 標記的唯一未驗維度。

### AC(先定)
1. 純 Python 重現 Spine 3.8 骨骼世界變換 + weighted 頂點蒙皮;setup pose 3 件幾何乾淨。
2. 真實 In/Loop/Out 動畫逐幀跑,美術真值 mesh 應乾淨(建立 benchmark)。
3. 負對照(破壞權重)被抓到 → 閘有鑑別力。

### 產出
- `tools/mesh_gen/weighted_deform_eval.py`:
  - Spine 3.8 骨骼 `updateWorldTransform`(normal mode)+ 緊湊 bezier 內插 + weighted 蒙皮。
  - `eval_weighted_mesh()` 逐幀跑 self-intersection/flip/degenerate(沿用 `deform_eval` 的檢查)。
  - 正對照 + 2 種負對照(hard-1-bone / shuffle-bind)+ 判定 `evaluator_discriminative`。
- `knowledge/s3-weighted-deform-evaluator.md` + 索引;更新 STATE/PLAN。

### 結果
- **結構件 左手/身體:In/Loop/Out 全乾淨**(0 自交/翻面/退化)→ 重現正確。
- **shuffle 負對照:三件全被抓到**(si 155~467)→ 鑑別力。`evaluator_discriminative = True`。
- **光暈(軟發光層):In 折疊是真值不是 bug** —— flip 空間叢聚於高倍率骨 4_LEG6(In 期 scale=1.667,
  影響 49/78 頂點),鄰件共用同骨鏈卻全乾淨 → 排除管線 bug。設計啟示:嚴格幾何閘只適用結構件。

### 關鍵修正(踩雷)
- Spine timeline 省略的 keyframe 值取該 timeline **中性值**,不是 0:**scale 缺鍵=1.0 非 0**。
  一開始當 0 → 內插到 0 → mesh 面積比塌到 0.0(`Award_Legend_In` 全件假性退化)。改 per-timeline
  預設後結構件立即全乾淨。

## 誠實界定 / 限制
- 未解 transform-constraint(`4_LEG7→4_LEG8`);身體用到 4_LEG8 但實測仍乾淨,受約束骨擴充時需補。
- 閘量化的是「拓樸不壞」(自交/翻面/退化);連續平滑度(抖動/加加速度)未量,屬後續若需。

## 下一步(見 STATE 候選 2 主體)
在我方生成 mesh 拓樸上做「內部取樣密度控制 + BBW 權重」,用本閘量化 ≥ 美術基準。
建議先做**左手**(2 骨最單純)端到端:生成拓樸→BBW→`eval_weighted_mesh`→對照。純 CPU 可自驅。
