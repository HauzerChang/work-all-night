# 2026-08-26 — session 001

## 這次做了什麼(bounded chunk:候選 2 的評估器側)

**目標(AC-first)**:補上前一里程碑(`s3-robot-mesh-vs-award.md`)明列的唯一未驗維度 ——
weighted mesh「靜態覆蓋率 IoU PASS ≠ 骨骼變形平滑度對等」。依 RULES「每能力必配評估器」,
先做出可量測、且**自我可信**的 weighted-mesh 骨骼變形閘,再用藝術家真值校準門檻。

### 產出:`tools/mesh_gen/weighted_deform.py`(純 CPU)
- 重現 Spine 3.8 **normal-inherit** bone world transform 合成 + `rotate/translate/scale/shear`
  timeline(相鄰 keyframe 線性內插)+ **LBS**(每頂點權重正規化混合)。
- 幾何閘重用 `deform_eval.check`;新增嚴重度指標 **`flip_area_frac`**(翻面三角面積÷總面積)。

### 可信度(checker validation)—— 通過
- **仿射協變 2.3e-13(機器精度)**:對所有骨 world 左乘同一剛體 R,skin 後每點精確 = R·原點 → LBS 數學正確。
- 校準教訓:未正規化時誤差 = `(Σw−1)·T` ≈ 1.25e-4,完全對得上 JSON 權重精度(~1e-5),證明非 bug。
- setup pose 7/7 mesh 乾淨;Legend_In settle 幀位移回 ≈0 → bone chain 對齊 setup(額外自洽證據)。

### 藝術家真值校準(Award 7 weighted mesh × 12 動畫)
| 類型 | mesh | 最大位移 | max flip_area_frac |
|---|---|---|---|
| 實心(有內部頂點) | OMG/megawin1/megawin2/左手/身體 | 238~328px | **0.0000** |
| 實心(sliver 內三角) | superwin | 363px | 0.0074 |
| 邊界唯一 additive glow | 光暈(hull=nv=78) | 627px | 0.1386 |

## 三個結論
1. **內部取樣密度 = 變形平滑度槓桿(量化證實)**:實心角色 mesh 被拉到 300px+ 仍 0 翻面 → 正是上次「推測」的證明。
2. **binary 0-fold 閘對 weighted 是錯的**:藝術家真值 superwin 也有 0.7% sliver 翻面 → 改用 `flip_area_frac` 嚴重度。
3. **mesh 類別決定閘**:additive glow 自交 13.9% 但視覺無害 → glow/halo 不套折疊閘;實心角色才套。
   建議門檻:實心件 `flip_area_frac ≤ 0.01`。

## 產出清單
- 新工具 `tools/mesh_gen/weighted_deform.py`;圖 `knowledge/figures/weighted_deform_calibration.png`
- 新知識 `knowledge/s3-weighted-deform-evaluator.md` + 索引;`requirements.txt` 加 matplotlib
- 更新 `STATE.md`(里程碑 + 候選 2 拆成評估器側 ✅ / 生成側 ⬜ + progress log)

## 下一步(見 STATE 候選 2 生成側,最高優先)
用 `weighted_deform.py` 當自主收斂閘,做 **S3 自產 weighted mesh**:
(i) `generate_mesh_v2` 加內部取樣密度控制;(ii) BBW 權重(scipy cotangent Laplacian + 邊界約束 QP)綁 Award 骨架;
(iii) 對同批動畫跑閘,要求實心件 `flip_area_frac ≤ 0.01`。真值全在 `Award.json`,純 CPU 可自驅。
