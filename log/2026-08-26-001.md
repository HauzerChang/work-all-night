# 2026-08-26 — session 001

## 這次做了什麼(候選 2 子步驟 2a:weighted-mesh 變形閘)

推進 STATE 最高優先候選 2(S3 weighted+BBW)的**第一個 bounded chunk**。依 RULES「每能力必配評估器」,
先做「weighted mesh 骨骼變形品質」的**可信變形閘**,生成才有自主收斂判準。

### 新工具
- `tools/mesh_gen/skinning_eval.py`:**Spine 3.8 pose 引擎 + Linear Blend Skinning 評估器**。
  - `Skel`:讀 bones 階層 + setup 局部值 → 任一動畫任一時刻算每骨 world matrix(normal transform mode,
    rotate/translate/scale/shear 疊加;keyframe 精確、相鄰間線性內插細分)。
  - `skin_deform`:weighted vertices(`[n,boneIdx,bindX,bindY,w,...]`)→ LBS 世界頂點。
  - 幾何閘重用 `deform_eval`(自交/翻面/退化/面積比)+ 新增平滑度 `edge_cv`/`area_cv`。
  - `gate_against_baseline`:**校準式相對閘**(生成 mesh 變形指標須不劣於美術基準線+margin)。
- `tools/mesh_gen/validate_weighted_deform.py`:閘的**雙向可信度自驗**(正:自一致;負:破壞綁定被抓)。

### 結果
- `validate_weighted_deform.py`:Award 7 件美術 weighted mesh **正對照 7/7 自一致、負對照 7/7 抓到破壞** → exit 0。
- 負對照力度:身體件交換相鄰頂點綁定 → si 由 0 暴增到 669。
- 視覺 `knowledge/figures/weighted_deform_lbs.png`:身體/左手/光暈/superwin 在真實動畫下皆 coherent skinning。
- 迴歸:`validate_against_real.py --gen v2`、`compare_robot_mesh.py` 皆仍 PASS(只新增檔,未改既有)。

## 關鍵發現 / 校準教訓(又一次「絕對閾值 miscalibration」)
初版用絕對 `si==0 & flips==0` → 7 件有 2 件美術真值**自己就非零**(`_checker_validated=False`)。
逐幀定位確認**在精確 keyframe 時刻**發生(非內插假象)、引擎正確,是**閘定義錯**:
- **機器人拆件/光暈**(si=71):軟邊發光 mesh 被展開骨拉扯必然自重疊(additive,視覺無破)。
- **superwin_角色**(si=76):(a) `2_SUP2` Super_In t=0 有 `scale=0.396` 蓄力擠壓(area_ratio→0.14,微小不可見);
  (b) idle loop 一撮 sliver 三角(setup 面積 −12~−18 摺疊細節)翻面。
→ 都出貨且視覺正常。改**錨在美術基準線的相對閘**(與 compare_robot_mesh IoU−0.03、deform_eval 自一致性一致)。

## 產出
- 新工具 `skinning_eval.py`、`validate_weighted_deform.py`;圖 `weighted_deform_lbs.png`
- 新知識 `knowledge/s3-weighted-deform-evaluator.md` + 索引;更新 STATE(里程碑+候選 2 拆子步驟)

## 下一步(見 STATE 候選 2 子步驟 2b/2c)
- **2b**:BBW/harmonic 權重生成 — 對 S3 生成 mesh 依骨骼算平滑權重 → weighted vertices → 用本閘對照美術基準線。
- **2c**:內部取樣密度旋鈕(Poisson/格點)— 量 edge_cv/area_cv 逼近美術(身體 98v 密內部點服務變形平滑度)。
