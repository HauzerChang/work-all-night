# 2026-08-26 — session 001

## 這次做了什麼(bounded chunk:補上唯一未驗維度)

### 研究:S3/S2 weighted-mesh 骨骼變形評估器
STATE 反覆標記的唯一未驗維度 = weighted(綁骨)mesh 的**骨骼變形平滑度**(靜態 IoU 不涵蓋;
`deform_eval` 只處理 unweighted 的逐頂點 offset)。本次補上,並遵守「每能力必配評估器」——
先把 weighted 變形的**自我品質閘**做出來且自驗可信,才有資格下一步做 BBW 生成器。

- 新增 `tools/mesh_gen/weighted_deform_eval.py`:忠實重現 Spine 3.8
  - 骨骼世界變換(Award 77 骨全 normal transform、無 shear、parent-before-child → 精確可重現);
  - 動畫 timeline:rotate(加)/translate(加)/scale(乘),keyframe + substep 線性取樣;
  - linear blend skinning(LBS);
  - **可見性 gating**:讀 slot color timeline 的 alpha + attachment timeline,只評估可見幀(雷點 #3);
  - `MeshChecker`:向量化自交/翻面/退化(原純 Python O(E²) 掃全 pose 會 timeout → numpy 重寫)。
- 新增 `tools/mesh_gen/validate_weighted_deform.py`:三 AC 自驗閘,對 `assets/Award.json` **全 PASS**。

### AC 結果(對 Award 7 個真實 weighted mesh)
- **AC1 skinning 正確性**:7/7 setup pose 重現皆有效幾何(0 自交/0 退化)= 骨/LBS 正確性硬自檢 → PASS。
- **AC2 可見性 gating**:superwin `Super_In` t=0 alpha=0 的 76 自交髒幀被正確排除;各 hero mesh 經 gating
  只落在自己 tier 的 3 動畫 → PASS。
- **AC3 鑑別力(負對照)**:藝術乾淨的 `身體` 腐化骨綁 → shift-bone 506 自交、jitter±100 1602 自交/2 翻面
  全被抓;乾淨基準維持 → PASS。

### ⚠️ 關鍵校準發現(誠實界定)
**「所有生產 weighted mesh 可見幀都拓樸乾淨」= 假**。實測可見幀:乾淨 5/7
(OMG/megawin1/megawin2/機器人左手/機器人身體);非乾淨 2/7 —— `superwin_角色`(hero,112v)在其
擠壓/縮放 In 可見階段**keyframe 上就自交**(非內插假象,area_ratio 到 1.31),`Super_Loop` 殘 1 自交+1 翻面;
`光暈`(soft halo)`Legend_In` 可見殘 4 自交。hero/halo 靠貼圖+快速運動遮掩,藝術家容忍。
→ **不存在「所有藝術 mesh worst==0」的通用閘**(同型於先前 stress_field miscalibration 陷阱);
評估器可信度來自 AC1(skinning 正確)+ AC3(鑑別力)+ AC2(gating 語意),report key 已區分
`_setup_all_valid`(正確性)vs `_artist_clean_visible`(觀察值)。

### 回歸
- unweighted `deform_eval` main_draw benchmark 仍 `_checker_validated=True`;
- `validate_against_real.py --gen v2` 4 mesh 仍 `overall_pass=True`。本次純新增檔,無改動既有工具。

## 產出
- 新工具 `weighted_deform_eval.py`、`validate_weighted_deform.py`
- 新知識 `knowledge/s3-weighted-deform-evaluator.md` + 索引
- 更新 `STATE.md`(里程碑 + 下一步改指向 BBW 生成器)

## 下一步(見 STATE.md 候選 2)
**BBW 權重生成器**:評估器前置已備齊。給定件+骨架子集(Award 該部位骨綁為真值),生成 weighted mesh,
用本評估器對照**同部位同動畫**可見乾淨率(乾淨部位要求 0 自交;hero/halo 要求不比藝術差)。
純 CPU(scipy biharmonic)可自驅;建議先從 `機器人拆件/身體`(3/3 乾淨)對照起。
