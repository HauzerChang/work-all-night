# 2026-09-26 run 001 — S1 體積守恆反相雙軸 shear(candidate G-4''''''-vol)

## 目標(bounded chunk)

推進 STATE「下一步」首選候選 **(G-4''''''-vol) volume-conserving twist**:補上 twist 系列一路明列的
honest boundary —— 反相雙軸 shear 會改變面積(`det=cos(shearY−shearX)≠1`),加耦合 scale 使完整
Spine local 矩陣行列式 det≡1(擰而不變面積)。

## AC(可檢查,先定後做)

1. 生成器 `gen_twist_vol` 產出反相雙軸 shear + 耦合均勻 scale,使每個 shear 極值幀完整
   `det(M)=sx·sy·cos(shearY−shearX)≈1`(|det−1| 小)。
2. 首尾 identity(shear (0,0)、scale (1,1))→ 可插 Loop 間。
3. 與 squash 乾淨分離:twist_vol 守**完整矩陣**(scaleX·scaleY>1)、squash 守 **scale 子塊**(≡1)——
   負對照證「守子塊 ≠ 守完整矩陣」。
4. 端到端 `build_spine --shear-pivot` 在 scale+雙軸 shear 同時驅動下 pivot 殘差小(< 0.5px)。
5. 負對照鑑別力(純-twist/定值-scale/squash-式皆 FALSE、正控 TRUE)。
6. 回歸零破壞(twist 及變體逐位元不變、shearY 隔離、加性)。

## 做了什麼

- **關鍵幾何推導**:真實 Spine local `det(M)=a·d−b·c=sx·sy·sin(90+shy−shx)=sx·sy·cos(shearY−shearX)`
  (與 rot 無關)。純 twist(sx=sy=1)⇒ det=cos((1+φ)·shearX)<1。令 sx=sy=s,det=s²·cos(shy−shx)≡1
  ⟺ **s=1/√(cos(shearY−shearX))**(均勻各向同性面積修正)。
- `beat_templates.py`:新增 `gen_twist_vol` + `_twist_vol_env` + `TWIST_VOL_KEYWORDS`。
- `gen_animations.py`:註冊 `_DISPATCH["twist_vol"]` + `_CAT_KEYWORDS`(置 twist 前,exact 精確路由)。
- `genre_priors.py`:slot_bigwin 加 twist_vol beat + `_BIGWIN_ROLES["twist_vol"]`(additive)。
- `tier_variants.py`:`SHEAR_CATS += {twist_vol}`(**刻意不進** MAIN_SHOW_CATS,honest boundary)。
- `validate_twist_gen.py`:TW6c shearY 隔離改認 `{twist, twist_vol}`。
- **新** `validate_twist_vol.py`:6 AC(VT1–VT6,VT4 = 完整矩陣體積守恆 crux)。
- `check_readiness.py`:新增 cap `twist_volume_conserving` L2(spine-anim-forge)。
- `knowledge/s1-twist-volume-conserving.md` + 索引。

## 自我驗證結果

- `validate_twist_vol.py` **6 AC 全 PASS**:VT1 shearX 峰 16°/shearY 峰 11.2°/scale 峰 0.06;
  VT4 每極值 |det−1|≤8.8e-5、scale 均勻(gap 0)、scaleX·scaleY 峰 1.045–1.124>1、純-twist 縮小
  0.044–0.111;VT5 pivot 殘差 0.004–0.016px vs 負對照 8.5–29.5px;VT6 squash-式守衛
  (prod≈1 但完整 det≠1)乾淨分離。
- 端到端 `build_spine --animate --tier-variants --shear-pivot` 直出 `twist_vol`(**無** `twist_vol__tier`,
  honest boundary);`twist` 及 `__{tier}` 逐位元不變。
- **回歸**:`check_readiness.py` 全跑 → **23 閘全綠、0 RED**(退出 0)。

## 決策 / 發現

- **修好長期潛伏 bug(repo health)**:regression 揭出 `validate_analyzer_award.py` 自 **candidate E**
  起就 RED(2 caps 共用它)—— 其分鏡結構判準用**過時的精確相等** `proposed_beats == {In,Loop,Out}`,
  但先驗庫自 E 起**刻意**多提 PROPOSAL 節拍(validate_priors 早已誠實列 prior_beats_unused)。改為
  **beat 召回**語意 `beat_kinds ⊆ proposed_beats`(真值全被涵蓋、多提 PROPOSAL 不算失配,對齊①件召回與
  priors 覆蓋率)→ 該閘復綠。**先前多個里程碑聲稱「N 閘全綠」時,這 2 個 cap 實際 RED 未被察覺**
  (因它們屬 HOLD 區塊、check_readiness 輸出很長)。
- **副作用(C 類決策點,待使用者拍板)**:修好 analyze_target 閘後,`spine-asset-forge` 區塊
  **從 HOLD → READY**(其生成器本就全綠,僅被 stale 驗證器誤擋)。依 RULES/run.md **打包/sync skill 屬
  C 類需使用者拍板**,本 session **不自動打包**;僅記錄此狀態變更供使用者決定是否產出 `skills/spine-asset-forge/`
  套件並 sync。
- **關鍵發現**:面積守恆有「守誰」之分(scale 子塊 vs 完整矩陣);至此 Spine local 一般仿射四自由度
  (rotate+非均勻/均勻 scale+雙軸 shear)全被真實 beat 驅動、且面積可依需求守恆。

## 下一步(擇一,皆純自主)

- (J-3) cascade count-aware(波速/散佈/件數隨檔位)。
- (G-4'''''-charge) charge 蓄力段數 count-aware。
- (G-4''''''-vol-tier) twist_vol 接 tier/count-aware(檔位放大須**重算**守恆 scale,非 amplify)。
- (G-4''''''-vol-nonuniform) 非均勻守恆 twist(sx·sy=1/cos 但 sx≠sy = squash×twist 合體)。
- **C 類**:spine-asset-forge 是否打包 skill(見上決策點)。
