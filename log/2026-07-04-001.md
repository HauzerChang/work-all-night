# 2026-07-04 — session 001

## 這次做了什麼(bounded chunk:S3+S4 端到端對真實生產標的驗收)

接續 STATE 最高優先候選 #1:**PSD 切件 → S3 `generate_mesh_v2` → 對照 Award 真實藝術家 mesh**。

### AC(先定)
1. 覆蓋:S3 mesh 對件輪廓覆蓋率 ≥ 藝術家 mesh 覆蓋率 − margin(0.03)。
2. 頂點預算:S3 頂點數 ≤ 藝術家頂點數(更精簡)。
3. 拓樸:0 自交 / 0 退化 / 0 未覆蓋孤島 / 格式合法。
4. 真值可信:藝術家重建對自身 silhouette 覆蓋率須高(自一致性校驗)。

### 做了
1. 新工具 `tools/mesh_gen/compare_award_mesh.py`:對機器人 3 個 mesh 件(光暈/左手/身體)端到端比對。
   - alpha 來源:`atlas_crop.extract`(去旋轉切件)。
   - 藝術家 mesh 重建:實測 Award mesh `uvs` 為 **region-local**(非 page-global);rotate 件軸向有 90° 歧義
     → 用 **8 dihedral 朝向搜尋** 挑與 silhouette IoU 最佳者(不猜方向)。自一致性校驗:三件藝術家覆蓋
     0.975/0.982/0.983(best=r0)→ 真值可信。
2. **發現失敗**:光暈(放射狀高曲率輪廓)FAIL — v1 預設 `epsilon_frac=0.008` 過度簡化(hull=14)
   → `filter_triangles` 挖凹口 → 覆蓋 0.934 + 1 孤島。
3. **修正(1 輪內)**:epsilon 掃描顯示 0.004→0 孤島/0.969、0.002→0.982。改 v1 fallback 為
   **自適應 epsilon** `gen_v1_adaptive`(由粗到細,取達 cover_target=0.96 且 0 孤島中最省頂點者,cap 80v)。
4. **重驗**:3 件全 PASS(光暈 61v/0.969、左手 52v/0.959、身體 61v/0.968,全 0 孤島)。OVERALL PASS。
5. **回歸檢查**:main_draw 4-mesh 基準全 strip 模式(與自適應 v1 隔離)→ curtain_left/right、shadow 重驗全過、
   0 自交(shadow2 與 shadow 共用 region,無獨立 region 名,非回歸)。
6. 產圖 `knowledge/figures/s3-award-mesh-compare.png`(silhouette | 藝術家 | S3 線框,視覺確認重建與覆蓋)。

## 關鍵結論

- **里程碑:S3+S4 首次端到端對「真實生產 spine + 藝術家真值」驗收通過**。「PSD → 件 → mesh」對真實標的可用。
- **S3 改良**:高曲率輪廓需自適應加密 hull;固定 epsilon 不通用。自適應版對高/低曲率件都收斂且維持精簡。
- **方法論**:真值(藝術家 mesh)先做朝向搜尋 + 自一致性校驗才可信;沿用專案「評估器先校驗再判定」守則。
- **實測更正**:Award mesh uvs 為 region-local(先前 `s4-psd-to-spine-real.md` 記為 atlas UV,已於新知識檔更正)。

## 產出

- 新增 `tools/mesh_gen/compare_award_mesh.py`、`knowledge/s3-award-mesh-e2e.md`、`knowledge/figures/s3-award-mesh-compare.png`
- 改 `tools/mesh_gen/generate_mesh_v2.py`(加 `gen_v1_adaptive` + `_coverage_orphans`,`generate` 預設 adaptive)
- 更新 `knowledge/README.md`(索引)、`STATE.md`(進度/下一步/log)、本 log

## 下一步(見 STATE.md)

最高優先:**SkelToJson 組裝** — 把件→Spine attachment 慣例(命名/padding/mesh 用 S3 自適應/剛體用 region+旋轉)
固化成端到端工具,產出可載入 Spine JSON,打通「PSD → 可用 Spine 資產」CPU pipeline。
