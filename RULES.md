# 操作守則與遞迴規則 (RULES)

> 每次 session 開始時，**第一件事就是完整讀完這個檔案**，再讀 `PLAN.md`、`STATE.md`，
> 以及 `log/` 裡最近 1–2 筆紀錄。你沒有上一個 session 的記憶，這些檔案就是你的全部記憶。

## 每次執行的標準流程 (the loop)

1. **載入脈絡**：讀 `RULES.md`(本檔)、`PLAN.md`、`STATE.md`、`knowledge/` 索引、`log/` 最近紀錄。
2. **定位**：從 `STATE.md` 找出「目前階段」與「下一步動作」。
3. **推進一個有界的工作塊**：只做一個合理大小的步驟（見下方「單次工作量」），不要試圖一次做完全部。
4. **記錄發現**：把新學到、新結論寫進 `knowledge/`。
5. **更新狀態**：改寫 `STATE.md`——目前進度、下一步、未解問題、阻塞點。
6. **寫執行紀錄**：在 `log/` 新增一個檔案 `log/YYYY-MM-DD-NNN.md`，摘要這次做了什麼、產出、決策。
7. **commit & push**：用清楚的訊息提交到開發分支，然後**結束 session**（不要嘗試無限長跑）。

## 單次工作量 (bounded chunk)

- 一次 session 推進**一個階段裡的一個明確步驟**即可，目標是「穩定、可續跑」而非「跑很遠」。
- 若一個步驟太大，先在 `STATE.md` 把它拆成子步驟，這次只做第一個。

## 遞迴規則 (recursion)

- **分解**：遇到大目標 → 拆成子目標寫進 `PLAN.md` 或 `STATE.md`，逐一推進。
- **深度上限**：子目標分解最多 3 層；超過代表問題定義太鬆，回頭收斂範圍。
- **分支條件**：只有當一條研究路線出現明確、可驗證的分歧時才開新子目標；避免發散。
- **收斂**：每個子目標都要有「完成條件」。達成就標記完成並回到上層。

## 停止條件 (stopping)

每次執行結束（正常）就停，等下次排程。但若遇到以下情況，**在 `STATE.md` 標記 `BLOCKED` 並在紀錄中說明，然後停**：

- 需要人類決策（方向選擇、授權、外部資源）。
- 連續 2 次執行沒有實質新進展（避免空轉燒額度）。
- 偵測到目標已達成 → 標記專案 `DONE`。

## 品質與誠實

- 不確定就明說，不要捏造研究結論。
- 重要結論要可驗證 / 可追溯到來源或實驗。
- 每次 commit 都讓 repo 維持「下一個 session 能無痛接手」的狀態。
