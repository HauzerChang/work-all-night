# 自主 Spine 工作流 SOP

> 目標：把「影片／需求 → 完美貼近的 Spine 動畫」這條流程，從**每一步都要使用者參與**，改成 **AI 自主發掘問題、克服問題、持續逼近需求**，使用者只在三個關鍵點介入。
>
> 適用：Spine 3.8.99 / Cocos Creator 3.7.3 / lula 專案。搭配 `spine-ai-editor` skill 的 validator、patch 範本與 Cocos 9 步推送。

---

## 0. 核心原則（為什麼這樣設計）

上一版流程慢，根因不是「Claude 不會並行」，而是**驗證被外包給使用者** —— 每一句「這樣對嗎？」都變成一次來回。本 SOP 的設計圍繞四條原則：

1. **驗證閉環在 AI 端**：render → 截圖／gif → 對照 reference → 評分 → 決策，整個迴圈不需要使用者介入。
2. **介入點收斂成三種**：開頭定契約、無法自決的岔路、階段審查。不再有零碎小問題。
3. **一切可重跑、可 diff、可恢復**：每個決定都落地成檔案（契約、迭代日誌、patch 腳本），中斷也能接續。
4. **並行優先**：獨立子問題（pose 分析、切圖、keyframe 生成、驗證）派給不同 subagent 同時做。

---

## 1. 角色（誰做什麼）

| 角色 | 由誰擔任 | 職責 |
|---|---|---|
| **Orchestrator** | 主 Claude | 拆解任務、派工、整合結果、跑驗證迴圈、決定何時升級 |
| **Pose 分析 agent** | subagent | ffmpeg 抽幀 → 逐幀識別 pose → 輸出可機讀的 pose 規格 |
| **資產 agent** | subagent | 切圖、atlas 對照、補圖候選生成 |
| **Patch agent** | subagent | 依 pose 規格寫 `patch_*.py`、跑 validator、minify |
| **驗證 agent** | subagent | 把 Cocos 預覽截圖對照 reference + 驗收契約，輸出 per-criterion 評分 |
| **Human（你）** | 使用者 | ① 開頭裁定驗收契約 ② 岔路決策 ③ 階段審查。其餘放手 |

---

## 2. 六階段 Pipeline

```
Phase 0  Intake      盤點資產（影片 / json / atlas / png），跑 validator 當 sanity check
Phase 1  Contract    ★ 定義驗收契約（你唯一的開頭介入）
Phase 2  Decompose   拆解成獨立子問題、建任務清單、派 subagent
Phase 3  Build-Verify 自主迭代迴圈（核心，無使用者介入）
Phase 4  Escalation  只在岔路/超預算卡關時，批次升級給你
Phase 5  Review       階段里程碑彙整審查
Phase 6  Delivery    交付 + 寫下可重跑的 artifacts
```

Phase 3 是心臟，Phase 1 是讓 Phase 3 能自主跑的前提。

---

## 3. 驗收契約（Acceptance Spec）— 你唯一的開頭介入

把模糊的「貼近影片」翻譯成**可被 AI 自己檢查**的條目。沒有這份契約，每次迭代都得靠你主觀判斷。一份契約長這樣：

| 編號 | 檢查項 | 目標 | 量測方式 | 容忍度 |
|---|---|---|---|---|
| C1 | 關鍵 pose @ 時間點 | 0.0s 蹲、0.4s 起跳、0.8s 揮手頂點 | 截該幀 vs 影片該幀，vision 比對 | 視覺一致 |
| C2 | 主要關節角度 | 起跳幀大腿 −60°、小腿 +30° | 讀 bone rotate 值 | ±8° |
| C3 | 輪廓吻合 | 整體 silhouette 對齊影片 | 疊圖比對 | 主體不偏移 |
| C4 | 節奏／時長 | 總長 0.9s，緩動近似影片加速度 | 比對關鍵幀時間 | ±1 幀 |
| C5 | 視覺品質 gate | 無破圖、無穿插、無 atlas 撕裂 | 預覽掃描 | 0 缺陷 |

> 契約一旦敲定，Phase 3 之後我都對著它自主驗證；你不必再逐輪判斷。
> 多數情況我會**先草擬一份契約草案**讓你改，而不是要你從零寫。

---

## 4. 自我驗證迴圈（Build-Verify Loop）— 心臟

每一個檢查項（criterion）都跑這個閉環，**全程不需要你**：

```
1. 設計／調整 keyframe → 寫 patch_*.py
2. validator 跑過（0 error）→ minify
3. Cocos 9 步推送（含 loop / premultipliedAlpha / useTint 三條補償）
4. 截圖 / 錄 gif（對應契約的關鍵時間點逐幀）
5. 我用 vision 把預覽幀 ⟷ reference 幀，逐條對照契約評分
6. 決策：
   ├─ 全項 pass            → 進下一階段
   ├─ 有 fail 且在迭代預算內 → 自動描述差距、調整數值、回到步驟 1
   └─ fail 且超出迭代預算   → 升級給你（Phase 4），附我試過什麼 + 選項
```

**評分輸出範例**（我自己產，寫進 iteration_log）：

```
Round 3 — 動畫 jump
  C1 起跳 pose ........ PASS
  C2 大腿角度 ......... FAIL  目前 −48°，目標 −60°（差 12°，超容忍 ±8°）→ 自動 +12°
  C3 輪廓 ............. PASS
  C5 品質 gate ........ FAIL  右臂在 0.6s 穿插軀幹 → reparent 或調 z-order
決策：C2 自動修，C5 嘗試調 draworder；下一輪重驗
```

> **誠實的限制**：vision 比對能抓「角度、pose、輪廓、破圖」這類客觀差距；但**細微的緩動手感、重量感**這種主觀品質，我能逼近、未必能完美自評 —— 這類會放進 Phase 5 讓你看。

---

## 5. 升級政策（Escalation）— 你何時會被打擾

只有三種情況我會回來找你，**全部批次化、附選項與我的建議**：

| 類型 | 觸發 | 我會給你 |
|---|---|---|
| **A. 不可自決岔路** | 創意/結構性分歧（例：影片轉到背面但原圖無背面） | 2–3 個選項（生成／造假／略過）+ 我的推薦 |
| **B. 超預算卡關** | 某 criterion 自動迭代 N 輪仍 fail | 卡在哪、試過什麼、可能要你提供的東西 |
| **C. 階段審查** | 每個 phase 末或每 N 輪 | 進度彙整 + gif + 待你拍板的主觀項 |

**自主程度分級（旋鈕，預設 L2）：**

- **L1 保守**：每個 phase 末都停下等你確認；岔路一律問。
- **L2 平衡（預設）**：客觀 criterion 全自主迭代；只在 A/B 與里程碑停。
- **L3 放手**：連多數岔路都用我的最佳判斷先做，最後一次性給你審；只有「會破壞既有資產 / 成本高的不可逆動作」才停。

---

## 6. 並行調度（Parallel Orchestration）

獨立、無相依的子問題同時派 subagent（必要時用隔離 worktree，避免互踩）：

```
影片 → [Pose 分析 agent] ┐
原圖 → [資產/切圖 agent] ┼→ [Patch agent 整合 keyframe] → [驗證 agent 評分] → Orchestrator 決策
契約 → ───────────────┘
```

可並行：pose 分析、切圖/補圖候選、atlas 盤點。
必須串行：patch → validator → Cocos 推送 → 驗證（有前後相依）。

---

## 7. 切圖／補圖 — 最耗人工點的最小化策略

老實說，**這才是真正的瓶頸，不是調度**。把平面角色拆成乾淨可綁定的部件、補畫被遮住的區域，「乾淨」的品質標準常需要你的眼睛。最小化做法：

- **不跟你共做，改成讓你挑**：我自動生 N 版候選（切圖方案 / AI 補圖），批次給你選或否決。
- **補圖**：先用程式化方法（鏡像、延展、內容感知填補）出草稿；不足再 AI 生成多版。
- **切圖**：能程式化分離的（依圖層/顏色/連通區）先自動做；真正模糊的邊界才升級為 A 類岔路。

---

## 8. 落地產物（Artifacts）— 可恢復、可 diff

每次任務都寫下這些，讓流程可中斷續跑、可版本控制：

| 檔案 | 內容 |
|---|---|
| `anatomy.md` | 骨架/slot/skin/mesh 解析（如本次的 main_draw 報告） |
| `acceptance_spec.md` | 驗收契約（Phase 1 的產出，全程的真相來源） |
| `iteration_log.md` | 每輪 per-criterion 評分與決策 |
| `patch_*.py` | 每個改動的可重跑腳本（不靠 LLM 手拼 JSON） |
| `preview/*.gif` | 每個里程碑的預覽，對照 reference |

---

## 9. 通知（tg-flow）

掛上 `tg-flow`：每次任務狀態變更（phase 切換、升級、完成）自動發 Telegram。
你不必盯畫面 —— 只在收到 A/B/C 三類通知時回應即可。

---

## 10. 可調旋鈕（一次調好，之後沿用）

這份 SOP 唯一需要你拍板的就是這幾個值：

| 旋鈕 | 選項 | 預設 |
|---|---|---|
| **自主程度** | L1 保守 / L2 平衡 / L3 放手 | **L2** |
| **每 criterion 迭代預算** | 失敗幾輪後升級 | **5 輪** |
| **里程碑頻率** | 每 phase 末 / 每 N 輪 | **每 phase 末** |
| **驗證真相來源** | 我的 vision 自評是否足夠，或哪些項一定要你親驗 | **客觀項自評、主觀手感你驗** |
| **切圖/補圖** | 全自動候選讓你挑 / 一定要你過目每步 | **自動候選 → 你挑** |
| **TG 通知** | 開 / 關 | **開** |

---

## 附：一次完整任務的時間線（L2 預設下）

```
你：給影片 + 資產 + 一句目標
  ↓ （我自動）
Phase 0 盤點 → Phase 1 我草擬契約
  ↓
★ 你：花 2 分鐘改契約、確認旋鈕   ← 你的第①次介入
  ↓ （我自主跑，TG 報進度）
Phase 2 派工 → Phase 3 迭代（自我驗證，可能數十輪，你不介入）
  ↓ 遇到背面無圖
★ 你：選「AI 生成背面」            ← 你的第②次介入（A 類岔路）
  ↓ （我繼續自主跑）
Phase 5 里程碑：我給 gif + 主觀手感待確認
★ 你：看 gif，指「起跳再快一點」    ← 你的第③次介入
  ↓
Phase 6 交付
```

從「每一步」壓到「三次」。
